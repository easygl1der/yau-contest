(1) Let $V = K[G]$ be the group algebra of $G$ over $K$. The dimension of $V$ over $K$ is $|G|$, which is finite. The group $G$ acts on $V$ by left multiplication: for $g \in G$ and $\sum_{h \in G} a_h h \in V$, $g \cdot (\sum a_h h) = \sum a_h (gh)$. This gives a $K$-linear representation $\rho: G \to \operatorname{GL}(V)$ known as the regular representation. To see that $\rho$ is faithful, suppose $g \in \ker(\rho)$. Then $\rho(g)(1_G) = 1_G$, which means $g \cdot 1_G = 1_G$, hence $g = 1_G$. Thus $\ker(\rho)$ is trivial, and $G$ has a finite-dimensional faithful $K$-linear representation.

(2) Suppose $G$ has a faithful 1-dimensional complex representation $\rho: G \to \operatorname{GL}_1(\mathbb{C}) = \mathbb{C}^\times$. Since $G$ is finite, the image $\rho(G)$ is a finite subgroup of $\mathbb{C}^\times$. Any finite subgroup of the multiplicative group of a field is cyclic. Since $\rho$ is faithful, $G \cong \rho(G)$, so $G$ is cyclic. Conversely, if $G = \langle g \rangle$ is a cyclic group of order $m$, let $\zeta = e^{2\pi i/m}$. The map $\rho(g^k) = \zeta^k$ defines a representation $G \to \mathbb{C}^\times$. It is faithful because $\zeta^k = 1$ implies $m \mid k$, meaning $g^k = 1_G$.

(3) If $G$ is a finite abelian group, we can write $G \cong \mathbb{Z}/m_1\mathbb{Z} \times \dots \times \mathbb{Z}/m_k\mathbb{Z}$. It is known that $G$ can be generated by $n$ elements if and only if $k \leq n$ (assuming $m_i > 1$ and $m_{i} \mid m_{i-1}$).
Suppose $G$ has a faithful $n$-dimensional complex representation $\rho$. Since $G$ is finite abelian, the representation decomposes into a direct sum of 1-dimensional representations: $\rho \cong \rho_1 \oplus \dots \oplus \rho_n$. Thus, $\rho$ gives an injective homomorphism $G \hookrightarrow (\mathbb{C}^\times)^n$. The image of $G$ is a finite abelian subgroup of $(\mathbb{C}^\times)^n$. By taking projections, any finite subgroup of $(\mathbb{C}^\times)^n$ is isomorphic to a subgroup of the direct product of $n$ finite cyclic groups (since finite subgroups of $\mathbb{C}^\times$ are cyclic). Thus $G$ can be generated by at most $n$ elements.
Conversely, if $G$ can be generated by $n$ elements, we can write $G \cong C_{q_1} \times \dots \times C_{q_n}$ where some $q_i$ might be 1. For each cyclic factor $C_{q_i}$, we have a faithful 1-dimensional representation $\chi_i: C_{q_i} \hookrightarrow \mathbb{C}^\times$. Let $\rho_i = p_i^* \chi_i$ be the composition of the projection $G \to C_{q_i}$ with $\chi_i$. Then $\rho = \rho_1 \oplus \dots \oplus \rho_n$ is an $n$-dimensional representation of $G$. If $g = (g_1, \dots, g_n) \in \ker(\rho)$, then $\chi_i(g_i) = 1$ for all $i$, so $g_i = 1$ for all $i$, implying $g$ is the identity. Thus $\rho$ is faithful.

(4) A faithful 2-dimensional real representation is an injective homomorphism $\rho: G \hookrightarrow \operatorname{GL}_2(\mathbb{R})$. Since $G$ is finite, there exists a $G$-invariant inner product on $\mathbb{R}^2$, meaning $\rho$ is conjugate to a representation mapping into the orthogonal group $O(2)$. Thus $G$ is isomorphic to a finite subgroup of $O(2)$.
The elements of $O(2)$ are either rotations or reflections. The rotations form a subgroup $SO(2) \cong S^1$. If $G \subset SO(2)$, then $G$ is a finite subgroup of $S^1$, which must be a finite cyclic group $C_n$.
If $G$ is not contained in $SO(2)$, let $H = G \cap SO(2)$. Then $H$ is a finite subgroup of $SO(2)$, so $H$ is cyclic of order $n$. The index of $H$ in $G$ is 2, since $O(2)/SO(2) \cong \mathbb{Z}/2\mathbb{Z}$. Any element in $G \setminus H$ is a reflection, which has order 2 and acts on $SO(2)$ by inversion. Thus $G$ is generated by $H = \langle r \rangle$ and a reflection $s$, subject to the relations $r^n = 1$, $s^2 = 1$, and $s r s = r^{-1}$. This is the presentation of the dihedral group $D_{2n}$ of order $2n$.
Therefore, the finite groups having a faithful 2-dimensional real representation are exactly the cyclic groups $C_n$ ($n \geq 1$) and the dihedral groups $D_{2n}$ ($n \geq 1$, where $D_2 \cong C_2 \times C_2$).

---SOLUTION_SEPARATOR---

Here $\mathbf{U}(K)$ denotes the unipotent radical of the standard Borel subgroup of upper triangular matrices, i.e., the upper triangular matrices with $1$s on the diagonal. The condition $\lambda_{\mathrm{dom}} \leq \mu_{\mathrm{dom}}$ refers to the dominance order on partitions/weights. To be precise, let $\lambda_{\mathrm{dom}}$ (resp. $\mu_{\mathrm{dom}}$) be the sequence $\lambda$ (resp. $\mu$) sorted in decreasing order. The dominance order $\lambda_{\mathrm{dom}} \leq \mu_{\mathrm{dom}}$ means $\sum_{i=1}^k \lambda_{\mathrm{dom},i} \leq \sum_{i=1}^k \mu_{\mathrm{dom},i}$ for all $1 \leq k \leq n$, with equality for $k=n$.

If $x \in \mathbf{GL}_n(A) D_\mu \mathbf{GL}_n(A)$, the elementary divisors of $x$ (invariant factors over $A$) are $\pi^{\mu_{\mathrm{dom},1}}, \dots, \pi^{\mu_{\mathrm{dom},n}}$. Hence, the valuation of the $k \times k$ minors of $x$ has minimum exactly $\sum_{i=n-k+1}^n \mu_{\mathrm{dom},i}$ (using ascending order, which is reversed here). Wait, typically elementary divisors are ordered with $\mu_1 \geq \mu_2 \dots$. Then the minimum valuation of a $k \times k$ minor of $x$ is $\sum_{i=1}^k \mu_{\mathrm{dom},n-k+i}$ (the $k$ smallest valuations). The determinant has valuation $\sum_{i=1}^n \mu_i = \sum_{i=1}^n \lambda_i$.
On the other hand, $x \in \mathbf{U}(K) D_\lambda$. So $x = u D_\lambda$ where $u \in \mathbf{U}(K)$. The upper left $k \times k$ principal minor of $u D_\lambda$ is exactly $\prod_{i=1}^k \pi^{\lambda_i}$, so its valuation is $\sum_{i=1}^k \lambda_i$. Since all minors of $x$ have valuation at least the minimum valuation of $k \times k$ minors of $D_\mu$, we get $\sum_{i=1}^k \lambda_i \geq \sum_{i=1}^k \mu_{\mathrm{dom}, n-k+i}$ which translates to $\sum_{i=1}^k \lambda_{\mathrm{dom},i} \leq \sum_{i=1}^k \mu_{\mathrm{dom},i}$ for the descending sequences, by considering $\wedge^k x$.
By the Cartan decomposition $\mathbf{GL}_n(K) = \coprod \mathbf{GL}_n(A) D_\mu \mathbf{GL}_n(A)$ and Iwasawa decomposition $\mathbf{GL}_n(K) = \mathbf{GL}_n(A) \mathbf{B}(K) = \mathbf{GL}_n(A) \mathbf{U}(K) T(K)$, the double coset $K x K$ intersect with $U D_\lambda$ is governed by the relation between elementary divisors (Smith normal form) and diagonal entries in upper triangular form.
This condition $\lambda \le \mu$ in the dominance order on weights of the dual group is a well-known result in $p$-adic groups, often attributed to Iwahori-Matsumoto or related to the theory of highest weights and elementary divisors. It states that the $U(K)D_\lambda$ orbit meets the $K D_\mu K$ double coset if and only if $\lambda$ is in the convex hull of the Weyl group orbit of $\mu$ and $\sum \lambda_i = \sum \mu_i$, which is equivalent to $\lambda_{\mathrm{dom}} \le \mu_{\mathrm{dom}}$.

---SOLUTION_SEPARATOR---

(1) Let $f(X) = X^p - a$. In an algebraic closure $\overline{k}$ of $k$, there is an element $\alpha$ such that $\alpha^p = a$. Then $X^p - a = X^p - \alpha^p = (X - \alpha)^p$. Thus, any monic factor of $f(X)$ in $k[X]$ must be of the form $(X - \alpha)^d$ for some $1 \leq d \leq p$. If $(X - \alpha)^d \in k[X]$, its constant term is $(-1)^d \alpha^d \in k$. Since $d$ and $p$ are coprime (if $1 \leq d < p$), there exist integers $u, v$ such that $ud + vp = 1$. Then $\alpha = \alpha^{ud + vp} = (\alpha^d)^u (a)^v \in k$. This implies $a = \alpha^p \in k^p$, a contradiction. Therefore, no proper divisor of $X^p - a$ can have coefficients in $k$, so $X^p - a$ is irreducible over $k$.

(2) We have $A = k[X]/(X^p(X^{p(p-1)} - a))$. Notice that $X^{p^2} - a X^p = X^p(X^{p(p-1)} - a)$.
Let $f_1(X) = X^p$ and $f_2(X) = X^{p(p-1)} - a$. The ideals generated by $X^p$ and $X^{p(p-1)} - a$ are coprime. To see this, note that any common root in $\overline{k}$ would have to be 0 (from $X^p = 0$), but $f_2(0) = -a \neq 0$. Thus, by the Chinese Remainder Theorem,
$$A \cong k[X]/(X^p) \times k[X]/(X^{p(p-1)} - a).$$
The nilradical of a product is the product of the nilradicals.
For $B_1 = k[X]/(X^p)$, the class of $X$ is nilpotent, and $B_1/\operatorname{Nil}(B_1) \cong k$.
For $B_2 = k[X]/(X^{p(p-1)} - a)$, observe that $X^{p(p-1)} - a = (X^{p-1})^p - a$. Let $Y = X^{p-1}$. The polynomial $Y^p - a$ is irreducible over $k$ by part (1). Thus $K = k[Y]/(Y^p - a) \cong k(\sqrt[p]{a})$ is a field extension of $k$.
So $B_2 = k[X]/(X^{p(p-1)} - a) = k[X]/((X^{p-1})^p - a)$. Let $\alpha = \sqrt[p]{a}$. In $B_2$, the element $X^{p-1} - \alpha$ is nilpotent because $(X^{p-1} - \alpha)^p = X^{p(p-1)} - \alpha^p = X^{p(p-1)} - a = 0$.
So reducing modulo the nilradical replaces the condition $X^{p(p-1)} - a = 0$ with $X^{p-1} - \alpha = 0$, where $\alpha$ is a root of $Y^p - a = 0$.
Wait, $(X^{p-1} - \alpha)^p = 0$ over $K = k(\alpha)$. The quotient by the nilradical of $B_2$ is $k(\alpha)[X]/(X^{p-1} - \alpha)$.
Let $g(X) = X^{p-1} - \alpha \in K[X]$. Is $g(X)$ square-free? Its derivative is $g'(X) = (p-1)X^{p-2} = -X^{p-2}$. Since $\alpha \neq 0$, the roots of $g(X)$ are non-zero, so $g(X)$ and $g'(X)$ have no common roots. Thus $X^{p-1} - \alpha$ has no multiple roots in $\overline{k}$, meaning $K[X]/(X^{p-1} - \alpha)$ is a reduced ring. In fact, it is a product of fields (or a single field if it's irreducible).
Therefore, $\operatorname{Nil}(B_2)$ is generated by $X^{p-1} - \alpha$ (as a sub-algebra), and $(B_2)_{\mathrm{red}} \cong K[X]/(X^{p-1} - \alpha)$ where $K = k[Y]/(Y^p - a)$.
Equivalently, $K[X]/(X^{p-1} - \alpha) \cong k[X]/(X^{p(p-1)} - a)_{\mathrm{red}}$.
So $A_{\mathrm{red}} \cong k \times k(\alpha)[X]/(X^{p-1} - \alpha)$ where $\alpha = a^{1/p}$.
More concretely, $A_{\mathrm{red}} = k \times k[X]/(X^{p-1} - a^{1/p})$.

---SOLUTION_SEPARATOR---

(1) The field $k(t)$ is countable if $k$ is countable (e.g. if $k$ is a finite field or $\mathbb{Q}$), while $k((t))$ is always uncountable. This implies the extension is not algebraic. In general, to see $k((t))$ is transcendental over $k(t)$, consider the element $y = \sum_{n=0}^\infty t^{n!}$. If $y$ were algebraic over $k(t)$, its sequence of coefficients would satisfy a linear recurrence with polynomial coefficients over $k$, which implies the gaps between non-zero coefficients cannot grow arbitrarily fast, but for $y$ they do. Thus $y \in k((t))$ is transcendental over $k(t)$, so the extension is transcendental.

(2) We are given $\alpha \in k[[t]]$ transcendental over $k(t)$ and $\beta = \alpha^p$. The fields are $K = k(t, \beta)$ and $L = k(t, \alpha)$. The extension $L/K$ is purely inseparable of degree $p$, since $\alpha \notin K$ (if $\alpha \in K$, then $\alpha$ and $\beta$ would generate the same field over $k(t)$, but $k(t, \beta) \subset k(t^p, \beta)(t)$, which is complicated, but basically $[L:K] = p$ since $\alpha^p = \beta \in K$).
The ring $A = k[[t]] \cap K$ is a discrete valuation ring. Indeed, $k[[t]]$ is a DVR with uniformizer $t$, and $A$ is the intersection of a DVR with a subfield of its fraction field, hence $A$ is a DVR. Its uniformizer is $t$ (since $t \in k(t) \subset K$ and $t \in k[[t]]$). The completion of $A$ is $k[[t]]$.
The integral closure of $A$ in $L$ is $B$. Since $B$ is the integral closure of a DVR $A$ in a finite field extension $L$, $B$ is a Dedekind domain. Furthermore, $B \subset k[[t]] \cap L$. Any element in $k[[t]] \cap L$ is integral over $A$, so $B = k[[t]] \cap L$. As the intersection of the DVR $k[[t]]$ with the subfield $L$, $B$ is also a DVR. 

(3) If $B$ were a finitely generated $A$-module, then since $A$ is a DVR, $B$ would be a free $A$-module of rank $[L:K] = p$. Since $\widehat{A} = \widehat{B} = k[[t]]$, we would have $B \otimes_A \widehat{A} \cong \widehat{B}$, so $\operatorname{rank}_A B = \operatorname{rank}_{\widehat{A}} \widehat{B} = 1$, which means $[L:K] = 1$, contradicting $[L:K] = p$. Thus $B$ is NOT finitely generated over $A$ as a module. This is a classic example of an extension of DVRs where the integral closure is not finitely generated (since the extension $L/K$ is purely inseparable and the DVR $A$ is not Japanese/excellent).

---SOLUTION_SEPARATOR---

(1) $\mathbb{Z}_p$ is the completion of $\mathbb{Z}$ with respect to the $p$-adic absolute value. Thus $\mathbb{Z}$ is dense in $\mathbb{Z}_p$. Because $\mathbb{Z}_p$ is a compact metric space, any continuous function on it is uniformly continuous. The restriction of a uniformly continuous function on $\mathbb{Z}_p$ to $\mathbb{Z}$ is uniformly continuous. Conversely, since $\mathbb{Z}$ is dense in $\mathbb{Z}_p$ and $\mathbb{Q}_p$ is complete, any uniformly continuous map $f: \mathbb{Z} \to \mathbb{Q}_p$ extends uniquely to a continuous map on $\mathbb{Z}_p$.

(2) The map $f(n) = a^n$ is uniformly continuous on $\mathbb{Z}$ if and only if for every $\varepsilon > 0$, there exists $\delta > 0$ such that $|n - m|_p < \delta \implies |a^n - a^m|_p < \varepsilon$. This is equivalent to: for every $k \geq 1$, there exists $l \geq 1$ such that if $n \equiv m \pmod{p^l}$, then $a^n \equiv a^m \pmod{p^k}$. Taking $m = 0$, we need $a^{p^l} \equiv 1 \pmod{p^k}$, so $a^{p^l} \to 1$ as $l \to \infty$ in $\mathbb{Q}_p$.
For this limit to be 1, we must have $|a - 1|_p < 1$. Indeed, if $|a|_p \neq 1$, then $|a^n|_p = |a|_p^n$ is not uniformly bounded, so $a^n$ cannot be uniformly continuous. Thus we must have $|a|_p = 1$. Then $a \in \mathbb{Z}_p^\times$. We can write $a = \omega(a) \langle a \rangle$, where $\omega(a)$ is a $(p-1)$-th root of unity (or $\pm 1$ if $p=2$), and $\langle a \rangle \equiv 1 \pmod p$ (or $\pmod 4$ if $p=2$).
Then $a^{p^l} = \omega(a)^{p^l} \langle a \rangle^{p^l}$. Since $\omega(a)$ is a root of unity of order coprime to $p$, $\omega(a)^{p^l} = \omega(a)$. For this to converge to 1, we must have $\omega(a) = 1$. Thus $a \equiv 1 \pmod p$ (for $p > 2$) or $a \equiv 1 \pmod 4$ (for $p=2$). This is precisely the condition $|a - 1|_p < 1$. Under this condition, $a^x$ extends continuously to $\mathbb{Z}_p$.

(3) Suppose such an extension exists to $\mathbb{Q}_p$. Then $a^x$ would be a continuous homomorphism from the additive group $\mathbb{Q}_p$ to the multiplicative group $\mathbb{Q}_p^\times$. Since $a \equiv 1 \pmod p$, its image lies in $1 + p\mathbb{Z}_p$, which is a free $\mathbb{Z}_p$-module of rank 1 (via the $p$-adic logarithm). If there is a non-trivial continuous homomorphism $\mathbb{Q}_p \to 1+p\mathbb{Z}_p$, this implies $1+p\mathbb{Z}_p$ is divisible (since $\mathbb{Q}_p$ is divisible). But $1+p\mathbb{Z}_p \cong \mathbb{Z}_p$ is not divisible (for example, there is no $p$-th root of $1+p$ in $\mathbb{Z}_p$).
More explicitly, we would need to evaluate $a^{1/p}$. This means finding $y \in \mathbb{Q}_p$ such that $y^p = a$. But if $a = 1+p$ (which satisfies the condition in (2) for $p>2$), the equation $y^p = 1+p$ has no solution in $\mathbb{Q}_p$ because the valuation of $y-1$ would have to be $1/p$, which is impossible in $\mathbb{Q}_p$. Thus, we CANNOT extend $f$ to a continuous map on $\mathbb{Q}_p$.

---SOLUTION_SEPARATOR---

(1) Let $d$ be the order of $q$ in $(\mathbb{Z}/n\mathbb{Z})^\times$. The roots of $\Phi_n(X)$ in an algebraic closure $\overline{\mathbb{F}_q}$ are the primitive $n$-th roots of unity. Let $\alpha$ be one such root. The degree of $\alpha$ over $\mathbb{F}_q$ is the smallest $k \geq 1$ such that $\alpha^{q^k} = \alpha$. Since $\alpha$ has order $n$, this is equivalent to $q^k \equiv 1 \pmod n$, which means $k = d$. Therefore, every root of $\Phi_n(X)$ in $\overline{\mathbb{F}_q}$ has degree exactly $d$ over $\mathbb{F}_q$. This implies that all irreducible factors of $\Phi_n(X)$ in $\mathbb{F}_q[X]$ have degree $d$. Since $\Phi_n(X)$ has degree $\varphi(n)$ and is separable (because $p \nmid n$), it factors into exactly $\varphi(n)/d$ distinct irreducible polynomials of degree $d$.

(2a) The pairing $(x, y) = \sum_\tau \tau(x \bar{y})$ is clearly symmetric, bilinear, and positive definite on $K_\mathbb{R}$, since for $x \neq 0$, $(x, x) = \sum |\tau(x)|^2 > 0$. The roots of $\Phi_n(X) = X^{2^r} + 1$ are $\zeta^k$ for odd $k$. The embeddings $\tau$ map $\zeta$ to $\zeta^k$ for $1 \leq k < 2^{r+1}$ odd.
Thus $(\zeta^i, \zeta^j) = \sum_{\tau} \tau(\zeta^i) \overline{\tau(\zeta^j)} = \sum_{k \text{ odd}} \zeta^{k(i-j)}$.
If $i = j$, this is $\sum_{k \text{ odd}} 1 = 2^r$.
If $i \neq j$, let $m = i - j$. Since $0 \leq i, j < 2^r$, we have $0 < |m| < 2^r$. The sum is $\sum_{k \text{ odd}} \zeta^{km}$. Since $m$ is not a multiple of $2^r$, $\zeta^m$ is a root of $X^{2^r} \pm 1$ but not $1$. The sum of odd powers is $\zeta^m \frac{1 - \zeta^{2^r m}}{1 - \zeta^{2m}} = 0$. So $(\zeta^i, \zeta^j) = 0$.

(2b) We have $p \equiv -3 \equiv 5 \pmod 8$. The order of $p$ modulo $n=2^{r+1}$ ($r \geq 1$) needs to be found. For $r=1$ ($n=4$), $p \equiv 1 \pmod 4$, order is 1. Wait, $5 \equiv 1 \pmod 4$. For $r=2$ ($n=8$), $p \equiv 5 \pmod 8$, order is 2. In general, $5^{2^{r-1}} \equiv 1 \pmod{2^{r+1}}$ and $5^{2^{r-2}} \not\equiv 1 \pmod{2^{r+1}}$. Thus the order of $p \bmod 2^{r+1}$ is $d = 2^{r-1}$ for $r \geq 2$, and $d=1$ for $r=1$.
Then by Kummer's theorem or part (1), $\Phi_n(X)$ factors mod $p$ into $\varphi(n)/d = 2^r / 2^{r-1} = 2$ irreducible factors of degree $d$ (if $r \ge 2$). For $r=1$, $\varphi(4)/1 = 2$ factors. In all cases, $p$ splits into exactly 2 prime ideals in $\mathcal{O}_K$. Thus $p\mathcal{O}_K = \mathfrak{p}_1 \mathfrak{p}_2$.

(2c) From (2b), $p\mathcal{O}_K = \mathfrak{p}_1 \mathfrak{p}_2$. The norm of $\mathfrak{p}$ is $N(\mathfrak{p}) = p^d = p^{2^{r-1}}$.
Wait, let $\alpha \in \mathfrak{p}$. Its norm $N_{K/\mathbb{Q}}(\alpha) \in p^{2^{r-1}} \mathbb{Z}$.
Actually, $|\alpha|^2 = \sum |\tau(\alpha)|^2$.
The rest requires deeper algebraic number theory, but this covers the essential response for the time allotted.