# GROUP TEST S.-T YAU COLLEGE MATH CONTESTS 2012

# Analysis and Differential Equations

Please solve 5 out of the following 6 problems.

1. Let $A = [ a _ { i j } ]$ be a real symmetric $n \times n$ matrix. Define $f : \mathbb { R } ^ { n } \longrightarrow \mathbb { R }$ by $\begin{array} { r } { f ( x _ { 1 } , \cdot \cdot \cdot , x _ { n } ) = \exp ( - \frac { 1 } { 2 } \sum _ { i , j = 1 } ^ { n } a _ { i j } x _ { i } x _ { j } ) } \end{array}$ . Prove that $f$ is in $L ^ { 1 } ( \mathbb { R } ^ { n } )$ if and only if the matrix $A$ is positive definite.   
Compute $\begin{array} { r } { \int _ { \mathbb { R } ^ { n } } \exp ( - \frac { 1 } { 2 } \sum _ { i , j = 1 } ^ { n } a _ { i j } x _ { i } x _ { j } + \Sigma _ { i = 1 } ^ { n } b _ { i } x _ { i } ) d x } \end{array}$ when $A$ is positive definite.   
2. Let $V$ be a simply connected region in the complex plane and $V \neq \mathbb { C }$ . Let $a$ , $b$ be two distinct points in $V$ . Let $\phi _ { 1 } , \phi _ { 2 }$ be two one-to-one holomorphic maps of $V$ onto itself. If $\phi _ { 1 } ( a ) = \phi _ { 2 } ( a )$ and $\phi _ { 1 } ( b ) = \phi _ { 2 } ( b )$ , show that $\phi _ { 1 } ( z ) = \phi _ { 2 } ( z )$ for all $z \in V$ .   
3. In the unit interval [0, 1] consider a subset

$E = \{ x \}$ in the decimal expansion of $x$ there is no $4 \}$

show that $E$ is measurable and calculate its measure.

4. Let $1 < p < \infty , L ^ { p } ( [ 0 , 1 ] , d m )$ be the completion of $C [ 0 , 1 ]$ with the norm: $\begin{array} { r } { | | f | | p = ( \int _ { 0 } ^ { 1 } | f ( x ) | ^ { p } d m ) ^ { \frac { 1 } { p } } } \end{array}$ , where $d m$ is the Lebesgue measure. Show that $\begin{array} { r } { \operatorname* { l i m } _ { \lambda \to \infty } \lambda ^ { p } m ( x | | f ( x ) | > \lambda ) = 0 } \end{array}$ .   
5. Let $\mathfrak { F } = \{ e _ { \nu } \} , \nu = 1 , 2 , . . . , n$ $\mathfrak { F } = \{ e _ { \nu } \}$ or $\nu = 1 , 2 , \ldots$ is an orthonormal basis in an inner product space $H$ . Let $E$ be the closed linear subspace spanned by $\mathfrak { F }$ . For any $x \in H$ show that the following are equivalent: 1) $x \in E$ ; 2) $\lvert \lvert x \rvert \rvert ^ { 2 } = \Sigma _ { \nu } \lvert ( x , e _ { \nu } ) \rvert ^ { 2 } ; 3 )$ $\boldsymbol { x } = \Sigma _ { \nu } ( x , e _ { \nu } ) e _ { \nu }$ .

$H = L ^ { 2 } [ 0 , 2 \pi ]$ $\begin{array} { r } { < f , g > = \frac { 1 } { \pi } \int _ { 0 } ^ { 2 \pi } f ( x ) g ( x ) d x . } \end{array}$

$\mathfrak { F } = \{ \frac { 1 } { \sqrt { 2 } }$ $\cos x , \sin x , . . . , \cos n x , \sin n x , . . . \}$

be an orthonormal basis. Show that the closed linear sub-space $E$ spanned by $\mathfrak { F }$ is $H$ .

6. Let $\mathcal { H } = L ^ { 2 } [ 0 , 1 ]$ relative to the Lebesgue measure and define $( K f ) ( s ) =$ $\textstyle \int _ { 0 } ^ { s } f ( t ) d t$ for each $f$ in $\mathcal { H }$ . Show that $K$ is a compact operator without eigenvalues.