S.-T. Yau College Student Mathematics Contests 2013

# Analysis and Differential Equations Individual

Please solve 5 out of the following 6 problems.

1. Suppose that $f$ is an integrable function on $\mathbf { R } ^ { d }$ . For each $\alpha > 0$ , let $E _ { \alpha } = \{ x | | f ( x ) | > \alpha \}$ . Prove that:

$$
\int_ {\mathbf {R} ^ {d}} | f (x) | d x = \int_ {0} ^ {\infty} m (E _ {\alpha}) d \alpha .
$$

2. Let $p ( z )$ be a polynomial of degree $d \ge ~ 2$ , with distinct roots $a _ { 1 } , a _ { 2 } , \cdots , a _ { d }$ . Show that

$$
\sum_ {i = 1} ^ {d} \frac {1}{p ^ {\prime} (a _ {i})} = 0.
$$

3. Let $\alpha$ be a number such that $\alpha / \pi$ is not a rational number. Show that:

$\begin{array} { r } { \operatorname* { l i m } _ { N \to \infty } \Sigma _ { n = 1 } ^ { N } e ^ { i k ( x + n \alpha ) } = \frac { 1 } { 2 \pi } \int _ { - \pi } ^ { \pi } e ^ { i k t } d t } \end{array}$

−2) For every continuous periodic function $f : \mathbf { R }  \mathbf { C }$ of period $2 \pi$ , we have

$$
\lim  _ {N \rightarrow \infty} \frac {1}{N} \Sigma_ {n = 1} ^ {N} f (x + n \alpha) = \frac {1}{2 \pi} \int_ {- \pi} ^ {\pi} f (t) d t.
$$

4. Let $u$ be a positive harmonic function over the punctured complex plane $\mathbf { C } / \{ 0 \}$ . Show that $u$ must be a constant function.

5. Suppose $H = L ^ { 2 } ( B ) , B$ is the unit ball in $\mathbf { R } ^ { d }$ . Let $K ( x , y )$ b e a measurable function on $B \times B$ that satisfies

$$
| K (x, y) | \leq A | x - y | ^ {- d + \alpha}
$$

for some $\alpha > 0$ , whenever $x , y \in B$ . Define

$$
T f (x) = \int_ {B} K (x, y) f (y) d y
$$

(a) Prove that $T$ is a bounded operator on $H$ .

(b) Prove that $T$ is compact.

we define: 6. Let $A$ be a $\begin{array} { r } { \int _ { \mathbf { R } } \exp ( i \lambda x ^ { 2 } ) d x = \operatorname* { l i m } _ { \epsilon \to 0 ^ { + } } \int _ { - \infty } ^ { \infty } \exp ( i \lambda x ^ { 2 } - \frac { 1 } { 2 } \epsilon x ^ { 2 } ) d x } \end{array}$ $n \times n$ real non-degenerate symmetric matrix. For $\lambda \in \mathbf { R } ^ { + }$ . Sh ow , that:

$$
\begin{array}{l} \int_ {\mathbf {R} ^ {n}} \exp (i \frac {\lambda}{2} <   A x, x > - i <   x, \xi >) d x \\ = (\frac {2 \pi}{\lambda}) ^ {n / 2} | \det  (A) | ^ {- 1 / 2} \exp \left(- \frac {i}{2 \lambda} <   A ^ {- 1} \xi , \xi >\right) \exp \left(\frac {i \pi}{4} s g n A\right). \\ \end{array}
$$

Here $\lambda \in \mathbf { R } ^ { + } , \xi \in \mathbf { R } ^ { n }$ , $s g n ( A ) = \nu _ { + } ( A ) - \nu _ { - } ( A ) , \nu _ { + } ( A ) ( \nu _ { - } ( A ) )$ is the number of positive (negative) eigenvalues of $A$ .