## 2.4 Conditioning on a σ-Field

We are now in a position to make the final step towards the general definition of conditional expectation. It is based on the observation that $E \left( \xi | \eta \right)$ depends only on the o-field $\sigma \left( \eta \right)$ generated by 7, rather than on the actual values of η.

## Proposition 2.2

If $\sigma ( \eta ) = \sigma ( \eta ^ { \prime } )$ , then $E ( \xi | \eta ) = E ( \xi | \eta ^ { \prime } )$ a.s. (Compare this with (2.2).)

## Proof

This is an immediate consequence of Lemma 2.1.

Because of Proposition 2.2 it is reasonable to talk of conditional expectation given a σ-feld. The definition below differs from Definition 2.3 only by using an arbitrary σ-field G in place of a σ-field σ (r) generated by a random variable η.

## Definition 2.4

Let ξ be an integrable random variable on a probability space $( \Omega , { \mathcal { F } } , P )$ , and let $\mathfrak { g }$ be a σ-field contained in $\mathcal { F }$ . Then the conditional expectation of ξ given G is defined to be a random variable $E \left( \xi | \mathcal { G } \right)$ such that

1 $E ( \xi | \mathcal { G } )$ is G-measurable;

For any $A \in { \mathcal { G } }$

$$
\int _ { A } E ( \xi | \mathcal G ) d P = \int _ { A } \xi d P .\tag{2.3}
$$

## Remark 2.2

The conditional probability of an event $A \in { \mathcal { F } }$ given a σ-field $\mathfrak { g }$ can be defined by

$$
P \left( A | \mathcal { G } \right) = E \left( 1 _ { A } | \mathcal { G } \right) ,
$$

where ${ \bf 1 } _ { A }$ is the indicator function of A.

The notion of conditional expectation with respect to a σ-field extends conditioning on a random variable η in the sense that

$$
E \left( \xi | \sigma ( \eta ) \right) = E \left( \xi | \eta \right) ,
$$

where $\sigma ( \eta )$ is the σ-feld generated by η.

## Proposition 2.3

$E ( \xi | \mathcal { G } )$ exists and is unique in the sense that if ${ \pmb \xi } = { \pmb \xi } ^ { \prime } { \bf a . s . }$ , then $E ( \xi | \mathcal { G } ) =$ $E ( \xi ^ { \prime } | \mathcal { G } )$ a.s.

Proof

Existence and uniqueness follow, respectively, from Theorem 2.1 below and Lemma 2.1.

## Theorem 2.1 (Radon-Nikodym)

Let $( \Omega , { \mathcal { F } } , P )$ be a probability space and let G be a σ-field contained in F. Then for any random variable ξ there exists a G-measurable random variable ζ such that

$$
\int _ { A } \xi d P = \int _ { A } \zeta d P
$$

for each ${ \pmb A } \in { \pmb G } .$

The Radon-Nikodym theorem is important from a theoretical point of view. However, in practice there are usually other ways of establishing the existence of conditional expectation, for example, by finding an explicit formula, as in the examples and exercises in the previous section. The proof of the Radon-Nikodym theorem is beyond the scope of this course and is omitted.

Exercise 2.10

Show that if ${ \mathcal { G } } = \{ \emptyset , \varOmega \}$ , then $E ( \xi | \mathcal { G } ) = E ( \xi ) ~ \mathrm { a } . s$

Hint What random variables are G-measurable if ${ \mathcal { G } } = \{ \emptyset , \varOmega \} ?$

## Exercise 2.11

Show that if ξ is G-measurable, then $E ( \xi | \mathcal { G } ) = \xi \mathsf { a . s } ,$

Hint The conditions of Definition 2.4 are trivially satisfied by ξ if ξ is G-measurable.

Exercise 2.12

Show that if $B \in { \mathcal { G } }$ , then

$$
E \left( E \left( \xi | \mathcal { G } \right) | B \right) = E \left( \xi | B \right) .
$$

Hint The conditional expectation on either side of the equality involves an integral over B. How are these integrals related to one another?

## 2.5 General Properties

## Proposition 2.4

Conditional expectation has the following properties:

1) $E ( a \xi + b \zeta | \mathcal { G } ) = a E ( \xi | \mathcal { G } ) + b E ( \zeta | \mathcal { G } ) ( l i n e a r i t y ) .$

2 $E ( E ( \xi | { \mathcal { G } } ) ) = E ( \xi )$

3) $E ( \xi \zeta | \mathcal { G } ) = \xi E ( \zeta | \mathcal { G } ) \ \mathrm { i f } \ \xi$ is G-measurable (taking out what is known);

4) E(ξ|G) = E(ξ) if ξ is independent of $\mathfrak { g }$ (an independent condition drops out);

5) $E ( E ( \xi | \mathcal { G } ) | \mathcal { H } ) = E ( \xi | \mathcal { H } )$ if $\mathcal { H } \subset \mathcal { G }$ (tower property);

If $\xi \ge 0$ , then $E ( \xi | \mathcal { G } ) \ge 0$ (positivity).

Here a, b are arbitrary real numbers, $\xi , \zeta$ are integrable random variables on a probability space $( \Omega , { \mathcal { F } } , P )$ and $\mathcal { G } , \mathcal { H }$ are o-fields on $\pmb { \varOmega }$ contained in $\mathcal { F } .$ In3we also assume that the product $\pmb { \xi } \pmb { \zeta }$ is integrable. All equalities and the inequalities in 6) hold P-a.s.

Proof

For any $B \in { \mathfrak { g } }$

$$
\begin{array} { l } { \displaystyle \int _ { B } \left( a E ( \xi | \mathcal { G } ) + b E ( \zeta | \mathcal { G } ) \right) d P = a \int _ { B } E ( \xi | \mathcal { G } ) d P + b \int _ { B } E ( \zeta | \mathcal { G } ) d P } \\ { \displaystyle \ } \\ { \displaystyle = a \int _ { B } \xi d P + b \int _ { B } \zeta d P } \\ { \displaystyle = \int _ { B } \left( a \xi + b \zeta \right) d P . } \end{array}
$$

By uniqueness this proves the desired equality.

This follows by putting $A = \Omega$ in (2.3). Also, 2) is a special case of 5) when ${ \mathcal { H } } = \{ \emptyset , \varOmega \}$

3) We first verify the result for $\xi = 1 _ { A }$ , where $A \in { \mathcal { G } }$ . In this case

$$
\begin{array} { r l } { \displaystyle \int _ { B } 1 _ { A } E ( \eta | \mathcal { G } ) d P = \displaystyle \int _ { A \cap B } E ( \eta | \mathcal { G } ) d P } & { } \\ { = \displaystyle \int _ { A \cap B } \eta d P } & { } \\ { = \displaystyle \int _ { B } 1 _ { A } \eta d P } \end{array}
$$

for any $B \in { \mathfrak { G } }$ , which implies that

$$
1 _ { A } E ( \eta | \mathcal { G } ) = E ( 1 _ { A } \eta | \mathcal { G } )
$$

by uniqueness. In a similar way we obtain the result if ξ is a G-measurable step $\xi$ function,

$$
\xi = \sum _ { j = 1 } ^ { m } a _ { j } 1 _ { A _ { j } } ,
$$

where $A _ { j } \in { \mathcal { G } }$ for $j = 1 , \dots , m$ Finally, the result in the general case follows by approximating $\xi$ by G-measurable step functions.

Since $\pmb { \xi }$ is independent of $\mathfrak { g }$ , the random variables $\xi$ and $1 _ { B }$ are independent for any $B \in { \mathfrak { g } }$ . It follows by Proposition 1.1 (independent random variables are uncorrelated) that

$$
\begin{array} { l } { \displaystyle \int _ { B } E ( \xi ) d P = E ( \xi ) E ( 1 _ { B } ) } \\ { = E ( \xi 1 _ { B } ) } \\ { = \displaystyle \int _ { B } \xi d P , } \end{array}
$$

which proves the assertion.

5)By Definition 2.4

$$
\int _ { B } E ( \xi | \mathcal { G } ) d P = \int _ { B } \xi d P
$$

for every $B \in { \mathfrak { G } }$ , and

$$
\int _ { B } E ( \xi | \mathcal { H } ) d P = \int _ { B } \xi d P
$$

for every $B \in \mathcal { H }$ .Because $\mathcal { H } \subset \mathcal { G }$ it follows that

$$
\int _ { B } E ( \xi | \mathcal { G } ) d P = \int _ { B } E ( \xi | \mathcal { H } ) d P
$$

for every $B \in { \mathcal { H } }$ Applying Definition 2.4 once again, we obtain

$$
E \left( E ( \xi | \mathcal { G } ) | \mathcal { H } \right) = E ( \xi | \mathcal { H } ) .
$$

)For any n we put

$$
A _ { n } = \left\{ E \left( \xi | \mathcal { G } \right) \leq - \frac { 1 } { n } \right\} .
$$

Then $A _ { n } \in { \mathcal { G } }$ . If $\xi \ge 0$ a.s., then

$$
0 \leq \int _ { A _ { n } } \xi d P = \int _ { A _ { n } } E \left( \xi | \mathcal { G } \right) d P \leq - \frac { 1 } { n } P \left( A _ { n } \right) ,
$$

which means that $P \left( A _ { n } \right) = 0$ . Because

$$
\{ E \left( \xi | \mathcal { G } \right) < 0 \} = \bigcup _ { n = 1 } ^ { \infty } A _ { n }
$$

it follows that

$$
P \left\{ E \left( \xi | \mathcal { G } \right) < 0 \right\} = 0 ,
$$

completing the proof.

The next theorem, which will be stated without proof, involves the notion of a convex function, such as max $( 1 , x )$ or $e ^ { - { \pmb x } }$ , for example. In this course the theorem will be used mainly for $| x |$ , which is also a convex function. In general, we call a function $\varphi : \mathbb { R }  \mathbb { R }$ convex if for any ${ \pmb x } , { \pmb y } \in \mathbb { R }$ and any $\lambda \in [ 0 , 1 ]$

$$
\varphi \left( \lambda x + \left( 1 - \lambda \right) y \right) \leq \lambda \varphi \left( x \right) + \left( 1 - \lambda \right) \varphi \left( y \right) .
$$

This condition means that the graph of $\varphi$ lies below the cord connecting the points with coordinates $( { \pmb x } , \varphi \left( { \pmb x } \right) )$ and $( { \pmb y } , \varphi \left( { \pmb y } \right) )$

## Theorem 2.2 (Jensen's Inequality)

Let $\varphi : \mathbb { R }  \mathbb { R }$ be a convex function and let ξ be an integrable random variable on a probability space $( \Omega , { \mathcal { F } } , P )$ such that $\varphi \left( \xi \right)$ is also integrable. Then

$$
\varphi \left( E \left( \xi | \mathcal { G } \right) \right) \leq E \left( \varphi \left( \xi \right) | \mathcal { G } \right) \quad \mathrm { ~ a . s . }
$$

for any σ-field G on 2 contained in $\mathcal { F }$

## 2.6 Various Exercises on Conditional Expectation

## Exercise 2.13

Mrs. Jones has made a steak and kidney pie for her two sons. Eating more than a half of it will give indigestion to anyone. While she is away having tea with a neighbour, the older son helps himself to a piece of the pie. Then the younger son comes and has a piece of what is left by his brother. We assume that the size of each of the two pieces eaten by Mrs. Jones' sons is random and uniformly distributed over what is currently available. What is the expected size of the remaining piece given that neither son gets indigestion?

Hint All possible outcomes can be represented by pairs of numbers, the portions of the pie consumed by the two sons. Therefore Ω can be chosen as a subset of the plane. Observe that the older son is restricted only by the size of the pie, while the younger one is restricted by what is left by his brother. This will determine the shape of Ω. Next introduce a probability measure on Ω consistent with the conditions of the exercise. This can be done by means of a suitable density over Ω. Now you are in a position to compute the probability that neither son will get indigestion. What is the corresponding subset of Ω? Finally, define a random variable on $\pmb { \it { \Omega } }$ representing the portion of the pie left by the sons and compute the conditional expectation.

## Exercise 2.14

As a probability space take $\varOmega = [ 0 , 1 )$ with the o-field of Borel sets and the Lebesgue measure on [0, 1). Find $E \left( \xi | \eta \right)$ if

$$
\xi \left( x \right) = 2 x ^ { 2 } , \qquad \eta \left( x \right) = \left\{ \begin{array} { l l } { 2 x } & { \mathrm { ~ f o r ~ } 0 \leq x < \frac { 1 } { 2 } , } \\ { 2 x - 1 } & { \mathrm { ~ f o r ~ } \frac { 1 } { 2 } \leq x < 1 . } \end{array} \right.
$$

Hint What do events in $\sigma \left( \eta \right)$ look like? What do σ (7)-measurable random variables look like? If you devise a neat way of describing these, it will make the task of finding $E \left( \xi | \eta \right)$ much easier. You will need to transform the integrals in condition 2) of Definition 2.3 to find a formula for the conditional expectation.

## Exercise 2.15

Take $\varOmega = \{ 0 , 1 \}$ ] with the σ-field of Borel sets and P the Lebesgue measure on [0, 1]. Let

$$
\eta ( x ) = x ( 1 - x )
$$

for ${ \boldsymbol { x } } \in [ 0 , 1 ]$ . Show that

$$
E ( \xi | \eta ) ( x ) = \frac { \xi ( x ) + \xi ( 1 - x ) } { 2 }
$$

for any ${ \pmb x } \in [ 0 , 1 ]$

Hint Observe that $\eta \left( x \right) = \eta \left( 1 - x \right)$ . What does it tell you about the o-field generated by $\eta ?$ Is $\begin{array} { r } { \frac { 1 } { 2 } \left( \xi \left( x \right) + \xi \dot { \left( 1 - x \right) } \right) } \end{array}$ ) measurable with respect to this σ-field? If so, it remains to verify condition 2)of Definition 2.3.

## Exercise 2.16

Let $\xi , \eta$ be integrable random variables with joint density $f _ { \xi , \eta } ( x , y )$ . Show that

$$
E ( \xi | \eta ) = \frac { \int _ { \mathbb { R } } x f _ { \xi , \eta } ( x , \eta ) d x } { \int _ { \mathbb { R } } f _ { \xi , \eta } ( x , \eta ) d x } .
$$

Hint Study the solutions to Exercises 2.7 and 2.8.

## Remark 2.3

If we put

$$
f _ { \xi , \eta } \left( x | y \right) = \frac { f _ { \xi , \eta } \left( x , y \right) } { f _ { \eta } \left( y \right) } ,
$$

where

$$
f _ { \eta } \left( y \right) = \int _ { \mathbb { R } } f _ { \xi , \eta } ( x , y ) d x
$$

is the density of η, then by the result in Exercise 2.16

$$
E \left( \xi | \eta \right) = \int _ { \mathbb { R } } x f _ { \xi , \eta } \left( x | \eta \right) d x .
$$

We call $f _ { \xi , \eta } \left( x | y \right)$ the conditional density of ξ given η. $\pmb { \xi }$

Exercise 2.17

Consider $L ^ { 2 } \left( \mathcal { F } \right) = L ^ { 2 } \left( \varOmega , \mathcal { F } , P \right)$ as a Hilbert space with scalar product

$$
L ^ { 2 } \left( \mathcal { F } \right) \times L ^ { 2 } \left( \mathcal { F } \right) \ni \left( \xi , \zeta \right) \mapsto E ( \xi \zeta ) \in \mathbb { R } .
$$

Show that if ξ is a random variable in $L ^ { 2 } \left( { \mathcal { F } } \right)$ and $\mathfrak { g }$ is a σ-field contained in ${ \mathcal { F } } ,$ then $E ( \xi | \mathcal { G } )$ is the orthogonal projection of ξ onto the subspace ${ \pmb \xi }$ $L ^ { 2 } \left( { \mathcal { G } } \right)$ in $L ^ { 2 } \left( \mathcal { F } \right)$ consisting of G-measurable random variables.

Hint Observe that condition 2) of Definition 2.4 means that ${ \pmb \xi } - { \pmb E } ( { \pmb \xi } | { \pmb \mathcal { G } } )$ is orthogonal (in the sense of the scalar product above) to the indicator function ${ \pmb { 1 } } _ { \pmb { A } }$ for any $\bar { A \in { \mathfrak { G } } }$

## 2.7 Solutions

Solution 2.1

Since $P \left( \Omega \right) = 1$ and $\begin{array} { r } { \int _ { \Omega } \xi d P = E \left( \xi \right) } \end{array}$

$$
E \left( \xi | \varOmega \right) = \frac 1 { P \left( \varOmega \right) } \int _ { \varOmega } \xi d P = E \left( \xi \right) .
$$

Solution 2.2

By Definition 2.1

$$
\begin{array} { c } { { E ( 1 _ { A } | B ) = \displaystyle \frac { 1 } { P ( B ) } \int _ { B } 1 _ { A } ~ d P } } \\ { { = \displaystyle \frac { 1 } { P ( B ) } \int _ { A \cap B } d P } } \end{array}
$$

$$
\begin{array} { l } { { = { \frac { P ( A \cap B ) } { P ( B ) } } } } \\ { { = { \cal P } ( { \cal \tilde { A } } | B ) . } } \end{array}
$$

## Solution 2.3

Since  is constant, it has only one value $c \in \mathbb { R } ,$ for which

$$
\{ \eta = c \} = \varOmega .
$$

Therefore $E \left( \xi | \eta \right)$ is constant and equal to

$$
E ( \xi | \eta ) ( \omega ) = E \left( \xi | \left\{ \eta = c \right\} \right) = E \left( \xi | \varOmega \right) = E \left( \xi \right)
$$

for each $\omega \in \Omega$ . The last equality has been verified in Exercise 2.1.

## Solution 2.4

The indicator function 1b takes two values 1 and 0. The sets on which these values are taken are

$$
\left\{ 1 _ { B } = 1 \right\} = B , \qquad \left\{ 1 _ { B } = 0 \right\} = \varOmega \setminus B .
$$

Thus, for $\omega \in B$

$$
E ( 1 _ { A } | 1 _ { B } ) ( \omega ) = E ( 1 _ { A } | B ) = P ( A | B ) ,
$$

as in Exercise 2.2. Similarly, for $\omega \in \mathcal { Q } \backslash B$

$$
E ( \boldsymbol { 1 } _ { A } | \boldsymbol { 1 } _ { B } ) ( \omega ) = E ( \boldsymbol { 1 } _ { A } | \boldsymbol { \varOmega } \setminus B ) = P ( A | \boldsymbol { \varOmega } \setminus B ) .
$$

## Solution 2.5

First we observe that

$$
\int _ { B } E ( \xi | B ) d P = \int _ { B } \left( { \frac { 1 } { P ( B ) } } \int _ { B } \xi d P \right) d P = \int _ { B } \xi d P\tag{2.4}
$$

for any event B.

Since η is discrete, it has countably many values $y _ { 1 } , y _ { 2 } , \ldots$ We can assume that these values are pairwise distinct, i.e. $y _ { i } \neq y _ { j }$ if $i \neq j$ . The sets $\left\{ \eta = y _ { 1 } \right\} , \left\{ \eta = y _ { 2 } \right\} , . . . .$ are then pairwise disjoint and they cover the whole space Ω. Therefore, by (2.4)

$$
\begin{array} { l } { E ( E ( \xi | \eta ) ) = \displaystyle \int _ { \Omega } E ( \xi | \eta ) d P } \\ { \displaystyle = \sum _ { n } \int _ { \{ \eta = y _ { n } \} } E ( \xi | \{ \eta = y _ { n } \} ) d P } \end{array}
$$

$$
\begin{array} { l } { \displaystyle = \sum _ { n } \int _ { \{ \eta = y _ { n } \} } \xi d P } \\ { \displaystyle = \int _ { \Omega } \xi d P } \\ { \displaystyle = E ( \xi ) . } \end{array}
$$

## Solution 2.6

First we need to describe the o-field $\sigma \left( \eta \right)$ generated by η. Observe that η is symmetric about $\frac { 1 } { 2 }$

$$
\eta \left( x \right) = \eta \left( 1 - x \right)
$$

for any $\boldsymbol { x } \in [ 0 , 1 ]$ . We claim that $\sigma \left( \eta \right)$ consists of all Borel sets $A \subset [ 0 , 1 ]$ symmetric about $\frac { 1 } { 2 }$ , i.e. such that

$$
A = 1 - A .\tag{2.5}
$$

Indeed, if A is such a set, then $A = \{ \eta \in B \}$ , where

$$
B = \{ 2 x : x \in A \cap [ 0 , { \frac { 1 } { 2 } } ] \}
$$

is a Borel set, so $A \in \sigma ( \eta )$ . On the other hand, if $A \in \sigma \left( \eta \right)$ , then there is a Borel set B in R such that $A = \{ \eta \in B \}$ . Then

$$
\begin{array} { l } { x \in A \ \Leftrightarrow \ \eta \left( x \right) \in B } \\ { \Leftrightarrow \ \eta \left( 1 - x \right) \in B } \\ { \Leftrightarrow \ 1 - x \in A , } \end{array}
$$

so A satisfies (2.5).

We are ready to find E (ξln). If it is to be $\sigma \left( \eta \right)$ -measurable, it must be symmetric about $\frac { 1 } { 2 }$ , i.e.

$$
E \left( \xi | \eta \right) ( x ) = E \left( \xi | \eta \right) ( 1 - x )
$$

for any $x \in [ 0 , 1 ]$ .For any $A \in \sigma \left( \eta \right)$ we shall transform the integral below so as to make the integrand symmetric about $\textstyle { \frac { 1 } { 2 } }$ .

$$
{ \begin{array} { r l } { \displaystyle \int _ { A } 2 x ^ { 2 } d x = } & { \displaystyle \int _ { A } x ^ { 2 } d x + \int _ { A } x ^ { 2 } d x } \\ { = } & { \displaystyle \int _ { A } x ^ { 2 } d x + \int _ { 1 - A } \left( 1 - x \right) ^ { 2 } d x } \\ { } & { = } \end{array} }
$$

<!-- image-->

<!-- image-->

<!-- image-->  
Figure 2.3. The graph of $E \left( \xi | \eta \right)$ in Exercise 2.6

It follows that

$$
E \left( \xi | \eta \right) ( x ) = x ^ { 2 } + \left( 1 - x \right) ^ { 2 } .
$$

The graphs of $\xi , \eta$ and $E \left( \xi | \eta \right)$ are shown in Figure 2.3.

Solution 2.7

Since

$$
\{ \eta \in B \} = [ 0 , 1 ] \times B
$$

for any Borel set $B ,$ we have

$$
{ \begin{array} { r l } { \displaystyle \int _ { \{ \eta \in B \} } \xi d P \ = \ \int _ { B } \displaystyle \int _ { \mathbb { R } } x f _ { \xi , \eta } ( x , y ) d x d y } \\ { \displaystyle \ } & { \ = \ \int _ { B } \left( \displaystyle \int _ { [ 0 , 1 ] } x ( x + y ) d x \right) d y } \\ { \displaystyle \ } & { \ = \ \int _ { B } \left( { \frac { 1 } { 3 } } + { \frac { 1 } { 2 } } y \right) d y } \end{array} }
$$

and

$$
\begin{array} { r l } { \displaystyle \int _ { \{ \eta \in B \} } \frac { 2 + 3 \eta } { 3 + 6 \eta } d P = \displaystyle \int _ { B } \int _ { \mathbb { R } } \frac { 2 + 3 y } { 3 + 6 y } f _ { \xi , \eta } ( x , y ) d x d y } \\ { \displaystyle } & { = \displaystyle \int _ { B } \frac { 2 + 3 y } { 3 + 6 y } \left( \displaystyle \int _ { [ 0 , 1 ] } ( x + y ) \ d x \right) d y } \\ { \displaystyle } & { = \displaystyle \int _ { B } \left( \frac { 1 } { 3 } + \frac { 1 } { 2 } y \right) d y . } \end{array}
$$

Because each event in $\sigma \left( \eta \right)$ is of the form $\{ \eta \in B \}$ for some Borel set $B .$ ,this means that condition 2) of Definition 2.3 is satisfied. The random variable $\frac { 2 + 3 \eta } { 3 + 6 \eta }$

is σ (r)-measurable, so condition 1) holds too. It follows that

$$
E \left( \xi | \eta \right) = \frac { 2 + 3 \eta } { 3 + 6 \eta } .
$$

Solution 2.8

We are looking for a Borel function $F : \mathbb { R }  \mathbb { R }$ such that for any Borel set B

$$
\int _ { \{ \eta \in B \} } \xi d P = \int _ { \{ \eta \in B \} } F \left( \eta \right) d P .\tag{2.6}
$$

Then $E \left( \xi | \eta \right) = F \left( \eta \right)$ by Definition 2.3.

We shall transform both integrals above using the joint density ${ f } _ { \xi , \eta } \left( x , y \right)$ in much the same way as in the solution to Exercise 2.7, except that here we do not know the exact form of $\pmb { F } \left( \pmb { y } \right)$ . Namely,

$$
\begin{array} { l } { { \displaystyle \int _ { \{ \eta \in B \} } \xi d P = \int _ { B } \int _ { \mathbb { R } } x f _ { \xi , \eta } ( x , y ) d x d y } } \\ { { \displaystyle \quad = \frac { 3 } { 2 } \int _ { B } \left( \int _ { [ 0 , 1 ] } x \left( x ^ { 2 } + y ^ { 2 } \right) d x \right) d y } } \\ { { \displaystyle \quad = \frac { 3 } { 2 } \int _ { B } \left( \frac { 1 } { 4 } + \frac { 1 } { 2 } y ^ { 2 } \right) d y } } \end{array}
$$

and

$$
\begin{array} { r l } { \displaystyle \int _ { \{ \eta \in B \} } F \left( \eta \right) d P = } & { \displaystyle \int _ { B } \int _ { \mathbb { R } } F \left( y \right) f _ { \xi , \eta } ( x , y ) d x d y } \\ & { = \frac { 3 } { 2 } \displaystyle \int _ { B } F \left( y \right) \left( \displaystyle \int _ { [ 0 , 1 ] } \left( x ^ { 2 } + y ^ { 2 } \right) d x \right) d y } \\ & { = \frac { 3 } { 2 } \displaystyle \int _ { B } F \left( y \right) \left( \frac { 1 } { 3 } + y ^ { 2 } \right) d y . } \end{array}
$$

Then, (2.6) will hold for any Borel set B if

$$
F \left( y \right) = \frac { \frac { 1 } { 4 } + \frac { 1 } { 2 } y ^ { 2 } } { \frac { 1 } { 3 } + y ^ { 2 } } = \frac { 3 + 6 y ^ { 2 } } { 4 + 1 2 y ^ { 2 } } .
$$

It follows that

$$
E \left( \xi | \eta \right) = F \left( \eta \right) = \frac { 3 + 6 \eta ^ { 2 } } { 4 + 1 2 \eta ^ { 2 } } .
$$

Solution 2.9

We are looking for a Borel function F : R → R such that for any Borel set $B \subset \mathbb { R }$

$$
\int _ { \{ \eta \in B \} } \xi ^ { 2 } d P = \int _ { \{ \eta \in B \} } F \left( \eta \right) d P .\tag{2.7}
$$

Then, by Definition 2.3 we shall have $E \left( \xi ^ { 2 } | \eta \right) = F \left( \eta \right)$

Let us transform both sides of (2.7). To do so we observe that the random variables ξ and η have uniform joint distribution over the unit disc $\Omega = \left\{ ( x , y ) : x ^ { 2 } + y ^ { 2 } \leq 1 \right\}$ , with density

$$
f _ { \xi , \eta } \left( x , y \right) = \frac { 1 } { \pi }
$$

if $x ^ { 2 } + y ^ { 2 } \leq 1$ , and ${ f } _ { \xi , \eta } \left( x , y \right) = 0$ otherwise. It follows that

$$
\begin{array} { l } { { \displaystyle \int _ { \{ \eta \in B \} } \xi ^ { 2 } d P = \int _ { B } \int _ { \mathbb { R } } x ^ { 2 } f _ { \xi , \eta } \left( x , y \right) d x d y } } \\ { { \mathrm { ~ } = \displaystyle \frac { 1 } { \pi } \int _ { B } \int _ { - \sqrt { 1 - y ^ { 2 } } } ^ { \sqrt { 1 - y ^ { 2 } } } x ^ { 2 } d x d y } } \\ { { \mathrm { ~ } = \displaystyle \frac { 2 } { 3 \pi } \int _ { B } \left( 1 - y ^ { 2 } \right) ^ { 3 / 2 } d y } } \end{array}
$$

and

$$
\begin{array} { r l } { \displaystyle \int _ { \{ \eta \in B \} } F \left( \eta \right) d P = } & { \displaystyle \int _ { B } \int _ { \mathbb { R } } F \left( y \right) f _ { \xi , \eta } \left( x , y \right) d x d y } \\ { = } & { \displaystyle \frac { 1 } { \pi } \int _ { B } F \left( y \right) \int _ { - \sqrt { 1 - y ^ { 2 } } } ^ { \sqrt { 1 - y ^ { 2 } } } d x d y } \\ { = } & { \displaystyle \frac { 2 } { \pi } \int _ { B } F \left( y \right) \left( 1 - y ^ { 2 } \right) ^ { 1 / 2 } d y . } \end{array}
$$

If (2.7) is to be satisfied for all Borel sets B, then $B ,$

$$
F \left( y \right) = \frac { 1 } { 3 } \left( 1 - y ^ { 2 } \right) .
$$

This means that

$$
E \left( \xi ^ { 2 } | \eta \right) \left( x , y \right) = F \left( \eta \left( x , y \right) \right) = F \left( y \right) = \frac { 1 } { 3 } \left( 1 - y ^ { 2 } \right)
$$

for any (x,y) in Ω.

Solution 2.10

If ${ \mathcal { G } } = \{ \emptyset , \varOmega \}$ , then any constant random variable is G-measurable. Since

$$
\int _ { \Omega } \xi d P = E \left( \xi \right) = \int _ { \Omega } E \left( \xi \right) d P
$$

and

$$
\int _ { \varnothing } \xi d P = 0 = \int _ { \varnothing } E \left( \xi \right) d P ,
$$

it follows that $E ( \xi | \mathcal { G } ) = E ( \xi ) \ \mathrm { a . s . } ,$ as required.

## Solution 2.11

Because the trivial identity

$$
\int _ { A } \xi d P = \int _ { A } \xi d P ~ .
$$

holds for any $A \in { \mathcal { G } }$ and ξ is G-measurable, it follows that $E ( \xi | \mathcal { G } ) = \xi \mathbf { a . s } ,$

## Solution 2.12

By Definition 2.3

$$
\int _ { B } E \left( \xi | \mathcal { G } \right) d P = \int _ { B } \xi d P ,
$$

since $B \in { \mathcal { G } }$ It follows that

$$
\begin{array} { l } { { E ( E ( \xi | \mathcal { G } ) | B ) = \displaystyle \frac { 1 } { P ( B ) } \displaystyle \int _ { B } E ( \xi | \mathcal { G } ) d P } } \\ { { \displaystyle =  \frac { 1 } { P ( B ) } \displaystyle \int _ { B } \xi d P  } } \\ { { \displaystyle =  E ( \xi | B ) . } } \end{array}
$$

## Solution 2.13

The whole pie will be represented by the interval [0, 1]. Let ${ \pmb x } \in [ { \bf 0 } , { \bf 1 } ]$ be the portion consumed by the older son. Then [0, 1 - x] will be available to the younger one, who takes a portion of size $y \in [ 0 , 1 - x ]$ . The set of all possible outcomes is

$$
\Omega = \left\{ ( x , y ) : x , y \geq 0 , x + y \leq 1 \right\} .
$$

The event that neither of Mrs. Jones' sons will get indigestion is

$$
A = \left\{ ( x , y ) \in \varOmega : x , y < \frac { 1 } { 2 } \right\} .
$$

These sets are shown in Figure 2.4. If x is uniformly distributed over [0, 1] and y is uniformly distributed over $[ 0 , 1 - x ]$ , then the probability measure P over Ω with density

$$
f ( x , y ) = { \frac { 1 } { 1 - x } }
$$

will describe the joint distribution of outcomes $( { \pmb x } , { \pmb y } )$ , see Figure 2.5.

Now we are in a position to compute

$$
{ \begin{array} { r l } & { P ( A ) \ = \ \displaystyle \int _ { A } f ( x , y ) d x d y } \\ & { \quad \quad = \ \displaystyle \int _ { 0 } ^ { \frac { 1 } { 2 } } \int _ { 0 } ^ { \frac { 1 } { 2 } } { \frac { 1 } { 1 - x } } d x d y } \\ & { \quad \quad = \ \ln { \sqrt { 2 } } . } \end{array} }
$$

<!-- image-->  
Figure 2.4. The sets Ω and A in Exercise 2.13

<!-- image-->  
Figure 2.5. The density ${ \pmb f } \left( { \pmb x } , { \pmb y } \right)$ in Exercise 2.13

The random variable

$$
\xi \left( x , y \right) = 1 - x - y
$$

defined on Ω represents the size of the portion left by Mrs. Jones' sons. Finally, we find that

$$
{ \begin{array} { l } { E ( \xi | A ) = { \frac { 1 } { P ( A ) } } \displaystyle \int _ { A } \left( 1 - x - y \right) f ( x , y ) d x d y } \\ { = { \frac { 1 } { \ln { \sqrt { 2 } } } } \displaystyle \int _ { 0 } ^ { \frac { 1 } { 2 } } \int _ { 0 } ^ { \frac { 1 } { 2 } } { \frac { 1 - x - y } { 1 - x } } d x d y } \\ { = { \frac { 1 - \ln { \sqrt { 2 } } } { \ln { 4 } } } . } \end{array} }
$$

## Solution 2.14

The o-field $\sigma \left( \eta \right)$ generated by n consists of sets of the form $B \cup \left( B + { \frac { 1 } { 2 } } \right)$ for some Borel set $B \subset [ 0 , \frac { 1 } { 2 } )$ . Thus, we are looking for a σ (r)-measurable random

variable $\zeta$ such that for each Borel set $B \subset [ 0 , \frac { 1 } { 2 } )$

$$
\int _ { B \cup \left( B + { \frac { 1 } { 2 } } \right) } \xi \left( x \right) d x = \int _ { B \cup \left( B + { \frac { 1 } { 2 } } \right) } \zeta \left( x \right) d x .\tag{2.8}
$$

Then $E \left( \xi | \eta \right) = \zeta$ by Definition 2.3.

Transforming the integral on the left-hand side, we obtain

$$
\begin{array} { c l } { { \displaystyle \int _ { B \cup \left( B + \frac { 1 } { 2 } \right) } \xi \left( x \right) d x = \displaystyle \int _ { B } 2 x ^ { 2 } d x + \displaystyle \int _ { B + \frac { 1 } { 2 } } 2 x ^ { 2 } d x } } \\ { { \displaystyle = \displaystyle \int _ { B } 2 x ^ { 2 } d x + \displaystyle \int _ { B } 2 \left( x + \frac { 1 } { 2 } \right) ^ { 2 } d x } } \\ { { \displaystyle = \displaystyle 2 \int _ { B } \left( x ^ { 2 } + \left( x + \frac { 1 } { 2 } \right) ^ { 2 } \right) d x . } } \end{array}
$$

For $\zeta$ to be $\sigma \left( \eta \right)$ -measurable it must satisfy

$$
\begin{array} { r } { \zeta \left( x \right) = \zeta \left( x + \frac { 1 } { 2 } \right) } \end{array}\tag{2.9}
$$

for each $x \in \{ 0 , \textstyle { \frac { 1 } { 2 } } \}$ . Then

$$
\begin{array} { l } { \displaystyle \int _ { B \cup \{ B + \frac { 1 } { 2 } \} } \zeta \left( x \right) d P = \displaystyle \int _ { B } \zeta \left( x \right) d x + \displaystyle \int _ { B + \frac { 1 } { 2 } } \zeta \left( x \right) d x } \\ { = \displaystyle \int _ { B } \zeta \left( x \right) d x + \displaystyle \int _ { B } \zeta \left( x + \frac { 1 } { 2 } \right) d x } \\ { = \displaystyle \int _ { B } \zeta \left( x \right) d x + \displaystyle \int _ { B } \zeta \left( x \right) d x } \\ { = \displaystyle 2 \int _ { B } \zeta \left( x \right) d x . } \end{array}
$$

If (2.8) is to hold for any Borel set $B \subset [ 0 , \frac { 1 } { 2 } )$ , then

$$
\zeta \left( x \right) = x ^ { 2 } + \left( x + { \textstyle { \frac { 1 } { 2 } } } \right) ^ { 2 }
$$

for each $x \in [ 0 , \textstyle { \frac { 1 } { 2 } } )$ . The values of $\zeta \left( x \right)$ for $x \in [ \textstyle { \frac { 1 } { 2 } } , 1 )$ can be obtained from (2.9). It follows that

$$
E \left( \xi | \eta \right) ( x ) = \zeta \left( x \right) = \left\{ \begin{array} { l l } { x ^ { 2 } + \left( x + \frac { 1 } { 2 } \right) ^ { 2 } \quad } & { \mathrm { f o r ~ } 0 \leq x < \frac { 1 } { 2 } , } \\ { \left( x - \frac { 1 } { 2 } \right) ^ { 2 } + x ^ { 2 } \quad } & { \mathrm { f o r ~ } \frac { 1 } { 2 } \leq x < 1 . } \end{array} \right.
$$

The graphs of $\xi , \eta$ and $E \left( \xi | \eta \right)$ are shown in Figure 2.6.

<!-- image-->

<!-- image-->

<!-- image-->  
Figure 2.6. The graph of E (ξ|n) in Exercise 2.14

## Solution 2.15

Since $\eta ( \pmb { x } ) = \eta ( 1 - \pmb { x } )$ , the o-field $\sigma ( \eta )$ consists of Borel sets $B \subset [ 0 , 1 ]$ such that

$$
B = 1 - B ,
$$

where $1 - B = \{ 1 - x : x \in B \}$ . For any such B

$$
{ \begin{array} { r l } { \displaystyle \int _ { B } \xi ( { \boldsymbol { x } } ) d { \boldsymbol { x } } \ = \ { \frac { 1 } { 2 } } \int _ { B } \xi ( { \boldsymbol { x } } ) d { \boldsymbol { x } } + { \frac { 1 } { 2 } } \int _ { B } \xi ( { \boldsymbol { x } } ) d { \boldsymbol { x } } } & { } \\ { \displaystyle = \ { \frac { 1 } { 2 } } \int _ { B } \xi ( { \boldsymbol { x } } ) d { \boldsymbol { x } } + { \frac { 1 } { 2 } } \int _ { 1 - B } \xi ( 1 - { \boldsymbol { x } } ) d { \boldsymbol { x } } } & { } \\ { \displaystyle = \ { \frac { 1 } { 2 } } \int _ { B } \xi ( { \boldsymbol { x } } ) d { \boldsymbol { x } } + { \frac { 1 } { 2 } } \int _ { B } \xi ( 1 - { \boldsymbol { x } } ) d { \boldsymbol { x } } } & { } \\ { \displaystyle } & { = \ \int _ { B } { \frac { \xi ( { \boldsymbol { x } } ) + \xi ( 1 - { \boldsymbol { x } } ) } { 2 } } d { \boldsymbol { x } } . } \end{array} }
$$

Because $\begin{array} { r } { \frac { 1 } { 2 } \left( \xi \left( x \right) + \xi \left( 1 - x \right) \right) } \end{array}$ is σ (r)-measurable, it follows that

$$
E \left( \xi | \eta \right) \left( x \right) = \frac { \xi ( x ) + \xi ( 1 - x ) } { 2 } .
$$

## Solution 2.16

We are looking for a Borel function $F \left( y \right)$ such that

$$
\int _ { \{ \eta \in B \} } \xi d P = \int _ { \{ \eta \in B \} } F \left( \eta \right) d P
$$

for any Borel set B in R. Because $F \left( \eta \right)$ is $\sigma \left( \eta \right)$ -measurable and each event in $\sigma \left( \eta \right)$ can be written as $\{ \eta \in B \}$ for some Borel set B, this will mean that $E \left( \xi | \eta \right) = F \left( \eta \right)$

Let us transform the two integrals above using the joint density of $\pmb { \xi }$ and $\pmb { \eta } \colon$

$$
\begin{array} { l } { { \displaystyle \int _ { \{ \eta \in B \} } \xi d P = \int _ { B } \int _ { \mathbb { R } } x f _ { \xi , \eta } \left( x , y \right) d x d y } } \\ { { \displaystyle \qquad = \int _ { B } \left( \int _ { \mathbb { R } } x f _ { \xi , \eta } \left( x , y \right) d x \right) d y } } \end{array}
$$

and

$$
\begin{array} { r l } { \displaystyle \int _ { \{ \eta \in B \} } F \left( \eta \right) d P = } & { \displaystyle \int _ { B } \int _ { \mathbb { R } } F \left( y \right) f _ { \xi , \eta } \left( x , y \right) d x d y } \\ & { = \displaystyle \int _ { B } F \left( y \right) \left( \displaystyle \int _ { \mathbb { R } } f _ { \xi , \eta } \left( x , y \right) d x \right) d y . } \end{array}
$$

If these two integrals are to be equal for each Borel set $B$ , then

$$
F \left( y \right) = \frac { \int _ { \mathbb { R } } x f _ { \xi , \eta } \left( x , y \right) d x } { \int _ { \mathbb { R } } f _ { \xi , \eta } \left( x , y \right) d x } .
$$

It follows that

$$
E \left( \xi | \eta \right) = F \left( \eta \right) = \frac { \int _ { \mathbb { R } } x f _ { \xi , \eta } \left( x , \eta \right) d x } { \int _ { \mathbb { R } } f _ { \xi , \eta } \left( x , \eta \right) d x } .
$$

Solution 2.17

We denote by $\zeta$ the orthogonal projection of $\pmb { \xi }$ onto the subspace $L ^ { 2 } \left( { \mathcal { G } } \right) \subset$ $L ^ { 2 } \left( { \mathcal { F } } \right)$ consisting of G-measurable random variables. Thus, $\xi - \zeta$ is orthogonal to $L ^ { 2 } \left( { \mathcal { G } } \right)$ , that is,

$$
E [ ( \xi - \zeta ) \gamma ] = 0
$$

for each $\gamma \in L ^ { 2 } \left( \mathcal G \right)$ .But for any $A \in { \mathfrak { g } }$ the indicator function $\mathbf { 1 } _ { A }$ belongs to $L ^ { 2 } \left( { \mathcal { G } } \right)$ , so

$$
E [ ( \xi - \zeta ) 1 _ { A } ] = 0 .
$$

Therefore

$$
\int _ { A } \xi d P = E ( \xi 1 _ { A } ) = E ( \zeta 1 _ { A } ) = \int _ { A } \zeta d P
$$

for any $A \in { \mathfrak { G } }$ . This means that $\zeta = E ( \xi | \mathcal { G } )$

# Martingales in Discrete Time

## 3.1 Sequences of Random Variables

A sequence $\xi _ { 1 } , \xi _ { 2 } , \ldots$ of random variables is typically used as a mathematical model of the outcomes of a series of random phenomena, such as coin tosses or the value of the FTSE All-Share Index at the London Stock Exchange on consecutive business days. The random variables in such a sequence are indexed by whole numbers, which are customarily referred to as discrete time. It is important to understand that these whole numbers are not necessarily related to the physical time when the events modelled by the sequence actually occur. Discrete time is used to keep track of the order of events, which may or may not be evenly spaced in physical time. For example, the share index is recorded only on business days, but not on Saturdays, Sundays or any other holidays. Rather than tossing a coin repeatedly, we may as well toss 100 coins at a time and count the outcomes.

## Definition 3.1

The sequence of numbers $\xi _ { 1 } \left( \omega \right) , \xi _ { 2 } \left( \omega \right) , \ldots$ . for any fixed $\omega \in \Omega$ is called a sample path.

A sample path for a sequence of coin tosses is presented in Figure 3.1 (+1 stands for heads and -1 for tails). Figure 3.2 shows the sample path of the FTSE All-Share Index up to 1997. Strictly speaking the pictures should con-

<!-- image-->  
Figure 3.1. Sample path for a sequence of coin tosses

<!-- image-->  
Figure 3.2. Sample path representing the FTSE All-Share Index up to 1997

sist of dots, representing the values $\xi _ { 1 } \left( \omega \right) , \xi _ { 2 } \left( \omega \right) , \ldots .$ , but it is customary to connect them by a broken line for illustration purposes.

## 3.2 Filtrations

As the time n increases, so does our knowledge about what has happened in the past. This can be modelled by a filtration as defined below.

## Definition 3.2

A sequence of o-fields $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots$ . on Ω such that

$$
\mathcal { F } _ { 1 } \subset \mathcal { F } _ { 2 } \subset \cdots \subset \mathcal { F }
$$