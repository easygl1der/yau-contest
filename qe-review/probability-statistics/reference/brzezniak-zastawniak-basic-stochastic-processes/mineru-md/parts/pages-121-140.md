## Exercise 5.26

Show that the following modification of 2) above is true: If i is recurrent and $i  j$ ,then $j  i$ Deduce that if i is recurrent and $i  j ,$ then j is recurrent and $j  i$

Hint Is it possible for a chain starting from i to visit j and then never return to i? Is such a situation possible when i is a recurrent state?

The following result describes how the state space S can be partitioned into a countable sum of classes. One of these classes consists of all transient states. Each of the other class consists of interconnecting recurrent states. If the chain enters one of the classes of second type, it will never leave it. However, if the chain enters the class of transient states, it will eventually leave it (and so never return to it). We begin with a definition.

## Definition 5.9

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain on a countable state space S.

A set $c \subset S$ is called closed if once the chain enters C it will never leave it, i.e.

$$
P \left( \xi _ { k } \in S \setminus C { \mathrm { ~ f o r ~ s o m e ~ } } k \geq n | \xi _ { n } \in C \right) = 0 .\tag{5.45}
$$

A set $C \subset S$ is called irreducible if any two elements $i , j$ of C intercommunicate, i.e. for all $i , j \in C$ there exists an $\pmb { n } \in \mathbb { N }$ such that $p _ { n } ( j | i ) > 0$

## Theorem 5.3

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain on a countable state space S. Then

$$
S = T \cup \bigcup _ { j = 1 } ^ { N } C _ { j } , ~ { \mathrm { ( d i s j o i n t ~ s u m ) } } ,\tag{5.46}
$$

where T is the set of all transient states in S and each $C _ { j }$ is a closed irreducible set of recurrent states.

## Exercise 5.27

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain on a countable state space S. Show that a set $C \subset S$ is closed if and only if ${ \pmb p } ( j | i ) = 0$ for all $i \in C$ and $j \in S \setminus C$

Hint One implication is trivial. For the other one use the countable additivity of the measure P.

## Proof (of Theorem 5.3)

Let $R = S \setminus T$ denote the set of all recurrent states. If $i  j$ , then both i and j belong either to T or to R. It follows that the interconnection relation ← restricted to R is an equivalence relation as well. Therefore, $\begin{array} { r } { R = \bigcup _ { j = 1 } ^ { N } C _ { j } } \end{array}$ $C _ { j } = [ s _ { j } ] , s _ { j } \in R$ . Here N denotes the number of different equivalent classes. Since by definition each Cy is an irreducible set, we only need to show that it $C _ { j }$ is closed. But this follows from Exercise 5.26. Indeed, if $i \in C _ { k }$ and $i  j$ , then $i  j ,$ and so $j \in C _ { k \cdot } \boxed { \begin{array} { r l } \end{array} }$

## 5.3 Long-Time Behaviour of Markov Chains: General Case

For convenience we shall denote the countable state space S by $\{ 1 , 2 , 3 , \cdots \}$ when S is an infinite set and by $\{ 1 , 2 , \cdots , n \}$ when S is finite.

## Proposition 5.8

Let $P = \left[ p ( j | i ) \right]$ be the transition matrix of a Markov chain with state space S. Suppose that for all $i , j \in S$

$$
\operatorname* { l i m } _ { n  \infty } p _ { n } ( j | i ) = : \pi _ { j } .\tag{5.47}
$$

(In particular, the limit is independent of i.) Then

1) $\textstyle \sum _ { j } \pi _ { j } \leq 1$

2 $\begin{array} { r } { \sum _ { i } p ( j | i ) \pi _ { i } = \pi _ { j } } \end{array}$

either $\textstyle \sum _ { j } \pi _ { j } = 1$ , or $\pi _ { j } = 0$ for all $j \in S$

## Proof

To begin with, let us assume that S is finite with m elements. Using the Chapman-Kolmogorov equations (5.18), we have

$$
\begin{array} { l l } { \displaystyle \sum _ { j \in S } \pi _ { j } \ = \ \sum _ { j = 1 } ^ { m } \pi _ { j } = \sum _ { j = 1 } ^ { m } \displaystyle \operatorname* { l i m } _ { n \to \infty } p _ { n } ( j | i ) } \\ { \displaystyle \ = \ \operatorname* { l i m } _ { n \to \infty } \sum _ { j = 1 } ^ { m } p _ { n } ( j | i ) = \displaystyle \operatorname* { l i m } _ { n \to \infty } 1 = 1 , } \end{array}
$$

since $\begin{array} { r } { \sum _ { j = 1 } ^ { m } p _ { n } ( j | i ) = 1 } \end{array}$ for any $\pmb { n } \in \mathbb { N }$ (see Exercise 5.2). This proves 1) and 3) simultaneously. Moreover, it shows that the second alternative in 3) can never occur. To prove 2) we argue in a similar way. Let us fix $j \in S$ and (an auxiliary) $\pmb { k } \in S$ Then,

$$
\begin{array} { l } { { \displaystyle \sum _ { i = 1 } ^ { m } p ( j | i ) \pi _ { i } ~ = ~ \sum _ { i = 1 } ^ { m } \operatorname* { l i m } _ { n \to \infty } p ( j | i ) p _ { n } ( i | k ) } } \\ { { ~ = ~ \displaystyle \operatorname* { l i m } _ { n \to \infty } \sum _ { i = 1 } ^ { m } p ( j | i ) p _ { n } ( i | k ) = \operatorname* { l i m } _ { n \to \infty } p _ { n + 1 } ( j | k ) = \pi _ { j } , } } \end{array}
$$

since $\begin{array} { r } { \sum _ { i = 1 } ^ { m } p ( j | i ) p _ { n } ( i | k ) = p _ { n + 1 } ( j | k ) } \end{array}$ by the Chapman-Kolmogorov equations.

When the set S is infinite, we cannot just repeat the above argument. The reason is quite simple: in general the two operations lim and $\Sigma$ cannot be interchanged. They can when the sum is finite, and we used this fact above. But if S is infinite, then the situation is more subtle. One possible solution of the difficulty is contained in the following version of the Fatou lemma.

Lemma 5.1 (Fatou)

Suppose that ${ \pmb a } _ { j } ( { \pmb n } ) \geq 0$ for $j , n \in \mathbb { N } .$ Then

$$
\sum _ { j } \operatorname* { l i m } _ { n } \operatorname* { i n f } a _ { j } ( n ) \leq \operatorname* { l i m } _ { n } \operatorname* { i n f } \sum _ { j } a _ { j } ( n ) .\tag{5.48}
$$

Moreover, if $a _ { j } ( n ) \leq b _ { j }$ for $j , n \in \mathbb { N }$ and $\textstyle \sum _ { j } b _ { j } < \infty$ , then

$$
\operatorname* { l i m } _ { n } \operatorname* { s u p } \sum _ { j } a _ { j } ( n ) \leq \sum _ { j } \operatorname* { l i m } _ { n } \operatorname* { s u p } a _ { j } ( n ) .\tag{5.49}
$$

Using the fact that for a convergent sequence lim and lim inf coincide, by the Fatou lemma we have

$$
\begin{array} { r l r } { \displaystyle \sum _ { j \in S } \pi _ { j } \ = \ \sum _ { j = 1 } ^ { \infty } \pi _ { j } = \displaystyle \sum _ { j = 1 } ^ { \infty } \displaystyle \operatorname* { l i m } _ { n \to \infty } p _ { n } ( j | i ) } & \\ { \displaystyle } & { \displaystyle \leq \ \operatorname* { l i m } _ { n \to \infty } \displaystyle \sum _ { j = 1 } ^ { \infty } p _ { n } ( j | i ) = \displaystyle \operatorname* { l i m } _ { n \to \infty } 1 = 1 } \end{array}
$$

since, as before, $\textstyle \sum _ { j = 1 } ^ { \infty } p _ { n } ( j | i ) = 1$ for any $\pmb { n } \in \mathbb { N } .$ This proves 1). A similar argument shows 2). Indeed, with $j \in S$ and $\pmb { k } \in \mathcal { S }$ fixed, by the Chapman-Kolmogorov equations and the Fatou lemma we have

$$
\sum _ { i = 1 } ^ { \infty } p ( j | i ) \pi _ { i } \ = \ \sum _ { i = 1 } ^ { \infty } \operatorname* { l i m } _ { n \to \infty } p ( j | i ) p _ { n } ( i | k )
$$

$$
\leq \operatorname* { l i m i n f } _ { n \to \infty } \sum _ { i = 1 } ^ { \infty } p ( j | i ) p _ { n } ( i | k ) = \operatorname* { l i m i n f } _ { n \to \infty } p _ { n + 1 } ( j | k ) = \pi _ { j } .
$$

To complete the proof of 2) suppose that for some $k \in S$

$$
\sum _ { i = 1 } ^ { \infty } p ( k | i ) \pi _ { i } < \pi _ { k } .
$$

Then, since $\begin{array} { r } { \sum _ { j \in S } \pi _ { j } = \sum _ { j \neq k } \pi _ { j } + \pi _ { k } } \end{array}$ , by the part of ) already proven we have

$$
\begin{array} { r l } { \displaystyle \sum _ { j = 1 } ^ { \infty } \pi _ { j } ~ > ~ \sum _ { j \neq k } \left( \sum _ { i } p ( j | i ) \pi _ { i } \right) + \sum _ { i } p ( k | i ) \pi _ { i } } & { } \\ { \displaystyle } & { = \sum _ { j = 1 } ^ { \infty } \sum _ { i } ^ { \infty } p ( j | i ) \pi _ { i } = \sum _ { i = 1 } ^ { \infty } \sum _ { j } ^ { \infty } p ( j | i ) \pi _ { i } } \\ { \displaystyle } & { = \sum _ { i = 1 } ^ { \infty } \pi _ { i } \left( \sum _ { j } ^ { \infty } p ( j | i ) \right) = \sum _ { i = 1 } ^ { \infty } \pi _ { i } . } \end{array}
$$

We used the fact that $\begin{array} { r } { \sum _ { j } ^ { \infty } p ( j | i ) = 1 } \end{array}$ together with (5.65). This contradiction proves 2).

In order to verify 3) observe that by iterating 2) we obtain

$$
\sum _ { i } p _ { n } ( j | i ) \pi _ { i } = \pi _ { j } .
$$

Hence,

$$
\begin{array} { r l r } { \pi _ { j } } & { = } & { \underset { n  \infty } { \operatorname* { l i m } } \displaystyle \sum _ { i } p _ { n } ( j | i ) \pi _ { i } } \\ { \displaystyle \sum _ { i } \underset { n  \infty } { \operatorname* { l i m } } p _ { n } ( j | i ) \pi _ { i } } & { = } & { \displaystyle \sum _ { i } \pi _ { j } \pi _ { i } = \pi _ { j } \sum _ { i } \pi _ { i } . } \end{array}
$$

Therefore, the product $\textstyle \pi _ { j } \left( \sum _ { i } \pi _ { i } - 1 \right)$ is equal to 0 for all $j \in S$ . As a result, 3) follows. Indeed, if $\textstyle \sum _ { i } \pi _ { i } \neq 1$ , then $\pi _ { j } = 0$ for all $j \in S . \sqsubseteq$

## Definition 5.10

A probability measure $\begin{array} { r } { \mu : = \sum _ { j \in S } \mu _ { j } \delta _ { j } } \end{array}$ is an invariant measure of a Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ with transition probability matrix $P = \left[ p ( j | i ) \right]$ if for all $\pmb { n } \in \mathbb { N }$ and all $j \in S$

$$
\sum _ { i \in S } p _ { n } ( j | i ) \mu _ { i } = \mu _ { j } .
$$

## Exercise 5.28

Under the assumptions of Proposition 5.8 show that if $\textstyle \sum _ { j \in S } \pi _ { j } \ = \ 1$ , then $\begin{array} { r } { \pmb { \mu } : = \sum _ { j \in S } \pi _ { j } \delta _ { j } } \end{array}$ is the unique invariant measure of our Markov chain. Here $\delta _ { j }$ is the Dirac delta measure at $j$

Hint μ is an invariant measure of a Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ if and only if for each $n \in \mathbb { N } ,$ the distribution of $\xi _ { n }$ equals $\pmb { \mu } ,$ provided the same holds for $\pmb { \xi _ { 0 } }$

## Exercise 5.29

Show that if $\pi _ { j } = 0$ for all $j \in S ,$ ,then there is no invariant measure.

Hint Look closely at the uniqueness part of the solution to Exercise 5.28.

The following exercise shows that a unique invariant measure may exist, even though the condition (5.47) is not satisfied.

## Exercise 5.30

Find all invariant measures for a Markov chain whose graph is given in Figure 5.2.

<!-- image-->  
Figure 5.2. Transition probabilities of the Markov chain in Exercise 5.30

Hint Find the transition probability matrix P and solve the vector equation $\scriptstyle P \pi = \pi$ for $\pi = ( \pi _ { 1 } , \pi _ { 2 } )$ , subject to the condition $\pi _ { 1 } + \pi _ { 2 } = 1$

We shall study some general properties of invariant measures. Above we have seen examples of Markov chains with a unique invariant measure. In what follows we shall investigate the structure of the set of all invariant measures.

## Exercise 5.31

Show that if $\pmb { \mu }$ and v are invariant measures and $\pmb { \theta } \in [ \mathbf { 0 } , \mathbf { 1 } ]$ , then $( 1 - \theta ) \mu + \theta \nu$ is also an invariant measure.

Hint Apply the definition of an invariant measure.

## Exercise 5.32

Show that if µ is an invariant measure of a Markov chain $\pmb { \mu }$ $\xi _ { n } , n \in \mathbb { N }$ with state space $s ,$ then supp $\mu \subset S \backslash T$ ', where T denotes (as usual) the set of all transient $\pmb { T }$ states.

Hint If j is a transient state, then ${ p _ { n } ( j | i ) \to 0 }$ for all ${ \dot { \pmb { \mathscr { \Sigma } } } } \in { \pmb { \cal S } } ,$

The above result shows that there is a close relationship between invariant measures and recurrent states. Below we shall present without proof a couple of results on the existence of such measures and their properties.

## Theorem 5.4

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain on a state space $S = T \cup C$ ,where $T$ is the set of all transient states and C is a closed irreducible set of recurrent $C$ states.³3 Then there exists an invariant measure if and only if each element of C is positive-recurrent. Moreover, if this is the case, then the invariant measure is unique and it is given by $\begin{array} { r } { \mu = \sum _ { i } \mu _ { i } \delta _ { i } } \end{array}$ , where

$$
\mu _ { i } = \frac { 1 } { m _ { i } }
$$

with $\mathbf { \nabla } m _ { i }$ being the mean recurrence time of the state i, see (5.44).

Note, that by Exercise 5.32, the unique invariant measure in Theorem 5.4 is supported by C.

## Remark 5.5

If $\begin{array} { r } { C = \bigcup _ { j = 1 } ^ { N } C _ { j } } \end{array}$ j, where each Cj is a closed irreducible set of recurrent states, $C _ { j }$ then the above result holds, except for the uniqueness part. In fact, if each element of some Cj is positive-recurrent, then there exists a invariant measure $C _ { j }$ $\mu _ { j }$ supported by Cj. Moreover, µj is the unique invariant measure with support $C _ { j }$ $\mu _ { j }$ in Cj. In the special case when each element of C is positive-recurrent, every $C _ { j }$ $C$ invariant measure µ is a convex combination of the invariant measures $\mu$ $\mu _ { j }$ $j \in \{ 1 , \cdots , N \}$

## Theorem 5.5

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain with state space S. Let $j \in S$ be a recurrent state.

1)If j is aperiodic, then

$$
p _ { n } ( j | j )  \frac { 1 } { m _ { j } } .\tag{5.50}
$$

Moreover, for any $i \in S$

$$
p _ { n } ( j | i )  \frac { F _ { j i } ( 1 ) } { m _ { j } } ,\tag{5.51}
$$

where $F _ { j i } ( 1 )$ is the probability that the chain will ever visit state j if it starts at i, see (5.39), and where mj is the mean recurrence time of state j, $\mathbf { \nabla } m _ { j }$ $j ,$ see (5.44);

If j is a periodic state of period ${ \pmb d } \ge 2 .$ , then

$$
p _ { n d } ( j | j )  \frac { d } { m _ { j } } .\tag{5.52}
$$

## Exercise 5.33

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain with state space S. Let $j \in S$ be a transient state. Show that for any $i \in S$

$$
p _ { n } ( j | i )  0 .\tag{5.53}
$$

Hint Use Exercise 5.19.

## Definition 5.11

A Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ with state space $s$ is called ergodic if each $\textit { \textbf { i } } { \boldsymbol { \epsilon } } \textit { \textbf { S } }$ is ergodic, i.e. each state $i \in S$ is positive, recurrent and aperiodic.

## Exercise 5.34

Show that if $\xi _ { n } , \pmb { n } \in \mathbb { N } ,$ is an ergodic irreducible Markov chain with state space S, then $p _ { n } ( j | i ) \to \pi _ { j } { \mathrm { a s } } n \to \infty$ for any $j , i \in S$ ,where $\begin{array} { r } { \pi = \sum _ { i } { \pi _ { i } \delta _ { i } } } \end{array}$ is the unique invariant ineasure.

Hint Use Theorem 5.5. You may assume as a known fact that if j is recurrent and $i  j$ ,then $F _ { j i } ( 1 ) = 1$

## Exercise 5.35

Use the last result to investigate whether the random walk on Z has an invariant measure.

Hint Use Exercise 5.9.

Below we shall see that a converse result to Theorem 5.5 is also true.

## Theorem 5.6

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is an irreducible aperiodic Markov chain with state space S. Then $\xi _ { n } , n \in \mathbb { N } ,$ is ergodic if and only if it has a unique invariant measure.

## Proof

The $\mathfrak { e } _ { \mathbf { i } } \mathbf { f } ^ { \prime }$ part is proved in Exercise 5.34. We shall deal with the 'only if' part. $\mathbf { i f } ^ { \prime }$ Suppose that $\begin{array} { r } { \pi = \sum _ { j } \pi _ { j } \delta _ { j } } \end{array}$ is the unique invariant measure of the chain. Then $\pi _ { j } > 0$ for some $j \in { \dot { S } }$ S. Recall that due to Theorem 5.5 and the Exercise 5.33, $\scriptstyle \operatorname* { l i m } _ { n \to \infty } p _ { n } ( j | i )$ exists for all $i , j \in S$

Since $\begin{array} { r } { \sum _ { i } p _ { n } ( j | i ) \pi _ { i } = \pi _ { j } } \end{array}$ , by the Fatou lemma (inequality (5.49))

$$
\sum _ { i } \operatorname* { l i m } _ { n \to \infty } p _ { n } ( j | i ) \pi _ { i } \geq \operatorname* { l i m } _ { n \to \infty } \operatorname* { s u p } _ { i } p _ { n } ( j | i ) \pi _ { i } = \pi _ { j } .
$$

Hence, there exists an $\textit { \textbf { i } } \in \textit { \textbf { S } }$ such that $\mathrm { l i m } _ { n  \infty } p _ { n } ( j | i ) \pi _ { i } ~ > ~ 0$ Therefore $\begin{array} { r } { \operatorname* { l i m } _ { n \to \infty } p _ { n } ( j | i ) > 0 } \end{array}$ , which in view of Theorem 5.5 implies that $m _ { j } ~ < ~ \infty$ Thus, j is an ergodic state and, since the chain is irreducible, all states are ergodic as well.

## Exercise 5.36

Prove that if there exists an invariant measure for the Markov chain in Exercise 5.14, then $\textstyle \lambda ^ { \prime } : = \sum _ { j = 0 } ^ { \infty } j q _ { j } ^ { \prime } < 1$ . Assuming that the converse is also true, conclude that the chain is ergodic if and only if $\lambda ^ { \prime } < 1$ . Show that if such an invariant measure exists, then it is unique.

Hint Suppose that $\begin{array} { r } { \pi = \sum _ { j = 0 } ^ { \infty } \pi _ { j } \delta _ { j } } \end{array}$ is an invariant measure. Write down an infinite system of linear equations forj. If you don't know how to follow, look at the solution.

## 5.4 Long-Time Behaviour of Markov Chains with Finite State Space

As we have seen above, the existence of the $\pi _ { j }$ plays a very important role in the study of invariant measures. In what follows we shall investigate this question in the case when the state space S is finite.

## Theorem 5.7

Suppose that S is finite and the transition matrix $P = \{ p ( j | i ) \}$ of a Markov chain on $s$ satisfies the condition

$$
\exists n _ { 0 } \in \mathbb { N } \ \exists \varepsilon > 0 : p _ { n _ { 0 } } ( j | i ) \geq \varepsilon , i , j \in S .\tag{5.54}
$$

Then, the following limit exists for all $i , j \in S$ and is independent of i:

$$
\operatorname* { l i m } _ { n  \infty } p _ { n } ( j | i ) = \pi _ { j } .\tag{5.55}
$$

The numbers $\pi _ { j }$ satisfy

$$
\pi _ { j } > 0 , j \in S \mathrm { a n d } \sum _ { j \in S } \pi _ { j } = 1 .\tag{5.56}
$$

Conversely, if a sequence of numbers $\pi _ { j } , j \in S$ satisfies conditions (5.55)(5.56), then assumption (5.54) is also satisfied.

## Proof

Denote the matrix $P ^ { n _ { 0 } } = \left[ p _ { n _ { 0 } } ( j | i ) \right]$ by $Q = \left[ q ( j | i ) \right]$ . Then the process $\eta _ { k } = \xi _ { k n _ { 0 } }$ , $\pmb { k } \in \mathbb { N } ,$ is a Markov chain on S with transition probability matrix Q satisfying (5.54) with $\pmb { n _ { 0 } }$ equal to 1. Note that $p _ { k n _ { 0 } } ( j | i ) = q _ { k } ( j | i )$ due to the Chapman-Kolmogorov equations. Suppose that the properties (5.55)-(5.56) hold true for $\pmb { Q }$ In particular, lim $\dot { \kappa }  \infty \mathcal { P } k n _ { 0 } ( j \vert i ) = \pi _ { j }$ exists and is independent of i. We claim that they are also true for the original matrix P. Obviously, one only needs to check condition (5.55). The Chapman-Kolmogorov equations (and the fact that S is finite) imply that for any $r = 1 , \cdots , n _ { 0 } - 1$

$$
\begin{array} { l } { { p _ { k n _ { 0 } + r } ( j | i ) = \displaystyle \sum _ { s \in S } p _ { k n _ { 0 } } ( j | s ) p _ { r } ( s | i ) \to \displaystyle \sum _ { s \in S } \pi _ { j } p _ { r } ( s | i ) } } \\ { { \mathrm { } = \displaystyle \pi _ { j } \displaystyle \sum _ { s \in S } p _ { r } ( s | i ) = \pi _ { j } . } } \end{array}
$$

Therefore, by a simple result in calculus, according to which, if for a sequence $a _ { n } , n \in \mathbb { N } ,$ there exists a natural number $\pmb { n _ { 0 } }$ such that for each $r \in \{ 0 , 1 , \cdots , n _ { 0 } -$ 1} the limit lim $k {  } \infty \ { } ^ { ( l _ { k n _ { 0 } } + r }$ exists and is r-independent, then the sequence ${ a _ { n } }$ is convergent to the common limit of those subsequences, we infer that (5.55) is satisfied.

In what follows we shall assume that (5.54) holds with $\mathbf { \mathit { n } _ { 0 } } = 1$ . Let us put $p _ { 0 } ( j | i ) = \delta _ { j i }$ and for $j \in S$

$$
\begin{array} { r } { m _ { n } ( j ) \ : = \ \underset { i \in S } { \operatorname* { m i n } } p _ { n } ( j | i ) } \\ { M _ { n } ( j ) \ : = \ \underset { i \in S } { \operatorname* { m a x } } p _ { n } ( j | i ) } \end{array}
$$

Observe that $M _ { 0 } ( j ) = 1$ and $m _ { 0 } ( j ) = 0$ for all $j \in S$ From the Chapman-Kolmogorov equations it follows that the sequence $M _ { n } ( j ) , n \in \mathbb { N } ,$ is decreasing, while the sequence $m _ { n } ( j ) , n \in \mathbb { N } ,$ is increasing. Indeed, since $\begin{array} { r } { \sum _ { k } p ( k | i ) = 1 } \end{array}$ ,

$$
\begin{array} { l } { \displaystyle p _ { n + 1 } ( j | i ) = \sum _ { k \in S } p _ { n } ( j | k ) p ( k | i ) } \\ { \displaystyle \geq \operatorname* { m i n } _ { k } p _ { n } ( j | k ) \sum _ { k \in S } p ( k | i ) } \\ { \displaystyle = \operatorname* { m i n } _ { k } p _ { n } ( j | k ) = m _ { n } ( j ) . } \end{array}
$$

Hence, by taking the minimum over all ${ \mathfrak { i } } \in S$ , we arrive at

$$
m _ { n + 1 } ( j ) = \operatorname* { m i n } _ { i } p _ { n + 1 } ( j | i ) \geq m _ { n } ( j ) .
$$

Similarly,

$$
\begin{array} { l } { \displaystyle p _ { n + 1 } ( j | i ) = \sum _ { k \in S } p _ { n } ( j | k ) p ( k | i ) } \\ { \displaystyle \le \operatorname* { m a x } _ { k } p _ { n } ( j | k ) \sum _ { k \in S } p ( k | i ) } \\ { \displaystyle = \operatorname* { m a x } _ { k } p _ { n } ( j | k ) = M _ { n } ( j ) . } \end{array}
$$

Hence, by taking the maximum over all $\textit { i } \in S$ , we obtain

$$
M _ { n + 1 } ( j ) = \operatorname* { m a x } _ { i } p _ { n + 1 } ( j | i ) \leq M _ { n } ( j ) .
$$

Since $M _ { n } ( j ) \geq m _ { n } ( j )$ , the sequences $M _ { n } ( j )$ and $m _ { n } ( j )$ are bounded from below and from above, respectively. As a consequence, they both have limits. To show that the limits coincide we shall prove that

$$
\operatorname* { l i m } _ { n  \infty } ( M _ { n } ( j ) - m _ { n } ( j ) ) = 0 .\tag{5.57}
$$

For $\pmb { n } \geq 0$ we have

$$
\begin{array} { l } { { p _ { n + 1 } ( j | i ) = \displaystyle \sum _ { s \in S } p _ { n } ( j | s ) p ( s | i ) } } \\ { { = \displaystyle \sum _ { s \in S } p _ { n } ( j | s ) \left[ p ( s | i ) - \varepsilon p _ { n } ( s | j ) \right] + \varepsilon \sum _ { s \in S } p _ { n } ( j | s ) p _ { n } ( s | j ) } } \\ { { = \displaystyle \sum _ { s \in S } p _ { n } ( j | s ) \left[ p ( s | i ) - \varepsilon p _ { n } ( s | j ) \right] + \varepsilon p _ { 2 n } ( j | j ) } } \end{array}\tag{5.58}
$$

by the Chapman-Kolmogorov equations. The expression in square brackets is $\geq 0$ . Indeed, by assumption (5.54), $p ( s | i ) \geq \varepsilon$ and $\pmb { p _ { n } ( s | j ) } \leq 1$ . Therefore,

$$
\begin{array} { r l } { \displaystyle p _ { n + 1 } ( j | i ) ~ \geq ~ \operatorname* { m i n } _ { s \in S } p _ { n } ( j | s ) \sum _ { s \in S } [ p ( s | i ) - \varepsilon p _ { n } ( s | j ) ] + \varepsilon p _ { 2 n } ( j | j ) } & { } \\ { \displaystyle } & { = ~ ( 1 - \varepsilon ) m _ { n } ( j ) + \varepsilon p _ { 2 n } ( j | j ) . } \end{array}\tag{5.59}
$$

By taking the minimum over $i \in S ,$ we arrive at

$$
m _ { n + 1 } ( j ) \geq ( 1 - \varepsilon ) m _ { n } ( j ) + \varepsilon p _ { 2 n } ( j | j ) .\tag{5.60}
$$

Recycling the above argument, we obtain a similar inequality for the sequence $M _ { n } ( j )$ ••

$$
M _ { n + 1 } ( j ) \leq ( 1 - \varepsilon ) M _ { n } ( j ) + \varepsilon p _ { 2 n } ( j | j ) .\tag{5.61}
$$

Thus, by subtracting (5.60) from (5.61) we get

$$
M _ { n + 1 } ( j ) - m _ { n + 1 } ( j ) \leq ( 1 - \varepsilon ) \left( M _ { n } ( j ) - m _ { n } ( j ) \right) .\tag{5.62}
$$

Hence, by induction

$$
M _ { n } ( j ) - m _ { n } ( j ) \leq ( 1 - \varepsilon ) ^ { n } , n \in \mathbb { N } .
$$

This proves (5.57). Denote by πj the common limit of $\pmb { \pi _ { j } }$ $M _ { n } ( j )$ and $m _ { n } ( j )$ . Then (5.55) follows from (5.57). Indeed, if $i , j \in S$ , then

$$
m _ { n } ( j ) \leq p _ { n } ( j | i ) \leq M _ { n } ( j ) .
$$

To prove that $\pi _ { j } ~ > ~ 0$ let us recall that $m _ { n } ( j )$ is an increasing sequence and $m _ { 1 } ( j ) \geq \varepsilon$ by (5.54). We infer that $\pi _ { j } \geq \varepsilon . \ \square$

Exercise 5.37

Show that $p _ { n } ( j | i ) \to \pi _ { j }$ at an exponential rate.

Hint Recall that $m _ { n } ( j ) \leq \pi _ { j } \leq M _ { n } ( j )$ and use (5.62).

The above proves the following important result.

## Theorem 5.8

Suppose that the transition matrix $P = [ p ( j | i ) ]$ of a Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ satisfies assumption (5.54). Show that there exists a unique invariant measure $\mu .$ . Moreover, for some $A > 0$ ,and $\alpha < 1$

$$
| p _ { n } ( j | i ) - \pi _ { j } | \leq A \alpha ^ { n } , i , j \in S , n \in \mathbb { N } .\tag{5.63}
$$

Proof (of the converse part of Theorem 5.7)

Put

$$
\varepsilon = \frac { 1 } { 2 } \operatorname* { m i n } _ { j } { \pi } _ { j } .
$$

Since $p _ { n } ( j | i ) \to \pi _ { j }$ for all $i , j \in S$ , there is an $\displaystyle n _ { 0 } \in \mathbb { N } ,$ such that $p _ { k } ( j | i ) \geq \varepsilon$ for all for $k \geq n _ { 0 }$ and $( i , j ) \in S ^ { 2 }$ .Putting $k = n _ { 0 }$ proves that (5.54) is satisfied.

Let us observe that we have used only two facts: $\pi _ { j } > 0$ for all $j \in S ,$ and $p _ { n } ( j | i ) \to \pi _ { j }$ for all $i , j \in S . \ \square$

## Exercise 5.38

Investigate the existence and uniqueness of an invariant measure for the Markov chain in Proposition 5.1.

Hint Are the assumptions of Theorem 5.7 satisfied?

## Remark 5.6

The solution to Exercise 5.38 allows us to find the unique invariant measure by direct methods, i.e. by solving the linear equations (5.79)-(5.80).

## Exercise 5.39

Find the invariant measure from Exercise 5.38 by calculating the limits (5.55).

Hint Refer to Solution 5.5.

## Exercise 5.40

Ian plays a fair game of dice. After the nth roll of a die he writes down the maximum outcome $\xi _ { n }$ obtained so far. Show that $\xi _ { n }$ is a Markov chain and find its transition probabilities.

Hint $\xi _ { n + 1 } = \operatorname* { m a x } \{ \xi _ { n } , X _ { n + 1 } \}$ , where $X _ { k }$ is the outcome of the kth roll.

## Exercise 5.41

Analyse the Markov chain described in Exercise 5.40, but with fair die replaced by a fair pyramid.

Hint A pyramid has four faces only.

## Exercise 5.42

Suppose that $\textbf { \em a } \geq 1$ is a natural number. Consider a random walk on ${ \boldsymbol { S } } =$ $\{ 0 , 1 , \cdots , { \pmb \alpha } \}$ with absorbing barriers at 0 and a, and with probability $\pmb { p }$ of moving to the right and probability $q = 1 - p$ of moving to the left from any of the states $1 , \ldots , a - 1$ . Hence, our random walk is a Markov chain with transition probabilities

$$
p ( j | i ) = \left\{ \begin{array} { l l } { p \quad } & { \mathrm { i f } \quad 1 \leq i \leq a - 1 , j = i + 1 , } \\ { q \quad } & { \mathrm { i f } \quad 1 \leq i \leq a - 1 , j = i - 1 , } \\ { 1 \quad } & { \mathrm { i f } \quad i = j = 0 \quad \mathrm { o r } i = j = a , } \\ { 0 \quad } & { \mathrm { o t h e r w i s e . } } \end{array} \right.
$$

Find, (a) all invariant measures (there may be just one), (b) the probability of hitting the right-hand barrier prior to hitting the left-hand one.

Hint For (a) recall Exercise 5.30 and for (b) Exercise 5.12.

## 5.5 Solutions

## Solution 5.1

First we give a direct solution. With $A _ { n }$ and $B _ { n }$ being the events that the phone is free or busy in the nth minute, we have $y _ { n } = P ( B _ { n } )$ . The total probability formula then yields, for $n \in \mathbb { N } ,$

$$
P ( B _ { n + 1 } ) = P ( B _ { n + 1 } | A _ { n } ) P ( A _ { n } ) + P ( B _ { n + 1 } | B _ { n } ) P ( B _ { n } )
$$

i.e.

$$
y _ { n + 1 } = p + ( 1 - p - q ) y _ { n } .\tag{5.64}
$$

As before, assuming for the time being that $y = \operatorname* { l i m } _ { n } y _ { n }$ exists, we find that $y = p + ( 1 - p - q ) y$ , and so $\begin{array} { r } { y = \frac { p } { p + q } } \end{array}$ . In particular, $\begin{array} { r } { \frac { p } { p + q } = p + ( 1 - p - q ) \frac { p } { p + q } } \end{array}$ Subtracting the last equality from (5.64), we see that $\{ y _ { n } - \frac { p } { p + q } \}$ is a geometric sequence, so that $\begin{array} { r } { y _ { n } - \frac { p } { p + q } = \left( y _ { 0 } - \frac { p } { p + q } \right) ( 1 - p - q ) ^ { n } } \end{array}$ . Since $y _ { 0 } = 1$ , some simple algebra leads to the formula

$$
y _ { n } = { \frac { p } { p + q } } + { \frac { q } { p + q } } ( 1 - p - q ) ^ { n } , n \in \mathbb { N } .
$$

The last formula can be used to prove that the $\operatorname { l i m } _ { n } y _ { n }$ exists and is equal to $\frac { p } { p + q }$

Another approach is to use the results of Example 5.1. Since $x _ { 0 } = 1 - y _ { 0 }$ by (5.8) we have

$$
{ \begin{array} { r l } & { y _ { n } \ = \ 1 - x _ { n } = 1 - { \frac { q } { p + q } } - \left( 1 - y _ { 0 } - { \frac { q } { p + q } } \right) ( 1 - p - q ) ^ { n } } \\ & { \quad \ = \ { \frac { p } { p + q } } + { \frac { q } { p + q } } ( 1 - p - q ) ^ { n } , } \end{array} }
$$

which agrees with the first method of solution.

## Solution 5.2

We have to show that $\begin{array} { r } { \sum _ { j \in S } p ( j | i ) = 1 } \end{array}$ for every $\pmb { i } \in \pmb { S }$ We have

$$
\begin{array} { r l } { \displaystyle \sum _ { j \in S } p ( j | i ) = \sum _ { j \in S } P ( \xi _ { 1 } = j | \xi _ { 0 } = i ) } \\ { = } & { P \left( \cup _ { j \in S } \{ \xi _ { 1 } = j \} | \xi _ { 0 } = i \right) = P ( \xi _ { 1 } \in S | \xi _ { 0 } = i ) } \\ { = } & { P ( \varOmega | \xi _ { 0 } = i ) = 1 . } \end{array}
$$

## Solution 5.3

Suppose that $A = [ a _ { j i } ] _ { j , i \in S }$ and $B = [ b _ { j i } ] _ { j , i \in S }$ are two stochastic matrices. If $C = B A$ , then $\begin{array} { r } { c _ { j i } = \sum _ { k } b _ { j k } a _ { k i } } \end{array}$ . Hence, for any ${ \bf \chi } _ { i \in S }$

$$
\begin{array} { l } { \displaystyle \sum _ { j } c _ { j i } = \sum _ { j } \left( \sum _ { k } b _ { j k } a _ { k i } \right) = \sum _ { k } \left( \sum _ { j } b _ { j k } a _ { k i } \right) } \\ { = \sum _ { k } \left( \sum _ { j } b _ { j k } \right) a _ { k i } = \sum _ { k } a _ { k i } = 1 , } \end{array}
$$

where the last two equalities hold because B and A are stochastic matrices. We have used the well-known fact that

$$
\sum _ { i } \sum _ { j } a _ { i j } = \sum _ { j } \sum _ { i } a _ { i j } ,\tag{5.65}
$$

for any non-negative double sequence $( a _ { i j } ) _ { i , j = 1 } ^ { \infty }$ (see, for example, Rudin's book cited in the hint to Exercise 5.18). The above argument implies that $P ^ { 2 }$ is a stochastic matrix. The desired result follows by induction. •

To prove that $P ^ { n }$ is a double stochastic matrix whenever P is, it is enough to observe that AB is a double stochastic matrix if A and B are. The latter follows because $( A B ) ^ { t } = B ^ { t } A ^ { t }$ •

## Solution 5.4

Some simple algebra gives

$$
\begin{array} { c l } { { P ^ { 2 } = \left[ \begin{array} { c c } { { ( 1 - p ) ( 1 - p ) + q p } } & { { ( 1 - p ) q + q ( 1 - q ) } } \\ { { p ( 1 - p ) + ( 1 - q ) p } } & { { p q + ( 1 - q ) ( 1 - q ) } } \end{array} \right] } } \\ { { = } } & { { \left[ \begin{array} { c c } { { 1 + p ^ { 2 } - 2 p + p q } } & { { 2 q - p q - q ^ { 2 } } } \\ { { 2 p - p q - p ^ { 2 } } } & { { 1 + q ^ { 2 } - 2 q + p q } } \end{array} \right] . } } \end{array}
$$

## Solution 5.5

Put $x _ { n } = P ( \xi _ { n } = 0 | \xi _ { 0 } = 0 )$ and $y _ { n } = P ( \xi _ { n } = 1 | \xi _ { 0 } = 1 )$ . We have calculated the formulae for $x _ { n }$ and ${ y _ { n } }$ /n in Example 5.1 and Exercise 5.1. Since also

$$
\begin{array} { r } { 1 - x _ { n } \ = \ \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { n } , } \\ { 1 - y _ { n } \ = \ \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { n } , } \end{array}
$$

we arrive at the following formula for the n-step transition matrix:

$$
P _ { n } = \left[ \begin{array} { c c } { \frac { q } { q + p } + \frac { p } { q + p } ( 1 - p - q ) ^ { n } } & { \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { n } } \\ { \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { n } } & { \frac { p } { q + p } + \frac { q } { q + p } ( 1 - p - q ) ^ { n } } \end{array} \right] .\tag{5.66}
$$

## Solution 5.6

Simplifying, we have

$$
\begin{array} { r l } & { P _ { 2 } \ = \ \left[ \begin{array} { c c } { \frac { q } { q + p } + \frac { p } { q + p } ( 1 - p - q ) ^ { 2 } } & { \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { 2 } } \\ { \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { 2 } } & { \frac { p } { q + p } + \frac { q } { q + p } ( 1 - p - q ) ^ { 2 } } \end{array} \right] } \\ & { \ = \ \left[ \begin{array} { c c } { q p - 2 p + 1 + p ^ { 2 } } & { - ( q - 2 + p ) q } \\ { - ( q - 2 + p ) p } & { q ^ { 2 } - 2 q + q p + 1 } \end{array} \right] , } \end{array}
$$

which, in view of the formula in Exercise 5.4, proves that $P _ { 2 } = P ^ { 2 }$

We shall use induction to prove that $P _ { n } = P ^ { n }$ for all $\textstyle n \in \mathbb { N } .$ We already know that the assertion true for $\textbf { \em n } = \textbf { 1 }$ (and also for $n = 2 )$ . Suppose that $P _ { n } = P ^ { n }$ . Then some simple, but tedious algebra gives

$$
\begin{array} { r l } & { P ^ { n + 1 } = P P ^ { n } } \\ & { \quad = \left[ \begin{array} { l l } { 1 - p } & { q } \\ { \quad p } & { 1 - q } \end{array} \right] \left[ \begin{array} { l l } { \frac { q } { q + p } + \frac { p } { q + p } ( 1 - p - q ) ^ { n } } & { \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { n } } \\ { \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { n } } & { \frac { p } { q + p } + \frac { q } { q + p } ( 1 - p - q ) ^ { n } } \end{array} \right] } \\ & { \quad = \left[ \begin{array} { l l } { \frac { q } { q + p } + \frac { p } { q + p } ( 1 - p - q ) ^ { n + 1 } } & { \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { n + 1 } } \\ { \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { n + 1 } } & { \frac { p } { q + p } + \frac { q } { q + p } ( 1 - p - q ) ^ { n + 1 } } \end{array} \right] } \\ &  \quad = \begin{array} { l l } { \frac { p } { p + 1 } . } & { } \end{array} \end{array}
$$

## Solution 5.7

Recall that $P ^ { n + k } = P ^ { n } P ^ { k }$ . Since $p _ { n + k } ( j | i )$ are the entries of the matrix $P _ { n + k } =$ $P ^ { n + k } = P ^ { n } P ^ { k }$ , we obtain (5.18) directly from the definition of the product of two matrices.

## Solution 5.8

Since the $\pmb { \eta _ { i } }$ are independent, $\xi _ { n }$ and $\eta _ { n + 1 }$ are also independent. Therefore,

$$
\begin{array} { r l } & { P \left( \xi _ { n + 1 } = s | \xi _ { 0 } = s _ { 0 } , \cdot \cdot \cdot , \xi _ { n } = s _ { n } \right) } \\ & { = P \left( \xi _ { n } + \eta _ { n + 1 } = s | \xi _ { 0 } = s _ { 0 } , \cdot \cdot \cdot , \xi _ { n } = s _ { n } \right) } \\ & { = P \left( \eta _ { n + 1 } = s - s _ { n } | \xi _ { 0 } = s _ { 0 } , \cdot \cdot \cdot , \xi _ { n } = s _ { n } \right) } \\ & { = P \left( \eta _ { n + 1 } = s - s _ { n } \right) . } \end{array}
$$

Similarly,

$$
\begin{array} { r c l } { P \left( \xi _ { n + 1 } = s | \xi _ { n } = s _ { n } \right) = P \left( \xi _ { n } + \eta _ { n + 1 } = s | \xi _ { n } = s _ { n } \right) } \\ { = } & { } & { } \\ { = } & { } & { } \end{array}
$$

## Solution 5.9

Step 1. For $\pmb { n } = \pmb { 1 }$ the right-hand side of (5.19) is equal to 0 unless $| j - i | \leq 1$ and $1 + j - i$ is even. This is only possible when $j = i + 1 \mathrm { o r } j = i - 1$ . In the former case the right-hand side equals ${ \pmb p } ,$ and in the latter it equals q. This proves (5.19) for $\pmb { n = 1 }$ •

Step 2. Suppose that (5.19) is true for some n. We will use the following version of the total probability formula. If $H _ { i } \in \mathcal { F } , P ( H _ { i } \cap H _ { j } ) = 0 \mathrm { ~ f o r ~ } i \neq j$ and $P ( \bigcup _ { i } H _ { i } ) = 1$ , then

$$
P ( A | C ) = \sum _ { i } P ( A | C \cap H _ { i } ) P ( H _ { i } | C ) .\tag{5.67}
$$

Then the Markov property and (5.67) imply that

$$
\begin{array} { r l } & { P ( \xi _ { n + 1 } = j | \xi _ { 0 } = i ) } \\ & { = P ( \xi _ { n + 1 } = j | \xi _ { 0 } = i , \xi _ { n } = j - 1 ) P ( \xi _ { n } = j - 1 | \xi _ { 0 } = i ) } \\ & { \quad + P ( \xi _ { n + 1 } = j | \xi _ { 0 } = i , \xi _ { n } = j + 1 ) P ( \xi _ { n } = j + 1 | \xi _ { 0 } = i ) } \\ & { = P ( \xi _ { n + 1 } = j | \xi _ { n } = j - 1 ) P ( \xi _ { n } = j - 1 | \xi _ { 0 } = i ) } \\ & { \quad + P ( \xi _ { n + 1 } = j | \xi _ { n } = j + 1 ) P ( \xi _ { n } = j + 1 | \xi _ { 0 } = i ) } \\ & { = p \left( \frac { n } { n + j - 1 - i } \right) p ^ { \frac { n + i - 1 - i } { 2 } } q ^ { \frac { n - j + 1 + i } { 2 } } + q \left( \frac { n + j + 1 - i } { 2 } \right) p ^ { \frac { n + j + 1 - i } { 2 } } q ^ { \frac { n - j - 1 + i } { 2 } } } \end{array}
$$

$$
\begin{array} { r l } { \ } & { = \ \left[ \left( { \frac { n } { { \frac { n - 1 + j - i } { 2 } } } } \right) + \left( { \frac { n } { { \frac { n + 1 + j - i } { 2 } } } } \right) \right] p ^ { { \frac { n + 1 + j - i } { 2 } } } q ^ { { \frac { n + 1 - j + i } { 2 } } } } \\ { \ } & { = \ \left( { \frac { n + 1 } { { \frac { n + 1 + j - i } { 2 } } } } \right) p ^ { { \frac { n + 1 + j - i } { 2 } } } q ^ { { \frac { n + 1 - j + i } { 2 } } } . } \end{array}
$$

## Solution 5.10

Denote the right-hand side of (5.24) by $h ( x )$ . It follows by induction that

$$
h ^ { ( k ) } ( x ) = \frac { ( 2 k ) ! } { k ! } ( 1 - 4 x ) ^ { - 1 / 2 - k } , | x | < \frac { 1 } { 4 } .
$$

On the other hand, h is analytic and

$$
h ( x ) = \sum _ { k = 0 } ^ { \infty } { \frac { 1 } { k ! } } h ^ { ( k ) } ( 0 ) x ^ { k } , | x | < { \frac { 1 } { 4 } } .
$$

## Solution 5.11

We shall translate the problem into the Markov chain language. Denote by S the set of all natural numbers $\mathbb { N } = \{ 0 , 1 , 2 , \cdot \cdot \cdot \}$ . Let $\xi _ { n }$ denote the number of males in the nth year (or generation), where the present year is called year 0. If $\xi _ { n } = i ,$ , i.e. there are exactly i males in year n, then the probability that there will be j males in the next year is given by

$$
P \left( \xi _ { n + 1 } = j | \xi _ { n } = i \right) = P ( X _ { 1 } + \cdot \cdot \cdot X _ { i } = j ) ,\tag{5.68}
$$

where $( X _ { k } ) _ { k = 1 } ^ { \infty }$ is a sequence of independent identically distributed random variables with common distribution

$$
P ( X _ { 1 } = m ) = p _ { m } , \ m \in \mathbb { N } .
$$

Hence $\xi _ { n } , n \ge 0$ is a Markov chain on S with transition probabilities

$$
p ( j | i ) = P ( X _ { 1 } + \cdot \cdot \cdot X _ { i } = j ) .\tag{5.69}
$$

Notice that $p ( 0 | 0 ) = 1$ , i.e. if $\xi _ { n } = 0 .$ ,then $\xi _ { m } = 0$ for all $m \geq n$ .Dying out means that eventually ${ \pmb \xi } _ { n } = { \bf 0 }$ , starting from some $\pmb { n } \in \mathbb { N } .$ Once this happens, $\xi _ { n }$ will stay at 0 forever.

## Solution 5.12

We shall only deal with part 2), as in part 1) there is nothing to show. Suppose that ${ \pmb { \xi _ { 0 } } } = { \pmb i }$ Then $\pmb { \xi } _ { 1 } = X _ { 1 } + \cdots X _ { i }$ , where $X _ { j }$ are independent identically distributed Poisson random variables with parameter λ. Since the sum of such random variables has the Poisson distribution with parameter iλ, (5.27) follows readily.

## Solution 5.13

Let $\xi _ { n }$ denote the number of supporters at the end of day n. Then $\xi _ { n + 1 } - \xi _ { n }$ is equal to 1 or 0 and

$$
\begin{array} { r } { p ( j | i ) = \left\{ \begin{array} { l l } { p q , } & { \mathrm { i f } j = i + 1 , } \\ { 1 - p q , } & { \mathrm { i f } j = i , } \\ { 0 , } & { \mathrm { o t h e r w i s e } . } \end{array} \right. } \end{array}\tag{5.70}
$$

Thus $\xi _ { n }$ is a Markov chain on a state space ${ \pmb S } = { \mathbb N }$ with transition probabilities ${ \pmb p } ( j | i )$ given by (5.70).

## Solution 5.14

We shall use the notation introduced in the hint. Observe that $\xi _ { n + 1 } - \xi _ { n } - Z _ { n }$ equals -1 or 0. The latter case occurs with probability ${ \pmb p } ,$ i.e. when the car served at the beginning of the nth time interval was finished by the end of the nth time interval. The former case occurs with probability $1 - p$ Therefore

$$
P ( \xi _ { n + 1 } = j | \xi _ { n } = i ) = p \frac { \lambda ^ { j - i + 1 } } { ( j - i + 1 ) ! } e ^ { - \lambda } + ( 1 - p ) \frac { \lambda ^ { j - i } } { ( j - i ) ! } e ^ { - \lambda }
$$

for $i \ge 1 , j \ge i - 1$ . On the other hand, if $j \geq 0$ , then

$$
P ( \xi _ { n + 1 } = j | \xi _ { n } = 0 ) = { \frac { \lambda ^ { j } } { j ! } } e ^ { - \lambda } .
$$

Thus, $\xi _ { n }$ is a Markov chain with transition probabilities

$$
p ( j | i ) = \left\{ \begin{array} { l l } { q _ { j } , \quad } & { \mathrm { i f } i = 0 , j \in \mathbb N , } \\ { q _ { j - i + 1 } ^ { \prime } , \quad } & { \mathrm { i f } i \geq 1 , j \geq i - 1 , } \\ { 0 , \quad } & { \mathrm { o t h e r w i s e } , } \end{array} \right.\tag{5.71}
$$

where $\begin{array} { r } { q _ { k } = \frac { \lambda ^ { b } } { k ! } e ^ { - \lambda } } \end{array}$ and

$$
\begin{array} { r } { q _ { k } ^ { \prime } = \left\{ \begin{array} { l l } { p q _ { k } , \quad } & { \mathrm { i f } k = 0 , } \\ { ( 1 - p ) q _ { k - 1 } + p q _ { k } , \quad } & { \mathrm { i f } k \geq 1 . } \end{array} \right. } \end{array}\tag{5.72}
$$

The transition probability matrix of our chain takes the form

$$
P = \left[ \begin{array} { c c c c c c c } { q _ { 0 } } & { q _ { 0 } ^ { \prime } } & { 0 } & { 0 } & { \cdots } & { \cdots } \\ { q _ { 1 } } & { q _ { 1 } ^ { \prime } } & { q _ { 0 } ^ { \prime } } & { 0 } & { \cdots } & { \cdots } \\ { q _ { 2 } } & { q _ { 2 } ^ { \prime } } & { q _ { 1 } ^ { \prime } } & { q _ { 0 } ^ { \prime } } & { 0 } & { \cdots } \\ { \vdots } & { \vdots } & { q _ { 2 } ^ { \prime } } & { q _ { 1 } ^ { \prime } } & { \cdots } & { \cdots } \\ { \vdots } & { \vdots } & { \vdots } & { \vdots } & { \ddots } \\ { \vdots } & { \vdots } & { \vdots } & { \vdots } & { \ddots } \end{array} \right] .
$$

## Solution 5.15

If $p _ { k } ( j | i ) > 0$ , then $P ( \xi _ { n } = j$ for some $n \geq 0 | \xi _ { 0 } = i ) \geq p _ { k } ( j | i ) > 0 . \operatorname { I f } p _ { k } ( j | i ) = 0$ for all $k \geq 1$ , then

$$
P ( \xi _ { n } = j { \mathrm { ~ f o r ~ s o m e ~ } } n \geq 0 | \xi _ { 0 } = i ) \leq \sum _ { n = 1 } ^ { \infty } P ( \xi _ { n } = j | \xi _ { 0 } = i ) = \sum _ { n = 1 } ^ { \infty } p _ { n } ( j | i ) = 0 .
$$

## Solution 5.16

Since $P ( \xi _ { 0 } = i | \xi _ { 0 } = i ) = 1$ , it follows that i ←→ i, which proves 1). Assertion 2) is obvious. To prove 3) we proceed as follows. From the solution to Exercise 5.15 we can find $n , m \ge 1$ such that $p _ { n } ( j | i ) \ > \ 0$ and $p _ { m } ( k \vert j ) ~ > ~ 0$ . Hence, the Chapman-Kolmogorov equations yield

$$
p _ { m + n } ( k | i ) = \sum _ { s \in S } p _ { m } ( k | s ) p _ { n } ( s | i ) \geq p _ { m } ( k | j ) p _ { n } ( j | i ) > 0 .
$$

## Solution 5.17

Since $| p _ { n } ( j | i ) | \leq 1$ and $| f _ { n } ( j | i ) | \leq 1$ for all $\qquad n \in \mathbb { N } ,$ the radii of convergence of both power series are $\geq 1$ . To prove the equalities (5.40)-(5.41) we shall show that for ${ \pmb n } \geq { \bf 1 }$ and any $i , j \in S$ ,

$$
p _ { n } ( j | i ) = \sum _ { k = 1 } ^ { n } f _ { k } ( j | i ) p _ { m - k } ( j | j ) .\tag{5.73}
$$

By total probability formula and the Markov property

$$
\begin{array} { r l } { p _ { n } ( j | i ) } & { = P ( \xi _ { n } = j | \xi _ { 0 } = i ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } P ( \xi _ { n } = j , \xi _ { k } = j , \xi _ { i } \neq j , 1 \leq l \leq k - 1 | \xi _ { 0 } = i ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } P ( \xi _ { k } = j , \xi _ { i } \neq j , 1 \leq l \leq k - 1 | \xi _ { 0 } = i ) } \\ & { \qquad \quad \times P ( \xi _ { n } = j | \xi _ { k } = j , \xi _ { i } \neq j , 1 \leq l \leq k - 1 ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } f _ { k } ( j | \mathrm { i } ) P ( \xi _ { n } = j | \xi _ { k } = j ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } f _ { k } ( j | i ) p _ { n - k } ( j | j ) . } \end{array}
$$

## Solution 5.18

Since $0 \leq p _ { n } ( j | i ) \leq 1 \mathrm { a n d } 0 \leq f _ { n } ( j | i ) \leq 1$ , the result follows readily from Abel's lemma.

## Solution 5.19

Only the case of a recurrent state needs to be studied. Suppose that j is recurrent. Then $\textstyle \sum _ { n = 1 } ^ { \infty } f _ { n } ( j | j ) = 1$ . Hence, by Exercise 5.18, $F _ { j j } ( { \pmb x } ) \nearrow 1$ as x 1. Thus $P _ { j j } ( x ) = ( 1 - F _ { j j } ( x ) ) ^ { - 1 } $ ∞ as $\pmb { x } \nearrow 1$ , and so, again by Exercise 5.18, $\textstyle \sum _ { n = 1 } ^ { \infty } p _ { n } ( j | j ) \ = \infty$ . Conversely, suppose that $\textstyle \sum _ { n = 1 } ^ { \infty } p _ { n } ( j | j ) = \infty$ .Then, by Exercise 5.18, $P _ { j j } ( x ) = \to \infty$ as $\boldsymbol { x } \nearrow 1$ . Thus, $F _ { j j } ( x ) = 1 - ( P _ { j j } ( x ) ) ^ { - 1 }  1$ as $\boldsymbol { x } \nearrow 1$ . Hence, $\textstyle \sum _ { n = 1 } ^ { \infty } f _ { n } ( j | j ) = 1$ , which proves that j is recurrent.

To prove (5.43) we use (5.73) to get

$$
\begin{array} { r l } { \displaystyle \sum _ { n = 0 } ^ { \infty } p _ { n } ( 1 | 1 ) = } & { \displaystyle \sum _ { n = 0 } ^ { \infty } \sum _ { k = 0 } ^ { n - 1 } f _ { n - k } ( j | i ) p _ { k } ( j | j ) } \\ & { = \displaystyle \sum _ { k = 0 } ^ { \infty } \sum _ { m = 1 } ^ { \infty } f _ { m } ( j | i ) p _ { k } ( j | j ) } \\ & { = \displaystyle \sum _ { k = 0 } ^ { \infty } p _ { k } ( j | j ) \sum _ { m = 1 } ^ { \infty } f _ { m } ( j | i ) } \\ & { \le \displaystyle \sum _ { k = 0 } ^ { \infty } p _ { k } ( j | j ) . } \end{array}
$$

This implies (5.43) when $j$ is transient.

## Solution 5.20

We shall show that the state O is recurrent. The other case can be treated in a similar way. From Exercise 5.8 we have $\begin{array} { r } { p _ { n } ( 0 | 0 ) \ : = \ : \frac { q } { p + q } \ : + \ : \frac { p } { p + q } ( 1 - p - q ) ^ { n } } \end{array}$ Thus $\begin{array} { r } { p _ { n } ( 0 | 0 ) \to \frac { q } { p + q } > 0 } \end{array}$ , and so $\textstyle \sum _ { n = 0 } ^ { \infty } p _ { n } ( 0 | 0 ) = \infty$ , which proves that 0 is a recurrent state.

## Solution 5.21

Suppose each $j ~ \in ~ S$ is transient. Then by (5.43) $\begin{array} { r } { \sum _ { n } p _ { n } ( j | i ) \ < } \end{array}$ ∞ for all $\textit { i } \in \textit { S }$ Let us fix $\textit { \textbf { i } } \in \textit { \textbf { S } }$ .Then we would have $\begin{array} { r } { \sum _ { j \in S } \sum _ { n = 1 } ^ { \infty } p _ { n } ( j | i ) \ < \ \infty , } \end{array}$ since S is finite. However, this is impossible because $\begin{array} { r } { \sum _ { j \in S } \sum _ { n = 1 } ^ { \infty } p _ { n } ( j | i ) \ = } \end{array}$ $\begin{array} { r } { \sum _ { n = 1 } ^ { \infty } \sum _ { j \in S } p _ { n } ( j | i ) = \sum _ { n = 1 } ^ { \infty } 1 = \infty } \end{array}$

## Solution 5.22

Let us begin with a brief remark concerning the last part of the problem. Since the random walk is 'space homogenous', $\mathrm { i . e . } p ( j | i ) = p ( j - i | 0 )$ , it should be quite obvious, at least intuitively, that either all states are positive-recurrent or all states are null-recurrent. One can prove this rigorously without any particula difficulty. First, observe (and prove by induction) that $p _ { n } ( j | i ) = p _ { n } ( j - i | \hat { 0 } )$