is called a filtration.

Here $\mathcal { F } _ { n }$ represents our knowledge at time n. It contains all events A such that at time n it is possible to decide whether A has occurred or not. As n increases, there will be more such events A, i.e. the family Fn representing our $\mathcal { F } _ { n }$ knowledge will become larger. (The longer you live the wiser you become!)

## Example 3.1

For a sequence $\xi _ { 1 } , \xi _ { 2 } , . . .$ of coin tosses we take ${ \mathcal { F } } _ { n }$ to be the σ-field generated by $\xi _ { 1 } , \ldots , \xi _ { n }$ ,

$$
\mathcal { F } _ { n } = \sigma \left( \xi _ { 1 } , \ldots , \xi _ { n } \right) .
$$

Let

$$
A = \left\{ { \mathrm { t h e ~ f i r s t ~ 5 ~ t o s s e s ~ p r o d u c e ~ a t ~ l e a s t ~ 2 ~ h e a d s } } \right\} .
$$

At discrete time n = 5, i.e. once the coin has been tossed five times, it will be possible to decide whether A has occurred or not. This means that $A \in { \mathcal { F } } _ { 5 }$ However, at n = 4 it is not always possible to tell if A has occurred or not. If the outcomes of the first four tosses are, say,

$$
\mathrm { t a i l s , t a i l s , h e a d s , t a i l s , }
$$

then the event A remains undecided. We will have to toss the coin once more to see what happens. Therefore $A \notin { \mathcal { F } } _ { 4 }$

This example illustrates another relevant issue. Suppose that the outcomes of the first four coin tosses are

$$
\mathrm { t a i l s , h e a d s , t a i l s , h e a d s . }
$$

In this case it is possible to tell that A has occurred already at n = 4, whatever the outcome of the fifth toss will be. It does not mean, however, that A belongs to F4. The point is that for A to belong to F4 it must be possible to tell whether ${ \mathcal { F } } _ { 4 }$ ${ \mathcal { F } } _ { 4 }$ A has occurred or not after the first four tosses, no matter what the first four outcomes are. This is clearly not so in the example in hand.

## Exercise 3.1

Let $\xi _ { 1 } , \xi _ { 2 } , \ldots$ be a sequence of coin tosses and let ${ \mathcal { F } } _ { n }$ be the σ-field generated by $\xi _ { 1 } , \ldots , \xi _ { n }$ .For each of the following events find the smallest  such that the event belongs to ${ \mathcal { F } } _ { n }$ ..

A = {the first occurrence of heads is preceeded by no more than 10 tails},

B = {there is at least 1 head in the sequence $\xi _ { 1 } , \xi _ { 2 } , \ldots \}$

C = {the first 100 tosses produce the same outcome},

D = {there are no more than 2 heads and 2 tails among the first 5 tosses}.

Hint To find the smallest element in a set of numbers you need to make sure that the set is non-empty in the first place.

Suppose that $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is a sequence of random variables and $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots$ .is a filtration. A priori, they may have nothing in common. However, in practice the filtration will usually contain the knowledge accumulated by observing the outcomes of the random sequence, as in Example 3.1. The condition in the definition below means that $\mathcal { F } _ { n }$ contains everything that can be learned from the values of $\xi _ { 1 } , \ldots , \xi _ { n }$ . In general, it may and often does contain more information.

## Definition 3.3

We say that a sequence of random variables $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is adapted to a filtration $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots \mathrm { i f } \ \xi _ { n }$ is $\mathcal { F } _ { n }$ -measurable for each $n = 1 , 2 , \ldots .$

## Example 3.2

If $\mathcal { F } _ { n } = \sigma ( \xi _ { 1 } , \ldots , \xi _ { n } )$ is the σ-field generated by $\xi _ { 1 } , \ldots , \xi _ { n }$ , then $\xi _ { 1 } , \xi _ { 2 } , . . .$ is adapted to $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots$

## Exercise 3.2

Show that

$$
\mathcal { F } _ { n } = \sigma ( \xi _ { 1 } , \ldots , \xi _ { n } )
$$

is the smallest filtration such that the sequence $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is adapted to $\mathcal { F } _ { 1 }$ $\mathcal { F } _ { 2 } , \ldots$ That is to say, if $\mathcal { G } _ { 1 } , \mathcal { G } _ { 2 } , \ldots .$ is another filtration such that $\xi _ { 1 } , \xi _ { 2 } , . . .$ is adapted to $\mathcal { G } _ { 1 } , \mathcal { G } _ { 2 } , \ldots$ ,then ${ \mathcal { F } } _ { n } \subset { \mathcal { G } } _ { n }$ for each $\pmb { n }$ .

Hint For $\sigma ( \xi _ { 1 } , \ldots , \xi _ { n } )$ to be contained in $\boldsymbol { \mathfrak { G } } _ { n }$ you need to show that $\xi _ { 1 } , \ldots , \xi _ { n }$ are $\scriptstyle { \mathcal { G } } _ { n } - { \mathrm { m e a s u r a b l e } }$

## 3.3 Martingales

The concept of a martingale has its origin in gambling, namely, it describes a fair game of chance, which will be discussed in detail in the next section. Similarly, the notions of submartingale and supermartingale defined below are related to favourable and unfavourable games of chance. Some aspects of gambling are inherent in the mathematics of finance, in particular, the theory of financial derivatives such as options. Not surprisingly, martingales play a crucial role there. In fact, martingales reach well beyond game theory and appear in various areas of modern probability and stochastic analysis, notably, in diffusion theory. First of all, let us introduce the basic definitions and properties in the case of discrete time.

## Definition 3.4

A sequence $\xi _ { 1 } , \xi _ { 2 } , . . .$ . of random variables is called a martingale with respect to a filtration $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots \mathrm { i f }$

1) $\xi _ { n }$ is integrable for each ${ \pmb n = 1 , 2 , \dots }$

2) $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is adapted to $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots ;$

3) $E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) = \xi _ { n }$ a.s. for each $n = 1 , 2 , \ldots$

## Example 3.3

Let $\eta _ { 1 } , \eta _ { 2 } , \ldots ,$ be a sequence of independent integrable random variables such that $E ( \eta _ { n } ) = 0$ for all $n = 1 , 2 , \ldots { }$ We put

$$
\begin{array} { r } { \xi _ { n } = \eta _ { 1 } + \cdots + \eta _ { n } , } \\ { \mathcal { F } _ { n } = \sigma ( \eta _ { 1 } , \ldots , \eta _ { n } ) . } \end{array}
$$

Then $\xi _ { n }$ is adapted to the filtration ${ \mathcal { F } } _ { n }$ an  s intrabe because

$$
\begin{array} { l } { E ( | \xi _ { n } | ) = E ( | \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } | ) } \\ { \leq E ( | \eta _ { 1 } | ) + \cdot \cdot \cdot + E ( | \eta _ { n } | ) } \\ { < \infty . } \end{array}
$$

Moreover,

$$
\begin{array} { r l } { E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) = } & { E ( \eta _ { n + 1 } | \mathcal { F } _ { n } ) + E ( \xi _ { n } | \mathcal { F } _ { n } ) } \\ { = } & { E ( \eta _ { n + 1 } ) + \xi _ { n } } \\ { = } & { \xi _ { n } , } \end{array}
$$

since $\eta _ { n + 1 }$ is independent of $\mathcal { F } _ { n }$ ('and independent condition drops out') and $\xi _ { n }$ is ${ \mathcal { F } } _ { n } .$ -measurable ('taking out what is known'). This means that $\xi _ { n }$ is a martingale with respect to ${ \mathcal { F } } _ { n }$ •

## Example 3.4

Let ${ \pmb \xi }$ be an integrable random variable and let $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots$ be a filtration. We put

$$
\xi _ { n } = E ( \xi | \mathcal { F } _ { n } )
$$

for ${ \pmb n = 1 , 2 , \dots }$

Then $\xi _ { n }$ is $\mathcal { F } _ { n }$ -measurable,

$$
| \xi _ { n } | = | E ( \xi | \mathcal { F } _ { n } ) | \leq E ( | \xi | \ | \mathcal { F } _ { n } ) ,
$$

which implies that

$$
E ( | \xi _ { n } | ) \leq E \big ( E ( | \xi | \ | \mathcal { F } _ { n } ) \big ) = E ( | \xi | ) < \infty ,
$$

and

$$
E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) = E ( E ( \xi | \mathcal { F } _ { n + 1 } ) | \mathcal { F } _ { n } ) = E ( \xi | \mathcal { F } _ { n } ) = \xi _ { n } ,
$$

since $\mathcal { F } _ { n } \subset \mathcal { F } _ { n + 1 }$ (the tower property of conditional expectation). Therefore $\xi _ { n }$ is a martingale with respect to ${ \mathcal { F } } _ { n }$

## Exercise 3.3

Show that if $\xi _ { n }$ is a martingale with respect to $\mathcal { F } _ { n }$ , then

$$
E ( \xi _ { 1 } ) = E ( \xi _ { 2 } ) = \cdot \cdot \cdot \quad .
$$

Hint What is the expectation of $E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) ?$

## Exercise 3.4

Suppose that $\xi _ { n }$ is a martingale with respect to a filtration ${ \mathcal { F } } _ { n }$ . Show that $\xi _ { n }$ is a martingale with respect to the filtration

$$
{ \mathcal { G } } _ { n } = \sigma ( \xi _ { 1 } , \ldots , \xi _ { n } ) .
$$

Hint Observe that $\mathcal { G } _ { n } \subset \mathcal { F } _ { n }$ and use the tower property of conditional expectation.

## Exercise 3.5

Let $\xi _ { n }$ be a symmetric random walk, that is,

$$
\xi _ { n } = \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } ,
$$

where $\eta _ { 1 } , \eta _ { 2 } , \ldots$ is a sequence of independent identically distributed random variables such that

$$
P \{ \eta _ { n } = 1 \} = P \{ \eta _ { n } = - 1 \} = \frac { 1 } { 2 }
$$

(a sequence of coin tosses, for example). Show that $\xi _ { n } ^ { 2 } - n$ is a martingale with respect to the filtration

$$
{ \mathcal { F } } _ { n } = \sigma ( \eta _ { 1 } , \ldots , \eta _ { n } ) .
$$

Hint You want to transform $E \left( \xi _ { n + 1 } ^ { 2 } - \left( n + 1 \right) | \mathcal { F } _ { n } \right)$ to obtain ${ \pmb { \xi } } _ { n } ^ { 2 } - n$ Write

$$
\begin{array} { l } { { \xi _ { n + 1 } ^ { 2 } ~ = ~ \left( { { \xi _ { n } } + { \eta _ { n + 1 } } } \right) ^ { 2 } } } \\ { { ~ = ~ { \eta _ { n + 1 } ^ { 2 } } + 2 { \eta _ { n + 1 } } { \xi _ { n } } + { \xi _ { n } ^ { 2 } } } } \end{array}
$$

and observe that $\xi _ { n }$ is $\mathcal { F } _ { n } { \cdot } \mathrm { m e a s u r a b l e } .$ ,while $\eta _ { n + 1 }$ is independent of $\mathcal { F } _ { n }$ To transform the conditional expectation you can 'take out what is known' and use the fact that 'an independent condition drops out'. Do not forget to verify that ${ \pmb { \xi } } _ { n } ^ { 2 } - n$ is integrable and adapted to $\mathcal { F } _ { n }$

## Exercise 3.6

Let ξn be a symmetric random walk and $\xi _ { n }$ $\mathcal { F } _ { n }$ the filtration defined in Exercise 3.5. Show that

$$
\zeta _ { n } = ( - 1 ) ^ { n } \cos ( \pi \xi _ { n } )
$$

is a martingale with respect to $\mathcal { F } _ { n }$

Hint You want to transform $E ( ( - 1 ) ^ { n + 1 } \cos ( \pi \xi _ { n + 1 } ) | \mathcal { F } _ { n } )$ to obtain $( - 1 ) ^ { n } \cos ( \pi \xi _ { n } )$ Use a similar argument as in Exercise 3.5 to achieve this. But, first of all, make sure that $\zeta _ { n }$ is integrable and adapted to $\mathcal { F } _ { n }$

## Definition 3.5

We say that $\xi _ { 1 } , \xi _ { 2 } , \ldots$ . is a supermartingale (submartingale) with respect to a filtration $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots \mathrm { i f }$

1) $\xi _ { n }$ is integrable for each $n = 1 , 2 , \ldots$

2) $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is adapted to $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots ;$

3) $E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) \leq \xi _ { n }$ (respectively, $E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) \geq \xi _ { n } )$ a.s. for each $\pmb { n = 1 , 2 , \dots } .$

## Exercise 3.7

Let ξn be a sequence of square integrable random variables. Show that if ξn is $\xi _ { n }$ $\xi _ { n }$ a martingale with respect to a filtration Fn, then ξ2 is a submartingale with $\mathcal { F } _ { n }$ $\xi _ { n } ^ { 2 }$ respect to the same filtration.

Hint Use Jensen's inequality with convex function $\varphi \left( x \right) = x ^ { 2 }$

## 3.4 Games of Chance

Suppose that you take part in a game such as the roulette, for example. Let $\eta _ { 1 } , \eta _ { 2 } , \ldots .$ . be a sequence of integrable random variables, where $\eta _ { n }$ are your

winnings (or losses) per unit stake in game n. If your stake in each game is one, then your total winnings after n games will be

$$
\xi _ { n } = \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } .
$$

We take the filtration

$$
\mathcal { F } _ { n } = \sigma ( \eta _ { 1 } , \dots , \eta _ { n } )
$$

and also put ${ \pmb \xi _ { 0 } } = { \bf 0 }$ and ${ \mathcal { F } } _ { 0 } = \{ \emptyset , \emptyset \}$ for notational simplicity.

If $\pmb { n } - 1$ rounds of the game have been played so far, your accumulated knowledge will be represented by the σ-field $\mathcal { F } _ { n - 1 }$ . The game is fair if

$$
E ( \xi _ { n } | \mathcal { F } _ { n - 1 } ) = \xi _ { n - 1 } ,
$$

that is, you expect that your fortune at step n will on average be the same as at step $\pmb { n } - 1$ . The game will be favourable to you if

$$
E ( \xi _ { n } | \mathcal { F } _ { n - 1 } ) \ge \xi _ { n - 1 } ,
$$

and unfavourable to you if

$$
E ( \xi _ { n } | \mathcal { F } _ { n - 1 } ) \leq \xi _ { n - 1 }
$$

for $n = 1 , 2 , \ldots$ . This corresponds to $\xi _ { n }$ being, respectively, a martingale, a submartingale, or a supermartingale with respect to $\mathcal { F } _ { n }$ , see Definitions 3.4 and 3.5.

Suppose that you can vary the stake to be $\alpha _ { n }$ in game n. (In particular, $\alpha _ { n }$ may be zero if you refrain from playing the nth game; it may even be negative if you own the casino and can accept other people's bets.) When the time comes to decide your stake ${ \pmb { \alpha _ { n } } }$ , you will know the outcomes of the first $n - 1$ games. Therefore it is reasonable to assume that $\alpha _ { n }$ is $\mathcal { F } _ { n - 1 } { \cdot } \mathrm { m e a s u r a b l e }$ , where $\mathcal { F } _ { n - 1 }$ represents your knowledge accumulated up to and including game $n \sim 1$ . In particular, since nothing is known before the first game, we take ${ \mathcal { F } } _ { 0 } = \{ \emptyset , \varOmega \}$

## Definition 3.6

A gambling strategy $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ (with respect to a filtration $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots )$ is a sequence of random variables such that $\alpha _ { n }$ is $\mathcal { F } _ { n - 1 }$ -measurable for each $\textbf { \em n } =$ $1 , 2 , \ldots ,$ where ${ \mathcal { F } } _ { 0 } = \{ \emptyset , \varOmega \}$ . (Outside the context of gambling such a sequence of random variables $\alpha _ { n }$ is called previsible.)

If you follow a strategy $\alpha _ { 1 } , \alpha _ { 2 } , \ldots ,$ then your total winnings after n games will be

$$
\begin{array} { r } { \zeta _ { n } = \alpha _ { 1 } \eta _ { 1 } + \cdot \cdot \cdot + \alpha _ { n } \eta _ { n } \qquad } \\ { = \alpha _ { 1 } \left( \xi _ { 1 } - \xi _ { 0 } \right) + \cdot \cdot \cdot + \alpha _ { n } \left( \xi _ { n } - \xi _ { n - 1 } \right) . \qquad } \end{array}
$$

We also put $\zeta _ { 0 } = 0$ for convenience.

The following proposition has important consequences for gamblers. It means that a fair game will always turn into a fair one, no matter which gambling strategy is used. If one is not in a position to wager negative sums of money (e.g. to run a casino), it will be impossible to turn an unfavourable game into a favourable one or vice versa. You cannot beat the system! The boundedness of the sequence $\alpha _ { n }$ means that your available capital is bounded and so is your credit limit.

## Proposition 3.1

Let $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ be a gambling strategy.

If $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ is a bounded sequence and $\xi _ { 0 } , \xi _ { 1 } , \xi _ { 2 } , \ldots$ is a martingale, then $\zeta _ { 0 } , \zeta _ { 1 } , \zeta _ { 2 } , \ldots$ is a martingale (a fair game turns into a fair one no matter what you do);

If $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ is a non-negative bounded sequence and $\xi _ { 0 } , \xi _ { 1 } , \xi _ { 2 } , \ldots$ is a supermartingale, then $\zeta _ { 0 } , \zeta _ { 1 } , \zeta _ { 2 } , . . .$ . is a supermartingale (an unfavourable game turns into an unfavourable one).

If $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ is a non-negative bounded sequence and $\xi _ { 0 } , \xi _ { 1 } , \xi _ { 2 } , \ldots$ is a submartingale, then $\zeta _ { 0 } , \zeta _ { 1 } , \zeta _ { 2 } , \ldots$ is a submartingale (a favourable game turns into a favourable one).

## Proof

Because $\alpha _ { n }$ and $\zeta _ { n - 1 }$ are $\mathcal { F } _ { n - 1 }$ -measurable, we can take them out of the expectation conditioned on $\mathcal { F } _ { n - 1 }$ ('taking out what is known', Proposition 2.4). Thus, we obtain

$$
\begin{array} { r } { E \left( \zeta _ { n } | \mathcal { F } _ { n - 1 } \right) = E \left( \zeta _ { n - 1 } + \alpha _ { n } \left( \xi _ { n } - \xi _ { n - 1 } \right) | \mathcal { F } _ { n - 1 } \right) } \\ { = \zeta _ { n - 1 } + \alpha _ { n } \left( E \left( \xi _ { n } | \mathcal { F } _ { n - 1 } \right) - \xi _ { n - 1 } \right) . } \end{array}
$$

If $\xi _ { n }$ is a martingale, then

$$
\begin{array} { r } { \alpha _ { n } \left( E \left( \xi _ { n } | \mathcal { F } _ { n - 1 } \right) - \xi _ { n - 1 } \right) = 0 , } \end{array}
$$

which proves assertion ). If $\xi _ { n }$ is a supermartingale and ${ \pmb { \alpha } } _ { \pmb { n } } \geq \bf { 0 }$ , then

$$
\begin{array} { r } { \alpha _ { n } \left( E \left( \xi _ { n } | \mathcal { F } _ { n - 1 } \right) - \xi _ { n - 1 } \right) \leq 0 , } \end{array}
$$

proving assertion Finally, assertion follows because

$$
\alpha _ { n } \left( E \left( \xi _ { n } | \mathcal { F } _ { n - 1 } \right) - \xi _ { n - 1 } \right) \geq 0
$$

if $\xi _ { n }$ is a submartingale and $\alpha _ { n } \geq 0 . \ \square$

## 3.5 Stopping Times

In roulette and many other games of chance one usually has the option to quit at any time. The nunber of rounds played before quitting the game will be denoted by t. It can be fixed, say, to be $\tau = 1 0$ if one decides in advance to stop playing after 10 rounds, no matter what happens. But in general the decision whether to quit or not will be made after each round depending on the knowledge accumulated so far. Therefore T is assumed to be a random variable with values in the set $\{ 1 , 2 , . . . \} \cup \{ \infty \}$ . Infinity is included to cover the theoretical possibility (and a dream scenario of some casinos) that the game never stops. At each step n one should be able to decide whether to stop playing or not, i.e. whether or not $\tau = n$ . Therefore the event that $\tau = n$ should be in the o-field $\mathcal { F } _ { n }$ representing our knowledge at time n. This gives rise to the following definition.

## Definition 3.7

A random variable  with values in the set $\{ 1 , 2 , . . . \} \cup \{ \infty \}$ is called a stopping time (with respect to a filtration $\mathcal { F } _ { n } )$ if for each $\ n = 1 , 2 , . . .$

$$
\{ \tau = n \} \in { \mathcal { F } } _ { n } .
$$

## Exercise 3.8

Show that the following conditions are equivalent:

1) $\{ \tau \leq n \} \in \mathcal { F } _ { n }$ for each $n = 1 , 2 , \ldots ;$

2 $\{ \tau = n \} \in { \mathcal F } _ { n }$ for each $n = 1 , 2 , \ldots .$

Hint Can you express $\{ \tau \leq n \}$ in terms of the events $\{ \tau = k \}$ , where ${ \pmb k } = { \bf 1 } , \dots , { \pmb n } ?$ Can you express $\bar { \{ \tau = n \} }$ in terms of the events $\{ \tau \leq k \}$ , where $\pmb { k } = \mathbf { 1 } , \dots , \pmb { n } ?$

## Example 3.5 (First hitting tme)

Suppose that a coin is tossed repeatedly and you win or lose £1, depending on which way it lands. Suppose that you start the game with, say, £5 in your pocket and decide to play until you have £10 or you lose everything. If $\xi _ { n }$ is the amount you have at step n, then the time when you stop the game is

$$
\tau = \displaystyle \operatorname* { m i n } \left\{ n : \xi _ { n } = 1 0 \mathrm { ~ o r ~ } 0 \right\} ,
$$

and is called the first hitting time (of 10 or 0 by the random sequence $\xi _ { n } )$ . It is a stopping time in the sense of Definition 3.7 with respect to the filtration

$\mathcal { F } _ { n } = \sigma \left( \xi _ { 1 } , \ldots , \xi _ { n } \right)$ . This is because

$$
\{ \tau = n \} = \{ 0 < \xi _ { 1 } < 1 0 \} \cap \cdots \cap \{ 0 < \xi _ { n - 1 } < 1 0 \} \cap \{ \xi _ { n } = 1 0 \mathrm { ~ o r ~ } 0 \} .
$$

Now each of the sets on the right-hand side belongs to Fn, so their intersection $\mathcal { F } _ { n }$ does too. This proves that

$$
\{ \tau = n \} \in { \mathcal F } _ { n }
$$

for each ${ \pmb n } ,$ so $\pmb { \tau }$ is a stopping time.

## Exercise 3.9

Let $\xi _ { n }$ be a sequence of random variables adapted to a filtration ${ \mathcal { F } } _ { n }$ and let $B \subset \mathbb { R }$ be a Borel set. Show that the time of first entry of ξn into B, $\xi _ { n }$

$$
\tau = \operatorname* { m i n } \left\{ n : \xi _ { n } \in B \right\}
$$

is a stopping time.

Hint Example 3.5 covers the case when $B = ( - \infty , 0 ] \cup [ 1 0 , \infty )$ . Extend the argument to an arbitrary Borel set B.

Let $\xi _ { n }$ be a sequence of random variables adapted to a filtration ${ \mathcal { F } } _ { n }$ and let τ be a stopping time (with respect to the same filtration). Suppose that $\pmb { \tau }$ $\xi _ { n }$ represents your winnings (or losses) after n rounds of a game. If you decide to quit after τ rounds, then your total winnings will be ξr. In this case your $\pmb { \tau }$ $\xi _ { \tau }$ winnings after n rounds will in fact be $\xi _ { T \wedge n }$ . Here a  b denotes the smaller of two numbers a and $\begin{array} { r } { \pmb { b } , } \end{array}$

$$
a \wedge b = \operatorname* { m i n } \left( a , b \right) .
$$

## Definition 3.8

We call $\xi _ { \tau \wedge n }$ the sequence stopped at . It is often denoted by $\xi _ { n } ^ { \tau }$ Thus, for each $\omega \in \Omega$

$$
\xi _ { n } ^ { \tau } ( \omega ) = \xi _ { \tau ( \omega ) \wedge n } ( \omega ) .
$$

## Exercise 3.10

Show that if ξn is a sequence of random variables adapted to a filtration $\xi _ { n }$ $\mathcal { F } _ { n }$ then so is the sequence $\xi _ { T \wedge n }$

Hint For any Borel set B express $\{ \xi , \mu _ { \sf } \in B \}$ in terms of the events $\{ \xi _ { k } \in B \}$ and $\{ \tau = k \}$ , where $k = 1 , \ldots , n$

We already know that it is impossible to turn a fair game into an unfair one, an unfavourable game into a favourable one, or vice versa using a gambling strategy. The next proposition shows that this cannot be achieved using a stopping time either (essentially, because stopping is also a gambling strategy).

## Proposition 3.2

Let t be a stopping time.

If $\xi _ { n }$ is a martingale, then so is $\xi _ { T \wedge n }$

If $\xi _ { n }$ is a supermartingale, then so is $\xi _ { T \wedge n }$

If $\xi _ { n }$ is a submartingale, then so is $\xi _ { T \wedge n }$

## Proof

This is in fact a consequence of Proposition 3.1. Given a stopping time $\tau ,$ we put

$$
\alpha _ { n } = { \left\{ \begin{array} { l l } { 1 \qquad { \mathrm { i f ~ } } \tau \geq n , } \\ { 0 \qquad { \mathrm { i f ~ } } \tau < n . } \end{array} \right. }
$$

We claim that $\alpha _ { n }$ is a gambling strategy (that is, $\alpha _ { n }$ is $\mathcal { F } _ { n - 1 } { \mathrm { - m e a s u r a b l e } } )$ . This is because the inverse image $\{ \alpha _ { n } \in B \}$ of any Borel set $B \subset \mathbb { R }$ is equal to

$$
\emptyset \in { \mathcal { F } } _ { n - 1 }
$$

if 0, 1 € B, or to

$$
\varOmega \in \mathcal F _ { n - 1 }
$$

if $0 , 1 \in B$ , or to

$$
\{ \alpha _ { n } = 1 \} = \{ \tau \geq n \} = \{ \tau > n - 1 \} \in \mathcal { F } _ { n - 1 }
$$

if ${ \bf 1 } \in { \bf B }$ and 0 ¢ B, or to

$$
\{ \alpha _ { n } = 0 \} = \{ \tau < n \} = \{ \tau \leq n - 1 \} \in \mathcal { F } _ { n - 1 }
$$

if 1 $\notin B$ and ${ \mathfrak { O } } \in B$ . For this gambling strategy

$$
\xi _ { \tau \wedge \eta } = \alpha _ { 1 } \left( \xi _ { 1 } - \xi _ { 0 } \right) + \cdot \cdot \cdot + \alpha _ { n } \left( \xi _ { n } - \xi _ { n - 1 } \right) .
$$

Therefore Proposition 3.1 implies assertions 1), 2) and 3) above.

## Example 3.6

(You could try to beat the system if you had unlimited capital and unlimited time.) The following gambling strategy is called 'the martingale'. (Do not confuse this with the general definition of a martingale earlier in this section.) Suppose a coin is flipped repeatedly. Let us denote the outcomes by $\eta _ { 1 } , \eta _ { 2 } , \ldots ,$ which can take values +1 (heads) or -1 (tails). You wager £1 on heads. If you win, you quit. If you lose, you double the stake and play again. If you win this time round, you quit. Otherwise you double the stake once more, and so on. Thus, your gambling strategy is

$$
\alpha _ { n } = \left\{ \begin{array} { l l } { 2 ^ { n - 1 } \quad } & { \mathrm { i f } \ \eta _ { 1 } = \cdot \cdot \cdot = \eta _ { n - 1 } = \mathrm { t a i l s } , } \\ { 0 \quad } & { \mathrm { o t h e r w i s e } . } \end{array} \right.
$$

Let us put

$$
\zeta _ { n } = \eta _ { 1 } + 2 \eta _ { 2 } + \cdot \cdot \cdot + 2 ^ { n - 1 } \eta _ { n }
$$

and consider the stopping time

$$
\tau = \operatorname* { m i n } \left\{ n : \eta _ { n } = \mathrm { h e a d s } \right\} .
$$

Then $\zeta _ { \tau \wedge n }$ will be your winnings after n rounds. It is a martingale (check it!). It can be shown that $P \left\{ \tau < \infty \right\} = 1$ (heads will eventually appear in the sequence $\eta _ { 1 } , \eta _ { 2 } , \ldots$ with probability one). Therefore it makes sense to consider $\zeta _ { \tau }$ .This would be your total winnings if you were able to continue to play the game no matter how long it takes for the first heads to appear. It would require unlimited time and capital. If you could afford these, you would be bound to win eventually because $\zeta _ { \tau } = 1$ identically, since

$$
- 1 - 2 - \cdot \cdot \cdot - 2 ^ { n - 1 } + 2 ^ { n } = 1
$$

for any n.

## Exercise 3.11

Show that if a gambler plays 'the martingale', his expected loss just before the ultimate win is infinite, that is,

$$
E \left( \zeta _ { \tau - 1 } \right) = - \infty .
$$

Hint What is the probability that the game will terminate at step ${ \pmb n } ,$ i.e. that $\tau = n ?$ ${ \textbf { I f } } { \boldsymbol { \tau } } = { \boldsymbol { n } }$ ,what is $\zeta _ { T - 1 }$ equal to? This will give you all possible values of $\zeta _ { \tau - 1 }$ and their probabilities. Now compute the expectation of $\zeta _ { \tau - 1 }$

## 3.6 Optional Stopping Theorem

If $\xi _ { n }$ is a martingale, then, in partr,

$$
E ( \xi _ { n } ) = E ( \xi _ { 1 } )
$$

for each n. Example 3.6 shows that $E ( \xi _ { \tau } )$ is not necessarily equal to $E ( \xi _ { 1 } )$ for a stopping time T. However, if the equality

$$
E ( \xi _ { \tau } ) = E ( \xi _ { 1 } )
$$

does hold, it can be very useful. The Optional Stopping Theorem provides sufficient conditions for this to happen.

## Theorem 3.1 (Optional Stopping Theorem)

Let $\xi _ { n }$ be a martingale and t a stopping time with respect to a filtration $\mathcal { F } _ { n }$ such that the following conditions hold:

1) $\tau < \infty \ \mathbf { a . s . } ,$

2) $\xi _ { \tau }$ is integrable,

3) $E ( \xi _ { n } 1 _ { \{ \tau > n \} } ) \to 0 { \mathrm { ~ a s ~ } } n \to \infty .$

Then

$$
E ( \xi _ { \tau } ) = E ( \xi _ { 1 } ) .
$$

Proof

Because

$$
\xi _ { \tau } = \xi _ { \tau \wedge n } + \left( \xi _ { \tau } - \xi _ { n } \right) 1 _ { \{ \tau > n \} } ,
$$

it follows that

$$
E ( \xi _ { \tau } ) = E ( \xi _ { \tau \wedge n } ) + E ( \xi _ { \tau } 1 _ { \{ \tau > n \} } ) - E ( \xi _ { n } 1 _ { \{ \tau > n \} } ) .
$$

Since $\xi _ { \tau \wedge n }$ is a martingale by Proposition 3.2, the first term on the right-hand side is equal to

$$
E ( \xi _ { \tau \wedge n } ) = E ( \xi _ { 1 } ) .
$$

The last term tends to zero by assumption 3). The middle term

$$
E ( \xi _ { \tau } 1 _ { \{ \tau > n \} } ) = \sum _ { k = n + 1 } ^ { \infty } E ( \xi _ { k } 1 _ { \{ \tau = k \} } )
$$

tends to zero as $n  \infty$ because the series

$$
E ( \xi _ { \tau } ) = \sum _ { k = 1 } ^ { \infty } E ( \xi _ { k } 1 _ { \{ \tau = k \} } )
$$

is convergent by 2). It follows that $E ( \xi _ { \tau } ) = E ( \xi _ { 1 } )$ , as required.

## Example 3.7 (Expectation of the first hitting time for a random walk)

Let $\xi _ { n }$ be a symmetric random walk as in Exercise 3.5 and let K be a positive integer. We define the first hitting time $( \circ \mathbf { f } \pm K$ by $\xi _ { n } )$ to be

$$
\tau = \operatorname* { m i n } \left\{ n : \left| \xi _ { n } \right| = K \right\} .
$$

By Exercise 3.9 T is a stopping time. By Exercise 3.5 we know that ${ \pmb { \xi } } _ { n } ^ { 2 } - n$ is a martingale. If the Optional Stopping Theorem can be applied, then

$$
E \left( \xi _ { \tau } ^ { 2 } - \tau \right) = E \left( \xi _ { 1 } ^ { 2 } - 1 \right) = 0 .
$$

This allows us to find the expectation

$$
E ( \tau ) = E ( \xi _ { \tau } ^ { 2 } ) = K ^ { 2 } ,
$$

since $| \xi _ { \tau } | = K$

Let us verify conditions 1)-3) of the Optional Stopping Theorem.

) We shall show that $P \left\{ \tau = \infty \right\} = 0 .$ . To this end we shall estimate $P \left\{ \tau > 2 K n \right\}$ . We can think of 2Kn tosses of a coin as n sequences of 2K tosses. A necessary condition for $\tau > 2 K n$ is that no one of these n sequences contains heads only. Therefore

$$
P \{ \tau > 2 K n \} \leq ( 1 - \frac { 1 } { 2 ^ { 2 K } } ) ^ { n }  0
$$

as $n  \infty$ Because $\left\{ \tau > 2 K n \right\}$ for ${ \pmb n = 1 , 2 , . . }$ . is a contracting sequence of sets (i.e. $\{ \tau > 2 K n \} \supset \{ \tau > 2 K ( n + 1 ) \} )$ , it follows that

$$
{ \begin{array} { r l } { P \left\{ \tau = \infty \right\} } & { = P \left( \bigcap _ { n = 1 } ^ { \infty } \left\{ \tau > 2 K n \right\} \right) } \\ & { = \underset { n \to \infty } { \operatorname* { l i m } } P \left\{ \tau > 2 K n \right\} = 0 , } \end{array} }
$$

completing the argument.

2) We need to show that

$$
E \left( \left| \xi _ { \tau } ^ { 2 } - \tau \right| \right) < \infty .
$$

Indeed,

$$
\begin{array} { r l } { E ( \tau ) = \displaystyle \sum _ { n = 1 } ^ { \infty } n P \left\{ \tau = n \right\} } & { } \\ & { = \displaystyle \sum _ { n = 0 } ^ { \infty } \sum _ { k = 1 } ^ { 2 K } \left( 2 K n + k \right) P \left\{ \tau = 2 K n + k \right\} } \\ & { \le \displaystyle \sum _ { n = 0 } ^ { \infty } \sum _ { k = 1 } ^ { 2 K } 2 K \left( n + 1 \right) P \left\{ \tau > 2 K n \right\} } \\ & { \le 4 K ^ { 2 } \displaystyle \sum _ { n = 0 } ^ { \infty } \left( n + 1 \right) \left( 1 - \frac { 1 } { 2 ^ { 2 K } } \right) ^ { n } } \\ & { < \infty , } \end{array}
$$

since the series $\textstyle \sum _ { n = 1 } ^ { \infty } ( n + 1 ) q ^ { n }$ is convergent for any $q \in ( - 1 , 1 )$ . Here we have recycled the estimate for $P \left\{ \tau > 2 K n \right\}$ used in 2). Moreover, $\xi _ { \tau } ^ { 2 } = K ^ { 2 }$ , so

$$
\begin{array} { l } { E \left( \left. \xi _ { \tau } ^ { 2 } - \tau \right. \right) \ \leq \ E \left( \xi _ { \tau } ^ { 2 } \right) + E \left( \tau \right) } \\ { \ = \ K ^ { 2 } + E ( \tau ) } \\ { \ < \ \infty . } \end{array}
$$

Since $\xi _ { n } ^ { 2 } \leq K ^ { 2 }$ on $\{ \tau > n \}$

$$
E ( \xi _ { n } ^ { 2 } 1 _ { \{ \tau > n \} } ) \leq K ^ { 2 } P \{ \tau > n \}  0
$$

as $n  \infty$ . Moreover,

$$
E \left( n 1 _ { \{ \tau > n \} } \right) \leq E \left( \tau 1 _ { \{ \tau > n \} } \right) \to 0
$$

as $n  \infty$ Convergence to 0 holds because $E \left( \tau \right) < \infty$ y and $\{ \tau > n \}$ is a contracting sequence of sets with intersection $\{ \tau = \infty \}$ of measure zero. It follows that

$$
E ( ( \xi _ { n } ^ { 2 } - n ) 1 _ { \{ \tau > n \} } )  0 ,
$$

as required.

## Exercise 3.12

Let $\xi _ { n }$ be a symmetric random walk and $\mathcal { F } _ { n }$ the filtration defined in Exercise 3.5. Denote by T the smallest n such that $| \xi _ { n } | = K$ as in Example 3.7. Verify that

$$
\zeta _ { n } = ( - 1 ) ^ { n } \cos \left[ \pi \left( \xi _ { n } + K \right) \right]
$$

is a martingale (see Exercise 3.6). Then show that $\zeta _ { n }$ and i satisfy the conditions of the Optional Stopping Theorem and apply the theorem to find $E [ ( - 1 ) ^ { \tau } ]$

Hint The equality $\zeta _ { \tau } = ( - 1 ) ^ { \tau }$ is a key to computing $E \left[ \left( - 1 \right) ^ { \tau } \right]$ with the aid of the Optimal Stopping Theorem. The first two conditions of this theorem are either obvious in the case in hand or have been verified elsewhere in this chapter. To make sure that condition 3) holds it may be helpful to show that

$$
\begin{array} { r } { \Big | E ( \zeta _ { n } 1 _ { \{ \tau > n \} } ) \Big | \le P \{ \tau > n \} . } \end{array}
$$

Use Jensen's inequality with convex function ${ \pmb \varphi } \left( { \pmb x } \right) = \left| { \pmb x } \right|$ to estimate the left-hand side. Do not forget to verify that $\zeta _ { n }$ is a martingale in the first place.

## 3.7 Solutions

## Solution 3.1

A belongs to $\mathcal { F } _ { 1 1 }$ , but not to $\mathcal { F } _ { 1 0 }$ . The smallest n is 11.

B does not belong to ${ \mathcal { F } } _ { n }$ for any n. There is no smallest n such that $B \in { \mathcal { F } } _ { n }$ C belongs to $\mathcal { F } _ { 1 0 0 }$ , but not to $\mathcal { F } _ { 9 9 }$ . The smallest n is 100.

Since $D = \emptyset$ , it belongs to ${ \mathcal { F } } _ { n }$ for each $n = 1 , 2 , \ldots$ Here the smallest n is 1.

## Solution 3.2

Because the sequence of random variables $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is adapted to the filtration $\mathcal { G } _ { 1 } , \mathcal { G } _ { 2 } , \ldots$ , it follows that $\xi _ { n }$ is $\scriptstyle { \mathcal { G } } _ { n }$ -measurable for each n. But

$$
{ \mathcal { G } } _ { 1 } \subset { \mathcal { G } } _ { 2 } \subset \cdots ~ ,
$$

so $\xi _ { 1 } , \ldots , \xi _ { n }$ are $\boldsymbol { \mathcal { G } } _ { n }$ -measurable for each n. As a consequence,

$$
\mathcal { F } _ { n } = \sigma ( \xi _ { 1 } , \ldots , \xi _ { n } ) \subset \mathcal { G } _ { n }
$$

for each n.

## Solution 3.3

Taking the expectation on both sides of the equality

$$
\xi _ { n } = E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) ,
$$

we obtain

$$
E ( \xi _ { n } ) = E ( E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) ) = E ( \xi _ { n + 1 } )
$$

for each n. This proves the claim.

## Solution 3.4

The random variables $\xi _ { n }$ are integrable because $\xi _ { n }$ is a martingale with respect

to $\mathcal { F } _ { n }$ .Since $\mathcal { G } _ { n }$ is the o-field generated by $\xi _ { 1 } , \ldots , \xi _ { n }$ , it follows that $\xi _ { n }$ is adapted to $\beta _ { n }$ . Finally, since $\mathcal { G } _ { n } \subset \mathcal { F } _ { n }$ 7

$$
\begin{array} { r c l } { \xi _ { n } } & { = } & { E ( \xi _ { n } | \mathcal { G } _ { n } ) } \\ & { = } & { E ( E ( \xi _ { n + 1 } | \mathcal { F } _ { n } ) | \mathcal { G } _ { n } ) } \\ & { = } & { E ( \xi _ { n + 1 } | \mathcal { G } _ { n } ) } \end{array}
$$

by the tower property of conditional expectation (Proposition 2.4). This proves that $\xi _ { n }$ is a martingale with respect to $\mathcal { G } _ { n }$

## Solution 3.5

Because

$$
\xi _ { n } ^ { 2 } - n = \left( \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } \right) ^ { 2 } - n
$$

is a function of $\eta _ { 1 } , \ldots , \eta _ { n } .$ , it is measurable with respect to the o-field ${ \mathcal { F } } _ { \mathfrak { n } }$ generated by $\eta _ { 1 } , \ldots , \eta _ { n } ,$ i.e. ${ \pmb { \xi } } _ { n } ^ { 2 } - n$ is adapted to $\mathcal { F } _ { n }$ . Since

$$
| \xi _ { n } | = | \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } | \leq | \eta _ { 1 } | + \cdot \cdot \cdot | \eta _ { n } | = n ,
$$

it follows that

$$
\begin{array} { r } { E ( | \xi _ { n } ^ { 2 } - n | ) \le E ( \xi _ { n } ^ { 2 } ) + n \le n ^ { 2 } + n < \infty , } \end{array}
$$

so ${ \pmb \xi } _ { n } ^ { 2 } - n$ is integrable for each n. Because

$$
\xi _ { n + 1 } ^ { 2 } = \eta _ { n + 1 } ^ { 2 } + 2 \eta _ { n + 1 } \xi _ { n } + \xi _ { n } ^ { 2 } ,
$$

where $\xi _ { n }$ and $\pmb { \xi } _ { n } ^ { 2 }$ are $\mathcal { F } _ { n }$ -measurable and $\eta _ { n + 1 }$ is independent of $\mathcal { F } _ { n } .$ ,we can use Proposition 2.4 ('taking out what is known' and 'independent condition drops out') to obtain

$$
\begin{array} { r l } & { E ( \xi _ { n + 1 } ^ { 2 } | \mathcal { F } _ { n } ) = E ( \eta _ { n + 1 } ^ { 2 } | \mathcal { F } _ { n } ) + 2 E ( \eta _ { n + 1 } \xi _ { n } | \mathcal { F } _ { n } ) + E ( \xi _ { n } ^ { 2 } | \mathcal { F } _ { n } ) } \\ & { = E ( \eta _ { n + 1 } ^ { 2 } ) + 2 \xi _ { n } E ( \eta _ { n + 1 } ) + \xi _ { n } ^ { 2 } } \\ & { = 1 + \xi _ { n } ^ { 2 } . } \end{array}
$$

This implies that

$$
E ( \xi _ { n + 1 } ^ { 2 } - n - 1 | \mathcal { F } _ { n } ) = \xi _ { n } ^ { 2 } - n ,
$$

SO $\xi _ { n } ^ { 2 } - n$ is a martingale.

## Solution 3.6

Being a function of $\xi _ { n }$ , the random variable $\zeta _ { n }$ is $\mathcal { F } _ { \mathfrak { n } }$ -measurable for each $\pmb { n } _ { \pmb { \imath } }$

since $\xi _ { n }$ is. Because $| \zeta _ { n } | \leq 1$ , it is clear that $\zeta _ { n }$ is integrable. Because $\eta _ { n + 1 }$ is independent of $\mathcal { F } _ { n }$ and $\xi _ { n }$ is $\mathcal { F } _ { n } .$ -measurable, it follows that

$$
\begin{array} { r l } { E ( \zeta _ { n + 1 } | \mathcal { F } _ { n } ) } & { = E \left( ( - 1 ) ^ { n + 1 } \cos [ \pi ( \xi _ { n } + \eta _ { n + 1 } ) ] | \mathcal { F } _ { n } \right) } \\ & { = \left( - 1 \right) ^ { n + 1 } E \left( \cos \left( \pi \xi _ { n } \right) \cos \left( \pi \eta _ { n + 1 } \right) | \mathcal { F } _ { n } \right) } \\ & { \quad - ( - 1 ) ^ { n + 1 } E \left( \sin \left( \pi \xi _ { n } \right) \sin \left( \pi \eta _ { n + 1 } \right) | \mathcal { F } _ { n } \right) } \\ & { = \left( - 1 \right) ^ { n + 1 } \cos \left( \pi \xi _ { n } \right) E \left( \cos \left( \pi \eta _ { n + 1 } \right) \right) } \\ & { \quad - ( - 1 ) ^ { n + 1 } \sin \left( \pi \xi _ { n } \right) E \left( \sin \left( \pi \eta _ { n + 1 } \right) \right) } \\ & { = \left( - 1 \right) ^ { n } \cos \left( \pi \xi _ { n } \right) } \\ & { = \zeta _ { n } , } \end{array}
$$

using the formula

$$
\cos \left( \alpha + \beta \right) = \cos \alpha \cos \beta - \sin \alpha \sin \beta .
$$

To compute $E \left( \cos { \left( \pi \eta _ { n + 1 } \right) } \right)$ and $E \left( \sin { \left( \pi \eta _ { n + 1 } \right) } \right)$ observe that $\eta _ { n + 1 } = 1 \mathrm { o r } - 1$ and

$$
\begin{array} { l } { \cos \pi \ : = \ : \cos \left( - \pi \right) = - 1 , } \\ { \sin \pi \ : = \ : \sin \left( - \pi \right) = 0 . } \end{array}
$$

It follows that ζn is a martingale with respect to the filtration $\zeta _ { n }$ $\mathcal { F } _ { n }$

## Solution 3.7

If $\xi _ { n }$ is adapted to $\mathcal { F } _ { n }$ , then so is $\pmb { \xi } _ { n } ^ { 2 }$ Since $\xi _ { n } = E \left( \xi _ { n + 1 } | \mathcal { F } _ { n } \right)$ for each n and $\varphi \left( x \right) = x ^ { 2 }$ is a convex function, we can apply Jensen's inequality (Theorem 2.2) to obtain

$$
\begin{array} { r } { \xi _ { n } ^ { 2 } = [ E \left( \xi _ { n + 1 } | \mathcal { F } _ { n } \right) ] ^ { 2 } \leq E \left( \xi _ { n + 1 } ^ { 2 } | \mathcal { F } _ { n } \right) } \end{array}
$$

for each n. This means that ξ2 is a submartingale with respect to $\pmb { \xi } _ { n } ^ { 2 }$ ${ \mathcal { F } } _ { n }$

## Solution 3.8

1)⇒2). If τ has property 1), then

$$
\{ \tau \leq n \} \in \mathcal { F } _ { n }
$$

and

$$
\left\{ \tau \leq n - 1 \right\} \in \mathcal { F } _ { n - 1 } \subset \mathcal { F } _ { n } ,
$$

so

$$
\{ \tau = n \} = \{ \tau \leq n \} \setminus \{ \tau \leq n - 1 \} \in \mathcal { F } _ { n } .
$$

2)⇒1). If τ has property 2), then

$$
\{ \tau = k \} \in { \mathcal { F } } _ { k } \subset { \mathcal { F } } _ { n }
$$

for each $k = 1 , \ldots , n$ Therefore

$$
\{ \tau \leq n \} = \{ \tau = 1 \} \cup \cdots \cup \{ \tau = n \} \in \mathcal { F } _ { n } .
$$

Solution 3.9

If

$$
r = \operatorname* { m i n } \left\{ n : \xi _ { n } \in B \right\} ,
$$

then for any n

$$
\{ \tau = n \} = \{ \xi _ { 1 } \notin B \} \cap \cdots \cap \{ \xi _ { n - 1 } \notin B \} \cap \{ \xi _ { n } \in B \} .
$$

Because B is a Borel set, each of the sets on the right-hand side belongs to the σ-field $\mathcal { F } _ { n } = \sigma \left( \xi _ { 1 } , \ldots , \xi _ { n } \right)$ , and their intersection does too. This proves that $\{ \tau = n \} \in { \mathcal { F } } _ { n }$ for each n, so t is a stopping time.

## Solution 3.10

Let $B \subset \mathbb { R }$ be a Borel set. We can write

$$
\left\{ \xi _ { \tau \wedge n } \in B \right\} = \left\{ \xi _ { n } \in B , \tau > n \right\} \cup \bigcup _ { k = 1 } ^ { n } \left\{ \xi _ { k } \in B , \tau = k \right\} ,
$$

where

$$
\{ \xi _ { n } \in B , \tau > n \} = \{ \xi _ { n } \in B \} \cap \{ \tau > n \} \in \mathcal { F } _ { n }
$$

and for each $k = 1 , \ldots , n$

$$
\left\{ \xi _ { k } \in B , \tau = k \right\} = \left\{ \xi _ { k } \in B \right\} \cap \left\{ \tau = k \right\} \in \mathcal { F } _ { k } \subset \mathcal { F } _ { n } .
$$

It follows that for each n

$$
\left\{ \xi _ { T \wedge n } \in B \right\} \in { \mathcal { F } } _ { n } ,
$$

as required.

## Solution 3.11

The probability that 'the martingale' terminates at step n is

$$
P \left\{ \tau = n \right\} = { \frac { 1 } { 2 ^ { n } } } \cdot
$$

$( n - 1$ tails followed by heads at step n). Therefore

$$
\begin{array} { l } { E \left( \zeta _ { \tau - 1 } \right) = \displaystyle \sum _ { n = 1 } ^ { \infty } \zeta _ { n - 1 } P \left\{ \tau = n \right\} } \\ { = \displaystyle \sum _ { n = 1 } ^ { \infty } \left( - 1 - 2 - \cdot \cdot \cdot - 2 ^ { n - 2 } \right) \frac { 1 } { 2 ^ { n } } } \\ { = - \displaystyle \sum _ { n = 1 } ^ { \infty } \frac { 2 ^ { n - 1 } - 1 } { 2 ^ { n } } = - \infty . } \end{array}
$$

## Solution 3.12

The proof that ζn is a martingale is almost the same as in Exercise 3.6. We $\zeta _ { n }$ need to verify that (n and 7 satisfy conditions 1)-3) of the Optional Stopping $\zeta _ { n }$ Theorem.

Condition 1) has in fact been verified in Example 3.7.

Condition 2) holds because $| \zeta _ { \tau } | \le 1$ , so $E ( | \zeta _ { \tau } | ) \leq 1 < \infty$

To verify condition 3) observe that $| \zeta _ { n } | \leq 1$ for all n, so

$$
\begin{array} { r l } & { \left| E ( \zeta _ { n } 1 _ { \{ \tau > n \} } ) \right| \ \leq \ E ( | \zeta _ { n } | 1 _ { \{ \tau > n \} } ) } \\ & { \leq \ E ( 1 _ { \{ \tau > n \} } ) } \\ & { = \ P \{ \tau > n \} . } \end{array}
$$

The family of events $\{ \tau > n \} , n = 1 , 2 , . .$ is a contracting one with intersection $\{ \tau = \infty \}$ . It follows that

$$
\left| E ( \zeta _ { n } 1 _ { \{ \tau > n \} } ) \right| \le P \{ \tau > n \} \searrow { P \{ \tau = \infty \} }
$$

as $n  \infty$ .But

$$
P \{ \tau = \infty \} = 0
$$

by 1), completing the proof.

The Optional Stopping Theorem implies that

$$
E \left( \zeta _ { \tau } \right) = E \left( \zeta _ { 1 } \right)
$$

Because $\xi _ { \tau } = K$ or -K, we have

$$
\zeta _ { \tau } = ( - 1 ) ^ { \tau } \cos [ \pi ( K + \xi _ { \tau } ) ] = ( - 1 ) ^ { \tau } .
$$

Let us compute

$$
\begin{array} { l } { { E \left( \zeta _ { 1 } \right) = - \displaystyle \frac { 1 } { 2 } \left( \cos \left[ \pi \left( 1 + K \right) \right] + \cos \left[ \pi \left( - 1 + K \right) \right] \right) } } \\ { { \mathrm { } = \cos \left( \pi K \right) = \left( - 1 \right) ^ { K } . } } \end{array}
$$

It follows that

$$
E [ ( - 1 ) ^ { \tau } ] = \left( - 1 \right) ^ { \kappa } .
$$