# Martingale Inequalities and Convergence

Results on the convergence of martingales provide an insight into their structure and have a multitude of applications. They also provide an important interpretation of martingales. Namely, it turns out that a large class of martingales can be represented in the form

$$
\xi _ { n } = E \left( \xi \vert \mathcal { F } _ { n } \right) ,\tag{4.1}
$$

where $\xi = \operatorname* { l i m } _ { n } \xi _ { n }$ is an integrable random variable and $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots$ is the filtration generated by $\xi _ { 1 } , \xi _ { 2 } , \ldots$ , see Theorem 4.4 below. This makes it possible to think of $\xi _ { 1 } , \xi _ { 2 } , \ldots$ as the results of a series of imperfect observations of some random quantity ξ. As n increases, the accumulated knowledge $\mathcal { F } _ { n }$ about ${ \pmb \xi }$ increases and $\xi _ { n }$ becomes a better approximation, approaching the observed quantity ${ \pmb \xi }$ in the limit.

We shall begin with a few classical inequalities for martingales, known as the Doob inequalities. They provide the tools we shall need to study the convergence of martingales and, later on, the properties of stochastic integrals. Then we shall present a classical result known as Doob's Martingale Convergence Theorem, which provides the limit $\operatorname* { l i m } _ { n } \xi _ { n }$ of a martingale. However, Doob's theorem has one inconvenient feature. It guarantees only that $\xi _ { n }$ converges a.s., even though the limit is known to be an integrable random variable. However, to obtain (4.1) we need convergence in L1, which gives rise to a condi- $L ^ { 1 }$ tion called uniform integrability. This condition and its consequences, including (4.3), will be studied in the second section. Finally, as an example of an application reaching beyond the theory of martingales, we present an elegant proof of Kolmogorov's 0-1 law.

## 4.1 Doob's Martingale Inequalities

## Proposition 4.1 (Doob's maximal inequality)

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a non-negative submartingale (with respect to a filtration $\mathcal { F } _ { n } )$ ). Then for any $\lambda > 0$

$$
\lambda P \left( \underset { k \leq n } { \operatorname* { m a x } } \xi _ { k } \geq \lambda \right) \leq E \left( \xi _ { n } 1 _ { \left\{ \operatorname* { m a x } _ { k \leq n } \xi _ { k } \geq \lambda \right\} } \right) ,
$$

where ${ \mathfrak { 1 } } _ { A }$ is the characteristic function of a set A.

Proof

We put $\xi _ { n } ^ { * } = \operatorname* { m a x } _ { k \le n } \xi _ { k }$ for brevity. For $\lambda > 0$ let us define

$$
\tau = \operatorname* { m i n } \left\{ k \leq n : \xi _ { k } \geq \lambda \right\} ,
$$

if there is a $k \leq n$ such that $\xi _ { k } \ge \lambda$ , and $\tau = n$ otherwise. Then $\pmb { \tau }$ is a stopping time such that $\tau \leq n \mathrm { ~ a . s ~ }$ Since $\xi _ { n }$ is a submartingale,

$$
E ( \xi _ { n } ) \geq E ( \xi _ { \tau } ) .
$$

But

$$
\begin{array} { r } { E ( \xi _ { \tau } ) = E \left( \xi _ { \tau } 1 _ { \{ \xi _ { n } ^ { * } \geq \lambda \} } \right) + E \left( \xi _ { \tau } 1 _ { \{ \xi _ { n } ^ { * } < \lambda \} } \right) . } \end{array}
$$

Observe that if $\xi _ { n } ^ { * } \geq \lambda$ , then $\xi _ { \tau } \ge \lambda$ .Moreover, if $\xi _ { n } ^ { * } < \lambda$ , then $\tau = n$ , and so $\xi _ { \tau } = \xi _ { n }$ . Therefore

$$
\begin{array} { r } { E ( \xi _ { n } ) \ge E ( \xi _ { \tau } ) \ge \lambda P \left( \xi _ { n } ^ { * } \ge \lambda \right) + E \left( \xi _ { n } 1 _ { \{ \xi _ { n } ^ { * } < \lambda \} } \right) , } \end{array}
$$

It follows that

$$
\lambda P \left( \xi _ { n } ^ { * } \geq \lambda \right) \leq E \left( \xi _ { n } \right) - E \left( \xi _ { n } 1 _ { \{ \xi _ { n } ^ { * } < \lambda \} } \right) = E \left( \xi _ { n } 1 _ { \{ \xi _ { n } ^ { * } \geq \lambda \} } \right) ,
$$

completing the proof.

## Theorem 4.1 (Doob's maximal $L ^ { 2 }$ inequality)

If $\xi _ { n } , n \in \mathbb { N } ,$ is a non-negative square integrable submartingale (with respect to a filtration $\mathcal { F } _ { n } )$ , then

$$
E \left| \operatorname* { m a x } _ { k \leq n } \xi _ { k } \right| ^ { 2 } \leq 4 E | \xi _ { n } | ^ { 2 } .\tag{4.2}
$$

## Proof

Put $\xi _ { n } ^ { * } = \operatorname* { m a x } _ { k \le n } \xi _ { k }$ . By Exercise 1.9, Proposition 4.1, the Fubini theorem and finally the Cauchy-Schwarz inequality

$$
\begin{array} { l } { E \left| \xi _ { n } ^ { * } \right| ^ { 2 } = 2 \displaystyle \int _ { 0 } ^ { \infty } t P \left( \xi _ { n } ^ { * } > t \right) d t \leq 2 \displaystyle \int _ { 0 } ^ { \infty } E \left( \xi _ { n } 1 _ { \left\{ \xi _ { n } ^ { * } \geq t \right\} } \right) d t } \\ { = 2 \displaystyle \int _ { 0 } ^ { \infty } \left( \displaystyle \int _ { \left\{ \xi _ { n } ^ { * } \geq t \right\} } \xi _ { n } d P \right) d t = 2 \displaystyle \int _ { \Omega } \xi _ { n } \left( \displaystyle \int _ { 0 } ^ { \xi _ { n } ^ { * } } d t \right) d P } \\ { = 2 \displaystyle \int _ { \Omega } \xi _ { n } \xi _ { n } ^ { * } d P = 2 E \left( \xi _ { n } \xi _ { n } ^ { * } \right) \leq 2 \left( E \left| \xi _ { n } \right| ^ { 2 } \right) ^ { 1 / 2 } \left( E \left| \xi _ { n } ^ { * } \right| ^ { 2 } \right) ^ { 1 / 2 } . } \end{array}
$$

Dividing by $\left( E \left| \xi _ { n } ^ { * } \right| ^ { 2 } \right) ^ { 1 / 2 }$ , we get (4.2).

The proof of Doob's Convergence Theorem in the next section hinges on an inequality involving the number of upcrossings.

## Definition 4.1

Given an adapted sequence of random variables $\xi _ { 1 } , \xi _ { 2 } , \ldots$ . and two real numbers $a < b$ , we define a gambling strategy $\alpha _ { 1 } , \alpha _ { 2 } , \ldots$ by putting

$$
\alpha _ { 1 } = 0
$$

and for $n = 1 , 2 , . . .$

$$
\alpha _ { n + 1 } = { \left\{ \begin{array} { l l } { 1 \qquad { \mathrm { i f ~ } } \alpha _ { n } = 0 { \mathrm { ~ a n d ~ } } \xi _ { n } < a , } \\ { 1 \qquad { \mathrm { i f ~ } } \alpha _ { n } = 1 { \mathrm { ~ a n d ~ } } \xi _ { n } \leq b , } \\ { 0 \qquad { \mathrm { o t h e r w i s e } } . } \end{array} \right. }
$$

It will be called the upcrossings strategy. Each $\pmb { k } = \mathbf { 1 } , \mathbf { 2 } , \dots$ such that $\alpha _ { k } = 1$ and $\alpha _ { k + 1 } = 0$ will be called an upcrossing of the interval $[ a , b ]$ . The upcrossings form a (finite or infinite) increasing sequence

$$
u _ { 1 } < u _ { 2 } < \cdots .
$$

The number of upcrossings made up to time n, that is, the largest k such that $u _ { k } \le n$ will be denoted by $U _ { n } [ a , b ]$ (we put $U _ { n } \{ a , b \} = 0$ if no such k exists).

The meaning of the above definition is this. Initially, we refrain from playing the game and wait until $\xi _ { n }$ becomes less than a. As soon as this happens, we start playing unit stakes at each round of the game and continue until $\xi _ { n }$ becomes greater than b. At this stage we refrain from playing again, wait until $\xi _ { n }$ becomes less than ${ \pmb a } ,$ and so on. The strategy $\pmb { \alpha _ { n } }$ is defined in such a way that $\alpha _ { n } \ = \ 0$ whenever we refrain from playing the nth game, and $\alpha _ { n } = 1$ otherwise. During each run of consecutive games with $\alpha _ { n } = 1$ the process $\xi _ { n }$ crosses the interval $[ a , b ]$ , starting below a and finishing above b. This is what is meant by an upcrossing. Observe that each upcrossing will increase our total winnings by at least ${ \pmb b } - { \pmb a }$ For convenience, we identify each upcrossing with its last step k, such that $\alpha _ { k } = 1$ and $\alpha _ { k + 1 } = 0$ . A typical sample path of the upcrossings strategy is shown in Figure 4.1.

## Exercise 4.1

Verify that the upcrossings strategy $\alpha _ { n }$ is indeed a gambling strategy.

Hint You want to prove that $\pmb { \alpha _ { n } }$ is $\mathcal { F } _ { n - 1 }$ -measurable for each n. Since the upcrossings strategy is defined by induction, a proof by induction on n may be your best bet.

## Lemma 4.1 (Upcrossings Inequality

If $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is a supermartingale and $a < b$ , then

$$
( b - a ) E ( U _ { n } [ a , b ] ) \leq E ( ( \xi _ { n } - a ) ^ { - } ) .
$$

By $\pmb { x } ^ { - }$ we denote the negative part of a real number ${ \pmb x } ,$ i.e. $\pmb { x } ^ { - } = \operatorname* { m a x } \left\{ 0 , - \pmb { x } \right\}$

Proof

Let

$$
\zeta _ { n } = \alpha _ { 1 } \left( \xi _ { 1 } - \xi _ { 0 } \right) + \cdot \cdot \cdot + \alpha _ { n } \left( \xi _ { n } - \xi _ { n - 1 } \right)
$$

be the total winnings at step $\pmb { n = 1 , 2 , . . . }$ if the upcrossings strategy is followed, see Figure 4.1. It will be convenient to put $\zeta _ { 0 } = 0$ By Proposition 3.1 (one cannot beat the system using a gambling strategy) $\zeta _ { n }$ is a supermartingale.

Let us fix an n and put $k = U _ { n } [ a , b ]$ , so that

$$
0 < u _ { 1 } < u _ { 2 } < \cdots < u _ { k } \leq n
$$

Clearly, each upcrossing increases the total winnings by $\pmb { b } - \pmb { a }$ 7

$$
\zeta _ { u _ { i } } - \zeta _ { u _ { i - 1 } } \geq b - a
$$

for $i = 1 , \ldots , k .$ (We put ${ \pmb u } _ { \bf 0 } = { \bf 0 }$ for simplicity.) Moreover,

$$
\zeta _ { n } - \zeta _ { u _ { k } } \geq - \left( \xi _ { n } - a \right) ^ { - } .
$$

It follows that

$$
\begin{array} { r } { \zeta _ { n } \geq ( b - a ) U _ { n } [ a , b ] - ( \xi _ { n } - a ) ^ { - } . } \end{array}
$$

<!-- image-->  
Figure 4.1. Typical paths of $\xi _ { n } , \alpha _ { n }$ and $\zeta _ { n } ;$ upcrossings are indicated by bold lines

taking the expectation on both sides, we get

$$
\begin{array} { r } { E \left( \zeta _ { n } \right) \geq ( b - a ) E ( U _ { n } [ a , b ] ) - E ( ( \xi _ { n } - a ) ^ { - } ) . } \end{array}
$$

But $\zeta _ { n }$ is a supermartingale, so

$$
0 = E ( \zeta _ { 1 } ) \geq E \left( \zeta _ { n } \right) ,
$$

which proves the Upcrossings Inequality.

## 4.2 Doob's Martingale Convergence Theorem

## Theorem 4.2 (Doob's Martingale Convergence Theorem)

Suppose that $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is a supermartingale (with respect to a filtration $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots )$ such that

$$
\operatorname* { s u p } _ { n } E \left( \left| \xi _ { n } \right| \right) < \infty .
$$

Then there is an integrable random variable ξ such that

$$
\operatorname* { l i m } _ { n  \infty } \xi _ { n } = \xi \quad \mathsf { a . s . }
$$

## Remark 4.1

In particular, the theorem is valid for martingales because every martingale is a supermartingale. It is also valid for submartingales, since $\xi _ { n }$ is a submartingale if and only if $- \xi _ { n }$ is a supermartingale.

## Remark 4.2

Observe that even though all the $\xi _ { n }$ as well as the limit ξ are integrable random variables, it is claimed only that $\xi _ { n }$ trends to ξ a.s. Note that no convergence in $L ^ { 1 }$ is asserted.

## Proof (of Doob's Martingale Convergence Theorem)

By the Upcrossings Inequality

$$
E \left( U _ { n } [ a , b ] \right) \leq \frac { E \left( \left( \xi _ { n } - a \right) ^ { - } \right) } { b - a } \leq \frac { M + \left| a \right| } { b - a } < \infty ,
$$

where

$$
M = \operatorname* { s u p } _ { n } E \left( \left| \xi _ { n } \right| \right) < \infty .
$$

Since $U _ { n } [ \pmb { a } , \pmb { b } ]$ is a non-decreasing sequence, it follows that

$$
E \left( \operatorname* { l i m } _ { n \to \infty } U _ { n } [ a , b ] \right) = \operatorname* { l i m } _ { n \to \infty } E \left( U _ { n } [ a , b ] \right) \leq \frac { M + | a | } { b - a } < \infty .
$$

This implies that

$$
P \{ \operatorname* { l i m } _ { n  \infty } U _ { n } [ a , b ] < \infty \} = 1 .
$$

for any $a < b$ Since the set of all pairs of rational numbers $a < b$ is countable, the event

$$
A = \bigcap _ { a < b { \mathrm { ~ r a t i o n a l } } } \left\{ \operatorname* { l i m } _ { n \to \infty } U _ { n } [ a , b ] < \infty \right\}\tag{4.3}
$$

has probability 1. (The intersection of countably many events has probability 1 if each of these events has probability 1.)

We claim that the sequence $\xi _ { n }$ converges a.s. to a limit ξ. Consider the set

$$
B = \{ \operatorname* { l i m } _ { n } { \mathrm { i n f } } \xi _ { n } < \operatorname* { l i m } _ { n } { \mathrm { s u p } } \xi _ { n } \} \subset \Omega
$$

on which the sequence $\xi _ { n }$ fails to converge. Then for any $\omega \in B$ there are rational numbers ${ \pmb a } , { \pmb b }$ such that

$$
\operatorname* { l i m } _ { n } \operatorname* { i n f } \xi _ { n } ( \omega ) < a < b < \operatorname* { l i m } _ { n } \operatorname* { s u p } _ { } \xi _ { n } ( \omega ) ,
$$

implying that $\begin{array} { r } { \operatorname* { l i m } _ { n  \infty } U _ { n } [ a , b ] ( \omega ) = \infty } \end{array}$ . This means that B and the event A in (4.3) are disjoint, so $P ( B ) = \mathbf { 0 }$ , since $P ( A ) = 1$ , which proves the claim.

It remains to show that the limit ξ is an integrable random variable. By Fatou's lemma

$$
\begin{array} { r l } { E \left( | \xi | \right) } & { = \displaystyle E \left( \operatorname* { l i m } _ { n } \operatorname* { i n f } | \xi _ { n } | \right) } \\ & { \leq \displaystyle \operatorname* { l i m } _ { n } \operatorname* { i n f } E \left( | \xi _ { n } | \right) } \\ & { < \displaystyle \operatorname* { s u p } _ { n } E \left( | \xi _ { n } | \right) < \infty . } \end{array}
$$

This completes the proof.

## Exercise 4.2

Show that if ξn is a non-negative supermartingale, then it converges a.s. to an $\xi _ { n }$ integrable random variable.

Hint To apply Doob's Theorem all you need to verify is that the sequence $\xi _ { n }$ is bounded in $\mathbf { \bar { L } } ^ { \mathbf { f } }$ , i.e. the supremum of $\dot { \boldsymbol E } ( | \boldsymbol \xi _ { n } | )$ is less than ∞.

## 4.3 Uniform Integrability and ${ { \pmb L } ^ { 1 } }$ Convergence of Martingales

The conditions of Doob's theorem imply pointwise $\left( \mathsf { a . s . } \right)$ convergence of martingales. In this section we shall study convergence in L1. To this end we introduce $L ^ { 1 }$ a stronger condition called uniform integrability. Proposition 4.2 shows that it is a necessary condition for L1 convergence. In Theorem 4.2 we prove that uni- $L ^ { 1 }$ form integrability is in fact sufficient for a martingale to converge in $L ^ { 1 }$ . This enables us to show that each integrable martingale is of the form $E \left( \xi | \mathcal { F } _ { n } \right)$ . As an application we give a martingale proof of Kolmogorov's 0-1 law.

## Exercise 4.3

Show that a random variable ξ is integrable if and only if for every $\varepsilon > 0$ there exists an $M > 0$ such that

$$
\int _ { \{ | \xi | > M \} } | \xi | \ d P < \varepsilon .
$$

Hint Split Ω into two sets: $\{ | \pmb { \xi } | > M \}$ and $\{ | \xi | \le M \}$ . The integrals of $| \pmb { \xi } |$ over these sets must add up to ${ \pmb E } ( | { \pmb \xi } | )$ As M ieases   he integrls ncease, hi other one decreases. Investigate their limits as $M \to \infty$

Thus, for any sequence $\xi _ { n }$ of integrable random variables and any $\varepsilon > 0$ there is a sequence of numbers $M _ { n } > 0$ such that

$$
\int \limits _ { \{ | \xi _ { n } | > M _ { n } \} } | \xi _ { n } | \ d P < \varepsilon .
$$

If the $M _ { n }$ are independent of n, then we say that the sequence $\xi _ { n }$ is uniformly integrable.

## Definition 4.2

A sequence $\xi _ { 1 } , \xi _ { 2 } , . . .$ . of random variables is called uniformly integrable if for every $\varepsilon > 0$ there exists an $M > 0$ such that

$$
\int \limits _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | \ d P < \varepsilon
$$

for all $n = 1 , 2 , \ldots .$

## Exercise 4.4

Let $\varOmega = [ 0 , 1 ]$ with the σ-field of Borel sets and Lebesgue measure. Take

$$
\xi _ { n } = n \mathbf { 1 } _ { ( 0 , \frac { 1 } { n } ) } .
$$

Show that the sequence $\xi _ { 1 } , \xi _ { 2 } , \ldots$ is not uniformly integrable.

Hint What is the integral of ${ \pmb \xi _ { n } }$ over $\left\{ \xi _ { n } > M \right\} \mathrm { i f } \ n > M ?$

## Proposition 4.2

Uniform integrability is a necessary condition for a sequence $\xi _ { 1 } , \xi _ { 2 } , \ldots .$ of integrable random variables to converge in $L ^ { 1 }$

## Lemma 4.2

If ξ is integrable, then for every $\epsilon > 0$ there is a $\delta > 0$ such that

$$
P ( A ) < \delta \Longrightarrow \int _ { A } | \xi | d P < \varepsilon .
$$

## Proof (of Lemma 4.2)

Let $\varepsilon > 0$ . Since $\pmb { \xi }$ is integrable, by Exercise 4.3 there is an $M > 0$ such that

$$
\int _ { \{ | \xi | > M \} } | \xi | \ d P < \frac { \varepsilon } { 2 } .
$$

Now

$$
\begin{array} { l } { { \displaystyle \int _ { A } | \xi | \ d P = \ \int _ { A \cap \{ | \xi | \leq M \} } | \xi | \ d P + \int _ { A \cap \{ | \xi | > M \} } | \xi | \ d P } } \\ { { \displaystyle \leq \ \int _ { A } M d P + \int _ { \{ | \xi | > M \} } | \xi | \ d P } } \\ { { \displaystyle ~ < \ M P ( A ) + \frac { \varepsilon } { 2 } . } } \end{array}
$$

Let $\begin{array} { r } { \delta = \frac { \varepsilon } { 2 M } } \end{array}$ . Then

$$
P ( A ) < \delta \Longrightarrow \int _ { A } | \xi | d P < \varepsilon ,
$$

as required.

## Exercise 4.5

Let ${ \pmb \xi }$ be an integrable random variable and $\mathcal { F } _ { 1 } , \mathcal { F } _ { 2 } , \ldots { } \mathrm { ~ \mathbf { a } }$ filtration. Show that $E \left( \xi \vert \mathcal { F } _ { n } \right)$ is a uniformly integrable martingale.

Hint Use Lemma 4.2.

## Proof (of Proposition 4.2)

Suppose that $\xi _ { n } \to \xi$ in $L ^ { 1 }$ , i.e. $E | \xi _ { n } - \xi |  0$ We take any $\varepsilon > 0$ There is an integer N such that

$$
n \geq N \Longrightarrow E \left| \xi _ { n } - \xi \right| < \frac { \varepsilon } { 2 } .
$$

By Lemma 4.2 there is a ${ \pmb \delta } > { \bf 0 }$ such that

$$
P ( A ) < \delta \Longrightarrow \int _ { A } | \xi | \ d P < \frac { \varepsilon } { 2 } .
$$

Taking a smaller ${ \pmb \delta } > { \bf 0 }$ if necessary, we also have

$$
P ( A ) < \delta \Longrightarrow \int _ { A } \left| \xi _ { n } \right| d P < \varepsilon \quad \mathrm { f o r } \ n = 1 , \ldots , N .
$$

We claim that there is an $M > 0$ such that

$$
P \left\{ | \xi _ { n } | > M \right\} < \delta
$$

for all n. Indeed, since

$$
E \left( \left| \xi _ { n } \right| \right) \geq \int _ { \left\{ \left| \xi _ { n } \right| > M \right\} } \left| \xi _ { n } \right| d P \geq M P \left\{ \left| \xi _ { n } \right| > M \right\} ,
$$

it suffices to take

$$
M = \frac { 1 } { \delta } \operatorname* { s u p } _ { n } E \left( \left| \xi _ { n } \right| \right) .
$$

(Because the sequence ξn converges in L1, it is bounded in L1, so the supremum $\xi _ { n }$ $L ^ { 1 }$ $L ^ { 1 }$ $\mathrm { i } \mathbf { s } < \infty . )$

Now, since $P \left\{ \left| \xi _ { n } \right| > M \right\} < \delta$

$$
\begin{array} { r l } { \displaystyle \int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P \le \int _ { \{ | \xi _ { n } | > M \} } | \xi | d P + \int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } - \xi | d P } & { } \\ { \le \displaystyle \int _ { \{ | \xi _ { n } | > M \} } | \xi | d P + E \left( | \xi _ { n } - \xi | \right) } & { } \\ { < \displaystyle \frac { \varepsilon } { 2 } + \displaystyle \frac { \varepsilon } { 2 } = \varepsilon . } \end{array}
$$

for any $\pmb { n } > N$ and

$$
\int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P < \varepsilon
$$

for any $n = 1 , \ldots , N$ , completing the proof.

## Exercise 4.6

Show that a uniformly integrable sequence of random variables is bounded in $L ^ { 1 }$ , i.e.

$$
\operatorname* { s u p } _ { n } E \left( \left| \xi _ { n } \right| \right) < \infty .
$$

Hint Write $E ( | \xi _ { n } | )$ as the sum of the integrals of $| \xi _ { n } |$ over $\{ | \xi _ { n } | > M \}$ and $\{ | \xi _ { n } | \leq M \}$

Exercise 4.6 implies that each uniformly integrable martingale satisfies the conditions of Doob's theorem. Therefore it converges a.s. to an integrable random variable. We shall show that in fact it converges in $L ^ { 1 }$

## Theorem 4.3

Every uniformly integrable supermartingale (submartingale) ξn converges in $\xi _ { n }$ $L ^ { 1 }$

## Proof

By Exercise 4.6 the sequence ξn is bounded in L1, so it satisfies the conditions $\xi _ { n }$ $L ^ { 1 }$ of Theorem 4.2 (Doob's Martingale Convergence Theorem). Therefore, there is an integrable random variable ξ such that $\xi _ { n } \to \xi$ a.s. We can assume without loss of generality that $\pmb { \xi } = \mathbf { 0 }$ (since $\xi _ { n } - \xi$ can be taken in place of $\xi _ { n } )$ . That is to say,

$$
P \left\{ \operatorname* { l i m } _ { n } \xi _ { n } = 0 \right\} = 1 .
$$

It follows that $\xi _ { n } \to 0$ in probability, i.e. for any $\varepsilon > 0$

$$
P \{ | \xi _ { n } | > \varepsilon \}  0
$$

as $\pmb { n }  \infty$ .This is because by Fatou's lemma

$$
\begin{array} { r l } { \underset { n } { \operatorname* { l i m } } s \ u \mathrm { p }  P \{ | \xi _ { n } | > \varepsilon \} \leq } & {  P ( \operatorname* { l i m } _ { n } s \mathrm { u p } \{ | \xi _ { n } | > \varepsilon \} )  } \\ { \leq } & {  P ( \varOmega \setminus \{ \underset { n } { \operatorname* { l i m } } \xi _ { n } = 0 \} )  } \\ { = } & { 0 . } \end{array}
$$

Let $\varepsilon > 0$ .By uniform integrability there is an $M > 0$ such that

$$
\int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P \leq \frac { \varepsilon } { 3 }
$$

for all n. Since ${ \pmb \xi } _ { n }  { \bf 0 }$ in probability, there is an integer N such that if $\pmb { n } > N$ then

$$
P \left\{ | \xi _ { n } | > \frac { \varepsilon } { 3 } \right\} < \frac { \varepsilon } { 3 M } .
$$

We can assume without loss of generality that $\begin{array} { r } { M > { \frac { \varepsilon } { 3 } } } \end{array}$ .Then

$$
\begin{array} { l } { \displaystyle E \left( | \xi _ { n } | \right) = \int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P + \int _ { \left\{ M \ge | \xi _ { n } | > \frac { \varepsilon } { 3 } \right\} } | \xi _ { n } | d P } \\ { \displaystyle + \int _ { \left\{ \frac { \varepsilon } { 3 } \ge | \xi _ { n } | \right\} } | \xi _ { n } | d P } \\ { \displaystyle \le \frac { \varepsilon } { 3 } + { M P \left\{ | \xi _ { n } | > \frac { \varepsilon } { 3 } \right\} } + \frac { \varepsilon } { 3 } P \left\{ \frac { \varepsilon } { 3 } \ge | \xi _ { n } | \right\} } \\ { \displaystyle < \varepsilon } \end{array}
$$

for all $n > N$ .This proves that $E (  \xi _ { n }  )  0$ , that is, $\xi _ { n } \to 0 \mathrm { ~ i n ~ } L ^ { 1 }$ .

Theorem 4.4

Let $\xi _ { n }$ be a uniformly integrable martingale. Then

$$
\xi _ { n } = E \left( \xi | \mathcal { F } _ { n } \right) ,
$$

where $\pmb { \xi } = \operatorname* { l i m } _ { n } \xi _ { n }$ is the limit of $\xi _ { n }$ in $L ^ { 1 }$ and $\mathcal { F } _ { n } = \sigma \left( \xi _ { 1 } , \ldots , \xi _ { n } \right)$ is the filtration generated by $\xi _ { n }$

Proof

For any $m > n$

$$
E \left( \xi _ { m } | \mathcal { F } _ { n } \right) = \xi _ { n } ,
$$

i.e. for any $A \in { \mathcal { F } } _ { n }$

$$
\int _ { A } \xi _ { m } d P = \int _ { A } \xi _ { n } d P .
$$

Let n be an arbitrary integer and let $A \in { \mathcal { F } } _ { n }$ . For any $m > n$

$$
\begin{array} { r } { \displaystyle  \int _ { A } ( \xi _ { n } - \xi ) d P  = \displaystyle  \int _ { A } ( \xi _ { m } - \xi ) d P  } \\ { \leq \displaystyle \int _ { A } \displaystyle  \xi _ { m } - \xi  d P } \\ { \leq E ( \displaystyle  \xi _ { m } - \xi  )  0 } \end{array}
$$

as $m \to \infty$ . It follows that

$$
\int _ { A } \xi _ { n } \ : d P = \int _ { A } \xi \ : d P
$$

for any $A \in { \mathcal { F } } _ { n }$ , so $\xi _ { n } = E \left( \xi | \mathcal { F } _ { n } \right) . \sqsupset$

Exercise 4.7

Show that if $\xi _ { n }$ is a martingale and $\xi _ { n } \ \to \ a$ in $L ^ { 1 }$ for some $a \in \mathbb { R } ,$ then $\pmb { \xi } _ { n } = \pmb { a } \ \mathbf { a } . \mathbf { s }$ for each n.

Hint Apply Theorem 4.4.

## Theorem 4.5 (Kolmogorov's 0-1 Law)

Let $\eta _ { 1 } , \eta _ { 2 } , \ldots$ be a sequence of independent random variables. We define the tail $\pmb { \sigma } \mathrm { - } \pmb { \mathscr { f } } e l d$

$$
\mathcal { T } = \mathcal { T } _ { 1 } \cap \mathcal { T } _ { 2 } \cap \ldots \ ,
$$

where $\mathcal { T } _ { n } = \sigma \left( \eta _ { n } , \eta _ { n + 1 } , \dots \right)$ .Then

$$
P \left( A \right) = 0 { \mathrm { ~ o r ~ } } 1
$$

for any $A \in \tau$

Proof

Take any $A \in \mathcal T$ and define

$$
\xi _ { n } = E \left( 1 _ { A } | \mathcal { F } _ { n } \right) ,
$$

where $\mathcal { F } _ { n } = \sigma \left( \eta _ { 1 } , \ldots , \eta _ { n } \right)$ . By Exercise 4.5 ${ \pmb \xi } _ { \pmb n }$ is a uniformly integrable martingale, so $\xi _ { n } \to \xi$ in $L ^ { 1 }$ . By Theorem 4.4

$$
E \left( \xi | \mathcal { F } _ { n } \right) = E \left( 1 _ { A } | \mathcal { F } _ { n } \right)
$$

for all n. Both $\xi = \operatorname* { l i m } _ { n } \xi _ { n }$ and $\mathbf { 1 } _ { A }$ are measurable with respect to the ${ \pmb { \sigma } } { \cdot } \hbar { \mathsf { e l d } }$

$$
\mathcal { F } _ { \infty } = \sigma \left( \eta _ { 1 } , \eta _ { 2 } , . . . \right) .
$$

The family $\mathfrak { g }$ consisting of all sets $B \in { \mathcal { F } }$ such that $\textstyle { \int _ { B } \xi d P = \int _ { B } 1 _ { A } d P }$ is a $\pmb { \sigma } -$ field containing $\mathcal { F } _ { 1 } \cup \mathcal { F } _ { 2 } \cup \ldots$ . As a result, $\mathfrak { g }$ contains the σ-field $\mathcal { F } _ { \infty }$ generated by the family $\mathcal { F } _ { 1 } \cup \mathcal { F } _ { 2 } \cup \ldots \ldots \mathbf { \mu } . \mathbf { B } \mathbf { y }$ Lemma 2.1 it follows that $\pmb { \xi } = \mathbb { 1 } _ { A } \ \mathbf { a . s } ,$

Since $\eta _ { n }$ is a sequence of independent random variables, the o-fields $\mathcal { F } _ { n }$ and $\mathcal { T } _ { n + 1 }$ are independent. Because $\mathcal T \subset \mathcal T _ { n + 1 }$ , the σ-fields $\mathcal { F } _ { n }$ and $\tau$ are independent. Being T-measurable, ${ \bf 1 } _ { A }$ is therefore independent of $\mathcal { F } _ { n }$ for any n. This means that

$$
\xi _ { n } = \cdot E \left( 1 _ { A } | \mathcal { F } _ { n } \right) = E \left( 1 _ { A } \right) = P ( A ) \quad \mathrm { a . s . }
$$

Therefore the limit $\scriptstyle \operatorname* { l i m } _ { n \to \infty } \xi _ { n } = \xi$ is also constant and equal to $P ( A )$ a.s. This means that $P ( A ) = 1 _ { A } { \mathrm { ~ a . s . } } ,$ so P(A) = 0 or 1.

## Exercise 4.8

Show that if $A _ { n } \in \sigma \left( \xi _ { n } \right)$ for each n, then the events

$$
\operatorname* { l i m } _ { n } \operatorname* { s u p } A _ { n } = \bigcap _ { j \geq 1 } \bigcup _ { i \geq j } A _ { i }
$$

and

$$
\operatorname* { l i m } _ { n } \operatorname* { i n f } A _ { n } = \bigcup _ { j \geq 1 } \bigcap _ { i \geq j } A _ { i }
$$

belong to the tail o-field $\tau$

Hint You need to write lim su ${ \bf { p } } _ { n } \ A _ { n }$ and lim inf An in terms of $A _ { k } , A _ { k + 1 } , \ldots \mathbf { f o r }$ any k, that is, to show that limsu ${ \mathfrak { p } } _ { n } A _ { \mathfrak { n } }$ and lim in $\mathbf { f } _ { n } \mathbf { A } _ { n }$ will not be affected if any finite number of sets are removed from the sequence $A _ { 1 } , A _ { 2 } , \ldots .$

## Exercise 4.9

Use Kolmogorov's 0-1 law to show that in a sequence of coin tosses there are a.s. infinitely many heads.

## Hint Show that the event

$$
\left\{ \eta _ { 1 } , \eta _ { 2 } , . . . \mathrm { c o n t a i n s i n f i n i t e l y m a n y h e a d s } \right\}
$$

belongs to the σ-field $\tau$ Can the probability of this event be 0? The probability of the event

$$
\left\{ \eta _ { 1 } , \eta _ { 2 } , . . . \mathrm { c o n t a i n s ~ i n f u n i t e l y ~ m a n y ~ t a i l s } \right\}
$$

should be the same. Can both probabilities be equal to 0? Can you simultaneously have finitely many heads and finitely many tails in the sequence $\eta _ { 1 } , \eta _ { 2 } , \ldots ?$

## 4.4 Solutions

## Solution 4.1

Because $\pmb { \alpha _ { 1 } } = \pmb { 0 }$ is constant, it is ${ \mathcal { F } } _ { 0 } = \{ \emptyset , \varOmega \}$ -measurable. Suppose that $\alpha _ { n }$ is $\mathcal { F } _ { n - 1 }$ -measurable for some $\pmb { n = 1 , 2 , . . . }$ Then

$$
\{ \alpha _ { n } = 0 , \xi _ { n } < a \} \cup \{ \alpha _ { n } = 1 , \xi _ { n } \leq b \} \in \mathcal { F } _ { n }
$$

because ${ \mathcal { F } } _ { n - 1 } \subset { \mathcal { F } } _ { n }$ and $\xi _ { n }$ $\mathcal { F } _ { n } .$ measurable. This means that

$$
\alpha _ { n + 1 } = 1 _ { \left\{ \alpha _ { n } = 0 , \xi _ { n } < a \right\} \cup \left\{ \alpha _ { n } = 1 , \xi _ { n } \leq b \right\} }
$$

is ${ \mathcal { F } } _ { n }$ -measurable. By induction it follows that $\alpha _ { n }$ is $\mathcal { F } _ { n - 1 }$ -measurable for each $n = 1 , 2 , \ldots , \thinspace \thinspace \thinspace \mathrm { s o } \ \alpha _ { 1 } , \alpha _ { 2 } , \ldots .$ . is a gambling strategy.

## Solution 4.2

For a non-negative supermartingale

$$
\operatorname* { s u p } _ { n } E \left( \left| \xi _ { n } \right| \right) = \operatorname* { s u p } _ { n } E \left( \xi _ { n } \right) \leq E \left( \xi _ { 1 } \right) = E \left( \left| \xi _ { 1 } \right| \right) < \infty ,
$$

since

$$
E \left( \xi _ { n } \right) \leq E \left( \xi _ { 1 } \right)
$$

for each $n = 1 , 2 , \ldots$ . Thus Doob's Martingale Convergence Theorem implies that ξn converges a.s. to an integrable limit. $\xi _ { n }$

## Solution 4.3

Necessity. Suppose that ξ is integrable. It follows that

$$
P \left\{ | \xi | < \infty \right\} = 1 .
$$

The sequence of random variables $| \xi | 1 _ { \{ | \xi | > M \} }$ indexed by $M = 1 , 2 , . . .$ is monotone and

$$
\left| \xi \right| 1 _ { \{ | \xi | > M \} } \searrow 0 \quad \mathrm { a s ~ } M \to \infty
$$

on the set $\{ | \xi | < \infty \}$ , i.e. a.s. By the monotone convergence theorem for integrals

$$
\int _ { \{ | \xi | > M \} } | \xi | \ d P \searrow 0 \quad { \mathrm { a s ~ } } M \to \infty .
$$

It follows that for every $\varepsilon > 0$ there exists an $M > 0$ such that

$$
\int _ { \{ | \xi | > M \} } | \xi | \ d P < \varepsilon .
$$

Sufficiency. Take $\varepsilon = 1$ . There exists an $M > 0$ such that

$$
\int _ { \{ | \xi | > M \} } | \xi | \ d P < 1 .
$$

Then

$$
\begin{array} { l } { E \left( | \xi | \right) = \displaystyle \int _ { \Omega } | \xi | \ d P \ ~ } \\ { \displaystyle = \ \int _ { \{ | \xi | > M \} } \left| \xi \right| \ d P + \displaystyle \int _ { \{ | \xi | \leq M \} } | \xi | \ d P } \\ { \displaystyle < \ 1 + M P \left\{ | \xi | \leq M \right\} } \\ { \displaystyle \leq \ 1 + M < \infty . } \end{array}
$$

## Solution 4.4

For any $M > 0$ and any $n > M$ we have

$$
\begin{array} { r } { ( 0 , \frac { 1 } { n } ) = \left\{ \xi _ { n } > M \right\} , } \end{array}
$$

so

$$
\int _ { \{ \xi _ { n } > M \} } \xi _ { n } d P = \int _ { ( 0 , \frac { 1 } { n } ) } n d P = 1 .
$$

This means that there is no $M > 0$ such that for all n

$$
\int _ { \{ \xi _ { n } > M \} } \xi _ { n } d P < \frac { 1 } { 2 } .
$$

The sequence $\xi _ { n }$ is not uniformly integrable.

## Solution 4.5

In Example 3.4 it was verified that $\xi _ { n } = E ( \xi | \mathcal { F } _ { n } )$ is a martingale. Let $\varepsilon > 0$ By Lemma 4.2 there is a $\delta > 0$ such that

$$
P ( A ) < \delta \Longrightarrow \int _ { A } | \xi | d P < \varepsilon .
$$

By Jensen's inequality $| \xi _ { n } | \leq E ( | \xi | | \mathcal { F } _ { n } )$ a.s., so

$$
E ( | \xi | ) \geq E ( | \xi _ { n } | ) \geq \int _ { \{ | \xi _ { n } | \geq M \} } | \xi _ { n } | d P \geq M P \{ | \xi _ { n } | > M \} .
$$

If we take $M > E ( | \xi | ) / \delta$ , then

$$
P \{ | \xi _ { n } | > M \} < \delta .
$$

Since $\left\{ \left. \xi _ { n } \right. > M \right\} \in \mathcal { F } _ { n }$ , it follows that

$$
\int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P \leq \int _ { \{ | \xi _ { n } | > M \} } E ( | \xi | | \mathcal { F } _ { n } ) d P = \int _ { \{ | \xi _ { n } | > M \} } | \xi | d P < \varepsilon ,
$$

proving that $\xi _ { n } = E ( \xi | \mathcal { F } _ { n } )$ is a uniformly integrable sequence.

## Solution 4.6

Because $\xi _ { n }$ is a uniformly integrable sequence, there is an $M > 0$ such that for all n

$$
\int _ { \{ | \xi _ { n } | > M \} } | \xi _ { n } | d P < 1 .
$$

It follows that

$$
\begin{array} { l } { E \left( \left| \xi _ { n } \right| \right) ~ = ~ \displaystyle \int _ { \left\{ | \xi _ { n } | > M \right\} } \left| \xi _ { n } \right| d P + \displaystyle \int _ { \left\{ | \xi _ { n } | \leq M \right\} } \left| \xi _ { n } \right| d P } \\ { ~ < ~ 1 + M P \left\{ | \xi _ { n } | \leq M \right\} } \\ { ~ < ~ 1 + M < \infty } \end{array}
$$

for all n, proving that $\xi _ { n }$ is a bounded sequence in $L ^ { 1 }$

## Solution 4.7

By Theorem 4.4, $\xi _ { n } = E \left( a | \mathcal { F } _ { n } \right)$ a.s. But $E \left( a | \mathcal { F } _ { n } \right) = a \mathrm { ~ a . s . }$ , which proves that $\pmb { \xi } _ { n } = \pmb { a } \ \mathbf { a } . \mathbf { s }$

Solution 4.8

Observe that

$$
\operatorname* { l i m } _ { n } \operatorname* { s u p } A _ { n } = \bigcap _ { j \geq k } \bigcup _ { i \geq j } A _ { i }
$$

for any k. Since

$$
A _ { i } \in \sigma ( \xi _ { i } ) \subset \mathcal T _ { k }
$$

for every $i \geq k$ , it follows that

$$
\operatorname* { l i m } _ { n } \operatorname* { s u p } A _ { n } = \bigcap _ { j \geq k } \bigcup _ { i \geq j } A _ { i } \in { \mathcal { T } } _ { k }
$$

for every k. Therefore

$$
\operatorname* { s u p } _ { n } A _ { n } \in \tau .
$$

The argument for lim inf $\gamma A _ { n }$ is similar.

## Solution 4.9

Let $\eta _ { 1 } , \eta _ { 2 } , \ldots ,$ be a sequence of coin tosses, i.e. independent random variables with values +1, -1 (heads or tails) taken with probability 2 each. Consider the $\frac { 1 } { 2 }$ following event:

$$
A = \left\{ \eta _ { 1 } , \eta _ { 2 } , . . . \mathrm { ~ c o n t a i n s ~ i n f i n i t e l y ~ m a n y ~ h e a d s } \right\} .
$$

This event belongs to the tail σ-field T because

$$
A = \operatorname* { l i m } _ { n } \operatorname* { s u p } _ { n } A _ { n } ,
$$

where

$$
A _ { n } = \left\{ \eta _ { n } = \mathrm { h e a d s } \right\} \in \sigma \left( \eta _ { n } \right)
$$

(see Exercise 4.8). Thus, by Kolmogorov's 0-1 law $P ( A ) = \mathbf { 0 }$ or 1. However, it cannot be 0 because the event

$$
B = \{ \eta _ { 1 } , \eta _ { 2 } , . . . { \mathrm { ~ c o n t a i n s ~ i n f i n i t e l y ~ m a n y ~ t a i l s } } \}
$$

has the same probability by symmetry and $\smash { \Omega = A \cup B }$ (there must be infinitely many heads or infinitely many tails).

This chapter is concerned with an interesting class of sequences of random variables taking values in a finite or countable set, called the state space, and satisfying the so-called Markov property. One of the simplest examples is provided by a symmetric random walk $\xi _ { n }$ with values in the set of integers Z. If $\xi _ { n }$ is equal to some $i \in \mathbb { Z }$ at time $\pmb { n }$ , then in the next time instance $\pm 1$ it will jump either to $i + 1$ , with probability ${ \frac { 1 } { 2 } } ;$ , or to $i - 1$ , also with probability $\frac { 1 } { 2 }$ . What makes this model interesting is that the value of $\xi _ { n + 1 }$ at time $\pm 1$ depends on the past only through the value at time ${ \pmb n } .$ This is the Markov property characterizing Markov chains. There are numerous examples of Markov chains, with a multitude of applications.

From the mathematical point of view, Markov chains are both simple and difficult. Their definition and basic properties do not involve any complicated notions or sophisticated mathematics. Yet, any deeper understanding of Markov chains requires quite advanced tools. For example, this is so for problems related to the long-time behaviour of Markov processes. In this chapter we shall try to maintain a balance between the accessibility of exposition and the depth of mathematical results. Various concepts will be introduced. In particular, we shall discuss the classification of states and its relevance to the asymptotic behaviour of transition probabilities. This will turn out to be closely linked to ergodicity and the existence and uniqueness of invariant measures.

## 5.1 First Examples and Definitions

## Example 5.1

In some homes the use of the telephone can become quite a sensitive issue. Suppose that if the phone is free during some period of time, say the nth minute, then with probability ${ \pmb p } ,$ where $0 < p < 1$ , it will be busy during the next minute. If the phone has been busy during the nth minute, it will become free during the next minute with probability q, where $0 < q < 1$ . Assume that the phone is free in the Oth minute. We would like to answer the following two questions.

) What is the probability ${ \pmb x _ { n } }$ that the telephone will be free in the nth minute?

What is $\operatorname* { l i m } _ { n \to \infty } { \mathfrak { X } } _ { n }$ , if it exists?

Denote by $A _ { n }$ the event that the phone is free during the nth minute and let $B _ { n } = \Omega \setminus A _ { n }$ be its complement, i.e. the event that the phone is busy during the nth minute. The conditions of the example give us

$$
P ( B _ { n + 1 } | A _ { n } ) \ = \ p ,\tag{5.1}
$$

$$
P ( A _ { n + 1 } | B _ { n } ) \ = \ q ,\tag{5.2}
$$

We also assume that $P ( A _ { 0 } ) = 1$ , i.e. ${ \pmb x } _ { 0 } = { \pmb 1 }$ . Using this notation, we have $x _ { n } = P ( A _ { n } )$ . Then the total probability formula, see Exercise 1.10, together with (5.1)-(5.2) imply that

$$
\begin{array} { r l } { x _ { n + 1 } } & { = P ( A _ { n + 1 } ) } \\ & { = P ( A _ { n + 1 } | A _ { n } ) P ( A _ { n } ) + P ( A _ { n + 1 } | B _ { n } ) P ( B _ { n } ) } \\ & { = ( 1 - p ) x _ { n } + q ( 1 - x _ { n } ) = q + ( 1 - p - q ) x _ { n } . } \end{array}\tag{5.3}
$$

It's a bit tricky to find an explicit formula for $\pmb { x _ { n } }$ . To do so we suppose first that the sequence $\{ x _ { n } \}$ is convergent, i.e.

$$
\operatorname* { l i m } _ { n \to \infty } x _ { n } = x .\tag{5.4}
$$

The elementary properties of limits and equation (5.3), i.e. $x _ { n + 1 } = q + ( 1 - p -$ ${ \pmb q } ) { \pmb x } _ { \pmb n }$ , yield

$$
x = q + ( 1 - p - q ) x .\tag{5.5}
$$

The unique solution to the last equation is

$$
x = { \frac { q } { q + p } } .\tag{5.6}
$$