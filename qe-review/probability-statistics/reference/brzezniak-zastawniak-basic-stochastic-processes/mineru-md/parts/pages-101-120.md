In particular,

$$
\frac { q } { q + p } = q + ( 1 - p - q ) \frac { q } { q + p } .\tag{5.7}
$$

Subtracting (5.7) from (5.3), we infer that

$$
x _ { n + 1 } - { \frac { q } { q + p } } = ( 1 - p - q ) \left( x _ { n } - { \frac { q } { q + p } } \right) .\tag{5.8}
$$

Thus, $\textstyle \left\{ x _ { n } - { \frac { q } { q + p } } \right\}$ is a geometric sequence and therefore, for all $n \in \mathbb { N } ,$

$$
x _ { n } - { \frac { q } { q + p } } = ( 1 - p - q ) ^ { n } \left( x _ { 0 } - { \frac { q } { q + p } } \right) .
$$

Hence, by taking into account the initial condition ${ { x } _ { 0 } } = 1$ , we have

$$
\begin{array} { l } { { x _ { n } = { \displaystyle \frac { q } { q + p } } + \left( x _ { 0 } - { \displaystyle \frac { q } { q + p } } \right) ( 1 - p - q ) ^ { n } } } \\ { { = { \displaystyle \frac { q } { q + p } } + { \displaystyle \frac { p } { q + p } } ( 1 - p - q ) ^ { n } . } } \end{array}\tag{5.9}
$$

Let us point out that although we have used the assumption (5.4) to derive (5.8), the proof of the latter is now complete. Indeed, having proven (5.9), we can show that the assumption (5.4) is indeed satisfied. This is because the conditions $0 < p , q < 1$ imply that $| 1 - p - q | < 1$ , and so $( 1 - p - q ) ^ { n } \to 0$ as $n  \infty$ . Thus, (5.4) holds. This provides an answer to the second part of the example, i.e. $\begin{array} { r } { \operatorname* { l i m } _ { n \to \infty } x _ { n } = \frac { q } { p + q } } \end{array}$

The following exercise is a modification of the last example.

## Exercise 5.1

In the framework of Example 5.1, let $y _ { n }$ denote the probability that the telephone is busy in the nth minute. Supposing that ${ \displaystyle y _ { \mathrm { 0 } } = 1 }$ , find an explicit formula for ${ \pmb y _ { n } }$ and, if it exists, $\scriptstyle \operatorname* { l i m } _ { n \to \infty } y _ { n }$

Hint This exercise can be solved directly by repeating the above argument, or indirectly by using some of the results in Example 5.1.

## Remark 5.1

The formulae (5.3) and (5.64) can be written collectively in a compact form by using vector and matrix notation. First of all, since $x _ { n } + y _ { n } = 1$ , we get

$$
x _ { n + 1 } = ( 1 - p ) x _ { n } + q y _ { n } ,
$$

$$
y _ { n + 1 } = p x _ { n } + ( 1 - q ) y _ { n } .
$$

Hence, the matrix version takes the form

$$
\left[ \begin{array} { c } { x _ { n + 1 } } \\ { y _ { n + 1 } } \end{array} \right] = \left[ \begin{array} { c c } { 1 - p } & { q } \\ { p } & { 1 - q } \end{array} \right] \left[ \begin{array} { c } { x _ { n } } \\ { y _ { n } } \end{array} \right] .
$$

The situation described in Example 5.1 is quite typical. Often the probability of a certain event at time $n + 1$ depends only on what happens at time n, but not further into the past. Example 5.1 provides us with a simple case of a Markov chain. See also the following definition and exercises.

## Definition 5.1

Suppose that S is a finite or a countable set. Suppose also that a probability space $( \Omega , { \mathcal { F } } , P )$ is given. An S-valued sequence of random variables $\xi _ { n } , n \in \mathbb { N } ,$ is called an S-valued Markov chain or a Markov chain on S if for all $\pmb { n } \in \mathbb { N }$ and all $s \in S$

$$
P ( \xi _ { n + 1 } = s | \xi _ { 0 } , \ldots , \xi _ { n } ) = P ( \xi _ { n + 1 } = s | \xi _ { n } ) .\tag{5.10}
$$

Here $P ( \xi _ { n + 1 } = s | \xi _ { n } )$ is the conditional probability of the event $\{ \xi _ { n + 1 } = s \}$ with respect to random variable $\xi _ { n }$ , or equivalently, with respect to the σ-field $\sigma ( \xi _ { n } )$ generated by $\xi _ { n }$ . Similarly, $P ( \xi _ { n + 1 } = s | \xi _ { 0 } , . . . , \xi _ { n } )$ is the conditional probability of $\{ \xi _ { n + 1 } = s \}$ with respect to the o-field $\sigma ( \xi _ { 0 } , \cdots , \xi _ { n } )$ generated by the random variables $\xi _ { 0 } , \cdots , \xi _ { n }$

Property (5.10) will usually be referred to as the Markov property of the Markov chain $\xi _ { n } , n \in \mathbb { N } .$ The set S is called the state space and the elements of S are called states.

## Proposition 5.1

The model in Example 5.1 and Exercise 5.1 is a Markov chain.

## Proof

Let $S = \{ 0 , 1 \}$ , where 0 and 1 represent the states of the phone being free or busy. First we need to construct an appropriate probability space. Let Ω be the set of all sequences $\omega _ { 0 } , \omega _ { 1 } , \ldots$ with values in S. Let $\mu _ { 0 }$ be any probability measure on S. For example, $\mu = \delta _ { 0 }$ corresponds to the case when the phone is free at time O. We shall define P by induction. For any S-valued sequence $s _ { 0 } , s _ { 1 } , . . .$ we put

$$
P ( \{ \omega \in \Omega : \ \omega _ { 0 } = s _ { 0 } \} ) = \mu _ { 0 } ( \{ s _ { 0 } \} )\tag{5.11}
$$

and

$$
P ( \{ \omega \in \mathcal { \Omega } : \omega _ { i } = s _ { i } , i = 0 , \cdot \cdot \cdot , n + 1 \} )\tag{5.12}
$$

$$
= p ( s _ { n + 1 } | s _ { n } ) P ( \{ \omega \in \varOmega : \omega _ { i } = s _ { i } , i = 0 , \cdots , n \} ) ,
$$

where $p ( \pmb { \mathscr { s } } | \pmb { r } )$ are the entries of the $\mathbf { 2 \times 2 }$ matrix

$$
\left[ \begin{array} { c c } { p ( 0 | 0 ) } & { p ( 0 | 1 ) } \\ { p ( 1 | 0 ) } & { p ( 1 | 1 ) } \end{array} \right] = \left[ \begin{array} { c c } { 1 - p } & { q } \\ { p } & { 1 - q } \end{array} \right] .
$$

It seems reasonable to expect P to be a probability measure (with respect to the trivial ${ \pmb { \sigma } } { \cdot } \mathbf { \hat { \Pi } } \mathbf { \hat { e } } \mathbf { l } \mathbf { d }$ of all subsets of Ω). Take this for granted, check only that $\varOmega )$ $P ( \Omega ) = 1$

How would you define the process $\xi _ { n } , n \in \mathbb { N } ?$ We shall do it in the standard way, i.e.

$$
\xi _ { n } ( \omega ) = \omega _ { n } , \omega \in \varOmega .\tag{5.13}
$$

First we shall show that the transition probabilities of ξn are what they should $\xi _ { n }$ be, i.e.

$$
\begin{array} { r } { P ( \xi _ { n + 1 } = 1 | \xi _ { n } = 0 ) = p , } \\ { P ( \xi _ { n + 1 } = 0 | \xi _ { n } = 1 ) = q . } \end{array}\tag{5.14}
$$

(5.15)

The definition of conditional probability yields

$$
P ( \xi _ { n + 1 } = 1 | \xi _ { n } = 0 ) = \frac { P ( \xi _ { n + 1 } = 1 , \xi _ { n } = 0 ) } { P ( \xi _ { n } = 0 ) } .
$$

Next, the definition of P gives

$$
\begin{array} { r l } & { P ( \xi _ { n + 1 } = 1 , \xi _ { n } = 0 ) } \\ & { = P ( \{ \omega \in \varOmega : \omega _ { n } = 0 , \omega _ { n + 1 } = 1 \} ) } \\ & { = \displaystyle \sum _ { s _ { 0 } , \cdots , s _ { n - 1 } \in S } P ( \{ \omega \in \varOmega : \omega _ { i } = s _ { i } , i = 0 , \cdots , n - 1 , \omega _ { n } = 0 , \omega _ { n + 1 } = 1 \} ) } \\ & { = \displaystyle \sum _ { s _ { 0 } , \cdots , s _ { n - 1 } \in S } p P ( \{ \omega \in \varOmega : \omega _ { i } = s _ { i } , i = 0 , \cdots , n - 1 , \omega _ { n } = 0 \} ) } \\ & { = p P ( \xi _ { n } = 0 ) , } \end{array}
$$

by (5.12). We have proven (5.14). Moreover, (5.15) follows by the same argument. A similar line of reasoning shows that ξn is indeed a Markov chain. For $\xi _ { n }$ this we need to verify that for any $\pmb { n } \in \mathbb { N }$ and any $s _ { 0 } , s _ { 1 } , \cdots , s _ { n + 1 } \in S$

$$
P ( \xi _ { n + 1 } = s _ { n + 1 } | \xi _ { 0 } = s _ { 0 } , \cdots , \xi _ { n } = s _ { n } ) = P ( \xi _ { n + 1 } = s _ { n + 1 } | \xi _ { n } = s _ { n } ) .
$$

We have

$$
\begin{array} { r l } & { P ( \xi _ { 0 } = s _ { 0 } , \cdots , \xi _ { n } = s _ { n } , \xi _ { n + 1 } = s _ { n + 1 } ) } \\ & { = P ( \{ \omega \in \Omega : \omega _ { i } = s _ { i } , i = 0 , \cdots , n + 1 \} ) } \\ & { = p ( s _ { n + 1 } | s _ { n } ) P ( \{ \omega \in \Omega : \omega _ { i } = s _ { i } , i = 0 , \cdots , n \} ) } \\ & { = p ( s _ { n + 1 } | s _ { n } ) P ( \xi _ { 0 } = s _ { 0 } , \cdots , \xi _ { n } = s _ { n } ) } \end{array}
$$

which, in view of the definition of conditional probability, gives

$$
P ( \xi _ { n + 1 } = s _ { n + 1 } | \xi _ { 0 } = s _ { 0 } , \cdot \cdot \cdot , \xi _ { n } = s _ { n } ) = p ( s _ { n + 1 } | s _ { n } ) .
$$

On the other hand, by an easy generalization of (5.14) and (5.15)

$$
P ( \xi _ { n + 1 } = s _ { n + 1 } | \xi _ { n } = s _ { n } ) = p ( s _ { n + 1 } | s _ { n } ) ,
$$

which proves (5.10).

In Example 5.1 the transition probabilities from state i to state j do not depend on the time n. This is an important class of Markov chains.

## Definition 5.2

An S-valued Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ , is called time-homogeneous or homogeneous if for all $n \in \mathbb { N }$ and all $i , j \in S$

$$
P ( \xi _ { n + 1 } = j | \xi _ { n } = i ) = P ( \xi _ { 1 } = j | \xi _ { 0 } = i ) .\tag{5.16}
$$

The number $P ( \xi _ { 1 } = j | \xi _ { 0 } = i )$ is denoted by ${ \pmb p } ( j | i )$ and called the transition probability from state i to state $j$ The matrix $P = [ p ( j | i ) ] _ { j , i \in S }$ is called the transition matrix of the chain $\xi _ { n }$

## Exercise 5.2

In the discussion so far we have seen an example of a transition matrix, $P =$ $\left[ \begin{array} { l l } { 1 - p } & { q } \\ { p } & { 1 - q } \end{array} \right]$ . Obviously the sum of the entries in each column of P is equal $P$ to 1. Prove that this is true in general.

Hint Remember that $P ( \varOmega | A ) = 1$ for any event A.

## Definition 5.3

$A = [ a _ { j i } ] _ { i , j \in S }$ is called a stochastic matrix if

1) $a _ { j i } \geq 0$ , for all $i , j \in S$

2)the sum of the entries in each column is 1, i.e. $\textstyle \sum _ { j \in S } a _ { j i } = 1$ for any $\textit { \textbf { i } } \in S$ A is called a double stochastic matrix if both A and its transpose $A ^ { t }$ are stochastic matrices.

## Proposition 5.2

Show that a stochastic matrix is doubly stochastic if and only if the sum of the entries in each row is 1, i.e. $\textstyle \sum _ { i \in S } a _ { j i } = 1$ for any $j \in S$

## Proof

Put $A ^ { t } = [ b _ { i j } ]$ Then, by the definition of the transposed matrix, $b _ { i j } = a _ { j i }$ Therefore, $A ^ { t }$ is a stochastic matrix if and only if

$$
\sum _ { i } a _ { j i } = \sum _ { i } b _ { i j } = 1 ,
$$

completing the proof.

## Exercise 5.3

Show that if $P = [ p _ { j i } ] _ { j , i \in S }$ is a stochastic matrix, then any natural power $P ^ { n }$ of P is a stochastic matrix. Is the corresponding result true for a double stochastic matrix?

Hint Show that if A and B are two stochastic matrices, then so is BA. For the second problem, recall that $( B A ) ^ { t } = A ^ { t } B ^ { t }$

## Exercise 5.4

Let $P = \left[ \begin{array} { c c } { { 1 - p } } & { { q } } \\ { { p } } & { { 1 - q } } \end{array} \right]$ . Show that

$$
P ^ { 2 } = \left[ \begin{array} { c c } { { 1 + p ^ { 2 } - 2 p + p q } } & { { 2 q - p q - q ^ { 2 } } } \\ { { 2 p - p q - p ^ { 2 } } } & { { 1 + q ^ { 2 } - 2 q + p q } } \end{array} \right] .
$$

Hint This is just simple matrix multiplication.

We see that there is a problem with finding higher powers of the matrix P. When multiplying $P ^ { 2 }$ by $P , P ^ { 2 }$ , and so on, we obtain more and more complicated expressions.

## Definition 5.4

The n-step transition matrix of a Markov chain $\xi _ { n }$ with transition probabilities $p ( j | i ) , j , i \in S$ is the matrix $P _ { n }$ with entries

$$
p _ { n } ( j | i ) = P ( \xi _ { n } = j | \xi _ { 0 } = i ) .\tag{5.17}
$$

## Exercise 5.5

Find an exact formula for $P _ { n }$ for the matrix P from Exercise 5.4.

Hint Put $x _ { n } = P ( \xi _ { n } = 0 | \xi _ { 0 } = 0 )$ and $y _ { n } = P ( \xi _ { n } = 1 | \xi _ { 0 } = 1 )$ ). Is it correct to suppose that $p _ { n } ( 0 | 0 ) = x _ { n }$ and $p _ { n } ( 1 | 1 ) = y _ { n } ?$ If yes, you may be able use Example 5.1 and Exercise 5.1.

## Exercise 5.6

You may suspect that $P _ { n }$ equals $P ^ { n }$ , the nth power of the matrix P. This holds for $\textbf { \em n } = \textbf { 1 }$ .Check if it is true for $\mathscr { n } = 2$ . If this is the case, try to prove that $P _ { n } = P ^ { n }$ for all $\textstyle n \in \mathbb { N } .$

Hint Once again, this is an exercise in matrix multiplication.

The following is a generalization of Exercise 5.6.

## Proposition 5.3 (Chapman-Kolmogorov equation)

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ I, is an S-valued Markov chain with n-step transition probabilities ${ p } _ { n } ( j | i )$ . Then for all $k , n \in \mathbb { N }$

$$
p _ { n + k } ( j | i ) = \sum _ { s \in S } p _ { n } ( j | s ) p _ { k } ( s | i ) , i , j \in S .\tag{5.18}
$$

Exercise 5.7

Prove Proposition 5.3.

Hint $p _ { n + k } ( j | i )$ are the entries of the matrix $P _ { n + k } = P ^ { n + k }$

## Proof (of Proposition 5.3)

Let P and Pn be, respectively, the transition probability matrix and the n-step $P _ { n }$ transition probability matrix. Since ${ p _ { n } ( j | i ) }$ are the entries of Pn, we only need $P _ { n }$ to show that $P _ { n } \ = \ P ^ { n }$ for all $\textbf { \em n } \in \ \mathbb { N } .$ This can be done by induction. The assertion is clearly true for $\scriptstyle n = 1$ . Suppose that $P _ { n } = P ^ { n }$ . Then, for $i , j \in S$ 1 by the total probability formula and the Markov property (5.10)

$$
\begin{array} { r l } & { p _ { n + 1 } ( j | i ) = { \cal P } ( \xi _ { n + 1 } = j | \xi _ { 0 } = i ) } \\ & { \quad \quad = \displaystyle \sum _ { s \in { \cal S } } { \cal P } ( \xi _ { n + 1 } = j | \xi _ { 0 } = i , \xi _ { n } = s ) { \cal P } ( \xi _ { n } = s | \xi _ { 0 } = i ) } \\ & { \quad \quad = \displaystyle \sum _ { s \in { \cal S } } { \cal P } ( \xi _ { n + 1 } = j | \xi _ { n } = s ) { \cal P } ( \xi _ { n } = s | \xi _ { 0 } = i ) } \end{array}
$$

$$
= \sum _ { s \in S } p ( j | s ) p _ { n } ( s | i ) ,
$$

which proves that $P _ { n + 1 } = P P _ { n }$ .

## Exercise 5.8 (random walk)

Suppose that ${ \pmb S } = { \pmb \mathbb { Z } }$ . Let $\eta _ { n } , n \geq 1$ be a sequence of independent identically distributed random variables with $P ( \eta _ { 1 } = 1 ) = p$ and $P ( \eta _ { 1 } = - 1 ) = q = 1 - p$ Define $\begin{array} { r } { \xi _ { n } = \sum _ { i = 1 } ^ { n } \eta _ { i } } \end{array}$ for $n \geq 1$ and ${ \xi } _ { 0 } = 0$ .Show that $\xi _ { n }$ is a Markov chain with transition probabilities

$$
p ( j | i ) = { \left\{ \begin{array} { l l } { p , \quad } & { { \mathrm { i f } } \quad j = i + 1 , } \\ { q , \quad } & { { \mathrm { i f } } \quad j = i - 1 , } \\ { 0 , \quad } & { { \mathrm { o t h e r w i s e } } . } \end{array} \right. }
$$

$\xi _ { n } , n \ge 0$ ), is called a random walk starting at 0. Replacing $\xi _ { 0 } = 0$ with $\xi _ { 0 } = i$ we get a random walk starting at i.

Hint $\xi _ { n + 1 } = \xi _ { n } + \eta _ { n + 1 }$ . Are $\xi _ { n }$ and $\eta _ { n + 1 }$ independent?

## Exercise 5.9

For the random walk $\xi _ { n }$ defined in Exercise 5.8 prove that

$$
P ( \xi _ { n } = j | \xi _ { 0 } = i ) = { \binom { n } { \frac { n + j - i } { 2 } } } p ^ { \frac { n + j - i } { 2 } } q ^ { \frac { n - j + i } { 2 } }\tag{5.19}
$$

if $n + j - i$ is an even non-negative integer, and $P ( \xi _ { n } = j | \xi _ { 0 } = i ) = 0$ otherwise.

Hint Use induction. Note that $( \frac { n + 1 ^ { n } } { 2 } - i ) p ^ { \frac { n + 1 - i } { 2 } }$ equals 0 if $| j - i | \geq n + 1$

## Proposition 5.4

For all ${ \pmb p } \in ( 0 , 1 )$

$$
P ( \xi _ { n } = i | \xi _ { 0 } = i ) \to 0 , \ \mathsf { a s } \ n \to \infty .\tag{5.20}
$$

## Proof

To begin with, we shall consider the case $p \neq { \frac { 1 } { 2 } }$ .When $j = i$ , formula (5.19) becomes

$$
P ( \xi _ { n } = i | \xi _ { 0 } = i ) = { \left\{ \begin{array} { l l } { { \frac { ( 2 k ) ! } { ( k ! ) ^ { 2 } } } ( p q ) ^ { k } , \quad } & { { \mathrm { i f } } \quad n = 2 k , } \\ { 0 , \quad } & { { \mathrm { i f } } \ n \ { \mathrm { i s } } \ { \mathrm { o d d } } . } \end{array} \right. }\tag{5.21}
$$

Then, denoting $\begin{array} { r } { { \pmb { a } } _ { { \pmb { k } } } = \frac { ( { \pmb { 2 } } { \pmb { k } } ) ! } { ( { \pmb { k } } ! ) ^ { 2 } } ( { \pmb { p } } q ) ^ { { \pmb { k } } } } \end{array}$ , we have

$$
\frac { a _ { k + 1 } } { a _ { k } } = p q \frac { ( 2 k + 1 ) ( 2 k + 2 ) } { ( k + 1 ) ^ { 2 } }  4 p q < 1 .
$$

Hence, $a _ { k } ~ \to ~ 0$ Thus, $P ( \xi _ { 2 k } \ = \ i | \xi _ { 0 } \ = \ i ) \  \ 0$ The result follows, since $P ( \xi _ { 2 k + 1 } = i | \xi _ { 0 } = i ) = 0 \to 0$

This argument does not work for $\begin{array} { r } { p = \frac { 1 } { 2 } } \end{array}$ because $4 p q = 1$ . In this case we shall need the Stirling formulal

$$
k ! \sim { \sqrt { 2 \pi k } } \left( { \frac { k } { e } } \right) ^ { k } , ~ { \mathrm { a s ~ } } k \to \infty .\tag{5.22}
$$

Here we use the standard convention: $a _ { n } \sim b _ { n }$ whenever $\smash { \frac { a _ { n } } { b _ { n } } } \to 1$ as $n  \infty$ By (5.22)

$$
\begin{array} { l } { { a _ { k } \ \sim \ \frac { \sqrt { 4 \pi k } } { 2 \pi k } ( \frac { 2 k } { e } ) ^ { 2 k } ( \frac { e } { k } ) ^ { 2 k } ( p q ) ^ { k } } } \\ { { \ = \ \frac { 1 } { \sqrt { \pi k } }  0 , \ \mathrm { a s } \quad k  \infty . } } \end{array}
$$

Let us note that the second method works in the first case too. However, in the first case there is no need for anything as sophisticated as the Stirling formula.

## Proposition 5.5

The probability that the random walk ξn ever returns to the starting point is $\xi _ { n }$

$$
1 - | p - q | .
$$

Proof

Suppose that ${ \pmb \xi } _ { 0 } \ = \ 0$ and denote by ${ f _ { 0 } ( n ) }$ the probability that the process returns to 0 at time n for the first time, i.e.

$$
f _ { 0 } ( n ) = P \left( \xi _ { n } = 0 , \xi _ { i } \neq 0 , i = 1 , \cdots , n - 1 \right) .
$$

If also $p _ { 0 } ( n ) = P \left( \xi _ { n } = 0 \right)$ for any $\displaystyle n \in \mathbb { N } ,$ then we can prove that

$$
\sum _ { n = 1 } ^ { \infty } p _ { 0 } ( n ) = \sum _ { n = 0 } ^ { \infty } p _ { 0 } ( n ) \sum _ { n = 1 } ^ { \infty } f _ { 0 } ( n ) .\tag{5.23}
$$

Since all the numbers involved are non-negative, in order to prove (5.23) we need only to show that

$$
p _ { 0 } ( n ) = \sum _ { k = 1 } ^ { n } f _ { 0 } ( k ) p _ { 0 } ( n - k ) \mathrm { f o r } n \geq 1 .
$$

The total probability formula and the Markov property (5.10) yield

$$
\begin{array} { r l } { p _ { 0 } ( n ) = \displaystyle \sum _ { k = 1 } ^ { n } P ( \xi _ { n } = 0 , \xi _ { k } = 0 , \xi _ { i } \neq 0 , i = 1 , \cdots , k - 1 ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } P ( \xi _ { k } = 0 , \xi _ { i } \neq 0 , i = 1 , \cdots , k - 1 ) } \\ & { \qquad \quad \times P ( \xi _ { n } = 0 | \xi _ { k } = 0 , \xi _ { i } \neq 0 , i = 1 , \cdots , k - 1 ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } P ( \xi _ { k } = 0 , \xi _ { i } \neq 0 , i = 1 , \cdots , k - 1 ) P ( \xi _ { n } = 0 | \xi _ { k } = 0 ) } \\ & { = \displaystyle \sum _ { k = 1 } ^ { n } f _ { 0 } ( k ) p _ { 0 } ( n - k ) . } \end{array}
$$

Having proved (5.23). we are going to make use of it. First we notice that the probability that the process will ever return to 0 equals $\scriptstyle \sum _ { n = 1 } ^ { \infty } f _ { 0 } ( n )$ . Next, from (5.23) we infer that

$$
\begin{array} { l } { { \displaystyle P ( \exists n \geq 1 : \xi _ { n } = 0 ) = \sum _ { n = 1 } ^ { \infty } f _ { 0 } ( n ) } } \\ { { = 1 - \left( \sum _ { n = 0 } ^ { \infty } p _ { 0 } ( n ) \right) ^ { - 1 } = 1 - \left( \sum _ { k = 0 } ^ { \infty } p _ { 0 } ( 2 k ) \right) ^ { - 1 } . } } \end{array}
$$

Since $\begin{array} { r } { p _ { 0 } ( 2 k ) = \frac { ( 2 k ) ! } { ( k ! ) ^ { 2 } } ( p q ) ^ { k } } \end{array}$ and

$$
\sum _ { k = 0 } ^ { \infty } { \binom { 2 k } { k } } x ^ { k } = ( 1 - 4 x ) ^ { - 1 / 2 } , \ | x | < { \frac { 1 } { 4 } } ,\tag{5.24}
$$

it follows, that for $p \neq 1 / 2$

$$
P ( \exists n \geq 1 : \xi _ { n } = 0 ) = 1 - ( 1 - 4 p q ) ^ { 1 / 2 } = 1 - | p - q | ,\tag{5.25}
$$

since, recalling that ${ \pmb q } = { \pmb 1 } - { \pmb p }$ ,we have $1 - 4 p q = 1 - 4 p + 4 p ^ { 2 } = ( 1 - 2 p ) ^ { 2 } =$ $( q - p ) ^ { 2 }$

The case $p = 1 / 2$ is more delicate and we shall not pursue this topic here. Let us only remark that the case $p = 1 / 2$ needs a special treatment as in Proposition 5.4.

## Exercise 5.10

Prove formula (5.24).

Hint Use the Taylor formula to expand the right-hand side of (5.24) into a power series.

## Exercise 5.11 (branching process)

On the island Elschenbieden there lives an almost extinct species called Vugiel. Vugiel's males can produce zero, one, two, ... male offspring with probability po, $\pmb { p _ { 1 } , p _ { 2 } , \cdots }$ respectively, where ${ \pmb p } _ { \bf i } \geq { \bf 0 }$ and $\textstyle \sum _ { i = 0 } ^ { \infty } p _ { i } = 1$ A challenging problem would be to find the Vugiel's chances of survival assuming that each individual lives exactly one year. At this moment, we ask you only to rewrite the problem in the language of Markov chains.

Hint The number of descendants of each male has the same distribution.

## Exercise 5.12

Consider the following two cases:

1) In Exercise 5.11 suppose that

$$
p _ { m } = { \binom { N } { m } } p ^ { m } ( 1 - p ) ^ { N - m } .
$$

for some $\pmb { p } \in ( \mathbf { 0 } , \mathbf { 1 } )$ and $N \in \mathbb { N } ^ { }$ , where $\mathbb { N } ^ { * } = \{ 1 , 2 , 3 , \cdot \cdot \cdot \}$ . (Note that $p _ { m } = 0 \ i f \ m > N . )$ Show that

$$
p ( j | i ) = { \binom { N i } { j } } p ^ { j } ( 1 - p ) ^ { N i - j } .\tag{5.26}
$$

Deduce that, in particular, $p ( j | i ) = 0 \ \mathrm { i f } \ j > N i$

## Suppose that

$$
p _ { m } = \frac { \lambda ^ { m } } { m ! } e ^ { - \lambda } , m \in \mathbb { N } ,
$$

for some $\lambda > 0$ .In other words, assume that each $X _ { j }$ has the Poisson distribution with mean λ. Show that

$$
p ( j | i ) = \frac { ( \lambda i ) ^ { j } } { j ! } e ^ { - \lambda i } , j , i \geq 0 .\tag{5.27}
$$

Hint If $x _ { 1 }$ has the binomial distribution $\begin{array} { r } { P ( X _ { 1 } = m ) = { \binom { N } { m } } p ^ { m } ( 1 - p ) ^ { N - m } , m \in \mathbb { N } , } \end{array}$ then there exists a finite sequence $\eta _ { 1 } ^ { 1 } , \cdots , \eta _ { N } ^ { 1 }$ of independent identically distributed random variables such that $P ( \eta _ { j } ^ { 1 } = 1 ) = p , P ( \eta _ { j } ^ { 1 } = 0 \bar { ) } = 1 - p$ and $X _ { j } = \eta _ { 1 } ^ { j } + \cdot \cdot +$

$\eta _ { N } ^ { j } = m$ Hence, $\begin{array} { r } { X _ { 1 } + \cdot \cdot \cdot + X _ { i } = \sum _ { r = 1 } ^ { i } \sum _ { s = 1 } ^ { N } \eta _ { s } ^ { r } } \end{array}$ , i.e. the sum of Ni independent identically distributed random variables with distribution as above. Hence we infer (5.26).

## Proposition 5.6

The probability of survival in Exercise 5.12, part 2) equals 0 if $\lambda \leq 1$ , and $1 - \hat { r } ^ { k }$ if $\lambda > 1$ , where k is the initial Vugiel population and ${ \hat { r } } \in ( 0 , 1 )$ is a solution to

$$
r = e ^ { ( r - 1 ) \lambda } .\tag{5.28}
$$

## Proof

We denote by $\phi ( i ) , i \in \mathbb { N }$ the probability of dying out subject to the condition $\pmb { \xi _ { 0 } } = \pmb { i }$ Hence, if $A = \{ \xi _ { n } = 0$ for ${ \mathfrak { n } } \in \mathbb { N } \}$ , then

$$
\phi ( i ) = P \left( A | \xi _ { 0 } = i \right) .\tag{5.29}
$$

Obviously, $\phi ( 0 ) = 1$ and the total probability formula together with the Markov property (5.10) imply that for each $i \in \mathbb { N }$

$$
\begin{array} { r c l } { \displaystyle \phi ( i ) = \sum _ { j = 0 } ^ { \infty } P \left( A | \xi _ { 0 } = i , \xi _ { 1 } = j \right) P \left( \xi _ { 1 } = j | \xi _ { 0 } = i \right) } \\ { \displaystyle } & { = \sum _ { j = 0 } ^ { \infty } P \left( A | \xi _ { 1 } = j \right) P \left( \xi _ { 1 } = j | \xi _ { 0 } = i \right) } \\ { \displaystyle } & { = \sum _ { j = 0 } ^ { \infty } \phi ( j ) p ( j | i ) . } \end{array}
$$

Therefore, the sequence $\phi ( i ) , i \in \mathbb { N }$ is bounded (by 1 from above and by 0 from below) and satisfies the following system of equations

$$
\begin{array} { l } { \displaystyle \phi ( \boldsymbol { i } ) ~ = ~ \sum _ { j = 0 } ^ { \infty } \phi ( j ) p ( j | \boldsymbol { i } ) , ~ \boldsymbol { i } \in \mathbb N , } \\ { \displaystyle \phi ( 0 ) ~ = ~ 1 . } \end{array}\tag{5.30}
$$

So far, we have not used any particular distribution of $X _ { j }$ . From now on, we shall assume that the $X _ { j }$ have the Poisson distribution. Hence, by Exercise 5.12, $\begin{array} { r } { p ( j | i ) = \frac { ( i \lambda ) ^ { j } } { j ! } e ^ { - i \lambda } } \end{array}$ . It is not an easy problem to find a solution to (5.30), even in this special case. $\phi ( i )$ is the probability that the population will die out, subject to the condition that initially there were i individuals. Since we assume that reproduction of different individuals is independent, it is reasonable to make the following Ansatz:

$$
\phi ( i ) = A [ \phi ( 1 ) ] ^ { i } , i \in \mathbb { N } ,\tag{5.31}
$$

for some $A > 0$ . Although it is possible to prove this Ansatz, we shall not do so here. Note that the boundary condition $\phi ( 0 ) = 1$ implies that $A = 1$ Substituting (5.31) (with $A = 1$ and $r : = \phi ( 1 ) )$ into (5.30), we get

$$
\begin{array} { l } { { r ^ { i } ~ = ~ \displaystyle \sum _ { j = 0 } ^ { \infty } r ^ { j } \frac { ( i \lambda ) ^ { j } } { j ! } e ^ { - i \lambda } } } \\ { { ~ = ~ e ^ { - i \lambda } \displaystyle \sum _ { j = 0 } ^ { \infty } \frac { 1 } { j ! } ( i r \lambda ) ^ { j } = e ^ { - i \lambda } e ^ { i r \lambda } . } } \end{array}
$$

Hence, r should satisfy

$$
r = e ^ { ( r - 1 ) \lambda } .\tag{5.32}
$$

Since the function $g ( r ) = e ^ { ( r - 1 ) \lambda } , r \in [ 0 , 1 ]$ , is convex, there exist at most two solutions to the equation (5.32). Obviously, one of them is $\pmb { r } = \pmb { 1 }$ . A bit of analysis, not included here, shows the following:

If $\lambda \leq 1$ , then the only solution to (5.32) in [0, 1} is $\pmb { r } = 1$

If $\lambda > 1$ , then there exists a second solution ${ \hat { r } } \in ( 0 , 1 )$ of the equation (5.32).

In case 1) the situation is simple. We have $\phi ( i ) = 1$ for all i, and thus the probability of extinction is 1 for any initial number of individuals. Case 2) is slightly more involved. The first question we need to address is which of the two solutions of (5.32) gives the correct value of $\phi ( 1 ) \smash { \vdots }$ Recall that $\begin{array} { r } { p _ { k } = \frac { \lambda ^ { k } } { k ! } e ^ { - \lambda } } \end{array}$ Define •

$$
F ( x ) = \sum _ { k = 0 } ^ { \infty } p _ { k } x ^ { k } = \sum _ { k = 0 } ^ { \infty } { \frac { \lambda ^ { k } } { k ! } } e ^ { - \lambda } x ^ { k } = e ^ { \lambda x } e ^ { - \lambda } , | x | \leq 1 .\tag{5.33}
$$

Since $P ( \xi _ { 1 } = 0 | \xi _ { 0 } = 1 ) = p _ { 0 }$ and

$$
\begin{array} { r l r } { P \left( \xi _ { 2 } = 0 | \xi _ { 0 } = 1 \right) = } & { { } \displaystyle \sum _ { i = 0 } ^ { \infty } P \left( \xi _ { 2 } = 0 | \xi _ { 1 } = i \right) P \left( \xi _ { 1 } = i | \xi _ { 0 } = 1 \right) } & { } \\ { \displaystyle } & { { } = \sum _ { i = 0 } ^ { \infty } ( p _ { 0 } ) ^ { i } p _ { i } = F ( p _ { 0 } ) = F ( F ( 0 ) ) , } \end{array}
$$

we guess that the following holds:

$$
P ( \xi _ { n } = 0 | \xi _ { 0 } = 1 ) = F ^ { ( n ) } ( 0 ) ,\tag{5.34}
$$

where $F ^ { ( n ) }$ is the n-fold composition of F. To prove (5.34) it is enough to prove it for n, while assuming it holds for $\pmb { n } - 1$ . We have

$$
P \left( \xi _ { n } = 0 | \xi _ { 0 } = 1 \right) = \sum _ { i = 0 } ^ { \infty } P \left( \xi _ { n } = 0 | \xi _ { 1 } = i \right) P \left( \xi _ { 1 } = i | \xi _ { 0 } = 1 \right)
$$

$$
\begin{array} { r l } {  { = \sum _ { i = 0 } ^ { \infty } p _ { i } P ( \xi _ { n - 1 } = 0 | \xi _ { 0 } = i ) } \quad } & { } \\ & { = \sum _ { i = 0 } ^ { \infty } p _ { i } [ P ( \xi _ { n - 1 } = 0 | \xi _ { 0 } = 1 ) ] ^ { i } } \\ & { = \sum _ { i = 0 } ^ { \infty } p _ { i } [ F ^ { ( n - 1 ) } ( 0 ) ] ^ { i } = F ( F ^ { ( n - 1 ) } ( 0 ) ) = F ^ { ( n ) } ( 0 ) . } \end{array}
$$

Since the event $\{ \xi _ { n } = 0 \}$ is contained in events $\{ \xi _ { n + 1 } = 0 \}$ for all $n \in \mathbb { N } ,$ we have

$$
\begin{array} { l } { \phi ( 1 ) ~ = ~ P \left\{ \xi _ { n } = 0 , ~ \mathrm { f o r ~ s o m e } ~ n \in \mathbb { N } | \xi _ { 0 } = 1 \right\} } \\ { ~ = ~ \displaystyle \operatorname* { l i m } _ { n \to \infty } P \left\{ \xi _ { n } = 0 | \xi _ { 0 } = 1 \right\} } \end{array}
$$

by the Lebesgue monotone convergence theorem. Therefore, we infer that

$$
\phi ( 1 ) = \operatorname* { l i m } _ { n \to \infty } F ^ { ( n ) } ( 0 ) .
$$

With $F ^ { ( 0 ) } ( x ) = x$ we only need to show that

$$
F ^ { ( n ) } ( 0 ) \leq { \hat { r } } , n \in \mathbb { N } .\tag{5.35}
$$

Indeed, once the inequality (5.35) is proven, we infer that $\phi ( 1 ) \leq { \hat { r } }$ and thus $\phi ( 1 ) = { \hat { r } }$ . We shall prove (5.35) by induction. It is obviously valid for $n = 0$ , so we need to study the inductive step. We have

$$
F ^ { ( n ) } ( 0 ) = F \left( F ^ { ( n - 1 ) } ( 0 ) \right) \leq F ( { \hat { r } } ) = { \hat { r } } ,
$$

since $\pmb { F }$ is increasing. We conclude that in the case $\lambda > 1$ the population will become extinct with positive probability.

In the simplest example of the binomial distribution case, i.e. when ${ N } = 1$ equations (5.30) become

$$
\phi ( i ) = \sum _ { j = 0 } ^ { i } \phi ( j ) { \binom { i } { j } } p ^ { j } ( 1 - p ) ^ { i - j } , i \in \mathbb { N } .
$$

Since ${ \phi ( 0 ) = 1 , \phi ( 1 ) }$ satisfies

$$
\phi ( 1 ) = q + \phi ( 1 ) p
$$

with ${ \pmb q } = { \pmb 1 } - { \pmb p }$ Hence, trivially, $\phi ( { \bf 1 } ) = { \bf 1 }$ . Then, by induction, one proves that $\phi ( i ) = 1$ . Therefore, whatever the initial number of individuals, extinction of the species is certain.

## Remark 5.2

The method presented in the last solution works for any distribution of the variables Xj. It turns out that the mean value λ of X1 plays the same role as $X _ { j }$ $X _ { 1 }$ above. One can show that if $\lambda \leq 1$ , then the population will become extinct with probability 1, while for $\lambda > 1$ the probability of extinction is larger than 0 and smaller than 1.

## Exercise 5.13

On the, now familiar, island of Elschenbieden the question of survival of the Vugiel is a hot political issue. The (human) population of the island is N. Those who believe that action should be taken in order to help the animals preach their conviction quite convincingly. For if a supporter discusses the issue with a non-supporter, the latter will change his mind with probability one. However, they do so only in face-to-face encounters. Suppose that the probability of an encounter of exactly one pair of humans during one day is p and that with probability q this pair is a supporter-non-supporter one. Write down a Markov chain model of this situation. Neglect the probability of two or more encounters during one day.

Hint On each day the number of supporters can either increase by 1 or remain unchanged. What is the probability of the former?

## Exercise 5.14 (queuing model)

A car wash machine can serve at most one customer at a time. With probability $p , 0 < p < 1$ , the machine can finish serving a customer in a unit time. If this happens, the next waiting car (if any) can be served at the beginning of the next unit of time. During the time interval between the nth and $( n + 1 )$ st unit of time the number of cars arriving has the Poisson distribution with parameter $\lambda > 0$ ). Let ξn denote the number of cars being served or waiting to be served $\xi _ { n }$ at the beginning of unit n. Show that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain and find its transition probabilities.

Hint Let $Z _ { n } , \ n \ = \ 0 , 1 , 2 , \cdot \cdot \cdot$ be a sequence of independent identically distributed random variables, each having the Poisson distribution with parameter λ. Then $\xi _ { n + 1 } -$ $\xi _ { n } - z _ { n }$ equals -1 or 0.

## Remark 5.3

In the last model we are interested in the behaviour of ξn for large values of n. $\xi _ { n }$ In particular, it is interesting to determine whether the limit of ξn or that of $\xi _ { n }$ $\mathbb { E } \xi _ { n }$ $( \textsf { a s n } \to \infty )$ exists. In Exercise 5.36 we shall find conditions which guarantee

the existence of a unique invariant measure and imply that the Markov chain in question is ergodic.

## 5.2 Classification of States

In what follows we fix an S-valued Markov chain with transition matrix $P =$ $[ p ( j | i ) ] _ { j , i \in S }$ , where S is a non-empty and at most countable set.

## Definition 5.5

A state i is called recurrent if the process $\xi _ { n }$ will eventually return to i given that it starts at i, i.e.

$$
P ( \xi _ { n } = i { \mathrm { ~ f o r ~ s o m e ~ } } n \geq 1 | \xi _ { 0 } = i ) = 1 .\tag{5.36}
$$

If the condition (5.36) is not satisfied, then the state i is called transient.

## Theorem 5.1

Show that for a random walk on Z with parameter $p \in ( 0 , 1 )$ , the state 0 is recurrent if and only if $p = 1 / 2$ . Show that the same holds if 0 is replaced by any other state $i \in \mathbb { Z }$

## Proof

We know from (5.25) that $P ( \xi _ { n } = i$ for some $n \geq 1 | \xi _ { 0 } = i ) = 1 - | p - q |$ for any $\ i \in \mathbb Z$ .

## Definition 5.6

We say that a state i communicates with a state j if with positive probability the chain will visit the state j having started at i, i..

$$
P ( \xi _ { n } = j { \mathrm { ~ f o r ~ s o m e ~ } } n \geq 0 | \xi _ { 0 } = i ) > 0 .\tag{5.37}
$$

If i communicates with j, then we shall write i → j. We say that the state i intercommunicates with a state j, and write i ←→ j, if i → j and $j  i$

## Exercise 5.15

Show that $i  j \ i f$ and only if $p _ { k } ( j | i ) > 0$ for some $k \geq 1$

Hint Recall that $p _ { k } ( j | i ) = P ( \xi _ { k } = j | \xi _ { 0 } = i )$

## Exercise 5.16

Show that

1) $i  i ,$

if $i  j$ then $j  i$ and

if $i  j , j  k$ then $i  k$

In other words, show that ←→ is an equivalence relation on S.

Hint 1) and 2) are obvious. For 3) use the Chapman-Kolmogorov equations.

## Exercise 5.17

For $| x | < 1$ and $j , i \in S$ define

$$
P _ { j i } ( x ) = \sum _ { n = 0 } ^ { \infty } p _ { n } ( j | i ) x ^ { n } ,\tag{5.38}
$$

$$
F _ { j i } ( x ) = \sum _ { n = 1 } ^ { \infty } f _ { n } ( j | i ) x ^ { n } ,\tag{5.39}
$$

where $f _ { n } ( j | i ) = P ( \xi _ { n } = j , \xi _ { k } \neq j , k = 1 , \cdot \cdot \cdot , n - 1 | \xi _ { 0 } = i )$ .Show that the power series in (5.38)(5.39) are absolutely convergent for $| x | < 1$ and that

$$
P _ { j i } ( x ) = F _ { j i } ( x ) P _ { j j } ( x ) , \mathrm { i f } j \ne i ,\tag{5.40}
$$

$$
P _ { i i } ( x ) = 1 + F _ { i i } ( x ) P _ { i i } ( x ) .\tag{5.41}
$$

Hint Note that $| p _ { n } ( j | i ) | \leq 1$ , so the radius of convergence of the power series (5.38) is $\geq 1$

## Exercise 5.18

Show that $\begin{array} { r } { \operatorname* { l i m } _ { x  1 } P _ { j j } ( x ) = \sum _ { n = 0 } ^ { \infty } p _ { n } ( j | j ) \quad } \end{array}$ and $\begin{array} { r } { \operatorname* { l i m } _ { x \mathcal { A } } F _ { j j } ( x ) = \sum _ { n = 0 } ^ { \infty } f _ { n } ( j | j ) } \end{array}$

Hint Apply Abel's lemma²: If $\pmb { a _ { k } } \geq 0$ for all ${ \pmb k } \geq { \pmb 0 }$ and lim $\operatorname* { s u p } _ { k \to \infty } \sqrt [ k ] { \left| a _ { k } \right| } \leq 1$ , then $\begin{array} { r } { \operatorname* { l i m } _ { x \nearrow 1 } \sum _ { k = 0 } ^ { \infty } a _ { k } x ^ { k } = \sum _ { k = 0 } ^ { \infty } a _ { k } } \end{array}$ , no matter whether this sum is finite or infinite.

## Exercise 5.19

Show that a state j is recurrent if and only if $j$ $\begin{array} { r } { \sum _ { n } p _ { n } ( j | j ) = \infty } \end{array}$ . Deduce that the state $j$ is transient if and only if

$$
\sum _ { n } p _ { n } ( j | j ) < \infty .\tag{5.42}
$$

Show that if j is transient, then for each $i \in S$

$$
\sum _ { n } p _ { n } ( j | i ) < \infty .\tag{5.43}
$$

Hint If $\pmb { j }$ is recurrent, then $\begin{array} { r } { F _ { j j } ( { \boldsymbol { x } } )  \sum _ { n } f _ { n } ( { \boldsymbol { j } } | { \boldsymbol { j } } ) = 1 } \end{array}$ as x 1. Use (5.41) in conjunction with Abel's Lemma.

## Exercise 5.20

For a Markov chain $\xi _ { n }$ with transition matrix $P = { \left[ \begin{array} { l l } { 1 - p } & { q } \\ { p } & { 1 - q } \end{array} \right] }$ show that both states are recurrent.

Hint Use Exercise 5.19 and 5.5.

One may suspect that if the state space S is finite, then there must exist at least one recurrent state. For otherwise, if all states were transient and $S = \{ 1 , 2 , \cdots , N \}$ }, then with positive probability a chain starting from 1 would visit 1 only a finite number of times. Thus, after visiting that state for the last time, the chain would move to a different state, say i2, in which it would $\mathbf { i } _ { 2 } ,$ stay for a finite time only with positive probability. Thus, in finite time, with positive probability, the chain will never return to states 1 and i2. By induction, $i _ { 2 }$ in finite time, with positive probability, the chain will never return to any of the states. This is impossible. The following exercise will give precision to this argument.

## Exercise 5.21

Show that if ξn is a Markov chain with finite state space S, then there exists $\xi _ { n }$ at least one recurrent state $i \in S$

Hint Argue by contradiction and use (5.43).

The following result is quoted here for reference. The proof is surprisingly difficult and falls beyond the scope of this book.

## Theorem 5.2

A state $j \in S$ is recurrent if and only if

$$
P ( \xi _ { n } = j { \mathrm { ~ f o r ~ i n f i n i t e l y ~ m a n y ~ } } n | \xi _ { 0 } = j ) = 1 ,
$$

and is transient if and only if

$$
P ( \xi _ { n } = j { \mathrm { ~ f o r ~ i n f i n i t e l y ~ m a n y ~ } } n | \xi _ { 0 } = j ) = 0 .
$$

## Definition 5.7

For an S-valued Markov chain $\xi _ { n } , n \in \mathbb { N } ,$ a state $i \in S$ is called null-recurrent if it is recurrent and its mean recurrence time $\mathbf { m _ { i } }$ defined by

$$
m _ { i } : = \sum _ { n = 0 } ^ { \infty } n f _ { n } ( i | i )\tag{5.44}
$$

equals ∞. A state $\textit { i } \in \textit { S }$ is called positive-recurrent if it is recurrent and its mean recurrence time $m _ { i }$ is finite.

## Remark 5.4

One can show that a recurrent state i is null-recurrent if and only if $p _ { n } ( i | i ) \to 0$

We already know that for a random walk on Z the state O is recurrent if and only if $\pmb { p = 1 / 2 , \mathrm { i } . e }$ and only if the random walk is symmetric. In the following problem we shall try to answer if 0 is a nul-recurrent or positive-recurrent state (when ${ \pmb p } = { \bf 1 } / 2 )$

## Exercise 5.22

Consider a symmetric random walk on Z. Show that O is a null-recurrent state. Can you deduce whether other states are positive-recurrent or null-recurrent?

Hint State 0 is null-recurrent if and only if $\begin{array} { r } { \sum _ { n } n f _ { n } ( 0 | 0 ) = \infty } \end{array}$ . As in Exercise 5.18, $\begin{array} { r } { \sum _ { n } n f _ { n } ( 0 | 0 ) = \operatorname* { l i m } _ { x \nearrow 1 } F _ { 0 0 } ^ { \prime } ( x ) } \end{array}$ , where ${ \pmb { F } } _ { 0 0 }$ is defined by (5.39).

## Exercise 5.23

For the Markov chain $\xi _ { n }$ from Exercise 5.20 show that not only are all states recurrent, but they are positive-recurrent.

Hint Calculate ${ f _ { n } ( 0 | 0 ) }$ directly.

The last two exercises suggest that the type of a state $\textit { \textbf { i } } { \boldsymbol { S } }$ ,i.e. whether it is transient, null-recurrent or positive-recurrent is invariant under the equivalence ←→. We shall investigate this question in more detail below, but even before doing so we need one more notion: that of a periodic state.

## Definition 5.8

Suppose that $\xi _ { n } , n \in \mathbb { N } ,$ is a Markov chain on a state space S. Let $i \in S$ .We say that i is a periodic state if and only if the greatest common divisor (gcd) of all $\pmb { n } \in \mathbb { N } ^ { \ast }$ , where $\mathbb { N } ^ { * } = \{ 1 , 2 , 3 , \cdot \cdot \cdot \}$ , such that $p _ { n } ( i | i ) > 0 \mathrm { ~ i s ~ } \geq 2$ Otherwise, the state i is called aperiodic. In both cases, the gcd is denoted by d(i) and is called the period of the state i. Thus, i is periodic if and only if $d ( i ) \geq 2$ A state i which is positive recurrent and aperiodic is called ergodic.

## Exercise 5.24

Is this claim that $p _ { d \left( i \right) } ( i | i ) > 0$ true or not?

Hint Think of a Markov chain in which it is possible to return to the starting point by two different routes. One route with four steps, the other one with six steps.

One of the by-products of the following exercise is another example of the type asked for in Exercise 5.24.

## Exercise 5.25

Consider a Markov chain on $S \ = \ \{ 1 , 2 \}$ with transition probability matrix $P = \left[ { \begin{array} { l l } { 0 } & { 1 / 2 } \\ { 1 } & { 1 / 2 } \end{array} } \right]$ . This chain can also be described by the graph in Figure 5.1. Find d(1) and d(2).

<!-- image-->  
Figure 5.1. Transition probabilities of the Markov chain in Exercise 5.25

$$
P ^ { 2 }
$$

$$
P ^ { 3 }
$$

## Proposition 5.7

Suppose that $i , j \in S$ and $i  j$ .Show that

i is transient if and only if j is;

i is recurrent if and only if j is;

3i is null-recurrent if and only if j is;

is positive-recurrent if and only if j is;

5) i is periodic if and only if j is, in which case $d ( i ) = d ( j )$

6)i is ergodic if and only if j is.

## Proof

It is enough to show properties 1), 4) and 5). Since i ←→ j one can find $n , m \in \mathbb { N }$ such that $p _ { m } ( j | i ) > 0$ and $p _ { n } ( i | j ) > 0$ Hence $\varepsilon : = p _ { m } ( j | i ) p _ { n } ( i | j )$ is positive. Let us take $k \in \mathbb { N } .$ Then by the Chapman-Kolmogorov equations

$$
p _ { m + k + n } ( j | j ) = \sum _ { r , s \in S } p _ { m } ( j | s ) p _ { k } ( s | r ) p _ { n } ( r | j ) \geq p _ { m } ( j | i ) p _ { k } ( i | i ) p _ { n } ( i | j ) = \varepsilon p _ { k } ( i | i ) .
$$

By symmetry

$$
p _ { n + k + m } ( i | i ) = \sum _ { r , s \in S } p _ { n } ( i | s ) p _ { k } ( s | r ) p _ { m } ( r | i ) \geq p _ { n } ( i | j ) p _ { k } ( j | j ) p _ { m } ( j | i ) = \varepsilon p _ { k } ( j | j ) .
$$

Hence, the series $\sum _ { k } p _ { k } ( i | i )$ and $\textstyle \sum _ { k } p _ { k } ( j | j )$ are simultaneously convergent or divergent. Hence 1) follows in view of Exercise 5.19.

To prove 5) it is enough to show that

$$
d ( i ) \leq d ( j ) .
$$

Using the first inequality derived above, we have

$$
p _ { n + k + m } ( i | i ) \geq \varepsilon p _ { k } ( j | j )
$$

for all $k \in \mathbb { N } .$ From this inequality we can draw two conclusions:

(a) $\pmb { d } ( i ) | n + m$ , since by taking $\pmb { k } = \mathbf { 0 }$ we get $p _ { n + m } ( i \vert i ) > 0$

b) if $p _ { k } ( j | j ) > 0$ , then $p _ { n + k + m } ( i | i ) > 0$

From (a) and (b) we can see that $d ( i ) | k$ provided that $p _ { \mathbb { k } } ( j | j ) > 0$ This proves what is required.