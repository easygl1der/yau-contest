## Exercise 6.11

From the formula in Exercise 6.10 deduce that the random variables $\eta _ { n } ^ { t }$ and $N ( t )$ are independent, and the ηh have the same probability distribution as $\eta _ { n } ^ { t }$ $\eta _ { n }$ i.e. exponential with parameter λ.

Hint What do you get if you take the expectation on both sides of the equality in Exercise 6.10? Can you deduce the probability distribution of $\eta _ { n } ^ { t } ?$ Can you see that the $\eta _ { n } ^ { t }$ are independent?

To prove that the $\eta _ { n } ^ { t }$ are independent of $N ( t )$ you need to be a little more careful and integrate over $\{ N ( t ) = n \}$ instead of taking the expectation.

Because $N ( t + s ) - N ( t )$ can be defined in terms of $\eta _ { 1 } ^ { t } , \eta _ { 2 } ^ { t } , \ldots$ in the same way the original Poisson process $N ( t )$ is defined in terms of $\eta _ { 1 } , \eta _ { 2 } , \ldots$ , the result in Exercise 6.11 proves the theorem below.

## Theorem 6.1

For any fixed $t \geq 0$

$$
N ^ { t } ( s ) = N ( t + s ) - N ( t ) ; \quad s \geq 0
$$

is a Poisson process independent of N(t) with the same probability law as $N ( s )$

That is to say, for any $s , t \geq 0$ the increment $N ( t + s ) - N ( t )$ is independent of $N ( t )$ and has the same probability distribution as $N ( s )$ . The assertion can be generalized to several increments, resulting in the following important theorem.

## Theorem 6.2

For any $0 \leq t _ { 1 } \leq t _ { 2 } \leq \cdots \leq t _ { n }$ the increments

$$
N ( t _ { 1 } ) , N ( t _ { 2 } ) - N ( t _ { 1 } ) , N ( t _ { 3 } ) - N ( t _ { 2 } ) , \ldots , N ( t _ { n } ) - N ( t _ { n - 1 } )
$$

are independent and have the same probability distribution as

$$
N ( t _ { 1 } ) . N ( t _ { 2 } - t _ { 1 } ) , N ( t _ { 3 } - t _ { 2 } ) , \ldots , N ( t _ { n } - t _ { n - 1 } ) .
$$

## Proof

From Theorem 6.1 it follows immediately that each increment $N ( t _ { i } ) - N ( t _ { i - 1 } )$ has the same distribution as $N ( t _ { i } - t _ { i - 1 } )$ for $i = 1 , \ldots , n$

It remains to prove independence. This can be done by induction. The case when $\pmb { n = 2 }$ is covered by Theorem 6.1. Now suppose that independence holds for n increments of a Poisson process for some $\mathbf { \mathscr { n } } \geq \mathbf { 2 } .$ Take any sequence

$$
0 \leq t _ { 1 } \leq t _ { 2 } \leq \cdots \leq t _ { n } \leq t _ { n + 1 } .
$$

By the induction hypothesis

$$
N ( t _ { n + 1 } ) - N ( t _ { n } ) , \dots , N ( t _ { 2 } ) - N ( t _ { 1 } )
$$

are independent, since they can be regarded as increments of

$$
N ^ { t _ { 1 } } ( s ) = N ( t _ { 1 } + s ) - N ( t _ { 1 } ) ,
$$

which is a Poisson process by Theorem 6.1. By the same theorem these increments are independent of $N ( t _ { 1 } )$ . It follows that the $\pmb { n } + \pmb { 1 }$ random variables

$$
N ( t _ { n + 1 } ) - N ( t _ { n } ) , \ldots , N ( t _ { 2 } ) - N ( t _ { 1 } ) , N ( t _ { 1 } )
$$

are independent, completing the proof.

## Definition 6.7

We say that a stochastic process $\xi ( t )$ , where $t \in T$ , has independent increments if

$$
\xi ( t _ { 1 } ) - \xi ( t _ { 0 } ) , \ldots , \xi ( t _ { n } ) - \xi ( t _ { n - 1 } )
$$

are independent for any $t _ { 0 } < t _ { 1 } < \cdots < t _ { n }$ such that $t _ { 0 } , t _ { 1 } , \ldots , t _ { n } \in T$

## Definition 6.8

A stochastic process $\xi ( t )$ , where $t \in T$ , is said to have stationary increments if for any $s , t \in T$ the probability distribution of $\xi ( t + h ) - \xi ( s + h )$ is the same for each h such that $s + h , t + h \in T$

Theorem 6.2 implies that the Poisson process has stationary independent increments. The result in the next exercise is also a consequence of Theorem 6.2.

## Exercise 6.12

Show that $N ( t ) - \lambda t$ is a martingale with respect to the filtration $\mathcal { F } _ { t }$ generated by the family of random variables $\{ N ( s ) : s \in [ 0 , t ] \}$

Hint Observe that $N ( t ) - N ( s )$ is independent of $\pmb { \mathcal { F } } _ { s }$ by Theorem 6.2.

## 6.2.4 Various Exercises on the Poisson Process

Exercise 6.13

Show that $\xi _ { 0 } < \xi _ { 1 } < \xi _ { 2 } < \cdots \mathrm { a } . s$

Hint What is the probability of the event $\{ \xi _ { n - 1 } < \xi _ { n } \} = \{ \eta _ { n } > 0 \} ?$ What is the probability of the intersection of all such events?

## Exercise 6.14

Show that $\operatorname* { l i m } _ { n \to \infty } \xi _ { n } = \infty { \mathrm { ~ a . s . } }$

Hint If $\operatorname* { l i m } _ { n \to \infty } \xi _ { n } < \infty$ , then the sequence $\pmb { \eta _ { 1 } } , \pmb { \eta _ { 2 } } , \dotsc$ of independent random variables, all having the same exponential distribution, must be bounded. What is the probability that such a sequence is bounded? Begin with computing the probability $P \{ \eta _ { 1 } \leq m , \ldots , \eta _ { n } \leq m \}$ for any fixed $\mathbf { \nabla } m > 0$ .

Although it is instructive to estimate $P \{ \operatorname* { l i m } _ { n  \infty } \xi _ { n } < \infty \}$ in this way, there is a more elegant argument based on the strong law of large numbers. What does the law of large numbers tell us about the limit of $\frac { \sin } { n }$ as $\pmb { n }  \infty ?$

Exercise 6.15

Show that $\xi _ { n }$ has absolutely continuous distribution with density

$$
f _ { n } ( t ) = \lambda e ^ { - \lambda t } \frac { ( \lambda t ) ^ { n - 1 } } { ( n - 1 ) ! }
$$

with parameters n and λ. The density $f _ { n } ( t )$ of the gamma distribution is shown in Figure 6.4 for ${ \pmb n } = { \pmb 2 } ,$ 4 and $\lambda = 1$

<!-- image-->  
Figure 6.4. Density ${ f _ { n } } ( t )$ of the gamma distribution with parameters ${ \pmb n } = { \pmb 2 } , \lambda = { \pmb 1 }$ and ${ \pmb n } = { \pmb 4 } , \pmb \lambda = { 1 }$

Hint Use the formula for $P \left\{ \xi _ { n } > t \right\}$ in the proof of Proposition 6.1 to find the distribution function of $\xi _ { n }$ . Is this function differentiable? What is the derivative?

Exercise 6.16

Prove that

$$
\operatorname* { l i m } _ { t  \infty } N ( t ) = \infty \quad \mathrm { a . s . }
$$

Hint What is the limit of $P \{ N ( k ) \geq n \}$ as $\pmb { k }  \infty ?$ Can you express $\{ \operatorname* { l i m } _ { t \to \infty } N ( t )$ $= \infty \}$ in terms of the sets $\{ \dot { N } ( k ) \geq n \} ?$

Exercise 6.17

Verify that

$$
\begin{array} { r } { P \{ N ( t ) \mathrm { ~ i s ~ o d d } \} = e ^ { - \lambda t } \sinh ( \lambda t ) , } \\ { P \{ N ( t ) \mathrm { ~ i s ~ e v e n } \} = e ^ { - \lambda t } \cosh ( \lambda t ) . } \end{array}
$$

Hint What is the probability that $N ( t ) = 2 n + 1 ?$ Compare this to the n-th term of the Taylor expansion of sinh λt.

Exercise 6.18

Show that

$$
\operatorname* { l i m } _ { t \to \infty } { \frac { N ( t ) } { t } } = \lambda \quad \mathrm { a . s . }
$$

if $N ( t )$ is a Poisson process with parameter λ.

Hint $N ( n )$ is the sum of independent identically distributed random variables $N ( 1 )$ ${ N } ( 2 ) - \dot { N } ( \dot { 1 } ) , \dots , { N } ( n ) - { N } ( \dot { n - 1 } )$ , so the strong law of large numbers can be applied to obtain the limit of ${ \cal N } ( n ) / n$ as $\pmb { n }  \infty$ . Because $N ( t )$ is non-decreasing, the limit will not be affected if n is replaced by a continuous parameter $\pm \ge 0$ .

## 6.3 Brownian Motion

Imagine a cloud of smoke in completely still air. In time, the cloud will spread over a large volume, the concentration of smoke varying in a smooth manner. However, if a single smoke particle is observed, its path turns out to be extremely rough due to frequent collisions with other particles. This exemplifies two aspects of the same phenomenon called diffusion: erratic particle trajectories at the microscopic level, giving rise to a very smooth behaviour of the density of the whole ensemble of particles. The Wiener process $W ( t )$ defined below is a mathematical device designed as a model of the motion of individual diffusing particles. In particular, its paths exhibit similar erratic behaviour to the trajectories of real smoke particles. Meanwhile, the density $f _ { W ( t ) }$ of the random variable $W ( t )$ is very smooth, given by the exponential function

$$
f _ { W ( t ) } ( x ) = { \frac { 1 } { \sqrt { 2 \pi t } } } e ^ { - { \frac { x ^ { 2 } } { 2 t } } } ,
$$

which is a solution of the diffusion equation

$$
\frac { \partial f } { \partial t } = \frac { 1 } { 2 } \frac { \partial ^ { 2 } f } { \partial x ^ { 2 } }
$$

and can be interpreted as the density at time t of a cloud of smoke issuing form single point source at time 0. The Wiener process $W ( t )$ is also associated with the name of the British botanist Robert Brown, who around 1827 observed the random movement of pollen particles in water. We shall study mainly the one-dimensional Wiener process, which can be thought of as the projection of the position of a smoke particle onto one of the axes of a coordinate system.

Apart from describing the motion of diffusing particles, the Wiener process is widely applied in mathematical models involving various noisy systems, for example, the behaviour of asset prices at the stock exchange. If the noise in the system is due to a multitude of independent random changes, then the Central Limit Theorem predicts that the net result will have the normal distribution, a property shared by the increments $W ( t ) - W ( s )$ of the Wiener process. This is one of the main reasons of the widespread use of $W ( t )$ in mathematical models.

## 6.3.1 Definition and Basic Properties

## Definition 6.9

The Wiener process (or Brownian motion) is a stochastic process $W ( t )$ with values in R defined for $t \in [ 0 , \infty )$ such that

1) $W ( 0 ) = 0 \ \mathbf { a . s . } ;$

2) the sample paths $t \mapsto W ( t )$ are a.s. continuous;

3) for any finite sequence of times $0 ~ < ~ t _ { 1 } ~ < ~ \cdots ~ < ~ t _ { n }$ and Borel sets $A _ { 1 } , \ldots , A _ { n } \subset \mathbb { R }$

$$
\begin{array} { l } { P \left\{ W ( t _ { 1 } ) \in A _ { 1 } , \ldots , W ( t _ { n } ) \in A _ { n } \right\} } \\ { = \displaystyle \int _ { A _ { 1 } } \cdots \int _ { A _ { n } } p \left( t _ { 1 } , 0 , x _ { 1 } \right) p \left( t _ { 2 } - t _ { 1 } , x _ { 1 } , x _ { 2 } \right) \cdots } \\ { \quad \quad \cdot \cdot p \left( t _ { n } - t _ { n - 1 } , x _ { n - 1 } , x _ { n } \right) d x _ { 1 } \cdot \cdot \cdot d x _ { n } , } \end{array}
$$

where

$$
p \left( t , x , y \right) = \frac { 1 } { \sqrt { 2 \pi t } } e ^ { - \frac { \left( x - y \right) ^ { 2 } } { 2 t } }\tag{6.5}
$$

defined for any ${ \pmb x } , { \pmb y } \in \mathbb { R }$ and $\pmb { t } > 0$ is called the transition density.

A typical sample path of the Wiener process is shown in Figure 6.5.

<!-- image-->  
Figure 6.5. A typical path of (t)

## Exercise 6.19

Show that

$$
f _ { W ( t ) } \left( x \right) = \frac { 1 } { \sqrt { 2 \pi t } } e ^ { - \frac { x ^ { 2 } } { 2 t } }
$$

is the probability density of $W ( t )$ and find the expectation and variance of $W ( t )$

Hint The density of $W ( t )$ can be obtained from condition 3) of Definition 6.9 written for a single time t and a single Borel set. You will need the formula

$$
\int _ { - \infty } ^ { + \infty } e ^ { - \frac { x ^ { 2 } } { 2 } } d x = \sqrt { 2 \pi }
$$

to compute the integrals in the expressions for the expectation and variance.

## Remark 6.1

The results of Exercise 6.19 mean that $W ( t )$ has the normal distribution with mean 0 and variance t.

Exercise 6.20

Show that

$$
E \left( W ( s ) W ( t ) \right) = \operatorname* { m i n } \left\{ s , t \right\} .
$$

Hint The joint density of $W ( s )$ and W(t) will be needed. It can be found from condition 3) of Definition 6.9 written for two times s and t and two Borel sets.

Exercise 6.21

Show that

$$
E \left( \left| W ( t ) - W ( s ) \right| ^ { 2 } \right) = \left| t - s \right| .
$$

Hint Expand the square and use the formula in Exercise 6.20.

## Exercise 6.22

Compute the characteristic function E (exp(iλW(t)) for any $\lambda \in \mathbb { R } .$

Hint Use the density of $W ( t )$ found in Exercise 6.19.

Exercise 6.23

Find $E \left( W ( t ) ^ { 4 } \right)$

Hint This can be done, for example, by expressing the expectation in terms of the density of $W ( t )$ and computing the resulting integral, or by computing the fourth derivative of the characteristic function of $\breve { W } ( t )$ at 0. The second method is more efficient.

## Definition 6.10

We call $W ( t ) ~ = ~ \left( W ^ { 1 } ( t ) , \ldots , W ^ { n } ( t ) \right)$ an n-dimensional Wiener process if $W ^ { 1 } ( t ) , \ldots , W ^ { n } ( t )$ are independent R-valued Wiener processes.

## Exercise 6.24

For a two-dimensional Wiener process $W ( t ) = \bigl ( W ^ { 1 } ( t ) , W ^ { 2 } ( t ) \bigr )$ find the probability that $| W ( t ) | ~ < ~ R$ where $\smash { R \ > \ 0 }$ and |x| is the Euclidean norm of $\mathbf { \vec { x } } = \left( x ^ { 1 } , x ^ { 2 } \right)$ in $\mathbb { R } ^ { 2 } , \mathrm { i . e . } \ \lvert x \rvert ^ { 2 } = \left( x ^ { 1 } \right) ^ { 2 } + \left( x ^ { 2 } \right) ^ { 2 }$

Hint Express the probability in terms of the joint density of $W ^ { 1 } ( t )$ and $W ^ { 2 } ( t )$ . Independence means that the joint density of $\Breve { \boldsymbol { W } ^ { 1 } } ( t )$ and $\tilde { W ^ { 2 } } ( t )$ is the product of their respective densities, which are known from Exercise 6.19. It is convenient to use polar coordinates to compute the resulting integral over a disc.

## 6.3.2 Increments of Brownian Motion

## Proposition 6.2

For any $0 \leq s < t$ the increment $W ( t ) - W ( s )$ has the normal distribution with mean 0 and variance $t - s$

## Proof

By condition 3) of Definition 6.9 the joint density of $W ( s ) , W ( t )$ is

$$
f _ { W \left( s \right) , W \left( t \right) } \left( x , y \right) = p \left( s , 0 , x \right) p \left( t - s , x , y \right) .
$$

Hence, for any Borel set A

$$
 \begin{array} { r l } { P \left\{ W ( t ) - W ( s ) \in A \right\} = { \int } _ { \left\{ \{ s \} , y \} , \ldots \in A \right\} P \left( s , 0 , x \right) P \left( t - s , x , y \right) d x d y } \\ { = } & { \int _ { - \infty } ^ { + \infty } p \left( s , 0 , x \right) \left( { \int } _ { \left\{ y y : x = c + A \right\} } p \left( t - s , x , y \right) d y \right) d x } \\ { = } & { \int _ { - \infty } ^ { + \infty } p \left( s , 0 , x \right) \left( \int _ { c y } p \left( t - s , x , x + u \right) d u \right) d x } \\ { = } & { \int _ { - \infty } ^ { + \infty } p \left( s , 0 , x \right) \left( \int _ { c } p \left( t - s , 0 , u \right) d u \right) d x } \\ { = } & { \int _ { - \infty } ^ { + \infty } p \left( t - s , 0 , u \right) d u \int _ { - \infty } ^ { + \infty } p \left( s , 0 , x \right) d x } \\ { = } & { \int _ { c } p \left( t - s , 0 , u \right) d u \int _ { - \infty } ^ { + \infty } p \left( s , 0 , x \right) d x } \\ { = } & { \int _ { c } p \left( t - s , 0 , u \right) d u . } \end{array} }
$$

But $f \left( u \right) = p \left( t - s , 0 , u \right)$ is the density of the normal distribution with mean 0 and variance t - s, which proves the claim.

## Corollary 6.1

Proposition 6.2 implies that W(t) has stationary increments.

## Proposition 6.3

For any $0 = t _ { 0 } \leq t _ { 1 } \leq \cdot \cdot \cdot \leq t _ { n }$ the increments

$$
W ( t _ { 1 } ) - W ( t _ { 0 } ) , \ldots , W ( t _ { n } ) - W ( t _ { n - 1 } )
$$

are independent.

## Proof

From Proposition 6.2 we know that the increments of $W ( t )$ have the normal distribution. Because normally distributed random variables are independent if and only if they are uncorrelated, it suffices to verify that

$$
E \left[ \left( W ( u ) - W ( t ) \right) \left( W ( s ) - W ( r ) \right) \right] = 0
$$

for any $0 \leq r \leq s \leq t \leq u$ . But this follows immediately from Exercise 6.20:

$$
\begin{array} { r l } & { E \left[ \left( W ( u ) - W ( t ) \right) ( W ( s ) - W ( r ) ) \right] = ~ E \left( W ( u ) W ( s ) \right) - E \left( W ( u ) W ( r ) \right) } \\ & { ~ - E \left( W ( t ) W ( s ) \right) + E \left( W ( t ) W ( r ) \right) } \\ & { ~ = ~ s - r - s + r } \\ & { ~ = ~ 0 , } \end{array}
$$

as required.

## Corollary 6.2

For any $0 \leq s < t$ the increment $W ( t ) - W ( s )$ is independent of the σ-field

$$
\mathcal { F } _ { s } = \sigma \left\{ W ( r ) : 0 \leq r \leq s \right\} .
$$

## Proof

By Proposition 6.3 the random variables $W ( t ) - W ( s )$ and $W ( r ) { - } W ( 0 ) = W ( r )$ are independent if $0 \leq r \leq s \leq t$ Because the o-field $\pmb { \mathscr { F } _ { \pmb { \mathscr { s } } } }$ is generated by such $W ( r )$ , it follows that $W ( t ) - W ( s )$ is independent of $\pmb { \mathcal { F } } _ { \bullet } , \sqcup$

## Exercise 6.25

Show that W(t) is a martingale with respect to the filtration $\mathcal { F } _ { t }$

Hint Take advantage of the fact that $W ( t ) - W ( s )$ is independent of $\pmb { \mathcal { F } } _ { \pmb { \mathscr { s } } }$ $\bullet < t$

## Exercise 6.26

Show that $\left| W ( t ) \right| ^ { 2 } - t$ is a martingale with respect to the filtration $\mathcal { F } _ { t }$

Hint Once again, use the fact that $W ( t ) - W ( s )$ is independent of $\mathcal { F } _ { s }$ if $s < t$

Let us state without proof the following useful characterization of the Wiener process in terms of its increments.

## Theorem 6.3

A stochastic process $W ( t ) , t \geq 0 .$ is a Wiener cs  and ly thefollwig conditions hold:

1) $W ( 0 ) = 0 { \mathrm { ~ a . s . ; } }$

)the sample paths $t \mapsto W ( t )$ are continuous a.s.;

3) $W ( t )$ has stationary independent increments;

the increment $W ( t ) - W ( s )$ has the normal distribution with mean 0 and variance t - s for any $0 \leq s < t$ .

Exercise 6.27

Show that for any $\mathbf { \nabla } T > 0$

$$
V ( t ) = W ( t + T ) - W ( T )
$$

is a Wiener process if $W ( t )$ is.

Hint Are the increments of V(t) independent? What is their distribution? Does V(t) have continuous paths? Is it true that $V ( 0 ) = 0 ?$

The Wiener process can also be characterized by its martingale properties. The following theorem is also given without proof.

Theorem 6.4 (Lévy's martingale characterization)

Let $W ( t ) , t \geq 0$ , be a stochastic process and let $\mathcal { F } _ { t } ~ = ~ \sigma ( W _ { s } , s ~ \le ~ t )$ be the filtration generated by it. Then $W ( t )$ is a Wiener process if and only if the following conditions hold:

1) $W ( 0 ) = 0 \ \mathbf { a } . \mathbf { s } .$

2) the sample paths $t \mapsto W ( t )$ are continuous a.s.;

3 $W ( t )$ is a maringle wi respct  the fion $\mathcal { F } _ { t }$

4) $\left| W ( t ) \right| ^ { 2 } - t$ is a martingale with respect to $\mathcal { F } _ { t }$

## Exercise 6.28

Let $c > 0$ Show that $\begin{array} { r } { V ( t ) = \frac { 1 } { c } W ( c ^ { 2 } t ) } \end{array}$ is a Wiener process if $W ( t )$ is.

Hint Is V(t) a martingale? With respect to which filtration? Is $| V ( t ) | ^ { 2 } - t$ a martingale? Are the paths of $\mathsf { \bar { V } } ( t )$ continuous? Is it true that $V ( 0 ) = 0 ?$

## 6.3.3 Sample Paths

Let

$$
0 = t _ { 0 } ^ { n } < t _ { 1 } ^ { n } < \cdots < t _ { n } ^ { n } = T ,
$$

where

$$
t _ { i } ^ { n } = \frac { i T } { n } ,
$$

be a partition of the interval [0, T] into n equal parts. We denote by

$$
\varDelta _ { i } ^ { n } W = W ( t _ { i + 1 } ^ { n } ) - W ( t _ { i } ^ { n } )
$$

the corresponding increments of the Wiener process $W ( t )$

## Exercise 6.29

Show that

$$
\operatorname* { l i m } _ { n  \infty } \sum _ { i = 0 } ^ { n - 1 } ( \varDelta _ { i } ^ { n } W ) ^ { 2 } = T \quad \mathrm { i n ~ } L ^ { 2 } .
$$

Hint You need to show that

$$
\operatorname* { l i m } _ { n  \infty } E ( [ \sum _ { i = 0 } ^ { n - 1 } ( \varDelta _ { i } ^ { n } W ) ^ { 2 } - T ] ^ { 2 } ) = 0 .
$$

Use the independence of increments to simplify the expectation. What are the expectations of $\Delta _ { i } ^ { \hat { n } } W , ( \varDelta _ { i } ^ { n } W ) ^ { 2 }$ and $( \Delta _ { i } ^ { n } W ) ^ { 4 } ?$

The next theorem on the variation of the paths of $W ( t )$ is a consequence of the result in Exercise 6.29. First, let us recall that the variation of a function is defined as follows.

## Definition 6.11

The variation of a function $f : [ 0 , T ] \to \mathbb { R }$ is defined to be

$$
\operatorname* { l i m } _ { \Delta t \to 0 } \sum _ { i = 0 } ^ { n - 1 } | f ( t _ { i + 1 } ) - f ( t _ { i } ) | ,
$$

where $t = ( t _ { 0 } , t _ { 1 } , \ldots , t _ { n } )$ is a partition of $\{ 0 , T \}$ ,i.e. $0 = t _ { 0 } < t _ { 1 } < \cdots < t _ { n } = T$ and where

$$
\varDelta t = \operatorname* { m a x } _ { i = 0 , \ldots , n - 1 } \left| t _ { i + 1 } - t _ { i } \right| .
$$

## Theorem 6.5

The variation of the paths of $W ( t )$ is infinite a.s.

## Proof

Consider the sequence of partitions $t ^ { n } = ( t _ { 0 } ^ { n } , t _ { 1 } ^ { n } , \ldots , t _ { n } ^ { n } )$ of $[ 0 , T ]$ into n equal parts. Then

$$
\sum _ { i = 0 } ^ { n - 1 } \left| \varDelta _ { i } ^ { n } W \right| ^ { 2 } \leq \left( \operatorname* { m a x } _ { i = 0 , \ldots , n - 1 } \left| \varDelta _ { i } ^ { n } W \right| \right) \sum _ { i = 0 } ^ { n - 1 } \left| \varDelta _ { i } ^ { n } W \right| .
$$

Since the paths of $W ( t )$ are a.s. continuous on [0, T],

$$
\operatorname * { l i m } _ { n  \infty } ( \operatorname * { m a x } _ { i = 0 , \ldots , n - 1 } | \Delta _ { i } ^ { n } W | ) = 0 \quad \mathrm { a . s . }
$$

By Exercise 6.29 there is a subsequence $t ^ { n _ { k } } = ( t _ { 0 } ^ { n _ { k } } , t _ { 1 } ^ { n _ { k } } , \ldots , t _ { n _ { k } } ^ { n _ { k } } )$ of partitions such that

$$
\operatorname* { l i m } _ { k  \infty } \sum _ { i = 0 } ^ { n _ { k } - 1 } | \varDelta _ { i } ^ { n _ { k } } W | ^ { 2 } = T \quad \mathrm { a . s . }
$$

This is because every sequence of random variables convergent in $L ^ { 2 }$ has a subsequence convergent a.s. It follows that

$$
\operatorname * { l i m } _ { k  \infty } \sum _ { i = 0 } ^ { n _ { k } - 1 } | \varDelta _ { i } ^ { n _ { k } } W | = \infty \quad \mathrm { a . s . } ,
$$

while

$$
\operatorname* { l i m } _ { k  \infty } \varDelta t ^ { n _ { k } } = \operatorname* { l i m } _ { k  \infty } \frac { T } { n _ { k } } = 0 ,
$$

which proves the theorem.

Theorem 6.5 has important consequences for the theory of stochastic integral presented in the next chapter.This is because n integral of the form

$$
\int _ { 0 } ^ { T } f ( t ) d W ( t )
$$

cannot be defined pathwise (that is, separately for each $\omega \in \Omega )$ as the Riemann-Stieltjes integral if the paths have infinite variation. It turns out that an intrinsically stochastic approach will be needed to tackle such integrals, see Chapter 7.

## Exercise 6.30

Show that W(t) is a.s. non-differentiable at $\pmb { t = 0 }$

Hint By Exercise 6.28 $\begin{array} { r } { V _ { c } ( t ) = \frac { 1 } { c } W ( c ^ { 2 } t ) } \end{array}$ is a Wiener process for any $c > 0$ Deduce that the probability

$$
\begin{array} { r } { P \left\{ \frac { | W ( t ) | } { t } > c M \mathrm { ~ f o r ~ s o m e ~ } t \in [ 0 , \frac { 1 } { c ^ { 2 } } ] \right\} } \end{array}
$$

is the same for each $c > 0$ What is the probability that the limit of $\frac { \lfloor W ( t ) \rfloor } { t }$ exists as ${ \pmb t } \searrow { \pmb 0 }$ , then?

## Exercise 6.31

Show that for any $t \geq 0$ the Wiener process $W ( t )$ is a.s. non-differentiable at t.

Hint $V _ { t } ( s ) = W ( s + t ) - W ( t )$ is a Wiener process for any ${ \pmb t } \geq { \bf 0 }$

A weak point in the assertion in Exercise 6.31 is that for each t the event of measure 1 in which $W ( t )$ is non-differentiable at t may turn out to be different for each $\pmb { t } \geq 0$ . The theorem below, which is presented without proof, shows that in fact the same event of measure 1 can be chosen for each $\pmb { t } \geq \mathbf { 0 }$ . This is not a trivial conclusion because the set of $t \geq 0$ is uncountable.

## Theorem 6.6

With probability 1 the Wiener process $W ( t )$ is non-differentiable at any ${ \pmb t } \geq { \bf 0 }$

## 6.3.4 Doob's Maximal ${ \pmb { L } } ^ { 2 }$ Inequality for Brownian Motion

The inequality proved in this section is necessary to study the properties of stochastic integrals in the next chapter. It can be viewed as an extension of Doob's maximal $L ^ { 2 }$ inequality in Theorem 4.1 to the case of continuous time. In fact, in the result below the Wiener process can be replaced by any square integrable martingale ξ(t), $t \geq 0$ with a.s. continuous paths.

## Theorem 6.7 (Doob's maximal $L ^ { 2 }$ inequality)

For any $\pmb { t } > 0$

$$
E \left( \underset { \substack { \mathscr { s } \leq t } } { \operatorname* { m a x } } | W ( \mathscr { s } ) | ^ { 2 } \right) \leq 4 E | W ( t ) | ^ { 2 } .\tag{6.6}
$$

Proof

For $\pm > 0$ and $\pmb { n } \in \mathbb { N }$ we define

$$
M _ { k } ^ { n } = \left| W \left( { \frac { k t } { 2 ^ { n } } } \right) \right| , 0 \leq k \leq 2 ^ { n } .\tag{6.7}
$$

Then, by Jensen's inequality, $M _ { k } ^ { n } , k = 0 , \cdots , 2 ^ { n }$ , is a non-negative square integrable submartingale with respect to the filtration $\mathcal { F } _ { k } ^ { n } = \mathcal { F } _ { \frac { k _ { \ell } } { 2 ^ { n } } }$ , so by Theorem 4.1

$$
E \left( \operatorname* { m a x } _ { k \leq 2 ^ { n } } | M _ { k } ^ { n } | ^ { 2 } \right) \leq 4 E | M _ { 2 ^ { n } } ^ { n } | ^ { 2 } = 4 E | W ( t ) | ^ { 2 } .
$$

Since $W ( t )$ has a.s. continuous paths,

$$
\operatorname* { l i m } _ { n  \infty } \operatorname* { m a x } _ { k \leq 2 ^ { n } } | M _ { k } ^ { n } | ^ { 2 } = \operatorname* { m a x } _ { s \leq t } | W ( s ) | ^ { 2 } \quad \mathrm { a . s . }
$$

Moreover, since $M _ { k } ^ { n } = M _ { 2 k } ^ { n + 1 }$ , the sequence $\begin{array} { r } { \operatorname* { s u p } _ { k \leq 2 ^ { n } } | M _ { k } ^ { n } | ^ { 2 } , n \in \mathbb { N } , } \end{array}$ is increasing. Hence by the Lebesgue monotone convergence theorem max. $_ { \cdot \bullet \leq t } | W ( s ) | ^ { 2 }$ is an integrable function and

$$
E \left( \underset { \bullet \leq t } { \operatorname* { m a x } } | W ( s ) | ^ { 2 } \right) = \underset { n \to \infty } { \operatorname* { l i m } } E \left( \underset { k \leq 2 ^ { n } } { \operatorname* { m a x } } | M _ { k } ^ { n } | ^ { 2 } \right) \leq 4 E | W ( t ) | ^ { 2 } ,
$$

completing the proof.

## 6.3.5 Various Exercises on Brownian Motion

## Exercise 6.32

Verify that the transition density ${ \pmb p } ( { \pmb t } , { \pmb x } , { \pmb y } )$ satisfies the diffusion equation

$$
\frac { \partial p } { \partial t } = \frac { 1 } { 2 } \frac { \partial ^ { 2 } p } { \partial y ^ { 2 } } .
$$

Hint Simply differentiate the expression (6.5) for the transition density.

## Exercise 6.33

Show that $Z ( t ) = - W ( t )$ is a Wiener process if $W ( t )$ is.

Hint Are the increments of $\pmb { Z } ( t )$ independent? How are they distributed? Are the paths of $\pmb { Z } ( t )$ continuous? Is it true that $\pmb { Z ( 0 ) = 0 ? }$

## Exercise 6.34

Show that for any $0 \leq s < t$

$$
P \left\{ W ( t ) \in A | W ( s ) \right\} = \int _ { A } p \left( t - s , W ( s ) , y \right) d y .
$$

Hint Write the conditional probability as the conditional expectation of $1 _ { A } ( W ( t ) )$ given $W ( s )$ . Compute the conditional expectation by transforming the integral of $\bar { \mathbf { 1 } } _ { A } ( W ( t ) )$ over any event in the σ-field generated by $\mathbf { \hat { \ b { W } } } ( \ b { \mathscr { s } } )$ ). This can be done using the joint density of $W ( s )$ and $W ( t )$ ). Refer to the chapter on conditional expectation if necessary.

## Exercise 6.35

Show that $e ^ { W ( t ) } e ^ { - \frac { t } { 2 } }$ is a martingale. (It is called the exponential martingale.)

Hint What is the expectation of $e ^ { W ( t ) - W ( \bullet ) }$ for $\beta < t ?$ By independence it is equal to the conditional expectation of $e ^ { W ( t ) - W ( s ) }$ given $\pmb { \mathcal { F } _ { \pmb { \mathscr { s } } } }$ . This will give the martingale condition.

## Exercise 6.36

Compute $E \left( W ( s ) | W ( t ) \right)$ for $0 \leq s < t$

Hint You want to find a Borel function F such that $E \left( W ( s ) | W ( t ) \right) = F \left( W ( t ) \right) , \mathrm { i . e }$

$$
\int _ { \{ W ( t ) \in A \} } W ( s ) d P = \int _ { \{ W ( t ) \in A \} } F \left( W ( t ) \right) d P .
$$

Either side of this equality can be transformed using the joint density of $W ( t )$ and $W ( s )$

## 6.4 Solutions

## Solution 6.1

Suppose that 7 is a random variable with exponential distribution of rate λ. The distribution function of $\pmb { \eta }$ is

$$
F ( t ) = P \left\{ \eta \leq t \right\} = 1 - P \left\{ \eta > t \right\} = \left\{ \begin{array} { l l } { 0 } & { \quad \mathrm { i f ~ } t < 0 , } \\ { 1 - e ^ { - \lambda t } } & { \quad \mathrm { i f ~ } t \geq 0 . } \end{array} \right.
$$

Therefore n has density

$$
f ( t ) = { \frac { d } { d t } } F ( t ) = { \left\{ \begin{array} { l l } { 0 } & { \quad { \mathrm { i f ~ } } t < 0 , } \\ { \lambda e ^ { - \lambda t } } & { \quad { \mathrm { i f ~ } } t > 0 . } \end{array} \right. }
$$

The distribution function $F ( t )$ and density f(t) are shown in Figure 6.6.

## Solution 6.2

Using the density $f ( t ) = \lambda e ^ { - \lambda t }$ found in Exercise 6.1 and integrating by parts,

<!-- image-->  
Figure 6.6. The distribution function ${ \pmb F } ( t )$ and density f(t) of a random variable with exponential distribution of rate $\lambda = 2$

we obtain

$$
\begin{array} { l } { \displaystyle E \left( \eta \right) = \int _ { - \infty } ^ { \infty } t f ( t ) d t = \int _ { 0 } ^ { \infty } t \lambda e ^ { - \lambda t } d t = - \int _ { 0 } ^ { \infty } t \frac { d } { d t } e ^ { - \lambda t } d t } \\ { = \left. - t e ^ { - \lambda t } \right| _ { 0 } ^ { \infty } + \int _ { 0 } ^ { \infty } e ^ { - \lambda t } d t = 0 - \left. \frac 1 \lambda e ^ { - \lambda t } \right| _ { 0 } ^ { \infty } = \frac 1 \lambda . } \end{array}
$$

In a similar way we compute

$$
\begin{array} { l } { \displaystyle E ( \eta ^ { 2 } ) = \int _ { - \infty } ^ { \infty } t ^ { 2 } f ( t ) d t = \int _ { 0 } ^ { \infty } t ^ { 2 } \lambda e ^ { - \lambda t } d t = - \int _ { 0 } ^ { \infty } t ^ { 2 } \frac { d } { d t } e ^ { - \lambda t } d t } \\ { = \left. - t ^ { 2 } e ^ { - \lambda t } \right| _ { 0 } ^ { \infty } + 2 \int _ { 0 } ^ { \infty } t e ^ { - \lambda t } d t = 0 + \displaystyle \frac { 2 } { \lambda } \int _ { 0 } ^ { \infty } t f ( t ) d t = \frac { 2 } { \lambda ^ { 2 } } . } \end{array}
$$

It follows that the variance is equal to

$$
\mathrm { v a r } \left( \eta \right) = E \left( \eta ^ { 2 } \right) - \left( E \left( \eta \right) \right) ^ { 2 } = \frac { 2 } { \lambda ^ { 2 } } - \frac { 1 } { \lambda ^ { 2 } } = \frac { 1 } { \lambda ^ { 2 } } .
$$

## Solution 6.3

By the definition of a random variable with exponential distribution

$$
P \left\{ \eta > s + t \right\} = e ^ { - \lambda \left( s + t \right) } = e ^ { - \lambda s } e ^ { - \lambda t } = P \left\{ \eta > s \right\} P \left\{ \eta > t \right\} .
$$

## Solution 6.4

By the definition of conditional probability

$$
\begin{array} { l } { { P \left\{ \eta > t + s | \eta > s \right\} = \frac { P \left\{ \eta > t + s , \eta > s \right\} } { P \left\{ \eta > s \right\} } } } \\ { { = \frac { P \left\{ \eta > t + s \right\} } { P \left\{ \eta > s \right\} } , } } \end{array}
$$

since $\left\{ \eta > t + s , \eta > s \right\} = \left\{ \eta > t + s \right\}$ (because $\eta > s + t$ implies that $\pmb { \eta } > \pmb { \mathscr { s } } )$ Now it is clear that (6.1) is equivalent to (6.2).

## Solution 6.5

The exponential distribution is the only probability distribution satisfying the lack of memory property because the only non-negative non-increasing solutions of the functional equation

$$
g ( t + s ) = g ( t ) g ( s )
$$

are of the form $g ( t ) = a ^ { t }$ for some $0 \leq a \leq 1$

To verify this observe that $[ g ( m / n ) ] ^ { n } = g ( m ) = [ g ( 1 ) ] ^ { m }$ for any integers m and $n \neq 0$ Let $a : = g ( 1 )$ . It follows that

$$
g ( q ) = a ^ { q } \quad { \mathrm { ~ f o r ~ a n y ~ } } q \in \mathbb { Q } .
$$

Since g is non-increasing, $0 \leq \alpha \leq 1$ and

$$
a ^ { t } = \operatorname* { i n f } _ { t > q \in \mathbb { Q } } g ( q ) \geq g ( t ) \geq \operatorname* { s u p } _ { t < q \in \mathbb { Q } } g ( q ) = a ^ { t } ,
$$

so indeed

$$
g ( t ) = a ^ { t } \quad { \mathrm { ~ f o r ~ a n y ~ } } t \in \mathbb { R } .
$$

As a result, $P \{ \eta > t \} = a ^ { t }$ for some $0 \leq a \leq 1$ . But the distribution function of a random variable cannot be constant, so $0 \neq a \neq 1$ . Hence $a = e ^ { - \lambda }$ for some $\lambda > 0$ , completing the argument.

## Solution 6.6

Since $N ( t )$ has the Poisson distribution with parameter λt we have $E \left( N ( t ) \right) =$ λt. Indeed

$$
\begin{array} { l l l } { { \displaystyle { E \left( N ( t ) \right) = \sum _ { n = 0 } ^ { \infty } n P \left\{ N ( t ) = n \right\} = \sum _ { n = 0 } ^ { \infty } n e ^ { - \lambda t } \frac { \left( \lambda t \right) ^ { n } } { n ! } } } } \\ { { \mathrm { } = \lambda t e ^ { - \lambda t } \displaystyle \sum _ { n = 1 } ^ { \infty } \frac { \left( \lambda t \right) ^ { n - 1 } } { \left( n - 1 \right) ! } = \lambda t e ^ { - \lambda t } e ^ { \lambda t } = \lambda t . } } \end{array}
$$

## Solution 6.7

Using the fact that $\eta _ { 1 } , \eta _ { 2 } , \ldots$ are independent and exponentially distributed, we obtain

$$
\begin{array} { r l } { P \left\{ N ( s ) = 1 , N ( t ) = 2 \right\} } & { = P \left\{ \xi _ { 1 } \leq s < \xi _ { 2 } \leq t < \xi _ { 3 } \right\} } \\ & { = P \left\{ \eta _ { 1 } \leq s < \eta _ { 1 } + \eta _ { 2 } \leq t < \eta _ { 1 } + \eta _ { 2 } + \eta _ { 3 } \right\} } \\ & { = \displaystyle \int _ { 0 } ^ { s } P \left\{ s < u + \eta _ { 2 } \leq t < u + \eta _ { 2 } + \eta _ { 3 } \right\} \lambda e ^ { - \lambda u } d u } \\ & { = \displaystyle \int _ { 0 } ^ { s } \left( \displaystyle \int _ { s - u } ^ { t - u } P \left\{ t < u + v + \eta _ { 3 } \right\} \lambda e ^ { - \lambda v } d v \right) \lambda e ^ { - \lambda u } d u } \end{array}
$$

$$
\begin{array} { l } { { \displaystyle \ = \ \int _ { 0 } ^ { \theta } \left( \int _ { s - u } ^ { t - u } e ^ { - \lambda ( t - u - v ) } \lambda e ^ { - \lambda v } d v \right) \lambda e ^ { - \lambda u } d u } } \\ { { \displaystyle \ = \ \lambda ^ { 2 } e ^ { - \lambda t } s \left( t - s \right) . } } \end{array}
$$

## Solution 6.8

Since

$$
\begin{array} { l } { { \xi _ { n } ^ { t } = \eta _ { 1 } ^ { t } + \cdot \cdot \cdot + \eta _ { n } ^ { t } } } \\ { { \qquad = \xi _ { N ( t ) + 1 } - t + \eta _ { N ( t ) + 2 } + \cdot \cdot \cdot + \eta _ { N ( t ) + n } } } \\ { { \qquad = \xi _ { N ( t ) + n } - t , } } \end{array}
$$

it follows that

$$
\begin{array} { r l } & { N ^ { t } ( s ) = \operatorname* { m a x } \{ n : \xi _ { n } ^ { t } \leq s \} } \\ & { = \operatorname* { m a x } \{ n : \xi _ { N ( t ) + n } \leq t + s \} } \\ & { = \operatorname* { m a x } \{ n : \xi _ { n } \leq t + s \} - N ( t ) } \\ & { = N ( t + s ) - N ( t ) . } \end{array}
$$

## Solution 6.9

It is easily verified that

$$
\begin{array} { c } { { \{ N ( t ) = n \} \ = \ \left\{ \eta _ { n + 1 } > t - \xi _ { n } , t \ge \xi _ { n } \right\} } } \\ { { \{ \eta _ { 1 } ^ { t } > s , N ( t ) = n \} \ = \ \left\{ \eta _ { n + 1 } > s + t - \xi _ { n } , t \ge \xi _ { n } \right\} } } \end{array}
$$

Since $\xi _ { n } , \eta _ { n + 1 }$ are independent and $\eta _ { n + 1 }$ satisfies the lack of memory property from Exercise 6.3,

$$
\begin{array} { r l } { P \left\{ \eta _ { 1 } ^ { t } > s , N ( t ) = n \right\} } & { = P \left\{ \eta _ { n + 1 } > s + t - \xi _ { n } , t \geq \xi _ { n } \right\} } \\ & { = \displaystyle \int _ { - \infty } ^ { t } P \left\{ \eta _ { n + 1 } > s + t - u \right\} P _ { \xi _ { n } } ( d u ) } \\ & { = P \left\{ \eta _ { n + 1 } > s \right\} \displaystyle \int _ { - \infty } ^ { t } P \left\{ \eta _ { n + 1 } > t - u \right\} P _ { \xi _ { n } } ( d u ) } \\ & { = P \left\{ \eta _ { n + 1 } > s \right\} P \left\{ \eta _ { n + 1 } > t - \xi _ { n } , t \geq \xi _ { n } \right\} } \\ & { = P \left\{ \eta _ { n + 1 } > s \right\} P \left\{ N ( t ) = n \right\} } \\ & { = P \left\{ \eta _ { 1 } > s \right\} P \left\{ N ( t ) = n \right\} . } \end{array}
$$

The last equality holds because $\eta _ { n + 1 }$ has the same distribution as $\pmb { \eta _ { 1 } }$ . Now divide both sides by ${ \mathcal { P } } \{ N ( t ) = n \}$ to get

$$
P \{ \eta _ { 1 } ^ { t } > s | N ( t ) = n \} = P \{ \eta _ { 1 } > s \}
$$

for any $n = 0 , 1 , 2 , \ldots , \mathrm { \bf A s }$ a result,

$$
P \{ \eta _ { 1 } ^ { t } > s | N ( t ) \} = P \{ \eta _ { 1 } > s \}
$$

because N(t) is a discrete random variable with values 0, 1, 2, . . . .

## Solution 6.10

As in Solution 6.9,

$$
\begin{array} { c } { { \{ N ( t ) = n \} \ = \ \{ \eta _ { n + 1 } > t - \xi _ { n } , t \ge \xi _ { n } \} , } } \\ { { \{ \eta _ { 1 } ^ { t } > s _ { 1 } , N ( t ) = n \} \ = \ \{ \eta _ { n + 1 } > s _ { 1 } + t - \xi _ { n } , t \ge \xi _ { n } \} } } \end{array}
$$

and, more generally,

$$
\begin{array} { c } { { \left\{ \eta _ { 1 } ^ { t } > s _ { 1 } , \eta _ { 2 } ^ { t } > s _ { 2 } , \ldots , \eta _ { k } ^ { t } > s _ { k } , N ( t ) = n \right\} } } \\ { { = \left\{ \eta _ { n + 1 } > s _ { 1 } + t - \xi _ { n } , t \ge \xi _ { n } \right\} \cap \left\{ \eta _ { n + 2 } > s _ { 2 } \right\} \cap \cdots \cap \left\{ \eta _ { n + k } > s _ { k } \right\} . } } \end{array}
$$

Since $\xi _ { n } , \eta _ { n + 1 } , \ldots , \eta _ { n + k }$ are independent and $\eta _ { n + 2 } , \ldots , \eta _ { n + k }$ have the same distribution as $\eta _ { 2 } , \ldots , \eta _ { k }$ , using Exercise 6.9 we find that

$$
\begin{array} { r l } & { P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , \eta _ { 2 } ^ { t } > s _ { 2 } , \ldots , \eta _ { k } ^ { t } > s _ { k } , N ( t ) = n \} } \\ & { = P \{ \eta _ { n + 1 } > s _ { 1 } + t - \xi _ { n } , \xi _ { n } \leq t \} P \{ \eta _ { n + 2 } > s _ { 2 } \} \cdots P \{ \eta _ { n + k } > s _ { k } \} } \\ & { = P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , N ( t ) = n \} P \{ \eta _ { 2 } > s _ { 2 } \} \cdots P \{ \eta _ { k } > s _ { k } \} } \\ & { = P \{ \eta _ { 1 } ^ { t } > s _ { 1 } | N ( t ) = n \} P \{ \eta _ { 2 } > s _ { 2 } \} \cdots P \{ \eta _ { k } > s _ { k } \} P \{ N ( t ) = n \} } \\ & { = P \{ \eta _ { 1 } > s _ { 1 } \} P \{ \eta _ { 2 } > s _ { 2 } \} \cdots P \{ \eta _ { k } > s _ { k } \} P \{ N ( t ) = n \} . } \end{array}
$$

As in Solution 6.9, this implies the desired equality.

## Solution 6.11

Take the expectation on both sides of the equality in Exercise 6.10 to find that

$$
P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , \ldots , \eta _ { k } ^ { t } > s _ { k } \} = P \{ \eta _ { 1 } > s _ { 1 } \} \cdots P \{ \eta _ { k } > s _ { k } \} .
$$

If all the numbers ${ \pmb { \mathscr { s } } } _ { \pmb { \mathscr { n } } }$ except perhaps one are zero, it follows that

$$
P \{ \eta _ { n } ^ { t } > s _ { n } \} = P \{ \eta _ { n } > s _ { n } \} , \quad n = 1 , \ldots , k ,
$$

so the random variables $\eta _ { n } ^ { t }$ have the same distribution as $\pmb { \eta _ { n } }$ . Inserting this back into the first equality, we obtain

$$
P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , . . . , \eta _ { k } ^ { t } > s _ { k } \} = P \{ \eta _ { 1 } ^ { t } > s _ { 1 } \} \cdot \cdot \cdot P \{ \eta _ { k } ^ { t } > s _ { k } \} ,
$$

so the $\eta _ { n } ^ { t }$ are independent.

To prove that the $\eta _ { n } ^ { t }$ are independent of N(t) integrate the formula in Exercise 6.10 over $\{ N ( t ) = n \}$ and multiply by $P \{ N ( t ) = n \}$ to get

$$
P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , \ldots , \eta _ { k } ^ { t } > s _ { k } , N ( t ) = n \} = P \{ \eta _ { 1 } > s _ { 1 } \} \cdots P \{ \eta _ { k } > s _ { k } \} P \{ N ( t ) = n \} .
$$

But $P \{ \eta _ { n } ^ { t } > s _ { n } \} = P \{ \eta _ { n } > s _ { n } \}$ , hence N(t) and the $\eta _ { n } ^ { t }$ are independent.

## Solution 6.12

We need to verify conditions 1), 2), 3) of Definition 6.3. Clearly, $N ( t ) - \lambda t$ is Ft-measurable. By Exercise 6.6

$$
E \left( | N ( t ) | \right) = E \left( N ( t ) \right) = \lambda t < \infty ,
$$

which means that N(t) is integrable, and so is $N ( t ) - \lambda t$

Theorem 6.2 implies that $N ( t ) - N ( s )$ is independent of $\mathcal { F } _ { s }$ for any $0 \leq s \leq t$ SO

$$
E \left( N ( t ) - N ( s ) | { \mathcal { F } } _ { s } \right) = E \left( N ( t ) - N ( s ) \right) = E \left( N ( t ) \right) - E \left( N ( s ) \right) = \lambda t - \lambda s .
$$

It follows that

$$
E \left( N ( t ) - \lambda t | \mathcal { F } _ { s } \right) = E \left( N ( s ) - \lambda s | \mathcal { F } _ { s } \right) = N ( s ) - \lambda s ,
$$

completing the proof.

Solution 6.13

Since $\eta _ { n } = \xi _ { n } - \xi _ { n - 1 }$ and $P \{ \eta _ { n } > 0 \} = e ^ { 0 } = 1$

$$
P \{ \xi _ { 0 } < \xi _ { 1 } < \xi _ { 2 } < \cdot \cdot \cdot \} = P \left( \bigcap _ { n = 1 } ^ { \infty } \{ \eta _ { n } > 0 \} \right) = 1 .
$$

Here we have used the property that if $P ( A _ { n } ) = 1$ for all $n = 1 , 2 , \ldots$ , then $P \left( \bigcap _ { n = 1 } ^ { \infty } A _ { n } \right) = 1$

Solution 6.14

Since

$$
\operatorname* { l i m } _ { n  \infty } \xi _ { n } = \sum _ { n = 1 } ^ { \infty } \eta _ { n } ,
$$

it follows that

$$
\begin{array} { r c l } { \displaystyle \left\{ \operatorname* { l i m } _ { n \to \infty } \xi _ { n } < \infty \right\} \subset \ \{ \eta _ { 1 } , \eta _ { 2 } , . . . \ \mathrm { i s ~ a ~ b o u n d e d ~ s e q u e n c e } \} } \\ { \displaystyle } & { \displaystyle = \ \bigcup _ { m = 1 } ^ { \infty } \bigcap _ { n = 1 } ^ { \infty } \{ \eta _ { n } \leq m \} . } \end{array}
$$

Let us compute the probability of this event. Because $\bigcap _ { n = 1 } ^ { N } \{ \eta _ { n } \leq m \} _ { \tilde { \nu } = 1 } ^ { \tilde { \nu } } =$ $1 , 2 , . . .$ is a contracting sequence of events,

$$
P \left( \bigcap _ { n = 1 } ^ { \infty } \left\{ \eta _ { n } \leq m \right\} \right) = \operatorname* { l i m } _ { N \to \infty } P \left( \bigcap _ { n = 1 } ^ { N } \left\{ \eta _ { n } \leq m \right\} \right)
$$