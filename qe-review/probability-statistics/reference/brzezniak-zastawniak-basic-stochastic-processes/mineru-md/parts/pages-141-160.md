Second, observe that the same holds for fn, i.e. $f _ { n } ( j | i ) = f _ { n } ( j - i | 0 )$ .Hence, in particular, $m _ { i } = m _ { 0 }$

To show that 0 is null-recurrent let us recall some useful tools:

$$
P _ { 0 0 } ( x ) = \sum _ { n = 0 } ^ { \infty } p _ { n } ( 0 | 0 ) x ^ { n } , - 1 < x < 1 ,
$$

$$
F _ { 0 0 } ( x ) = \sum _ { n = 1 } ^ { \infty } f _ { n } ( 0 | 0 ) x ^ { n } , - 1 < x < 1 .
$$

Since, see Exercise 5.9,

$$
p _ { 2 k } ( 0 | 0 ) \ = \ { \binom { 2 k } { k } } { \frac { 1 } { 4 ^ { k } } } ,
$$

$$
P _ { 0 0 } ( x ) = \sum _ { k = 0 } ^ { \infty } { \binom { 2 k } { k } } \left( { \frac { x ^ { 2 } } { 4 } } \right) ^ { k } = \left( 1 - x ^ { 2 } \right) ^ { - 1 / 2 } .
$$

Then, using (5.41), we infer that $F _ { 0 0 } ( x ) = 1 - ( 1 - x ^ { 2 } ) ^ { 1 / 2 }$ . Since $F _ { 0 0 } ^ { \prime } ( x ) ~ \nearrow$ ∞ as $\pmb { x } \nearrow 1$ and $\begin{array} { r } { F _ { 0 0 } ^ { \prime } ( x ) = \sum _ { n = 1 } ^ { \infty } n f _ { n } ( 0 | 0 ) x ^ { n } } \end{array}$ by using Abel's lemma (compare with Exercise 5.18), we infer that $\textstyle \sum _ { n = 1 } ^ { \infty } n f _ { n } ( 0 | 0 ) = \infty$ . This shows that $m _ { 0 } = \infty$ and thus 0 is null-recurrent.

## Solution 5.23

We know that O is a recurrent state. From definition

$$
f _ { n } ( 0 | 0 ) = p ( 0 | 1 ) p ( 1 | 1 ) ^ { n - 2 } p ( 1 | 0 ) = p q ( 1 - q ) ^ { n - 2 } .
$$

Since $| 1 - q | < 1$ , we infer that $\begin{array} { r } { \sum _ { n = 1 } ^ { \infty } n f _ { n } ( 0 | 0 ) = \sum _ { n = 1 } ^ { \infty } p q ( 1 - q ) ^ { n - 2 } < \infty } \end{array}$ Hence $m _ { 0 } < \infty$ and 0 is positive-recurrent. The same proof works for state 1.

## Solution 5.24

Consider a Markov chain on $S = \{ 1 , 2 , \cdots , 9 \}$ with transition probabilities given by the graph in Figure 5.3. Then, obviously, $p _ { 4 } ( 1 | 1 ) = 1 / 2$ and $p _ { 6 } ( 1 | 1 ) = 1 / 2$

<!-- image-->  
Figure 5.3. Transition probabilities of the Markov chain in Exercise 5.24

but pk(1|1) = 0 if $k \leq 6$ and $k \notin \{ 4 , 6 \}$ . Hence $d ( 1 ) = 2$ , but $p _ { 2 } ( 1 | 1 ) = 0$

## Solution 5.25

We begin with finding $P _ { 2 } = P ^ { 2 }$ in an algebraic way, i.e. by multiplying the matrix $P$ by itself. We have

$$
P _ { 2 } = P ^ { 2 } = { \left[ \begin{array} { l l } { 0 } & { 1 / 2 } \\ { 1 } & { 1 / 2 } \end{array} \right] } \left[ { \begin{array} { l l } { 0 } & { 1 / 2 } \\ { 1 } & { 1 / 2 } \end{array} } \right] = { \left[ \begin{array} { l l } { { \frac { 1 } { 2 } } } & { { \frac { 1 } { 4 } } } \\ { { \frac { 1 } { 2 } } } & { { \frac { 3 } { 4 } } } \end{array} \right] } .
$$

Alternatively, $P _ { 2 }$ can be found by observing that the only way one can get from 1 to 1 in two steps is to move from 1 to 2 (with probability 1) and then from 2 to 1 (with probability $1 / 2 )$ . Hence, the probability ${ p } _ { 2 } ( 1 | 1 )$ of going from 1 to 1 in two steps equals $1 / 2$ Analogously, we calculate $p _ { 2 } ( 1 | 2 )$ by observing that in order to move from 1 to 2 in two steps one needs first to move from 1 to 2 (with probability 1) and then stay at 2 (with probability $1 / 2 )$ . Hence, $p _ { 2 } ( 1 | 2 ) = 1 / 2$ The remaining two elements of the matrix $\pmb { P _ { 2 } }$ can be found by repeating the above argument, or, simply by adding the rows so that they equal 1. In the latter method we use the fact that P2 is a stochastic matrix, see Exercise 5.3. $P _ { 2 }$ The graph representing $\pmb { P _ { 2 } }$ is shown in Figure 5.4.

<!-- image-->  
Figure 5.4. Two-step transition probabilities in Exercise 5.25  
Using any of the mnethods presented above, we obtain

$$
P _ { 3 } = \left[ \begin{array} { c c } { { { \frac { 1 } { 4 } } } } & { { { \frac { 3 } { 8 } } } } \\ { { { \frac { 3 } { 4 } } } } & { { { \frac { 5 } { 8 } } } } \end{array} \right] .
$$

Therefore, $p _ { 1 } ( 1 | 1 ) = 0 , p _ { 2 } ( 1 | 1 ) = 1 / 2$ and $p _ { 3 } ( 1 | 1 ) \ = \ 1 / 4$ Hence $d ( 1 ) = 1$ (although $p _ { 1 } ( 1 | 1 ) = 0 )$ . Since $p _ { 1 } ( 2 | 2 ) = 1 / 2 > 0 ,$ , it follows that $d ( 2 ) = 1$

## Solution 5.26

Suppose that $\pmb { \xi _ { 0 } } = \pmb { i }$ i. Denote by Tk the minimum positive time when the chain $\pmb { \tau _ { k } }$ enters state k, i.e.

$$
\tau _ { k } = \operatorname* { m i n } \{ n \geq 1 : \xi _ { n } = k \} .
$$

Then, $P \{ \tau _ { j } ~ < ~ \tau _ { i } \} ~ = : ~ \varepsilon ~ > ~ 0 ~ \mathrm { i f } ~ i ~  ~ j$ and $\textit { \textbf { i } } \neq \textit { \textbf { j } }$ . If $j \ \not \to \ i ,$ then it would be impossible to return to i with probability at least $\varepsilon > 0$ But this cannot happen as i is a recurrent state. Indeed,

$$
\begin{array} { r l } & { 1 = P \left( \tau _ { i } < \infty \right) = P \left( \tau _ { i } < \infty | \tau _ { j } < \tau _ { i } \right) P \left( \tau _ { j } < \tau _ { i } \right) } \\ & { \qquad + P \left( \tau _ { i } < \infty | \tau _ { j } \geq \tau _ { i } \right) P \left( \tau _ { j } \geq \tau _ { i } \right) . } \end{array}
$$

The second term on the right-hand side is $\leq 1 - \varepsilon < 1$ , while the first factor in the first term is equal to 0 (since $i \not \to j )$ . This is a contradiction. The second part is obvious.

## Solution 5.27

As observed in the hint, we only need to show that (5.45) holds when $p ( j | i ) = 0$ for $\textit { i } \in \boldsymbol { C }$ and $j \in \mathcal { S } \setminus C$ . We begin with the following simple observation. If $( \Omega , { \mathcal { F } } , P )$ is a probability space and $A _ { n } \in { \mathcal { F } } , n \in \mathbb { N } ,$ then $\textstyle P ( \bigcup _ { n } A _ { n } ) = 0$ if and only if $P ( A _ { n } ) = 0$ for each $n \in \mathbb { N } .$ Hence (5.45) holds if and only if for each $k \geq n$

$$
P \left( \xi _ { k } \in S \setminus C | \xi _ { n } \in C \right) = 0 .\tag{5.74}
$$

In fact, the above holds if and only if it holds for $k = n + 1$ . Indeed, suppose that for each $\pmb { n } \in \mathbb { N }$

$$
P \left( \xi _ { n + 1 } \in S \setminus C | \xi _ { n } \in C \right) = 0 .\tag{5.75}
$$

Let us take $\displaystyle n \in \mathbb { N } .$ We shall prove by induction on $k \geq n$ that (5.74) holds. This is so for $k = n$ and $k = n + 1$ . Suppose that (5.74) holds for some ${ \pmb k } \geq { \pmb n } + { \pmb 1 }$ We shall verify that it holds for $k + 1$ . By the total probability formula (5.67) and the Markov property (5.10)

$$
\begin{array} { r l } & { P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { n } \in C \right) } \\ & { \qquad = P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { n } \in C , \xi _ { k } \in C \right) P ( \xi _ { k } \in C | \xi _ { n } \in C ) } \\ & { \qquad + P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { n } \in C , \xi _ { k } \in S \setminus C \right) P ( \xi _ { k } \in S \setminus C | \xi _ { n } \in C ) } \\ & { \qquad = P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { k } \in C \right) P ( \xi _ { k } \in C | \xi _ { n } \in C ) } \\ & { \qquad + P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { k } \in S \setminus C \right) P ( \xi _ { k } \in S \setminus C | \xi _ { n } \in C ) . } \end{array}
$$

By the induction hypothesis $P ( \xi _ { k } \in S \backslash C | \xi _ { n } \in C ) = 0$ and by (5.75) (applied to k rather than n) $P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { k } \in C \right) = 0$ Thus, $P \left( \xi _ { k + 1 } \in S \setminus C | \xi _ { n } \in C \right) =$ 0, which proves (5.74).

The time-homogeneity of the chain implies that (5.75) is equivalent to (5.74) for $\pmb { n = 0 }$ . Since P is a countably additive measure and S is a countable set, the latter holds if and only if ${ \pmb p } ( j | i ) = { \pmb 0 }$ for $\pmb { i } \in C$ and $j \in S \setminus C$

## Solution 5.28

Property 2) in Proposition 5.8 implies that µ is an invariant measure. Therefore it remains to prove uniqueness. Suppose that $\textstyle \nu = \sum _ { j = 1 } ^ { \infty } q _ { j } \delta _ { j }$ is an invariant measure. It is sufficient then to show that $\pi _ { j } = q _ { j }$ for all $j \in S$

Since $0 \ \leq \ p _ { n } ( j | i ) q _ { i } \ \leq \ q _ { i }$ for all $_ { i , j } \in S$ and $\textstyle \sum _ { i } q _ { i } \ = \ 1 \ < \ \infty$ , Lebesgue's dominated convergence theorem yields

$$
q _ { j } = \sum _ { i = 1 } ^ { \infty } p _ { n } ( j | i ) q _ { i }  \sum _ { i } \pi _ { j } q _ { i } = \pi _ { j } .
$$

It follows that $\mu = \nu$

## Solution 5.29

We shall argue as in the uniqueness part of Solution 5.28. If $\textstyle \pmb { \nu } = \sum _ { j = 1 } ^ { \infty } q _ { j } \delta _ { j }$ is an invariant measure, then by Lebesgue's dominated convergence theorem

$$
q _ { j } = \sum _ { i = 1 } ^ { \infty } p _ { n } ( j | i ) q _ { i }  \sum _ { i } \pi _ { j } q _ { i } = 0 .
$$

Hence ${ \pmb \nu } = { \bf 0 }$ ), which contradicts the assumption that v is a probability measure.

## Solution 5.30

Obviously, $P = { \left[ \begin{array} { l l } { 0 } & { 1 } \\ { 1 } & { 0 } \end{array} \right] }$ . Therefore equation $P \pi = \pi$ becomes

$$
\pi _ { 1 } ~ = ~ \pi _ { 2 } ,
$$

$$
\pi _ { 2 } ~ = ~ \pi _ { 1 } .
$$

The only solution of this system subject to the condition $\pi _ { 1 } + \pi _ { 2 } = 1$ is $\pi _ { 1 } =$ $\pi _ { 2 } = 1 / 2$

## Solution 5.31

Put $\begin{array} { r } { \mu = \sum _ { i \in S } \mu _ { i } \delta _ { i } } \end{array}$ and $\begin{array} { r } { \nu = \sum _ { i \in S } \nu _ { i } \delta _ { i } } \end{array}$ Then, for any $j \in S$ and $\pmb { n } \in \mathbb { N }$

$$
\begin{array} { l } { { \displaystyle \sum _ { i \in S } p _ { n } ( j | i ) \left( ( 1 - \theta ) \mu _ { i } + \theta \nu _ { i } \right) \ = \ ( 1 - \theta ) \sum _ { i \in S } p _ { n } ( j | i ) \mu _ { i } + \theta \sum _ { i \in S } p _ { n } ( j | i ) \nu _ { i } } } \\ { { \mathrm { } } } \\ { { \displaystyle \ = \ ( 1 - \theta ) \mu _ { j } + \theta \nu _ { j } = [ ( 1 - \theta ) \mu + \theta \nu ] _ { j } . } } \end{array}
$$

## Solution 5.32

Put $\begin{array} { r } { \mu = \sum _ { i \in S } \mu _ { i } \delta _ { i } } \end{array}$ . Then, for any $j \in T$

$$
\mu _ { j } = \sum _ { i \in S } p _ { n } ( j | i ) \mu _ { i }  0
$$

by the Lebesgue dominated convergence theorem. Indeed, by Exercise 5.19 $p _ { n } ( j | i )  0$ for all $i \in S ,$ ,and $p _ { n } ( j | i ) \mu _ { i } \leq \mu _ { i }$ , where $\sum \mu _ { i } < \infty$

## Solution 5.33

Since j is transient in view of (5.43), from Exercise 5.19 we readily get (5.53). $j$

## Solution 5.34

By Theorem 5.5 $\begin{array} { r } { p _ { n } ( j | i )  \frac { 1 } { m _ { j } } } \end{array}$ for all ${ i , j } \in S$ Put $\begin{array} { r } { \pi _ { j } = \frac { 1 } { m _ { j } } , j \in S } \end{array}$ .We need to show that

1) $\begin{array} { r } { \pi = \sum _ { j } \pi _ { j } \delta _ { j } } \end{array}$ is an invariant measure of the chain $\xi _ { n } , n \in \mathbb { N } ;$

2) π is the unique invariant measure.

Part 1) will follow from Proposition 5.8 as soon as we can show that $\pi _ { j } > 0$ for at least one $j \in S$ But if $j \in S ,$ then $j$ is positive-recurrent and thus $m _ { j } < \infty$ Part 2) follows from Exercise 5.28.

## Solution 5.35

Let us fix $i , j \in \mathbb { Z }$ .First suppose that $j - i = 2 a \in 2 \mathbb { Z }$ .Then $p _ { 2 k + 1 } ( j | i ) = 0$ for all $k \in \mathbb { N } ,$ and also $p _ { 2 k } ( j | i ) = 0 { \mathrm { ~ i f ~ } } k < | a |$ .Moreover, if $\pmb { k } \geq | \pmb { a } |$ , then

$$
p _ { 2 k } ( j | i ) = { \binom { 2 k } { k + a } } p ^ { k + a } q ^ { k - a } = { \binom { p } { q } } ^ { a } { \binom { 2 k } { k + a } } p ^ { k } q ^ { k } .
$$

Since ${ \binom { 2 k } { k + a } } \leq { \binom { 2 k } { k } }$ , it follows that

$$
p _ { 2 k } ( j | i ) \leq ( { \frac { p } { q } } ) ^ { a } { \binom { 2 k } { k } } p ^ { k } q ^ { k }  0
$$

by Proposition 5.4. We have therefore proven that $\pi _ { j }$ is well defined and that $\pi _ { j } = 0$ for all $j \in \mathbb Z$ .Hence, we infer that no invariant measure exists.

## Solution 5.36

Suppose that $\textstyle \pi = \sum _ { j = 0 } ^ { \infty } \pi _ { j } \delta _ { j }$ is an invariant measure of our Markov chain. Then $\begin{array} { r } { \sum _ { i } p ( j | i ) \pi _ { i } = \pi _ { j } } \end{array}$ for all $j \in S = \mathbb { N } .$ Using the exact form of the transition probability matrix in the solution to Exercise 5.14, we see that the sequence of non-negative numbers $( \pi _ { j } ) _ { j = 0 } ^ { \infty }$ solves the following infinite system of linear equations:

$$
\begin{array} { l l l } { { q _ { 0 } \pi _ { 0 } + q _ { 0 } ^ { \prime } \pi _ { 1 } } } & { { = } } & { { \pi _ { 0 } } } \\ { { q _ { 1 } \pi _ { 0 } + q _ { 1 } ^ { \prime } \pi _ { 1 } + q _ { 0 } ^ { \prime } \pi _ { 2 } } } & { { = } } & { { \pi _ { 1 } } } \\ { { q _ { 2 } \pi _ { 0 } + q _ { 2 } ^ { \prime } \pi _ { 1 } + q _ { 1 } ^ { \prime } \pi _ { 2 } + q _ { 0 } ^ { \prime } \pi _ { 3 } } } & { { = } } & { { \pi _ { 2 } } } \\ { { . . . } } & { { = } } & { { . . . } } \end{array}\tag{5.76}
$$

i.e.

$$
q _ { k } \pi _ { 0 } + \sum _ { j = 1 } ^ { k + 1 } q _ { k + 1 - j } ^ { \prime } \pi _ { j } = \pi _ { k } , k \in \mathbb { N } .
$$

Multiplying the kth equation in (5.76) by $x ^ { k } , k \geq 0$ , and summing all of them up we obtain

$$
\pi _ { 0 } Q ( x ) + \frac { G ( x ) } { x } \sum _ { j = 1 } ^ { \infty } \pi _ { j } x ^ { j } = \pi ( x ) , | x | < 1 ,\tag{5.77}
$$

where for $| x | < 1$

$$
\pi ( x ) = \sum _ { j = 0 } ^ { \infty } \pi _ { j } x ^ { j } ,
$$

$$
Q ( x ) = \sum _ { j = 0 } ^ { \infty } q _ { j } x ^ { j } ,
$$

$$
G ( x ) = \sum _ { j = 0 } ^ { \infty } q _ { j } ^ { \prime } x ^ { j } .
$$

Since $\begin{array} { r } { \sum _ { j = 1 } ^ { \infty } \pi _ { j } z ^ { j } = \pi ( x ) - \pi _ { 0 } } \end{array}$ , we see that $\pi , Q$ and $\pmb { G }$ satisfy the functional equation

$$
\pi ( x ) = \pi _ { 0 } \frac { G ( x ) - x Q ( x ) } { G ( x ) - x } , \quad | x | < 1 .\tag{5.78}
$$

Since all the coefficients $\pmb { \pi _ { j } }$ in the power series defining ⅡI are non-negative, Abel's lemma implies that

$$
\sum _ { j = 0 } ^ { \infty } \pi _ { j } = \operatorname* { l i m } _ { x \nearrow 1 } \pi ( x ) .
$$

It is possible to use (5.78) to calculate this limit. However, there is still some work be done. We have to use I'Hospital's rule. Since ${ \cal G } ( x ) - x  0 , { \cal G } ( x ) -$ $x Q ( x )  0 , G ^ { \prime } ( x ) - 1  \lambda ^ { \prime } - 1$ and $( G ( x ) - x Q ( x ) ) ^ { \prime }  \lambda ^ { \prime } - \lambda - 1$ (all limits are for $\pmb { x } \nearrow 1 )$ , we obtain (recall that $\begin{array} { r } { \lambda ^ { \prime } = \operatorname* { l i m } _ { z , \ x _ { 1 } } G ^ { \prime } ( x ) ) } \end{array}$

if $\lambda ^ { \prime } \neq 1$ , then $\begin{array} { r } { \pi ( x )  \pi _ { 0 } \frac { 1 + \lambda - \lambda ^ { \prime } } { 1 - \lambda ^ { \prime } } } \end{array}$

if $\lambda ^ { \prime } = 1$ , then $\pi ( x ) \to \infty$

In case 1) $\begin{array} { r } { \frac { 1 + \lambda - \lambda ^ { \prime } } { 1 - \lambda ^ { \prime } } \ge 1 } \end{array}$ , since $\pmb { \pi _ { 0 } } \leq \pmb { 1 }$ . It follows that $\frac { \lambda } { 1 - \lambda ^ { \prime } } \geq 0$ ,and so $\lambda ^ { \prime } < 1$ Moreover, in this case

$$
\pi _ { 0 } = \frac { 1 - \lambda ^ { \prime } } { 1 - \lambda ^ { \prime } + \lambda } .
$$

The above argument shows that if there exists an invariant measure, then it is unique.

Now suppose that an invariant measure exists (and then it is unique). Since our chain is irreducible and aperiodic (check this!), it follows from Theorem 5.6 that the Markov chain ξn is ergodic. Therefore, by Theorem 5.5, for any $\xi _ { n }$ $j \in \mathbb { N }$

$$
p _ { n } ( j | j )  \pi _ { j } , ~ { \mathrm { a s } } ~ n  \infty .
$$

Since $F _ { j i } ( 1 ) = 1$ , it also follows that

$$
p _ { n } ( j | i )  \pi _ { j } , ~ \mathrm { a s } ~ n  \infty .
$$

## Solution 5.37

We begin with the case $\scriptstyle n _ { 0 } = 1$ .Then $M _ { n } ( j ) - m _ { n } ( j ) \leq ( 1 - \varepsilon ) ^ { n }$ from (5.62). It follows that

$$
\begin{array} { r l } & { m _ { n } ( j ) \ \leq \ \pi _ { j } \leq M _ { n } ( j ) , } \\ & { m _ { n } ( j ) \ \leq \ p _ { n } ( j \vert i ) \leq M _ { n } ( j ) , } \end{array}
$$

and we infer that $| p _ { n } ( j | i ) - \pi _ { j } | \leq ( 1 - \varepsilon ) ^ { n } , n \in \mathbb { N } .$ Suppose that $\mathbf { \mathit { n } _ { 0 } } \geq 2$ Then for any $r = 1 , \cdots , n _ { 0 } - 1$

$$
\begin{array} { r l } { \displaystyle | p _ { k n _ { 0 } + r } ( j | i ) - \pi _ { j } | } & { = \displaystyle \left| \sum _ { s \in S } p _ { k n _ { 0 } } ( j | s ) p _ { r } ( s | i ) - \sum _ { s \in S } \pi _ { j } p _ { r } ( s | i ) \right| } \\ & { \le \displaystyle \sum _ { s \in S } | p _ { k n _ { 0 } } ( j | s ) - \pi _ { j } | p _ { r } ( s | i ) } \\ & { \le \displaystyle \operatorname* { m a x } _ { s \in S } | p _ { k n _ { 0 } } ( j | s ) - \pi _ { j } | \sum _ { s \in S } p _ { r } ( s | i ) \le ( 1 - \varepsilon ) ^ { n } . } \end{array}
$$

## Solution 5.38

The transition probability matrix P takes the form $\left[ \begin{array} { l l } { 1 - p } & { q } \\ { p } & { 1 - q } \end{array} \right]$ . Since all four numbers $p , q , 1 - p , 1 - q$ are strictly positive, the assumption (5.54) is satisfied, and so the limits $\pi _ { j } = \operatorname* { l i m } _ { n } p _ { n } ( j | i )$ exist and are i-independent. Hence, the unique invariant measure of the corresponding Markov chain is equal to $\pi _ { 0 } \delta _ { 0 } + \pi _ { 1 } \delta _ { 1 }$ . (Recall that $S = \{ 0 , 1 \}$ in this example.) We need to find the values πj. One way of finding them-is to use the definition. As in Hint 5.28, $\pmb { \pi _ { j } }$ the vector $\pi = ( \pi _ { 0 } , \pi _ { 1 } )$ solves the following linear equation in matrix form:

$$
\left[ \begin{array} { c c } { { 1 - p } } & { { q } } \\ { { p } } & { { 1 - q } } \end{array} \right] \left[ \begin{array} { c } { { \pi _ { 0 } } } \\ { { \pi _ { 1 } } } \end{array} \right] \ = \ \left[ \begin{array} { c } { { \pi _ { 0 } } } \\ { { \pi _ { 1 } } } \end{array} \right]\tag{5.79}
$$

$$
\pi _ { 0 } + \pi _ { 1 } = 1 .\tag{5.80}
$$

Some elementary algebra allows us to find the unique solution to the above problem:

$$
\pi _ { 0 } = { \frac { q } { p + q } } , \pi _ { 1 } = { \frac { p } { p + q } } .\tag{5.81}
$$

Hence, $\begin{array} { r } { \frac { q } { p + q } \delta _ { 0 } + \frac { p } { p + q } \delta _ { 1 } } \end{array}$ is the unique invariant measure of the Markov chain.

## Solution 5.39

From (5.66) we infer that

$$
p _ { n } ( 0 | 0 ) = \frac { q } { q + p } + \frac { p } { q + p } ( 1 - p - q ) ^ { n }  \frac { q } { q + p } ,
$$

$$
\begin{array} { r } { p _ { n } ( 1 | 0 ) = \displaystyle \frac { p } { q + p } - \frac { p } { q + p } ( 1 - p - q ) ^ { n } \to \frac { p } { q + p } , } \\ { p _ { n } ( 0 | 1 ) = \displaystyle \frac { q } { q + p } - \frac { q } { q + p } ( 1 - p - q ) ^ { n } \to \frac { q } { q + p } , } \\ { p _ { n } ( 1 | 1 ) = \displaystyle \frac { p } { q + p } + \frac { q } { q + p } ( 1 - p - q ) ^ { n } \to \frac { p } { q + p } . } \end{array}
$$

Hence $\textstyle \pi _ { 0 } = { \frac { q } { q + p } }$ and $\textstyle \pi _ { 1 } = { \frac { p } { q + p } }$ .This is in agreement with the previous solution.

## Solution 5.40

Suppose that $\xi _ { n } = i$ Then the value of $\xi _ { n + 1 }$ depends entirely on the outcome of the next roll of a die, say $X _ { n + 1 }$ , and the value of $\xi _ { n }$ . Since it depends on the past only through the value of $\xi _ { n }$ , intuitively we can see that we are dealing with a Markov chain. To define $\xi _ { n }$ in a precise way consider a sequence of independent identically distributed random variables $X _ { n } , n = 1 , 2 , 3 , \cdots$ such that $P ( X _ { 1 } =$ $i ) = 1 / 6$ for all $i = 1 , 2 , \cdots , 6$ Then, putting $\xi _ { n + 1 } = \operatorname* { m a x } \{ \xi _ { n } , X _ { n + 1 } \}$ , we can see immediately that $P ( \xi _ { n + 1 } = i _ { n + 1 } | \xi _ { 0 } = i _ { 0 } , \cdot \cdot \cdot , \xi _ { n } = i _ { n } ) = P ( \xi _ { n + 1 } = i _ { n + 1 } | \xi _ { n } =$ $i _ { n } )$ and

$$
\begin{array} { r l } { P ( \xi _ { n + 1 } = j | \xi _ { n } = i ) ~ = ~ P ( \xi _ { n + 1 } = j | \xi _ { n } = i , X _ { n + 1 } \leq i ) P ( X _ { n + 1 } \leq i ) ~ } & \\ & { \quad + P ( \xi _ { n + 1 } = j | \xi _ { n } = i , X _ { n + 1 } > i ) P ( X _ { n + 1 } > i ) } \\ & { = ~ \frac { i } { 6 } \left\{ \begin{array} { l l } { 0 } & { \mathrm { ~ i f ~ } j > i , } \\ { 1 } & { \mathrm { ~ i f ~ } j = i , } \\ { 0 } & { \mathrm { ~ i f ~ } j < i , } \end{array} \right. + \frac { 6 - i } { 6 } \left\{ \begin{array} { l l } { \frac { 1 } { 6 - i } } & { \mathrm { ~ i f ~ } j > i , } \\ { 0 } & { \mathrm { ~ i f ~ } j \leq i , } \end{array} \right. } \\ & { = ~ \left\{ \begin{array} { l l } { \frac { 1 } { 6 } } & { \mathrm { ~ i f ~ } j > i , } \\ { \frac { 1 } { 6 } } & { \mathrm { ~ i f ~ } j = i , } \\ { 0 } & { \mathrm { ~ i f ~ } j < i . } \end{array} \right. } \end{array}
$$

Thus,

$$
p ( j | i ) = { \left\{ \begin{array} { l l } { { \frac { 1 } { 6 } } \quad } & { { \mathrm { i f ~ } } j > i , } \\ { { \frac { i } { 6 } } \quad } & { { \mathrm { i f ~ } } j = i , } \\ { 0 \quad } & { { \mathrm { i f ~ } } j < i . } \end{array} \right. }
$$

It follows that

$$
P = \left[ \begin{array} { c c c c c c c } { \frac { 1 } { 6 } } & { 0 } & { 0 } & { 0 } & { 0 } & { 0 } \\ { \frac { 1 } { 6 } } & { \frac { 2 } { 6 } } & { 0 } & { 0 } & { 0 } & { 0 } \\ { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 3 } { 6 } } & { 0 } & { 0 } & { 0 } \\ { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 4 } { 6 } } & { 0 } & { 0 } \\ { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 5 } { 6 } } & { 0 } \\ { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { \frac { 1 } { 6 } } & { 1 } \end{array} \right] .
$$

## Solution 5.41

The graph of the chain is given in Figure 5.5. The transition matrix is

<!-- image-->  
Figure 5.5. Transition probabilities of the Markov chain in Exercise 5.41

$$
P = { \left[ \begin{array} { l l l l } { { \frac { 1 } { 4 } } } & { 0 } & { 0 } & { 0 } \\ { { \frac { 1 } { 4 } } } & { { \frac { 2 } { 4 } } } & { 0 } & { 0 } \\ { { \frac { 1 } { 4 } } } & { { \frac { 1 } { 4 } } } & { { \frac { 3 } { 4 } } } & { 0 } \\ { { \frac { 1 } { 4 } } } & { { \frac { 1 } { 4 } } } & { { \frac { 1 } { 4 } } } & { 1 } \end{array} \right] }
$$

Either from the graph or from the matrix we can see that $i  j$ if and only if $i \leq j$ The state $i = 1$ is transient. Indeed, $p ( 1 | 1 ) = 1 / 4$ and $p _ { n } ( 1 | 1 ) = ( 1 / 4 ) ^ { n }$ by induction. Therefore, $\begin{array} { r } { \sum _ { n } p _ { n } ( 1 | 1 ) < \infty } \end{array}$ and 1 is transient by Exercise 5.19. The same argument shows that the states 2 and 3 are also transient. On the other hand, $p ( 4 | 4 ) = 1$ 1, and so 4 is a positive-recurrent state. (As we know, that there should be at least one positive-recurrent state.)

We shall find invariant measures by solving the system of four linear equations $P \pi ~ = ~ \pi$ for $\pi = \left( \pi _ { 1 } , \pi _ { 2 } , \pi _ { 3 } , \pi _ { 4 } \right)$ , subject to the condition $\pi _ { 1 } + \pi _ { 2 } +$ $\pi _ { 3 } + \pi _ { 4 } = 1$ . Some elementary linear algebra shows that the only solution is $\pi _ { 1 } = \pi _ { 2 } = \pi _ { 3 } = 0 , \pi _ { 4 } = 1$ . Thus, the unique invariant measure is $\pi = \delta _ { 4 }$

The invariant measure can also be found by invoking Theorem 5.4. In our case $C = \{ 4 \}$ , and so there is exactly one class of recurrent sets. Moreover, as we have seen before, 4 is positive-recurrent. Therefore, there exists a unique invariant measure. Since its support is contained in C, we infer that $\pi = \delta _ { 4 }$

## Solution 5.42

The graph representing the Markov chain for $a = 4$ is presented in Figure 5.6. Obviously, $i  j$ for $i \in S \setminus \{ 0 , a \} = : \mathring { S }$ and $j \in S$ Moreover, $0  j$ if and only if $j = 0$ and $a  j$ if and only if $j = a$ .Since $p ( 0 | 0 ) \ : = \ : p ( a | a ) \ : = \ : 1$ both states 0 and a are positive-recurrent. All other states are transient. For if $i \in \mathring { S }$ were recurrent, i would be intercommunicating with 0 because $i  0$ by Exercise 5.26. This is impossible. Therefore, by Remark 5.5 there exist an infinite number of invariant measures: $\mu = ( 1 - \theta ) \delta _ { 0 } + \theta \delta _ { a }$ , where $\theta \in \{ 0 , 1 \}$ Indeed, δa is the only probability measure with support in the singleton {a}. $\pmb { \delta _ { a } }$ It is possible to verify this with bare hands. Next, let φ(i) denote the probability that the investigated Markov chain ξn hits the right-hand barrier prior to $\xi _ { n }$ hitting the left-hand one. Once ξn hits the left barrier it will never leave it, so $\xi _ { n }$ $\phi ( i )$ is actually the probability that $\xi _ { n }$ hits a. Put

<!-- image-->  
Figure 5.6. Transition probabilities of the Markov chain in Exercise 5.42

$$
A = \{ \exists n \in \mathbb { N } : \xi _ { n } = a \} .
$$

Then, by the total probability formula and the Markov property of ξn, we have $\xi _ { n }$ for $1 \leq i \leq a - 1$

$$
\begin{array} { l } { \displaystyle \phi ( i ) = P ( A | \xi _ { 0 } = i ) = \sum _ { j = 0 } ^ { a } P ( A | \xi _ { 0 } = i , \xi _ { 1 } = j ) P ( \xi _ { 1 } = j | \xi _ { 0 } = i ) } \\ { = \displaystyle \sum _ { j = 0 } ^ { a } P ( A | \xi _ { 1 } = j ) P ( \xi _ { 1 } = j | \xi _ { 0 } = i ) } \\ { = \displaystyle \sum _ { j = 0 } ^ { a } \phi ( j ) P ( \xi _ { 1 } = j | \xi _ { 0 } = i ) = p \phi ( i + 1 ) + q \phi ( i - 1 ) . } \end{array}
$$

Obviously,

$$
\begin{array} { r } { { \phi ( 0 ) = 0 , } } \\ { { \phi ( a ) = 1 . } } \end{array}
$$

Therefore, the sequence $\left( \phi ( i ) \right) _ { i = 0 } ^ { a }$ satisfies

$$
\begin{array} { l } { { \phi ( i ) ~ = ~ p \phi ( i + 1 ) + q \phi ( i - 1 ) , ~ 1 \leq i \leq a - 1 , } } \\ { { \phi ( 0 ) ~ = ~ 0 , ~ \phi ( a ) = 1 . } } \end{array}\tag{5.82}
$$

Since $\pmb { p } + \pmb { q } = \pmb { 1 }$ , equations (5.82) can be rewritten as follows:

$$
p \left[ \phi ( i + 1 ) - \phi ( i ) \right] = q \left[ \phi ( i ) - \phi ( i - 1 ) \right] , 1 \leq i \leq a - 1 .
$$

Hence,

$$
[ \phi ( i + 1 ) - \phi ( i ) ] = \left( \frac { q } { p } \right) ^ { i } x ,
$$

where $x = \phi ( 1 ) - \phi ( 0 )$ is to be determined. Using the boundary condition $\phi ( a ) = 1$ , we can easily find that

$$
\begin{array} { l } { { 1 = \phi ( a ) \ = \displaystyle \sum _ { i = 0 } ^ { a - 1 } ( \phi ( i + 1 ) - \phi ( i ) ) } } \\ { { \ = \ \frac { ( \frac { q } { p } ) ^ { a } \ - 1 } { \frac { q } { p } - 1 } x . } } \end{array}
$$

Here we assume that ${ \pmb { p } } \neq { \pmb q } .$ The case $p = q = 1 / 2$ can be treated in a similar way. In fact the latter is easier. It follows that

$$
x =  \frac { \underset { p } { \overset { q } { \} } - 1 } { ( { \underset { p } { \overset { q } { \ q } } } ) ^ { a } - 1 } }
$$

and therefore

$$
\phi ( i ) = \sum _ { k = 0 } ^ { i - 1 } { \bigl ( } \phi ( k + 1 ) - \phi ( k ) { \bigr ) } = { \frac { { \binom { q } { p } } ^ { i } - 1 } { { \binom { q } { p } } ^ { a } - 1 } } .
$$

# Stochastic Processes in Continuous Time

## 6.1 General Notions

The following definitions are straightforward extensions of those introduced earlier for sequences of random variables, the underlying idea being that of a family of random variables depending on time.

## Definition 6.1

A stochastic process is a family of random variables $\xi ( t )$ parametrized by $t \in T$ where $T \subset \mathbb { R }$ When $T = \{ 1 , 2 , \ldots \}$ , we shall say that ξ(t) is a stochastic process in discrete time (i.e. a sequence of random variables). When $_ { T }$ is an interval in R (typically $T = \{ \mathbf { 0 } , \infty ) \}$ , we shall say that ξ(t) is a stochastic process in continuous time.

For every $\omega \in \mathcal { \Omega }$ the function

$$
T \ni t \mapsto \xi ( t , \omega )
$$

is called a path (or sample path) of ξ(t).

## Definition 6.2

A family $\mathcal { F } _ { t }$ of σ-fields on Ω parametrized by $\pm \in T$ , where $T \subset \mathbb { R }$ is called a filtration if

$$
\mathcal { F } _ { s } \subset \mathcal { F } _ { t } \subset \mathcal { F }
$$

for any $s , t \in T$ such that $\bullet \leq t$

## Definition 6.3

A stochastic process ξ(t) parametrized by $t \in T$ is called a martingale (submartingale, supermartingale) with respect to a filtration $\mathcal { F } _ { t }$ if

1) ξ(t) is integrable for each $t \in T$

ξ(t) is $\mathcal { F } _ { t }$ -measurable for each $t \in T$ (in which case we say that $\xi ( t )$ is adapted to $\mathcal { F } _ { t } )$

3) $\xi ( s ) = E \left( \xi ( t ) | \mathcal { F } _ { s } \right)$ (respectively, $\leq \mathsf { o r } \geq )$ for every $s , t \in T$ such that $s \leq t$

In earlier chapters we have seen various stochastic processes, in particular, martingales in discrete time such as the symmetric random walk, for example. In what follows we shall study in some detail two processes in continuous time, namely, the Poisson process and Brownian motion.

## 6.2 Poisson Process

## 6.2.1 Exponential Distribution and Lack of Memory

## Definition 6.4

We say that a random variable n has the exponential distribution of rate $\lambda > 0$ if

$$
P \left\{ \eta > t \right\} = e ^ { - \lambda t }
$$

for all ${ \pmb t } \geq { \pmb 0 } ,$

For example, the emissions of particles by a sample of radioactive material (or calls made at a telephone exchange) occur at random times. The probability that no particle is emitted (no call is made) up to time t is known to decay exponentially as t increases. That is to say, the time r of the first emission has the exponential distribution, $P \left\{ \eta > t \right\} = e ^ { - \lambda t }$

## Exercise 6.1

What is the distribution function of a random variable n with exponential distribution? Does it have a density? If so, find the density.

Hint What is the probability that $\pmb { \eta } > \mathbf { 0 } ?$ What is the probability that $\pmb { \eta } > \pmb { t }$ given $t < 0 ?$ Can you express the distribution function in terms of $P \{ \eta > t \} ?$ distribution function differentiable?

## Exercise 6.2

Compute the expectation and variance of a random variable having the exponential distribution.

Hint Use the density found in Exercise 6.1.

## Exercise 6.3

Show that a random variable 7 with exponential distribution satisfies

$$
P \left\{ \eta > t + s \right\} = P \left\{ \eta > t \right\} P \left\{ \eta > s \right\}\tag{6.1}
$$

for any $s , t \geq 0$

Hint When the probabilities are replaced by exponents, the equality should become obvious.

## Exercise 6.4

Show that the equality in Exercise 6.3 is equivalent to

$$
P \left\{ \eta > t + s | \eta > s \right\} = P \left\{ \eta > t \right\}\tag{6.2}
$$

for any $s , t \geq 0$

Hint Recall how to compute conditional probability. Observe that $\eta > s + t$ implies $\eta > s$

The equality (6.2) (or, equivalently, (6.1)) is known as the lack of memory property. The odds that no particle will be emitted (no call will be made) in the next time interval of length t are not affected by the length of time s it has already taken to wait, given that no emission (no call) has occurred yet.

## Exercise 6.5

Show that the exponential distribution is the only probability distribution satisfying the lack of memory property.

Hint The lack of memory property means that $g ( t ) = P \left\{ \eta > t \right\}$ satisfies the functional equation

$$
g ( t + s ) = g ( t ) g ( s )
$$

for any $s , t \ > \ 0$ . Find all non-negative non-increasing solutions of this functional equation.

## 6.2.2 Construction of the Poisson Process

Let $\eta _ { 1 } , \eta _ { 2 } , . . .$ . be a sequence of independent random variables, each having the same exponential distribution of rate λ. For example, the times between the emissions of radioactive particles (or between calls made at a telephone exchange) have this property. We put

$$
\xi _ { n } = \eta _ { 1 } + \cdot \cdot \cdot + \eta _ { n } ,
$$

which can be thought of as the time of the nth emission (the nth call). We also put ${ \pmb \xi _ { 0 } } = { \bf 0 }$ for convenience. The number of emissions (calls) up to time $t \geq 0$ is an n such that $\xi _ { n + 1 } > t \geq \xi _ { n }$ . In other words, the number of emissions (calls) up to time $\pmb { t } \geq 0$ is equal to max $\{ n : t \geq \xi _ { n } \}$

## Definition 6.5

We say that N(t), where $\pmb { t } \geq 0$ , is a Poisson process if

$$
N ( t ) = \operatorname* { m a x } \left\{ n : t \geq \xi _ { n } \right\} .
$$

Thus, $N ( t )$ can be regarded as the number of particles emitted (calls made) up to time t. It is an example of a stochastic process in continuous time. A typical path of $N ( t )$ is shown in Figure 6.1. It begins at the origin, $N ( 0 ) = 0$ (no particles emitted at time 0), and is right-continuous, non-decreasing and piecewise constant with jumps of size 1 at the times $\xi _ { n }$

<!-- image-->  
Figure 6.1. A typical path of N(t) and the jump times $\xi _ { n }$

What is the distribution of $N ( t ) ?$ To answer this question we need to recall the definition of the Poisson distribution.

## Definition 6.6

A random variable v has the Poisson distribution with parameter $\alpha > 0$ if

$$
P \left\{ \nu = n \right\} = e ^ { - \alpha } \frac { \alpha ^ { n } } { n ! }
$$

for any $\pmb { n = 0 , 1 , 2 , \dots } .$

The probabilities $P \left\{ \nu = n \right\}$ for various values of α are shown in Figure 6.2.

<!-- image-->  
Figure 6.2. Poisson distribution with parameter α

Proposition 6.1

N(t) has the Poisson distribution with parameter λt,

$$
P \left\{ N ( t ) = n \right\} = e ^ { - \lambda t } \frac { { \left( \lambda t \right) } ^ { n } } { n ! } .
$$

Proof

First of all, observe that

$$
\{ N ( t ) < n \} = \{ \xi _ { n } > t \} .
$$

It suffices to compute the probability of this event for any n because

$$
\begin{array} { r } { P \left\{ N ( t ) = n \right\} \ = P \left\{ N ( t ) < n + 1 \right\} - P \left\{ N ( t ) < n \right\} } \\ { = P \left\{ \xi _ { n + 1 } > t \right\} - P \left\{ \xi _ { n } > t \right\} . } \end{array}\tag{6.3}
$$

We shall prove by induction on n that

$$
P \left\{ \xi _ { n } > t \right\} = e ^ { - \lambda t } \sum _ { k = 0 } ^ { n - 1 } \frac { \left( \lambda t \right) ^ { k } } { k ! } .\tag{6.4}
$$

For $\pmb { n } = 1$

$$
P \left\{ \xi _ { 1 } > t \right\} = P \left\{ \eta _ { 1 } > t \right\} = e ^ { - \lambda t } .
$$

Next, suppose that (6.4) holds for some n. Then, expressing $\xi _ { n + 1 }$ as the sum of the independent random variables $\xi _ { n }$ and $\eta _ { n + 1 }$ , we compute

$$
\begin{array} { r l } { P \left\{ \xi _ { n + 1 } > t \right\} } & { = P \left\{ \xi _ { n } + \eta _ { n + 1 } > t \right\} } \\ & { = P \left\{ \eta _ { n + 1 } > t \right\} + P \left\{ \xi _ { n } > t - \eta _ { n + 1 } , t \geq \eta _ { n + 1 } > 0 \right\} } \\ & { = e ^ { - \lambda t } + \displaystyle \int _ { 0 } ^ { t } P \left\{ \xi _ { n } > t - s \right\} f _ { \eta _ { n + 1 } } ( s ) d s } \\ & { = e ^ { - \lambda t } + \displaystyle \int _ { 0 } ^ { t } e ^ { - \lambda ( t - s ) } \sum _ { k = 0 } ^ { n - 1 } \frac { \left( \lambda \left( t - s \right) \right) ^ { k } } { k ^ { 1 } } \lambda e ^ { - \lambda s } d s } \\ & { = e ^ { - \lambda t } + e ^ { - \lambda t } \displaystyle \sum _ { k = 0 } ^ { n - 1 } \frac { \lambda ^ { k + 1 } } { k ^ { 1 } } \int _ { 0 } ^ { t } \left( t - s \right) ^ { k } d s } \\ & { = e ^ { - \lambda t } \displaystyle \sum _ { k = 0 } ^ { n } \frac { \left( \lambda t \right) ^ { k } } { k ^ { 1 } } , } \end{array}
$$

where $f _ { \eta _ { n + 1 } } ( s )$ is the density of $\eta _ { n + 1 }$ . By induction, (6.4) holds for any n. Now apply (6.3) to complete the proof.

## Exercise 6.6

What is the expectation of $N ( t ) ?$

Hint What are the possible values of $N ( t ) ?$ What are the corresponding probabilities? Can you compute the expectation from these? To simplify the result use the Taylor expansion of $e ^ { \pmb { x } }$

## Exercise 6.7

Compute $P \left\{ N ( s ) = 1 , N ( t ) = 2 \right\} { \mathrm { f o r ~ a n y ~ } } 0 \leq s < t$

Hint Express $\{ N ( s ) = 1 , N ( t ) = 2 \}$ as $\left\{ \eta 1 \leq s < \eta _ { 1 } + \eta _ { 2 } \leq t < \eta _ { 1 } + \eta _ { 2 } + \eta _ { 3 } \right\}$ You and independent.

## 6.2.3 Poisson Process Starts from Scratch at Time t

Imagine that you are to take part in an experiment to count the emissions of a radioactive particle. Unfortunately, in the excitement of proving the lack of memory property you forget about the engagement and arrive late to find that the experiment has already been running for time t and you have missed the first $N ( t )$ emissions. Determined to make the best of it, you start counting right away, so at time $\pmb { t } + \pmb { s }$ you will have registered $N ( t + s ) - N ( t )$ emissions. It will now be necessary to discuss $N ( t + s ) - N ( t )$ instead of $N ( s )$ in your report.

What are the properties of $N ( t { + } s ) { - } N ( t ) ?$ Perhaps you can guess something from the physical picture? After all, a sample of radioactive material will keep emitting particles no matter whether anyone cares to count them or not. So the moment when someone starts counting does not seem important. You can expect $N ( t + s ) - N ( t )$ to behave in a similar way as $N ( s )$ . And because radioactive emissions have no memory of the past, $N ( t + s ) - N ( t )$ should be independent of $N ( t )$

To study this conjecture recall the construction of a Poisson process $N ( t )$ based on a sequence of independent random variables $\eta _ { 1 } , \eta _ { 2 } , \ldots ,$ all having the same exponential distribution. We shall try to represent $N ( t + s ) - N ( t )$ in a similar way.

Let us put

$$
\eta _ { 1 } ^ { t } : = \xi _ { \mathrm { { \small ~ v ~ } } ( t ) + 1 } - t , \quad \eta _ { n } ^ { t } : = \eta _ { N ( t ) + n } , \quad n = 2 , 3 , \ldots ,
$$

see Figure 6.3. These are the times between the jumps of $N ( t + s ) - N ( t )$ . Then we define

$$
\begin{array} { c } { { \xi _ { n } ^ { t } = \eta _ { 1 } ^ { t } + \cdot \cdot \cdot + \eta _ { n } ^ { t } , } } \\ { { N ^ { t } ( s ) = \operatorname* { m a x } \{ n : \xi _ { n } ^ { t } \leq s \} . } } \end{array}
$$

<!-- image-->  
Figure 6.3. The random variables ηi, 72, 73, ...

## Exercise 6.8

Show that

$$
N ^ { t } ( s ) = N ( t + s ) - N ( t ) .
$$

Hint First show that $\xi _ { n } ^ { t } = \xi _ { N ( t ) + n } - t$

If we can show that $\eta _ { 1 } ^ { t } , \eta _ { 2 } ^ { t } , \ldots$ are independent random variables having the same exponential distribution as $\eta _ { 1 } , \eta _ { 2 } , \ldots ,$ it will mean that $N ( t + s ) - N ( t )$ is a Poisson process with the same probability distribution as $N ( s )$ . Moreover, if the $\eta _ { n } ^ { t }$ turn out to be independent of $N ( t )$ , it will imply that $N ( t + s ) - N ( t )$ is also independent of $N ( t )$

Before setting about this task beware of one common mistake. It is sometimes claimed that the times $\eta _ { 1 } ^ { t } , \eta _ { 2 } ^ { t } , \eta _ { 3 } ^ { t } , \dots$ between the jumps of $N ( t + s ) - N ( t )$ are equal to $\xi _ { n + 1 } - t , \eta _ { n + 2 } , \eta _ { n + 3 } , . . .$ for some n. Hence $\eta _ { 1 } ^ { t } , \eta _ { 2 } ^ { t } , \eta _ { 3 } ^ { t } , . . .$ . are independent because the random variables $\xi _ { n + 1 } , \eta _ { n + 2 } , \eta _ { n + 3 } , . . . \mathrm { a r e }$ .The flaw in this is that, in fact, $\eta _ { 1 } ^ { t } , \eta _ { 2 } ^ { t } , \eta _ { 3 } ^ { t } , \ldots$ are equal to $\xi _ { n + 1 } - t , \eta _ { n + 2 } , \eta _ { n + 3 } , . . .$ . only on the set $\{ N ( t ) = n \}$ . However, the argument can be saved by conditioning with respect to N(t). Our task becomes an exercise in computing conditional probabilities.

## Exercise 6.9

Show that

$$
\mathcal { P } \{ \eta _ { 1 } ^ { t } > s | N ( t ) \} = P \{ \eta _ { 1 } > s \} .
$$

Hint It suffices (why?) to verify that

$$
P \{ \eta _ { 1 } ^ { t } > s , N ( t ) = n \} = P \left\{ \eta _ { 1 } > s \right\} P \{ N ( t ) = n \}
$$

for any n. To this end, write the sets $\{ N ( t ) = n \}$ and $\{ \eta _ { 1 } ^ { t } > s , N ( t ) = n \}$ in terms of ${ \pmb \xi } _ { \pmb n }$ and $\eta _ { n + 1 }$ , which are independent random variables, and use the lack of memory property for $\eta _ { n + 1 }$ •

## Exercise 6.10

Show that

$$
P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , \ldots , \eta _ { k } ^ { t } > s _ { k } | N ( t ) \} = P \{ \eta _ { 1 } > s _ { 1 } \} \cdots P \{ \eta _ { k } > s _ { k } \} .
$$

Hint Verify that

$$
P \{ \eta _ { 1 } ^ { t } > s _ { 1 } , \eta _ { 2 } ^ { t } > s _ { 2 } , . . . , \eta _ { k } ^ { t } > s _ { k } , N ( t ) = n \} = P \{ \eta _ { 1 } > s _ { 1 } \} \cdot \cdot \cdot P \{ \eta _ { k } > s _ { k } \} P \{ N ( \tilde { \eta } _ { k } ^ { t } > \tilde { \eta } _ { k } ^ { s } , n \} ,
$$

for any n. This is done in Exercise 6.9 for $\pmb { k } = 1$