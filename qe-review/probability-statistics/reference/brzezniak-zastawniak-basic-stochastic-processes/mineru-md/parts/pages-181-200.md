$$
\begin{array} { l } { { = \displaystyle \operatorname* { l i m } _ { N  \infty } \prod _ { n = 1 } ^ { N } P \{ \eta _ { n } \leq m \} } } \\ { { = \displaystyle \operatorname* { l i m } _ { N  \infty } ( 1 - e ^ { - \lambda m } ) ^ { N } } } \\ { { = 0 . } } \end{array}
$$

It follows that

$$
\begin{array} { r l r } & { } & { P ( \underset { n  \infty } { \mathrm { l i m } } \xi _ { n } < \infty ) \ \leq \ P ( \underset { m = 1 } { \bigcup ^ { \infty } } \underset { n = 1 } { \overset { \infty } { \bigcap } } \{ \eta _ { n } \leq m \} ) } \\ & { } & { \qquad \quad \leq \underset { m = 1 } { \overset { \infty } { \sum } } P ( \underset { n = 1 } { \overset { \infty } { \bigcap } } \{ \eta _ { n } \leq m \} ) } \\ & { } & { \qquad = 0 , } \end{array}
$$

completing the proof.

While it is instructive to work through the above estimates, there exists a much more elegant argument. By the strong law of large numbers

$$
\operatorname* { l i m } _ { n  \infty } \frac { \xi _ { n } } { n } = \frac { 1 } { \lambda } \quad \mathrm { a . s . }
$$

Here $\textstyle { \frac { 1 } { \lambda } }$ is the expectation of each of the independent identically distributed random variables $\eta _ { n }$ (see Exercise 6.2). It follows that

$$
\operatorname* { l i m } _ { n \to \infty } \xi _ { n } = \infty \quad \mathrm { a . s . , }
$$

as required.

## Solution 6.15

In the proof of Proposition 6.1 it was shown that

$$
P \left\{ \xi _ { n } > t \right\} = e ^ { - \lambda t } \sum _ { k = 0 } ^ { n - 1 } \frac { \left( \lambda t \right) ^ { k } } { k ! }
$$

for $\pmb { t } \geq \mathbf { 0 }$ , see (6.4). Therefore the distribution function

$$
F _ { n } ( t ) = P \left\{ \xi _ { n } \le t \right\} = 1 - P \left\{ \xi _ { n } > t \right\} = e ^ { - \lambda t } \sum _ { k = n } ^ { \infty } \frac { ( \lambda t ) ^ { k } } { k ! }
$$

of $\xi _ { n }$ is differentiable, the density $f _ { n }$ of $\xi _ { n }$ being

$$
\begin{array} { l } { { f _ { n } ( t ) ~ = ~ { \displaystyle \frac { d } { d t } } F _ { n } ( t ) } } \\ { { ~ = ~ - \lambda e ^ { - \lambda t } \displaystyle \sum _ { k = n } ^ { \infty } \displaystyle \frac { ( \lambda t ) ^ { k } } { k ! } + \lambda e ^ { - \lambda t } \displaystyle \sum _ { k = n } ^ { \infty } \displaystyle \frac { ( \lambda t ) ^ { k - 1 } } { ( k - 1 ) ! } } } \\ { { ~ = ~ \lambda e ^ { - \lambda t } \displaystyle \frac { ( \lambda t ) ^ { n - 1 } } { ( n - 1 ) ! } } } \end{array}
$$

for $t > 0$ , and clearly $f _ { n } ( t ) = 0$ for ${ \pmb t } \le { \bf 0 }$

Solution 6.16

Because N(t) has non-decreasing trajectories

$$
\left\{ \operatorname* { l i m } _ { t \to \infty } N ( t ) = \infty \right\} = \bigcap _ { n = 1 } ^ { \infty } \bigcup _ { k = 1 } ^ { \infty } \{ N ( k ) \geq n \} .
$$

Also, $\{ N ( k ) \geq n \}$ $\pmb { k } = \mathbf { 1 } , \mathbf { 2 } , \dots$ is an expanding sequence of events and

$$
\begin{array} { l } { P \{ N ( k ) \geq n \} \ = \ e ^ { - \lambda k } \displaystyle \sum _ { i = n } ^ { \infty } \displaystyle \frac { ( \lambda k ) ^ { i } } { i ! } } \\ { \displaystyle = \ 1 - e ^ { - \lambda k } \displaystyle \sum _ { i = 0 } ^ { n - 1 } \displaystyle \frac { ( \lambda k ) ^ { i } } { i ! } \to 1 \quad \mathrm { a s } \ k \to \infty . } \end{array}
$$

It follows that

$$
P \left\{ \bigcup _ { k = 1 } ^ { \infty } \{ N ( k ) \geq n \} \right\} = 1 ,
$$

sO

$$
P \left\{ \operatorname* { l i m } _ { t \to \infty } N ( t ) = \infty \right\} = P \left\{ \bigcap _ { n = 1 } ^ { \infty } \bigcup _ { k = 1 } ^ { \infty } \{ N ( k ) \geq n \} \right\} = 1 .
$$

Solution 6.17

Since

$$
\sinh ( x ) \ = \ { \frac { e ^ { x } - e ^ { - x } } { 2 } } = \sum _ { n = 0 } ^ { \infty } { \frac { x ^ { 2 n + 1 } } { ( 2 n + 1 ) ! } } ,
$$

$$
\cosh ( x ) = { \frac { e ^ { x } + e ^ { - x } } { 2 } } = \sum _ { n = 0 } ^ { \infty } { \frac { x ^ { 2 n } } { ( 2 n ) ! } } ,
$$

we have

$$
\begin{array} { l } { { \displaystyle P \{ N ( t ) \mathrm { ~ i s ~ o d d } \} = \sum _ { n = 0 } ^ { \infty } P \{ N ( t ) = 2 n + 1 \} } } \\ { { \displaystyle \ = \sum _ { n = 0 } ^ { \infty } e ^ { - \lambda t } \frac { ( \lambda t ) ^ { 2 n + 1 } } { ( 2 n + 1 ) ! } \ } } \\ { { \displaystyle \ = \ e ^ { - \lambda t } \sinh ( \lambda t ) , } } \end{array}
$$

$$
\begin{array} { l } { { \displaystyle P \{ N ( t ) \mathrm { ~ i s ~ e v e n } \} ~ = ~ \sum _ { n = 0 } ^ { \infty } P \{ N ( t ) = 2 n \} } } \\ { { \displaystyle ~ = ~ \sum _ { n = 0 } ^ { \infty } e ^ { - \lambda t } \frac { ( \lambda t ) ^ { 2 n } } { ( 2 n ) ! } } } \\ { { ~ = ~ e ^ { - \lambda t } \cosh ( \lambda t ) . } } \end{array}
$$

## Solution 6.18

We can write

$$
N ( n ) = N ( 1 ) + ( N ( 2 ) - N ( 1 ) ) + \cdots + ( N ( n ) - N ( n - 1 ) ) ,
$$

where ${ \cal N } ( 1 ) , { \cal N } ( 2 ) - { \cal N } ( \mathrm { i } ) , { \cal N } ( 3 ) - { \cal N } ( 2 ) , \ldots $ is a sequence of independent identically distributed random variables with expectation

$$
E \left( N ( 1 ) \right) = E \left( N ( 2 ) - N ( 1 ) \right) = E \left( N ( 3 ) - N ( 2 ) \right) = \cdots = \lambda .
$$

By the strong law of large numbers

$$
\operatorname* { l i m } _ { n \to \infty } { \frac { N ( n ) } { n } } = \lambda \quad \mathrm { a . s . }\tag{6.8}
$$

Now, if $n \leq t \leq n + 1$ , then $N ( n ) \leq N ( t ) \leq N ( n + 1 )$ and

$$
\frac { N ( n ) } { n + 1 } \leq \frac { N ( t ) } { t } \leq \frac { N ( n + 1 ) } { n } .
$$

By (6.8) both sides tend to λ as $\pmb { n }  \infty$ , implying that

$$
\operatorname* { l i m } _ { t  \infty } \frac { N ( t ) } { t } = \lambda \quad \mathbf { a . s . }
$$

Solution 6.19

Condition 3) of Definition 6.9 implies that

$$
f _ { W ( t ) } \left( x \right) = p \left( t , 0 , x \right)
$$

is the density of $W ( t )$ ). Therefore, integrating by parts, we can compute the expectation

$$
\begin{array} { l } { E \left( W ( t ) \right) ~ = ~ \displaystyle \int _ { - \infty } ^ { + \infty } x p \left( t , 0 , x \right) d x } \\ { ~ = ~ \frac { 1 } { \sqrt { 2 \pi t } } \displaystyle \int _ { - \infty } ^ { + \infty } x e ^ { - \frac { t ^ { 2 } } { 2 \tau } } d x } \\ { ~ = ~ - \frac { t } { \sqrt { 2 \pi t } } \displaystyle \int _ { - \infty } ^ { + \infty } \frac { d } { d x } e ^ { - \frac { t ^ { 2 } } { 2 \tau } } d x } \\ { ~ = ~ - \frac { t } { \sqrt { 2 \pi t } } \left. e ^ { - \frac { t ^ { 2 } } { 2 \tau } } \right| _ { - \infty } ^ { + \infty } = 0 } \end{array}
$$

and variance

$$
\begin{array} { l l l } { \displaystyle E \left( \left( W ( t ) \right) ^ { 2 } \right) } & { = \displaystyle \int _ { - \infty } ^ { + \infty } x ^ { 2 } p \left( t , 0 , x \right) d x } \\ { \displaystyle } & { = } & { \displaystyle \frac { 1 } { \sqrt { 2 \pi t } } \int _ { - \infty } ^ { + \infty } x ^ { 2 } e ^ { - \frac { x ^ { 2 } } { 2 t } } d x } \\ { \displaystyle } & { = } & { \displaystyle - \frac { t } { \sqrt { 2 \pi t } } \int _ { - \infty } ^ { + \infty } x \frac { d } { d x } e ^ { - \frac { x ^ { 2 } } { 2 \tau } } d x } \\ { \displaystyle } & { = } & { \displaystyle - \frac { t } { \sqrt { 2 \pi t } } x e ^ { - \frac { x ^ { 2 } } { 2 \tau } } \Big | _ { - \infty } ^ { + \infty } + \frac { t } { \sqrt { 2 \pi t } } \int _ { - \infty } ^ { + \infty } e ^ { - \frac { x ^ { 2 } } { 2 \tau } } d x } \\ { \displaystyle } & { = } & { \displaystyle 0 + \frac { t } { \sqrt { 2 \pi } } \int _ { - \infty } ^ { + \infty } e ^ { - \frac { x ^ { 2 } } { 2 } } d u = t . } \end{array}
$$

We have used the substitution $u = \frac { x } { \sqrt { t } }$ and the formula stated in the hint.

Solution 6.20

Suppose that $s < t .$ Condition 3) of Definition 6.9 implies that the joint density of $W ( s )$ and $W ( t )$ is

$$
f _ { W \left( s \right) , W \left( t \right) } \left( x , y \right) = p \left( s , 0 , x \right) p \left( t - s , x , y \right) .
$$

It follows that

$$
\begin{array} { l } { E \left( W ( s ) W ( t ) \right) = \displaystyle \int _ { - \infty } ^ { + \infty } \int _ { - \infty } ^ { + \infty } x y \ p \left( s , 0 , x \right) p \left( t - s , x , y \right) d x d y } \\ { = \displaystyle \int _ { - \infty } ^ { + \infty } x p \left( s , 0 , x \right) \left( \displaystyle \int _ { - \infty } ^ { + \infty } y p \left( t - s , x , y \right) d y \right) d x } \\ { = \displaystyle \int _ { - \infty } ^ { + \infty } x ^ { 2 } p \left( s , 0 , x \right) d x = s . } \end{array}
$$

This is because by the results of Exercise 6.19

$$
\begin{array} { l } { \displaystyle \int _ { - \infty } ^ { + \infty } y p \left( t - s , x , y \right) d y = \int _ { - \infty } ^ { + \infty } \left( x + u \right) p \left( t - s , x , x + u \right) d u } \\ { = \displaystyle \int _ { - \infty } ^ { + \infty } \left( x + u \right) p \left( t - s , 0 , u \right) d u } \\ { = \displaystyle x \int _ { - \infty } ^ { + \infty } p \left( t - s , 0 , u \right) d u + \int _ { - \infty } ^ { + \infty } u p \left( t - s , 0 , u \right) d u } \\ { = \displaystyle x + 0 = x } \end{array}
$$

and

$$
\int _ { - \infty } ^ { + \infty } x ^ { 2 } p \left( s , 0 , x \right) d x = s .
$$

It follows that for arbitrary $s , t \geq 0$

$$
E \left( W ( s ) W ( t ) \right) = \operatorname* { m i n } \left\{ s , t \right\} .
$$

## Solution 6.21

Suppose that $s \leq t$ Then by Exercise 6.20

$$
\begin{array} { l } { E \left( \left| W ( t ) - W ( s ) \right| ^ { 2 } \right) \ = \ E \left( W ( t ) ^ { 2 } \right) - 2 E \left( W ( s ) W ( t ) \right) + E \left( W ( s ) ^ { 2 } \right) } \\ { \ = \ t - 2 s + s = t - s . } \end{array}
$$

In general, for arbitrary $\mathbf { \mathcal { s } } , \mathbf { \mathcal { t } } \geq 0$

$$
E \left( \left| W ( t ) - W ( s ) \right| ^ { 2 } \right) = \left| t - s \right| .
$$

## Solution 6.22

Using the density $f _ { W ( t ) } \left( x \right) = p \left( t , 0 , x \right)$ of $W ( t )$ , we compute

$$
\begin{array} { l } { E \left( \exp \left( i \lambda W ( t ) \right) \right) ~ = ~ \displaystyle \int _ { - \infty } ^ { + \infty } e ^ { i \lambda x } p \left( t , 0 , x \right) d x } \\ { ~ = ~ \displaystyle \frac { 1 } { \sqrt { 2 \pi t } } \int _ { - \infty } ^ { + \infty } e ^ { i \lambda x } e ^ { - \frac { z ^ { 2 } } { 2 t } } d x } \\ { ~ = ~ \displaystyle \frac { 1 } { \sqrt { 2 \pi t } } e ^ { - \frac { \lambda ^ { 2 } t } { 2 } } \int _ { - \infty } ^ { + \infty } e ^ { - \frac { ( z - i \lambda t ) ^ { 2 } } { 2 t } } d x } \\ { ~ = ~ e ^ { - \frac { \lambda ^ { 2 } t } { 2 } } . } \end{array}
$$

## Solution 6.23

Using the formula for the characteristic function of $W ( t )$ found in Exercise 6.22, we compute

$$
\begin{array} { l } { { E \left( W ( t ) ^ { 4 } \right) ~ = ~ \left. \frac { d ^ { 4 } } { d \lambda ^ { 4 } } \right| _ { \lambda = 0 } E \left( \exp { \left( i \lambda W ( t ) \right) } \right) } } \\ { { ~ = ~ \left. \frac { d ^ { 4 } } { d \lambda ^ { 4 } } \right| _ { \lambda = 0 } e ^ { - \frac { 1 } { 2 } \lambda ^ { 2 } t } } } \\ { { ~ = ~ 3 t ^ { 2 } . } } \end{array}
$$

## Solution 6.24

Since $W ^ { 1 } ( t ) , W ^ { 2 } ( t )$ are independent, their joint density is the product of the densities of $W ^ { 1 } ( t )$ and $W ^ { 2 } ( t )$ . Therefore

$$
P \left\{ | W ( t ) | < R \right\} \ = \ \int _ { \left\{ | x | < R \right\} } p \left( t , 0 , x \right) p \left( t , 0 , y \right) d x d y
$$

$$
{ \begin{array} { r l } & { = { \frac { 1 } { 2 \pi t } } \int _ { \{ | x | < R \} } e ^ { - { \frac { x ^ { 2 } + y ^ { 2 } } { 2 t } } } d x d y } \\ & { = { \frac { 1 } { 2 \pi t } } \int _ { 0 } ^ { R } \int _ { 0 } ^ { 2 \pi } r e ^ { - { \frac { x ^ { 2 } } { 2 t } } } d \varphi d r } \\ & { = \ - \int _ { 0 } ^ { R } { \frac { d } { d r } } e ^ { - { \frac { r ^ { 2 } } { 2 t } } } d r } \\ & { = \ 1 - e ^ { - { \frac { R ^ { 2 } } { 2 t } } } . } \end{array} }
$$

We have used the polar coordinates $R , \varphi$ to compute the integral.

Solution 6.25

For any $0 \leq s < t$

$$
\begin{array} { r l } { E \left( W ( t ) | \mathcal { F } _ { s } \right) = } & { E \left( W ( t ) - W ( s ) | \mathcal { F } _ { s } \right) + E \left( W ( s ) | \mathcal { F } _ { s } \right) } \\ { } & { = E \left( W ( t ) - W ( s ) \right) + W ( s ) } \\ { } & { = W ( s ) , } \end{array}
$$

since $W ( t ) - W ( s )$ is independent of $\pmb { \mathscr { F } _ { \pmb { \mathscr { s } } } }$ by Corollary 6.2, $W ( s )$ is $\mathcal { F } _ { \bullet } { \mathrm { - m e a s u r a b l e } }$ and $E \left( W ( t ) \right) = E \left( W ( s ) \right) = 0$

Solution 6.26

For any $0 \leq s < t$

$$
\begin{array} { r l } { E \left( W ( t ) ^ { 2 } | \mathcal { F } _ { s } \right) } & { = E \left( | W ( t ) - W ( s ) | ^ { 2 } | \mathcal { F } _ { s } \right) + E \left( 2 W ( t ) W ( s ) | \mathcal { F } _ { s } \right) } \\ & { \quad - E \left( W ( s ) ^ { 2 } | \mathcal { F } _ { s } \right) } \\ & { = E \left( | W ( t ) - W ( s ) | ^ { 2 } \right) + 2 W ( s ) E \left( W ( t ) | \mathcal { F } _ { s } \right) } \\ & { \quad - W ( s ) ^ { 2 } } \\ & { = t - s + 2 W ( s ) ^ { 2 } - W ( s ) ^ { 2 } } \\ & { = t - s + W ( s ) ^ { 2 } , } \end{array}
$$

since $W ( t ) - W ( s )$ is independent of $\pmb { \mathscr { F } } _ { \pmb { \mathscr { s } } }$ and has the normal distribution with mean 0 and variance $\mathbf { \boldsymbol { t } } - \mathbf { \boldsymbol { s } } , W ( \mathbf { \boldsymbol { s } } )$ is $\mathcal { F } _ { \bullet }$ -measurable, and $W ( t )$ is a martingale. It follows that

$$
E \left( W ( t ) ^ { 2 } - t | \mathcal { F } _ { s } \right) = W ( s ) ^ { 2 } - s ,
$$

as required.

Solution 6.27

For any $0 \leq t _ { 0 } < t _ { 1 } < \cdot \cdot \cdot < t _ { n }$ the increments

$$
V ( t _ { n } ) - V ( t _ { n - 1 } ) , \cdot \cdot \cdot , V ( t _ { 1 } ) - V ( t _ { 0 } )
$$

of V(t) are independent, since the increments

$$
W ( t _ { n } + T ) - W ( t _ { n - 1 } + T ) , \cdots , W ( t _ { 1 } + T ) - W ( t _ { 0 } + T )
$$

of $W ( t )$ are independent. For any $0 \leq s < t$ the increment $V ( t ) - V ( s )$ has the normal distribution with mean zero and variance t-s, since $W ( t { + } T ) { - } W ( s { + } T )$ does. Moreover, the paths $t \mapsto V ( t ) = W ( t + T ) - W ( T )$ are continuous and

$$
V ( 0 ) = W ( T ) - W ( T ) = 0 .
$$

By Theorem 6.3 V(t) is a Wiener process.

## Solution 6.28

It is clear that $V ( 0 ) = \textstyle { \frac { 1 } { c } } W ( 0 ) = 0 \ \mathbf { a } . s$ and the paths $\begin{array} { r } { t \mapsto V ( t ) = \frac { 1 } { c } W ( c ^ { 2 } t ) } \end{array}$ are a.s. continuous. We shall verify that $V ( t )$ and $\left| V ( t ) \right| ^ { 2 } - t$ are martingales with respect to the filtration

$$
\begin{array} { r l } & { \mathcal { G } _ { t } = \sigma \left\{ V ( s ) : 0 \le s \le t \right\} } \\ & { = \sigma \left\{ W ( c ^ { 2 } s ) : 0 \le s \le t \right\} } \\ & { = \sigma \left\{ W ( s ) : 0 \le s \le c ^ { 2 } t \right\} } \\ & { = \mathcal { F } _ { c ^ { 2 } t } . } \end{array}
$$

Indeed, if $\bullet < t$ ,then $c ^ { 2 } s < c ^ { 2 } t$ , so

$$
\begin{array} { l } { E \left( V ( t ) | \mathcal { G } _ { s } \right) = E \left( \displaystyle \frac { 1 } { c } W ( c ^ { 2 } t ) | \mathcal { F } _ { c ^ { 2 } s } \right) } \\ { = \displaystyle \frac { 1 } { c } E \left( W ( c ^ { 2 } t ) | \mathcal { F } _ { c ^ { 2 } s } \right) } \\ { = \displaystyle \frac { 1 } { c } W ( c ^ { 2 } s ) = V ( s ) } \end{array}
$$

and

$$
\begin{array} { l } { \displaystyle E \left( \left| V ( t ) \right| ^ { 2 } - t | \mathcal { G } _ { s } \right) = E \left( \frac { 1 } { c ^ { 2 } } \left| W ( c ^ { 2 } t ) \right| ^ { 2 } - t | \mathcal { F } _ { c ^ { 2 } s } \right) } \\ { \displaystyle = \frac { 1 } { c ^ { 2 } } E \left( \left| W ( c ^ { 2 } t ) \right| ^ { 2 } - c ^ { 2 } t | \mathcal { F } _ { c ^ { 2 } s } \right) } \\ { \displaystyle = \frac { 1 } { c ^ { 2 } } \left( \left| W ( c ^ { 2 } s ) \right| ^ { 2 } - c ^ { 2 } s \right) } \\ { \displaystyle = \left| V ( s ) \right| ^ { 2 } - s , } \end{array}
$$

since $W ( t )$ and $\left| W ( t ) \right| ^ { 2 } - t$ are martingales with respect to the filtration $\mathcal { F } _ { t }$ . It follows by Levy's martingale characterization that $V ( t )$ is a Wiener process.

Solution 6.29

Since the increments $\varDelta _ { i } ^ { n } W$ are independent and

$$
E \left( \varDelta _ { i } ^ { n } W \right) = 0 , \quad E \left( \left( \varDelta _ { i } ^ { n } W \right) ^ { 2 } \right) = \frac { T } { n } , \quad E \left( \left( \varDelta _ { i } ^ { n } W \right) ^ { 4 } \right) = \frac { 3 T ^ { 2 } } { n ^ { 2 } } ,
$$

it follows that

$$
\begin{array} { r l } { E ( [ \displaystyle \sum _ { i = 0 } ^ { n - 1 } ( \varDelta _ { i } ^ { n } W ) ^ { 2 } - T ] ^ { 2 } ) } & { = E ( [ \displaystyle \sum _ { i = 0 } ^ { n - 1 } ( ( \varDelta _ { i } ^ { n } W ) ^ { 2 } - \frac { T } { n } ) ] ^ { 2 } ) } \\ & { = \displaystyle \sum _ { i = 0 } ^ { n - 1 } E [ ( ( \varDelta _ { i } ^ { n } W ) ^ { 2 } - \frac { T } { n } ) ^ { 2 } ] } \\ & { = \displaystyle \sum _ { i = 0 } ^ { n - 1 } [ E ( ( \varDelta _ { i } ^ { n } W ) ^ { 4 } ) - \frac { 2 T } { n } E ( ( \varDelta _ { i } ^ { n } W ) ^ { 2 } ) + \frac { T ^ { 2 } } { n ^ { 2 } } ] } \\ & { = \displaystyle \sum _ { i = 0 } ^ { n - 1 } [ \frac { 3 T ^ { 2 } } { n ^ { 2 } } - \frac { 2 T ^ { 2 } } { n ^ { 2 } } + \frac { T ^ { 2 } } { n ^ { 2 } } ] = \frac { 2 T ^ { 2 } } { n }  0 } \end{array}
$$

as $n  \infty$

Solution 6.30

We claim that, with probability 1, for any positive integer n there is a $t \in \left\{ 0 , \frac { 1 } { n ^ { 4 } } \right\}$ such that $\frac { | W ( t ) | } { t } > n$ . This condition implies that $W ( t )$ is not differentiable at $\pmb { t = 0 }$

Let us put

$$
\begin{array} { r } { A _ { n } = \left\{ \frac { | W ( t ) | } { t } > n \mathrm { ~ f o r ~ s o m e ~ } t \in [ 0 , \frac { 1 } { n ^ { 4 } } ] \right\} } \end{array}
$$

By Exercise 6.28

$$
V _ { n } ( t ) = \frac { 1 } { n ^ { 2 } } W ( n ^ { 4 } t )
$$

is a Brownian motion for any n. Therefore

$$
\begin{array} { r l } { P \left( A _ { n } \right) \geq } & { P \left\{ \frac { \left| W \left( 1 / n ^ { 4 } \right) \right| } { 1 / n ^ { 4 } } > n \right\} } \\ { } & { = P \left\{ \frac { \left| V \left( 1 / n ^ { 4 } \right) \right| } { 1 / n ^ { 4 } } > n \right\} } \\ { } & { = P \left\{ | W ( 1 ) | > \frac { 1 } { n } \right\} \to 1 \quad \mathrm { a s ~ } n \to \infty . } \end{array}
$$

Since $A _ { 1 } , A _ { 2 } , \ldots$ is a contracting sequence of events,

$$
P \left( \bigcap _ { n = 1 } ^ { \infty } A _ { n } \right) = \operatorname* { l i m } _ { n \to \infty } P ( A _ { n } ) = 1 ,
$$

which proves the clain.

## Solution 6.31

By Exercise 6.27 $V _ { t } ( s ) = W ( s + t ) - W ( t )$ is a Wiener process for any $t \geq 0$ Therefore, by Exercise 6.30 $V _ { t } ( s )$ is a.s. non-differentiable at $s = 0$ .But this implies that $W ( t )$ is a.s. non-differentiable at t.

## Solution 6.32

Differentiating

$$
p ( t , x , y ) = \frac { 1 } { \sqrt { 2 \pi t } } e ^ { - \frac { ( y - z ) ^ { 2 } } { 2 t } } ,
$$

we obtain

$$
\frac { \partial } { \partial t } p ( t , x , y ) = \frac { y ^ { 2 } - 2 y x + x ^ { 2 } - t } { 2 t ^ { 2 } } p ( t , x , y ) ,
$$

$$
{ \frac { \partial } { \partial y } } p ( t , x , y ) = { \frac { x - y } { t } } p ( t , x , y ) ,
$$

$$
\frac { \partial ^ { 2 } } { \partial y ^ { 2 } } p ( t , x , y ) = \frac { y ^ { 2 } - 2 y x + x ^ { 2 } - t } { t ^ { 2 } } p ( t , x , y ) ,
$$

so

$$
\frac { \partial p } { \partial t } = \frac { 1 } { 2 } \frac { \partial ^ { 2 } p } { \partial y ^ { 2 } } ,
$$

as required.

## Solution 6.33

Clearly, $Z ( t ) = - W ( t )$ has a.s. continuous trajectories and $Z ( 0 ) = - W ( 0 ) =$ 0 a.s. If W(t) has stationary independent increments, then so does $Z ( t ) =$ $- W ( t )$ .Finally,

$$
Z ( t ) - Z ( s ) = - \left( W ( t ) - W ( s ) \right)
$$

has the same distribution as $W ( t ) - W ( s )$ , i.e. normal with mean 0 and variance t - s. By Theorem 6.3 Z(t) is a Wiener process.

## Solution 6.34

Let $0 \leq s < t$ Then

$$
\begin{array} { l l } { \displaystyle \int _ { \{ W ( s ) \in B \} } 1 _ { A } ( W ( t ) ) d P = P \left\{ W ( s ) \in B , W ( t ) \in A \right\} } \\ { \displaystyle } \\ { \displaystyle } \\ { \displaystyle } \\ { \qquad = \int _ { B } \int _ { A } p ( s , 0 , x ) p ( t - s , x , y ) d x d y } \\ { \displaystyle } \\ { \displaystyle } \\ { \displaystyle } \\ { \quad = \int _ { B } \left( \int _ { A } p ( t - s , x , y ) d y \right) p ( s , 0 , x ) d x } \\ { \displaystyle } \\ { \displaystyle } \end{array}
$$

for any Borel set $B \subset \mathbb { R }$ It follows that

$$
P \left\{ W ( t ) \in A | W ( s ) \right\} = E \left( 1 _ { A } ( W ( t ) ) | W ( s ) \right) = \int _ { A } p ( t - s , W ( s ) , y ) d y .
$$

## Solution 6.35

We shall prove that $e ^ { W ( t ) } e ^ { - \frac { \xi } { 2 } }$ is a martingale with respect to the filtration $\mathcal { F } _ { t }$ Clearly, it is adapted to the filtration $\pmb { \mathcal { F } } _ { t }$ ,since $W ( t )$ is. Let $0 \leq s < t$ Because $W ( t ) - W ( s )$ is independent of $\mathcal { F } _ { \bullet }$ and $W ( s )$ is $\pmb { \mathcal { F } } _ { \pmb { \mathfrak { s } } }$ -measurable,

$$
\begin{array} { r l } { E \left( e ^ { W ( t ) } | \mathcal { F } _ { \mathfrak { s } } \right) } & { = E \left( e ^ { W ( t ) - W ( \mathfrak { s } ) } e ^ { W ( \mathfrak { s } ) } | \mathcal { F } _ { \mathfrak { s } } \right) } \\ & { = e ^ { W ( \mathfrak { s } ) } E \left( e ^ { W ( t ) - W ( \mathfrak { s } ) } | \mathcal { F } _ { \mathfrak { s } } \right) } \\ & { = e ^ { W ( \mathfrak { s } ) } E \left( e ^ { W ( t ) - W ( \mathfrak { s } ) } \right) . } \end{array}
$$

The increment $W ( t ) - W ( s )$ has the normal distribution with mean 0 and variance t - s, so the expectation of $e ^ { W ( t ) - W ( s ) }$ is equal to

$$
\begin{array} { l } { { E \left( e ^ { W ( t ) - W ( s ) } \right) \ = \ \displaystyle \int _ { - \infty } ^ { + \infty } e ^ { x } p ( t - s , 0 , x ) d x } \ ~ } \\ { { \ = \ e ^ { \frac { t - s } { 2 } } \displaystyle \int _ { - \infty } ^ { + \infty } p ( t - s , 0 , x - t ) d x } \ ~ } \\ { { \ = \ e ^ { \frac { t - s } { 2 } } . } } \end{array}
$$

It follows that

$$
E \left( e ^ { W ( t ) } e ^ { - \frac { i } { 2 } } | \mathcal { F } _ { s } \right) = e ^ { W ( s ) } e ^ { - \frac { i } { 2 } } .
$$

It also follows that $e ^ { W ( t ) } e ^ { - \frac { t } { 2 } }$ is integrable. Therefore $e ^ { W ( t ) } e ^ { - \frac { t } { 2 } }$ is a martingale.

## Solution 6.36

Let $0 \leq s < t$ .We are looking for a Borel function F such that $E \left( W ( s ) | W ( t ) \right) =$ $F \left( W ( t ) \right)$ , i.e.

$$
\int _ { \{ W ( t ) \in A \} } W ( s ) d P = \int _ { \{ W ( t ) \in A \} } F \left( W ( t ) \right) d P
$$

for any Borel set A in R. The integral on the right-hand side can be written as

$$
\int _ { \{ W ( t ) \in A \} } F \left( W ( t ) \right) d P = \int _ { A } F \left( y \right) p \left( t , 0 , y \right) d y
$$

and the integral on the left-hand side as

$$
\int _ { \{ W ( t ) \in A \} } W ( s ) d P = \int _ { A } \left( \int _ { - \infty } ^ { + \infty } x p \left( s , 0 , x \right) p \left( t - s , x , y \right) d x \right) d y
$$

using the expression for the joint density of $W ( s )$ and $W ( t )$ in Solution 6.20. Let us compute the inner integral:

$$
\begin{array} { l } { { \displaystyle \int _ { - \infty } ^ { + \infty } x p ( s , 0 , x ) p ( t - s , x , y ) d x ~ = ~ p ( t , 0 , y ) \int _ { - \infty } ^ { \infty } x p \left( \frac { s ( t - s ) } { t } , \frac { s } { t } y , x \right) d x } } \\ { { ~ = ~ \frac { s } { t } y p ( t , 0 , y ) . } } \end{array}
$$

(To see that the first equality holds, just use formula (6.5) for $p ( t , x , y ) . )$ Therefore

$$
\int _ { \{ W ( t ) \in A \} } W ( s ) d P = \int _ { A } { \frac { s } { t } } y p \left( t , 0 , y \right) d y .
$$

It follows that $\begin{array} { r } { F ( \pmb { y } ) = \frac { \pmb { \mathfrak { s } } } { t } \pmb { y } } \end{array}$ ,i.e.

$$
E \left( W ( s ) | W ( t ) \right) = { \frac { s } { t } } W ( t ) .
$$

# Itô Stochastic Calculus

One of the first applications of the Wiener process was proposed by Bachelier, who around 1900 wrote a ground-breaking paper on the modelling of asset prices at the Paris Stock Exchange. Of course Bachelier could not have called it the Wiener process, but he used what in modern terminology amounts to $W ( t )$ as a description of the market fluctuations affecting the price X(t) of an asset. Namely, he assumed that infinitesimal price increments dX(t) are proportional to the increments $d W ( t )$ of the Wiener process,

$$
d X ( t ) = \sigma d W ( t ) ,
$$

where $\sigma$ is a positive constant. As a result, an asset with initial price $X ( 0 ) = x$ would be worth

$$
X ( t ) = x + \sigma W ( t )
$$

at time t. This approach was ahead of Bachelier's time, but it suffered from one serious flaw: for any $t > 0$ the price $X ( t )$ can be negative with non-zero probability. Nevertheless, for short times it works well enough, since the probability is negligible. But as t increases, so does the probability that $X ( t ) < 0$ , and the model departs from reality.

To remedy the flaw it was observed that investors work in terms of their potential gain or loss dX(t) in proportion to the invested sum X(t). Therefore, it is in fact the relative price $d X ( t ) / X ( t )$ of an asset that reacts to the market fluctuations, i.e. should be proportional to $d W ( t )$ ,

$$
d X ( t ) = \sigma X ( t ) d W ( t ) .\tag{7.1}
$$

What is the precise mathematical meaning of this equality? Formally, it resembles a differential equation, but this immediately leads to a difficulty because the paths of $W ( t )$ are nowhere differentiable. A way around the obstacle was found by Itô in the 1940s. In his hugely successful theory of stochastic integrals and stochastic differential equations Itô gave a rigorous meaning to equations such as (7.1) by writing them as integral equations involving a new kind of integral. In particular, (7.1) can be written as

$$
X ( t ) = x + \sigma \int _ { 0 } ^ { t } { X ( t ) d W ( t ) } ,
$$

where the integral with respect to $W ( t )$ on the right-hand side is called the Itô stochastic integral and will be defined in the next section. While at first sight one would expect the solution to this equation to be ${ \pmb x } e ^ { { \pmb W } ( { \pmb t } ) }$ , in fact it turns out to be

$$
X ( t ) = x e ^ { W ( t ) } e ^ { - \frac { t } { 2 } } ,
$$

which is the exponential martingale introduced in Exercise 6.35. The intriguing additional factor $e ^ { - \frac { \pi } { 2 } }$ is due to the non-differentiability of the paths of the Wiener process. Clearly, if $\pmb { x } > \pmb { 0 }$ , then $X ( t ) > 0$ for all ${ \pmb t } \geq { \bf 0 }$ , as required in the model of asset prices. In the following sections we shall learn how to transform and compute stochastic integrals and how to solve stochastic differential equations.

Throughout this chapter $W ( t )$ will denote a Wiener process adapted to a filtration $\mathcal { F } _ { t }$ and $L ^ { 2 }$ will b the pace  quare interable rndom varbles.

## 7.1 Itô Stochastic Integral: Definition

We shall follow a construction resembling that of the Riemann integral. First, the integral will be defined for a class of piecewise constant processes called random step processes. Then it will be extended to a larger class by approximation.

There are, however, at least two major differences between the Riemann and Itô integrals. One is the type of convergence. The approximations of the Riemann integral converge in R, while the Itô integral will be approximated by sequences of random variables converging in L2. The other difference is this. $L ^ { 2 }$ The Riemann sums approximating the integral of a function $f : [ 0 , T ] \to \mathbb { R }$ are of the form

$$
\sum _ { j = 0 } ^ { n - 1 } f ( s _ { j } ) ( t _ { j + 1 } - t _ { j } ) ,
$$

where $0 = t _ { 0 } < t _ { 1 } < \cdot \cdot \cdot < t _ { n } = T$ and ${ \pmb s } _ { j }$ is an arbitrary point in $[ t _ { j } , t _ { j + 1 } ]$ for each $j$ The value of the Riemann integral does not depend on the choice of the points $s _ { j } \in [ t _ { j } , t _ { j + 1 } )$ . In the stochastic case the approximating sums will have the form

$$
\sum _ { j = 0 } ^ { n - 1 } f ( s _ { j } ) \left( W ( t _ { j + 1 } ) - W ( t _ { j } ) \right) .
$$

It turns out that the limit of such approximations does depend on the choice of the intermediate points ${ \pmb { \mathscr { s } } } _ { j }$ in $[ t _ { j } , t _ { j + 1 } ]$ . In the next exercise we take $\textstyle f ( t ) = W ( t )$ and consider two different choices of intermediate points.

## Exercise 7.1

Let $0 = t _ { 0 } ^ { n } < t _ { 1 } ^ { n } < \cdot \cdot \cdot < t _ { n } ^ { n } = T$ , where $\begin{array} { r } { t _ { j } ^ { n } = \frac { i T } { n } } \end{array}$ , be a partition of the interval [0, T] into n equal parts. Find the following limits in $L ^ { 2 }$ ••

$$
\operatorname* { l i m } _ { n  \infty } \sum _ { j = 0 } ^ { n - 1 } W ( t _ { j } ^ { n } ) ( W ( t _ { j + 1 } ^ { n } ) - W ( t _ { j } ^ { n } ) )
$$

and

$$
\operatorname* { l i m } _ { n  \infty } \sum _ { j = 0 } ^ { n - 1 } W ( t _ { j + 1 } ^ { n } ) ( W ( t _ { j + 1 } ^ { n } ) - W ( t _ { j } ^ { n } ) ) .
$$

Hint Apply Exercise 6.29. You will need to transform the sums to make this possible. The identities

$$
{ \begin{array} { l } { a ( b - a ) = { \displaystyle \frac { 1 } { 2 } } \left( b ^ { 2 } - a ^ { 2 } \right) - { \displaystyle \frac { 1 } { 2 } } \left( a - b \right) ^ { 2 } , } \\ { b ( b - a ) = { \displaystyle \frac { 1 } { 2 } } \left( b ^ { 2 } - a ^ { 2 } \right) + { \displaystyle \frac { 1 } { 2 } } \left( a - b \right) ^ { 2 } } \end{array} }
$$

may be of help.

The ambiguity resulting from different choices of the intermediate points $\bullet _ { j }$ in each subinterval $[ t _ { j } , t _ { j + 1 } ]$ can be removed by insisting that the approximations of the integrand should consist only of processes adapted to the underlying filtration $\mathcal { F } _ { t }$ . This amounts to taking $\mathbf { \boldsymbol { s } } _ { j } = t _ { j }$ for each $j .$ The choice is motivated by the interpretation of $\mathcal { F } _ { t }$ : the value of the approximation at t may depend only on what has happened up to time t, but not on any future events.

## Definition 7.1

We shall call $f ( t ) , t \ \geq \ 0$ a random step process if there is a finite sequence of numbers $0 = t _ { 0 } < t _ { 1 } < . . . < t _ { n }$ and square integrable random variables

$\eta _ { 0 } , \eta _ { 1 } , \ldots , \eta _ { n - 1 }$ such that

$$
f ( t ) = \sum _ { j = 0 } ^ { n - 1 } \eta _ { j } 1 _ { [ t _ { j } , t _ { j + 1 } ) } ( t ) ,\tag{7.2}
$$

where $\eta _ { j }$ is $\mathcal { F } _ { t _ { j } }$ -measurable for $j = 0 , 1 , \ldots , n - 1$ . The set of random step processes will be denoted by $M _ { \mathrm { s t e p } } ^ { 2 } .$

Observe that the assumption that the $\pmb { \eta _ { j } }$ are to be $\mathcal { F } _ { t _ { j } }$ -measurable ensures that $f ( t )$ is adapted to the filtration $\mathcal { F } _ { t }$ . The assumption that the $\pmb { \eta _ { j } }$ are square integrable ensures that $f ( t )$ is square integrable for each t. Also, $M _ { \mathbf { s t e p } } ^ { 2 }$ is a vector space, that is, $a f + b g \in M _ { \mathrm { s t e p } } ^ { 2 }$ for any $f , g \in M _ { \mathrm { s t e p } } ^ { 2 }$ and $a , b \in \mathbb { R } .$

## Definition 7.2

The stochastic integral of a random step process $f \in M _ { \mathrm { s t e p } } ^ { 2 }$ of the form (7.2) is defined by

$$
I ( f ) = \sum _ { j = 0 } ^ { n - 1 } \eta _ { j } \left( W ( t _ { j + 1 } ) - W ( t _ { j } ) \right) .\tag{7.3}
$$

## Proposition 7.1

For any random step process $f \in M _ { \mathrm { { s t e p } } } ^ { 2 }$ the stochastic integral $I ( f )$ is a square integrable random variable, i.e. $I ( f ) \in L ^ { 2 }$ , such that

$$
E \left( | I ( f ) | ^ { 2 } \right) = E \left( \int _ { 0 } ^ { \infty } | f ( t ) | ^ { 2 } \ d t \right) .
$$

## Proof

Let us denote the increment $W ( t _ { j + 1 } ) - W ( t _ { j } )$ by $\varDelta _ { j } W$ and $t _ { j + 1 } - t _ { j }$ by $\varDelta _ { j } t$ for brevity. Then

$$
E \left( \varDelta _ { j } W \right) = 0 \quad { \mathrm { a n d } } \quad E \left( \varDelta _ { j } ^ { 2 } W \right) = \varDelta _ { j } t .
$$

First, we shall compute the expectation of

$$
\left| I ( f ) \right| ^ { 2 } = \sum _ { j = 0 } ^ { n - 1 } \sum _ { k = 0 } ^ { n - 1 } \eta _ { j } \eta _ { k } \varDelta _ { j } W \varDelta _ { k } W = \sum _ { j = 0 } ^ { n - 1 } \eta _ { j } ^ { 2 } \varDelta _ { j } ^ { 2 } W + 2 \sum _ { k < j } \eta _ { j } \eta _ { k } \varDelta _ { j } W \varDelta _ { k } W .
$$

Since $\pmb { \eta _ { j } }$ and $\varDelta _ { j } W$ are independent,

$$
E \left( \eta _ { j } ^ { 2 } \varDelta _ { j } ^ { 2 } W \right) = E \left( \eta _ { j } ^ { 2 } \right) E \left( \varDelta _ { j } ^ { 2 } W \right) = E \left( \eta _ { j } ^ { 2 } \right) \varDelta _ { j } t .
$$

If $k < j$ , then $\eta _ { j } \eta _ { k } \varDelta _ { k } W$ and $\varDelta _ { j } W$ are independent, so

$$
E \left( \eta _ { j } \eta _ { k } \varDelta _ { j } W \varDelta _ { k } W \right) = E \left( \eta _ { j } \eta _ { k } \varDelta _ { k } W \right) E \left( \varDelta _ { j } W \right) = 0 .
$$

Therefore

$$
E \left( | I ( f ) | ^ { 2 } \right) = \sum _ { j = 0 } ^ { n - 1 } E \left( \eta _ { j } ^ { 2 } \right) \varDelta _ { j } t .
$$

It follows that $I ( f ) \in L ^ { 2 }$ , since $\eta _ { 0 } , \eta _ { 1 } , \dots , \eta _ { n - 1 } \in L ^ { 2 }$

On the other hand,

$$
\left. f ( t ) \right. ^ { 2 } = \sum _ { j = 0 } ^ { n - 1 } \sum _ { k = 0 } ^ { n - 1 } \eta _ { j } \eta _ { k } 1 _ { [ t _ { j } , t _ { j + 1 } ) } ( t ) 1 _ { [ t _ { k } , t _ { k + 1 } ) } ( t ) = \sum _ { j = 0 } ^ { n - 1 } \eta _ { j } ^ { 2 } 1 _ { \{ t _ { j } , t _ { j + 1 } ) } ( t ) , 
$$

implying that

$$
E \left( \int _ { 0 } ^ { \infty } \left| f ( t ) \right| ^ { 2 } d t \right) = \sum _ { j = 0 } ^ { n - 1 } E \left( \eta _ { j } ^ { 2 } \right) \varDelta _ { j } t .
$$

This means that

$$
E \left( | I ( f ) | ^ { 2 } \right) = E \left( \int _ { 0 } ^ { \infty } | f ( t ) | ^ { 2 } d t \right) ,
$$

as required.

## Exercise 7.2

Verify that for any random step processes $f , g \in M _ { \mathrm { s t e p } } ^ { 2 }$

$$
E \left( I ( f ) I ( g ) \right) = E \left( \int _ { 0 } ^ { \infty } f ( t ) g ( t ) d t \right) .
$$

Hint Try to adapt the proof of Proposition 7.1. Use a common partition $0 = t _ { 0 } <$ $t _ { 1 } < \cdots < t _ { n }$ in which to represent both f and g in the form (7.2).

## Exercise 7.3

Show that $I : M _ { \tt s t e p } ^ { 2 }  L ^ { 2 }$ is a linear map, i.e. for any $f , g \in M _ { \mathrm { s t e p } } ^ { 2 }$ and any $\alpha , \beta \in \mathbb { R }$

$$
I ( \alpha f + \beta g ) = \alpha I ( f ) + \beta I ( g ) .
$$

Hint As in Exercise 7.2, use a common partition $0 = t _ { 0 } < t _ { 1 } < \cdot \cdot \cdot < t _ { n }$ in which to represent both f and g in the form (7.2).

The stochastic integral $I ( f )$ has been defined for any random step process $f \in M _ { \mathrm { { s t e p } } } ^ { 2 }$ The next stage is to extend I to a larger class of processes by approximation. This larger class can be defined as follows.

## Definition 7.3

We denote by $M ^ { 2 }$ the class of stochastic processes $f ( t ) , t \geq 0$ such that

$$
E \left( \int _ { 0 } ^ { \infty } | f ( t ) | ^ { 2 } d t \right) < \infty
$$

and there is a sequence $f _ { 1 } , f _ { 2 } , . . . \in M _ { \mathrm { s t e p } } ^ { 2 }$ of random step processes such that

$$
\operatorname* { l i m } _ { n \to \infty } E \left( \int _ { 0 } ^ { \infty } \left| f ( t ) - f _ { n } ( t ) \right| ^ { 2 } d t \right) = 0 .\tag{7.4}
$$

In this case we shall say that the sequence of random step processes $f _ { 1 } , f _ { 2 } , \ldots$ approximates f in $M ^ { 2 }$

## Definition 7.4

We call $I ( f ) \in L ^ { 2 }$ the Itô stochastic integral (from 0 to $\infty )$ of $f \in M ^ { 2 }$ if

$$
\operatorname* { l i m } _ { n \to \infty } E \left( \left| I ( f ) - I ( f _ { n } ) \right| ^ { 2 } \right) = 0\tag{7.5}
$$

for any sequence $f _ { 1 } , f _ { 2 } , \dots \in M _ { \mathrm { s t e p } } ^ { 2 }$ of random step processes that approximates f in $M ^ { 2 }$ , i.e. such that (7.4) is satisfied. We shall also write

$$
\int _ { 0 } ^ { \infty } f ( t ) d W ( t )
$$

in place of $I ( f )$

## Proposition 7.2

For any $\textbf { \textit { f } } \in \textbf { \textit { M } } ^ { 2 }$ the stochastic integral $I ( f ) \in L ^ { 2 }$ exists, is unique (as an e o , e. o withi equality and saties

$$
E \left( \left| I ( f ) \right| ^ { 2 } \right) = E \left( \int _ { 0 } ^ { \infty } \left| f ( t ) \right| ^ { 2 } d t \right) .\tag{7.6}
$$

Proof

It will be convenient to write

$$
\| f \| _ { M ^ { 2 } } = \sqrt { E \left( \int _ { 0 } ^ { \infty } \left| f ( t ) \right| ^ { 2 } d t \right) } \quad \mathrm { a n d } \quad \| \eta \| _ { L ^ { 2 } } = \sqrt { E \left( \eta ^ { 2 } \right) }
$$

for any $f \in M ^ { 2 }$ and $\eta \in L ^ { 2 }$ . These are norms1 in $M ^ { 2 }$ and $L ^ { 2 }$ , respectively. Let $f _ { 1 } , f _ { 2 } , \dots \in M _ { \mathrm { s t e p } } ^ { 2 }$ be a sequence of random step processes approximating $f \in M ^ { 2 }$ , i.e. satisfying (7.4), which can be written as

$$
\operatorname* { l i m } _ { n \to \infty } \| f - f _ { n } \| _ { M ^ { 2 } } = 0 .
$$

We claim that $I ( f _ { 1 } ) , I ( f _ { 2 } ) , . . .$ . is a Cauchy sequence in $L ^ { 2 }$ . Indeed, for any $\varepsilon > 0$ there is an N such that $\| f - f _ { n } \| _ { M ^ { 2 } } < \frac { \varepsilon } { 2 }$ for all $n > N$ . By Proposition 7.1

$$
\begin{array} { r l } { \| I ( f _ { m } ) - I ( f _ { n } ) \| _ { L ^ { 2 } } } & { = \| I ( f _ { m } - f _ { n } ) \| _ { L ^ { 2 } } } \\ & { = \| f _ { m } - f _ { n } \| _ { M ^ { 2 } } } \\ & { \leq \| f - f _ { m } \| _ { M ^ { 2 } } + \| f - f _ { n } \| _ { M ^ { 2 } } } \\ & { < \displaystyle \frac { \varepsilon } { 2 } + \frac { \varepsilon } { 2 } = \varepsilon } \end{array}
$$

for any $m , n > N$ , which proves the claim.

Because $L ^ { 2 }$ with the norm $\| \cdot \| _ { L ^ { 2 } }$ is a complete space (in fact a Hilbert space), every Cauchy sequence in $L ^ { 2 }$ has a limit. It follows that $I ( f _ { 1 } ) , I ( f _ { 2 } ) , \ldots$ has a limit in $L ^ { 2 }$ for any sequence $f _ { 1 } , f _ { 2 } , \ldots$ of random step processes approximating f. It remains to show that the limit is the same for all such sequences. Suppose that $f _ { 1 } , f _ { 2 } , \ldots$ and $g _ { 1 } , g _ { 2 } , \ldots$ are two sequences of random step processes approximating f. Then the interlaced sequence $f _ { 1 } , g _ { 1 } , f _ { 2 } , g _ { 2 } , . . .$ approximates f too, so the sequence $I ( f _ { 1 } ) , I ( g _ { 1 } ) , I ( f _ { 2 } ) , I ( g _ { 2 } ) , . .$ . has a limit in $L ^ { 2 }$ . But then all subsequences of the latter sequence, in particular, $I ( f _ { 1 } ) , I ( f _ { 2 } ) , . . .$ . and $I ( g _ { 1 } ) , I ( g _ { 2 } ) , . . .$ . have the same limit, which we denote by $I ( f )$ . We have shown that

$$
\operatorname* { l i m } _ { n \to \infty } \| I ( f ) - I ( f _ { n } ) \| _ { L ^ { 2 } } = 0 ,
$$

i.e. (7.5) holds for any sequence $f _ { 1 } , f _ { 2 } , \ldots$ . of random step processes approximating $f .$

Finally, by Proposition 7.1

$$
\| I ( f _ { n } ) \| _ { L ^ { 2 } } = \| f _ { n } \| _ { M ^ { 2 } }
$$

for each $\pmb { n } ,$ since the $f _ { n }$ are random step processes. By taking the limit as n → ∞ we obtain

$$
\| I ( f ) \| _ { L ^ { 2 } } = \| f \| _ { M ^ { 2 } } .
$$

But this is equality (7.6).

## Exercise 7.4

Show that for any $f , g \in M ^ { 2 }$

$$
E \left( I ( f ) I ( g ) \right) = E \left( \int _ { 0 } ^ { \infty } f ( t ) g ( t ) d t \right) .
$$

Hint Write the left-hand side in terms of $E \left( \left| I ( f ) + I ( g ) \right| ^ { 2 } \right)$ and $E \left( \left| I ( f ) - I ( g ) \right| ^ { 2 } \right)$ the right-hand side in terms of $\begin{array} { r } { E \left( \int _ { 0 } ^ { \infty } | f ( t ) + \dot { g } ( t ) | ^ { 2 } ~ d t \right) } \end{array}$ and $\begin{array} { r } { E \left( \int _ { 0 } ^ { \infty } | f ( t ) - g ( t ) | ^ { 2 } ~ d t \right) } \end{array}$ and then use (7.6).

Having defined the Itò stochastic integral from 0 to ∞, we are now in a position to consider stochastic integrals over any finite time interval [0, T].

## Definition 7.5

For any $T > 0$ we shall denote by $M _ { T } ^ { 2 }$ the space of all stochastic processes $f ( t ) , t \geq 0$ such that

$$
\mathbf { 1 } _ { [ 0 , T ) } f \in M ^ { 2 }
$$

The Ito stochastic integral (from 0 to T) of $f \in M _ { T } ^ { 2 }$ is defined by

$$
I _ { T } ( f ) = I \left( 1 _ { [ 0 , T ) } f \right) .\tag{7.7}
$$

We shall also write

$$
\int _ { 0 } ^ { T } f ( t ) d W ( t )
$$

in place of $I _ { T } ( f )$

## Exercise 7.5

Show that each random step process $f \in M _ { \mathrm { s t e p } } ^ { 2 }$ belongs to $M _ { t } ^ { 2 }$ for any $t > 0$ and

$$
I _ { t } ( f ) = \int _ { 0 } ^ { t } f ( s ) d W ( s )
$$

is a martingale.

Hint The stochastic integral of a random step process f is given by the sum (7.3). What is the conditional expectation of the jth term of this sum given $\mathcal { F } _ { s }$ if $\textbf { \^ s } < \pmb { t } _ { j } \mathrm { ~ }$ 7. What is it when $\mathrm { ~ \pmb ~ { \mathscr { s } } ~ } \geq t _ { j } ?$

The processes for which the stochastic integral exists have been defined as those that can be approximated by random step processes. However, it is not always easy to check whether or not such an approximation exists." For practical purposes it is important to have a straightforward sufficient condition for a process to have a stochastic integral. In calculus there is a well-known